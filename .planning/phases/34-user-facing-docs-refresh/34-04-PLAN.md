---
phase: 34-user-facing-docs-refresh
plan: 04
type: execute
wave: 2
depends_on:
  - 34-01
  - 34-02
  - 34-03
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "`README.md` starts with a project banner line or ASCII header and includes Python >=3.10 and MIT license badges"
    - "`README.md` contains a 60-second quickstart block with `pip install -e ".[dev,gui]"`, `surg-rl version --verbose`, and `surg-rl-gui scenes/simple_suturing.json`"
    - "`README.md` contains three demo walkthrough sections named Suturing, Knot-Tying, and Needle-Passing, each containing the corresponding embedded GIF (`docs/demos/{suturing,knot_tying,needle_passing}.gif`)"
    - "`README.md` contains a GUI editor section with the launch instruction `pip install '.[gui]' → surg-rl-gui` and at least one embedded screenshot (`docs/gui/viewport.png`)"
    - "`README.md` contains install instructions for core + `[gui]` + `[marl]` + `[dreamer]` extras with exact pip commands"
    - "`README.md` contains links to `CONTRIBUTING.md` and `CHANGELOG.md`"
    - "`README.md` does not reference paths under `tests/` for embedded assets; all embedded images use `docs/demos/` or `docs/gui/`"
    - "`README.md` embeds GIFs using standard markdown image syntax: `![alt text](docs/demos/xxx.gif)`"
  artifacts:
    - path: README.md
      provides: "v0.5.0 project landing page with banner, quickstart, demos, GUI section, install matrix, contributor links"
      min_lines: 120
      contains: "surg-rl-gui|docs/demos/suturing.gif|docs/demos/knot_tying.gif|docs/demos/needle_passing.gif|docs/gui/viewport.png|CONTRIBUTING.md|CHANGELOG.md"
  key_links:
    - from: README.md
      to: CONTRIBUTING.md
      via: "Inline link under 'Contributing' or 'Getting help'"
      pattern: "CONTRIBUTING.md"
    - from: README.md
      to: CHANGELOG.md
      via: "Inline link under 'Releases' or 'What's new'"
      pattern: "CHANGELOG.md"
    - from: README.md
      to: docs/demos/suturing.gif
      via: "Markdown image embed"
      pattern: "docs/demos/suturing.gif"
    - from: README.md
      to: docs/gui/viewport.png
      via: "Markdown image embed"
      pattern: "docs/gui/viewport.png"

threat_model:
  trust_boundaries:
    - "README.md is static markdown; no executable code paths"
  stride_register:
    - id: T-34-06
      category: Information_Disclosure
      component: README.md
      disposition: accept
      mitigation: "README contains only public project information. No API keys, internal endpoints, or PII. Badges use public shields.io endpoints."
    - id: T-34-07
      category: Tampering
      component: README.md
      disposition: accept
      mitigation: "External badge URLs are standard shields.io and PyPI-style static links. No user-supplied content is rendered."

---

<objective>
Rewrite `README.md` to showcase v0.5.0 features, embed the demo GIFs and GUI
screenshots, and link to the refreshed CONTRIBUTING and CHANGELOG.

Purpose: README.md is the primary adoption surface. This plan runs last in Phase 34
because it depends on the text updates (CONTRIBUTING/CHANGELOG), the demo GIFs, and
the GUI screenshots produced by the earlier plans.

Output: Rewritten `README.md`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (Phase 34 entry)
@.planning/REQUIREMENTS.md (DOC-01)
@.planning/phases/34-user-facing-docs-refresh/34-CONTEXT.md
@.planning/phases/34-user-facing-docs-refresh/34-RESEARCH.md
@.planning/phases/34-user-facing-docs-refresh/34-01-PLAN.md
@.planning/phases/34-user-facing-docs-refresh/34-02-PLAN.md
@.planning/phases/34-user-facing-docs-refresh/34-03-PLAN.md

@README.md
@CONTRIBUTING.md
@CHANGELOG.md
@docs/demos/suturing.gif
@docs/demos/knot_tying.gif
@docs/demos/needle_passing.gif
@docs/gui/viewport.png
@docs/gui/tree_form.png
@docs/gui/llm_panel.png
@pyproject.toml
</context>

<tasks>
  <task id="1">
    <read_first>
      <file>README.md</file>
      <file>CONTRIBUTING.md</file>
      <file>CHANGELOG.md</file>
      <file>pyproject.toml</file>
      <file>demos/NARRATION_TEMPLATE.md</file>
    </read_first>
    <action>
      Rewrite README.md from scratch using the structure in 34-RESEARCH.md. Include:
      - Banner + badges (Python >=3.10, MIT license).
      - One-sentence value proposition.
      - 60-second quickstart block with `pip install -e ".[dev,gui]"`, `surg-rl version --verbose`,
        and `surg-rl-gui scenes/simple_suturing.json`.
      - Condensed key-features list (8-12 bullets) reusing factual claims from the current README.
      - Simulator backends table.
      - Three demo walkthrough sections (Suturing, Knot-Tying, Needle-Passing), each with the
        5-stage narration flavor and the matching embedded GIF.
      - GUI editor section with `pip install '.[gui]'` and `surg-rl-gui`, plus an embedded screenshot.
      - Optional extras matrix covering core, gui, marl, dreamer, ros2, simulation, distributed,
        vision, llm, tracking, meshing, docs.
      - Links to CONTRIBUTING.md and CHANGELOG.md.
    </action>
    <acceptance_criteria>
      `grep -E '^# surg-rl|^## ' README.md` shows a clear heading hierarchy.
      `grep -E 'surg-rl-gui' README.md` exits 0.
      `grep -E 'docs/demos/suturing\.gif|docs/demos/knot_tying\.gif|docs/demos/needle_passing\.gif' README.md` exits 0.
      `grep -E 'docs/gui/viewport\.png|docs/gui/tree_form\.png|docs/gui/llm_panel\.png' README.md` exits 0.
      `grep -E 'CONTRIBUTING\.md' README.md` exits 0.
      `grep -E 'CHANGELOG\.md' README.md` exits 0.
    </acceptance_criteria>
    <verify>
      `python -c "from pathlib import Path; txt=Path('README.md').read_text(); print(all(x in txt for x in ['pip install -e ".[dev,gui]"','surg-rl version --verbose','surg-rl-gui scenes/simple_suturing.json','CONTRIBUTING.md','CHANGELOG.md']))"` prints `True`.
      `python -c "from pathlib import Path; import re; txt=Path('README.md').read_text(); print(len(re.findall(r'!\[.*?\]\(docs/(demos|gui)/.*?\)', txt)) >= 4)"` prints `True`.
    </verify>
  </task>

  <task id="2">
    <read_first>
      <file>README.md</file>
    </read_first>
    <action>
      Run a markdown link/image audit: ensure every `![...](path)` points to an existing file
      in `docs/demos/` or `docs/gui/`; fix any broken relative paths.
    </action>
    <acceptance_criteria>
      All embedded image paths in README.md resolve to existing files.
    </acceptance_criteria>
    <verify>
      `python -c "from pathlib import Path; import re; txt=Path('README.md').read_text(); imgs=re.findall(r'!\[.*?\]\((.*?)\)', txt); print(all(Path(p).exists() for p in imgs))"` prints `True`.
    </verify>
  </task>

  <task id="3">
    <read_first>
      <file>README.md</file>
    </read_first>
    <action>
      Add a README content regression test in `tests/test_doc_assets.py` that checks the banner,
      quickstart, demo GIFs, GUI screenshot, install extras, and contributor links.
    </action>
    <acceptance_criteria>
      `pytest tests/test_doc_assets.py -v -k readme` exits 0.
    </acceptance_criteria>
    <verify>
      `pytest tests/test_doc_assets.py -v -k readme` exits 0.
    </verify>
  </task>
</tasks>

<verification>
  - Run `pytest tests/test_doc_assets.py -v` and confirm README, GIF, and screenshot tests pass.
  - Render README.md with a markdown previewer or `python -m markdown README.md > /tmp/readme.html` (if `markdown` is installed) to confirm images embed correctly.
  - Run `grep -E 'TODO|FIXME|TBD' README.md` and confirm no placeholders remain.
</verification>

<success_criteria>
  - README.md has a banner, 60-second quickstart, three demo walkthrough sections with embedded GIFs,
    a GUI editor section with a screenshot, install instructions for core/gui/marl/dreamer extras,
    and links to CONTRIBUTING.md and CHANGELOG.md.
  - All embedded image paths resolve to existing files.
  - A regression test guards README.md structure and asset references.
</success_criteria>
