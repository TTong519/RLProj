---
phase: 34-user-facing-docs-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
  - CHANGELOG.md
autonomous: true
requirements:
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "`CONTRIBUTING.md` begins with a dev-setup block showing `pip install -e ".[dev,gui]"` followed by `cp .env.example .env`"
    - "`CONTRIBUTING.md` contains a branch / PR workflow section: fork → feature branch (`codex/` or user prefix) → `ruff check src/ tests/ && black --check src/ tests/ && mypy src/surg_rl` → PR template / review"
    - "`CONTRIBUTING.md` contains a GSD workflow overview section listing `/gsd-discuss-phase` → `/gsd-plan-phase` → `/gsd-execute-phase` with a note that user-facing docs phases also produce README/CHANGELOG artifacts"
    - "`CONTRIBUTING.md` contains an optional-dependency matrix with at least: core, dev, gui, marl, dreamer, ros2, simulation, distributed, vision, llm, tracking, meshing, docs"
    - "`CHANGELOG.md` header references Keep-a-Changelog 1.1.0 (updated from 1.0.0) and still references Semantic Versioning 2.0.0"
    - "`CHANGELOG.md` contains a `[0.5.0]` entry with Added, Changed, and Fixed subsections in that order"
    - "`CHANGELOG.md` `[0.5.0] Added` lists: PySide6 GUI scene editor, knot-tying demo, needle-passing demo, docs refresh"
    - "`CHANGELOG.md` `[0.5.0] Changed` lists: demo narration refactor via `demos/_common.py`"
    - "`CHANGELOG.md` `[0.5.0] Fixed` lists the 6 Phase 31 tech-debt closures: ruff-clean dreamer/, Dockerfile.ros2 $TARGETARCH, BaseSimulator.fluid_step hook, cut cooldown tests, PhiFlow multi-obstacle union() documentation, mjpython helper + `[gui]` extra + `surg-rl-gui` entry point"
  artifacts:
    - path: CONTRIBUTING.md
      provides: "Overhauled contributor guide with v0.5.0 dev setup, GSD workflow overview, optional-dep matrix"
      min_lines: 120
      contains: "pip install -e ".[dev,gui]"|/gsd-discuss-phase|/gsd-plan-phase|/gsd-execute-phase|ruff check|black --check|mypy src/surg_rl|optional-dependency"
    - path: CHANGELOG.md
      provides: "v0.5.0 release notes in Keep-a-Changelog 1.1.0 format"
      min_lines: 30
      contains: "[0.5.0]|### Added|### Changed|### Fixed|Keep-a-Changelog 1.1.0"
  key_links:
    - from: CONTRIBUTING.md
      to: docs/DEVELOPMENT.md
      via: "Link under 'Full development guide'"
      pattern: "docs/DEVELOPMENT.md"
    - from: CONTRIBUTING.md
      to: docs/GETTING_STARTED.md
      via: "Link under 'First-run instructions'"
      pattern: "docs/GETTING_STARTED.md"
    - from: CHANGELOG.md
      to: CONTRIBUTING.md
      via: "Bottom link or inline reference to contributor guide"
      pattern: "CONTRIBUTING.md"

threat_model:
  trust_boundaries:
    - "Contributor-facing markdown files contain no executable code paths"
  stride_register:
    - id: T-34-01
      category: Information_Disclosure
      component: CONTRIBUTING.md
      disposition: accept
      mitigation: "No API keys, tokens, or internal endpoints are listed. The `.env.example` reference is explicit and safe."
    - id: T-34-02
      category: Tampering
      component: CHANGELOG.md
      disposition: accept
      mitigation: "Changelog is hand-curated prose; no code injection surface. Git history provides rollback."

---

<objective>
Overhaul `CONTRIBUTING.md` and add the v0.5.0 entry to `CHANGELOG.md`.

Purpose: The contributor guide and release notes must reflect the v0.5.0 feature set
(GUI editor, two new demos, docs refresh) and the Phase 31 tech-debt closures. This plan
is independent of asset generation, so it can run in parallel with the GIF and screenshot
plans while they prepare the visual artifacts the README plan will embed.

Output: Rewritten `CONTRIBUTING.md` and updated `CHANGELOG.md`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (Phase 34 entry)
@.planning/REQUIREMENTS.md (DOC-02, DOC-03)
@.planning/phases/34-user-facing-docs-refresh/34-CONTEXT.md
@.planning/phases/34-user-facing-docs-refresh/34-RESEARCH.md

@CONTRIBUTING.md
@CHANGELOG.md
@docs/GETTING_STARTED.md
@docs/DEVELOPMENT.md
@pyproject.toml
</context>

<tasks>
  <task id="1">
    <read_first>
      <file>CONTRIBUTING.md</file>
      <file>docs/GETTING_STARTED.md</file>
      <file>docs/DEVELOPMENT.md</file>
      <file>pyproject.toml</file>
    </read_first>
    <action>
      Rewrite CONTRIBUTING.md preserving the Code of Conduct intro and the three-lint-command
      ordering (ruff → black → mypy). Update the dev-setup block to `pip install -e ".[dev,gui]"`.
      Add a "Branch / PR workflow" section, a "GSD workflow overview" section, and an
      "Optional dependency matrix" section derived from pyproject.toml `[project.optional-dependencies]`.
      Keep the existing Pydantic v2, simulator backend, Gymnasium/SB3, and testing convention notes.
    </action>
    <acceptance_criteria>
      `grep -E 'pip install -e ".\[dev,gui\]"|/gsd-discuss-phase|/gsd-plan-phase|/gsd-execute-phase' CONTRIBUTING.md` exits 0.
      `grep -E 'ruff check src/ tests/' CONTRIBUTING.md` exits 0.
      `grep -E 'black --check src/ tests/' CONTRIBUTING.md` exits 0.
      `grep -E 'mypy src/surg_rl' CONTRIBUTING.md` exits 0.
      The optional-dependency matrix contains at least 10 rows.
    </acceptance_criteria>
    <verify>
      `wc -l CONTRIBUTING.md` reports >= 120 lines.
      `grep -c 'docs/GETTING_STARTED.md' CONTRIBUTING.md` reports >= 1.
      `grep -c 'docs/DEVELOPMENT.md' CONTRIBUTING.md` reports >= 1.
    </verify>
  </task>

  <task id="2">
    <read_first>
      <file>CHANGELOG.md</file>
      <file>.planning/ROADMAP.md</file>
    </read_first>
    <action>
      Update the Keep-a-Changelog reference from 1.0.0 to 1.1.0 in the file header.
      Insert a new `[0.5.0]` section immediately below `[Unreleased]`, with `### Added`,
      `### Changed`, and `### Fixed` subsections populated from the Phase 34 scope
      (GUI editor, knot-tying/needle-passing demos, docs refresh, demo narration refactor,
      Phase 31 debt closures).
    </action>
    <acceptance_criteria>
      `grep -E 'Keep-a-Changelog 1\.1\.0' CHANGELOG.md` exits 0.
      `grep -E '^## \[0\.5\.0\]' CHANGELOG.md` exits 0.
      `grep -E '^### Added|^### Changed|^### Fixed' CHANGELOG.md` exits 0.
    </acceptance_criteria>
    <verify>
      `grep -c '\[0\.5\.0\]' CHANGELOG.md` reports >= 1.
      `grep -c 'PySide6\|GUI scene editor\|surg-rl-gui' CHANGELOG.md` reports >= 1.
      `grep -c 'knot-tying\|needle-passing' CHANGELOG.md` reports >= 1.
      `grep -c 'ruff\|fluid_step\|cut cooldown\|mjpython' CHANGELOG.md` reports >= 1.
    </verify>
  </task>
</tasks>

<verification>
  - Run `ruff check CONTRIBUTING.md CHANGELOG.md` if ruff supports markdown; otherwise skip.
  - Run `markdownlint CONTRIBUTING.md CHANGELOG.md 2>/dev/null || true` (optional).
  - Verify no placeholder text such as "TODO", "FIXME", "TBD" remains in either file.
</verification>

<success_criteria>
  - CONTRIBUTING.md has been rewritten with updated dev setup, branch/PR workflow, GSD workflow overview, optional-dep matrix, and retained project conventions.
  - CHANGELOG.md references Keep-a-Changelog 1.1.0 and contains a `[0.5.0]` entry with Added / Changed / Fixed subsections covering all required items.
  - Both files pass a basic grep-based content audit and contain no TODO/FIXME/TBD placeholders.
</success_criteria>
