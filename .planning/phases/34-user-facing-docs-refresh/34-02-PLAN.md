---
phase: 34-user-facing-docs-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - demos/capture_demo_gif.py
  - docs/demos/suturing.gif
  - docs/demos/knot_tying.gif
  - docs/demos/needle_passing.gif
  - docs/demos/.gitkeep
autonomous: true
requirements:
  - DOC-04

must_haves:
  truths:
    - "`docs/demos/suturing.gif`, `docs/demos/knot_tying.gif`, and `docs/demos/needle_passing.gif` exist and are each between 5 and 15 MB"
    - "Each GIF shows a recognizable surgical simulation: instruments, tissue, and at least one visible action (approach / grasp / suture / knot / needle pass)"
    - "`demos/capture_demo_gif.py` is importable and documents the imageio-based capture path; if imageio is unavailable at runtime, a documented ffmpeg fallback produces the three GIFs"
    - "The capture script imports `_omp_compat` and `_platform_guard` before MuJoCo/PyBullet to preserve the Phase 32 macOS mjpython compatibility contract"
    - "The capture script documents `imageio` as the preferred writer and provides a fallback note for environments where only `ffmpeg` is available"
    - "Each GIF is approximately 30 seconds of playback at 8-15 FPS (frame count 240-450)"
  artifacts:
    - path: demos/capture_demo_gif.py
      provides: "Headless demo-to-GIF capture script"
      min_lines: 100
      contains: "_omp_compat|_platform_guard|resolve_scene|imageio|mimsave|SurgicalEnv|ffmpeg"
    - path: docs/demos/suturing.gif
      provides: "~30s suturing demo GIF for README"
      min_size_bytes: 100000
    - path: docs/demos/knot_tying.gif
      provides: "~30s knot-tying demo GIF for README"
      min_size_bytes: 100000
    - path: docs/demos/needle_passing.gif
      provides: "~30s needle-passing demo GIF for README"
      min_size_bytes: 100000
  key_links:
    - from: demos/capture_demo_gif.py
      to: demos/_common.py
      via: "Imports `resolve_scene` and `DEFAULT_TRAINING_CONFIG` from `demos._common`"
      pattern: "from demos._common import"
    - from: demos/capture_demo_gif.py
      to: demos/suturing_demo.py
      via: "Imports `build_suturing_reward` or runs equivalent reward construction from the demo"
      pattern: "suturing_demo|build_suturing_reward"
    - from: docs/demos/suturing.gif
      to: README.md
      via: "Embedded via `![Suturing demo](docs/demos/suturing.gif)`"
      pattern: "docs/demos/suturing.gif"

threat_model:
  trust_boundaries:
    - "CLI argv → `demos/capture_demo_gif.py` → simulator render → output path"
  stride_register:
    - id: T-34-03
      category: Tampering
      component: demos/capture_demo_gif.py
      disposition: mitigate
      mitigation: "Validate `--output` resolves under the repo root; reject paths containing `..` or absolute paths outside the project. Use `Path.resolve()` and assert the output is inside `Path(__file__).resolve().parent.parent`."
    - id: T-34-04
      category: Denial_of_Service
      component: demos/capture_demo_gif.py
      disposition: mitigate
      mitigation: "Cap capture duration at 60s wall-clock and max frames at 600; `--steps` / `--frames` default to 300. Provide a `--backend` override but default to MuJoCo for reliable offscreen rgb_array."

---

<objective>
Generate three ~30-second demo GIFs at `docs/demos/{suturing,knot_tying,needle_passing}.gif`
by running the Phase 32 demos headlessly and capturing `rgb_array` frames.

Purpose: README demo walkthroughs need visual assets. The Phase 32 demos are already
narration-compliant and run headlessly; this plan turns their rendered output into
portable GIFs without requiring external screen-capture tools.

Output: `docs/demos/capture_demo_gif.py` plus three GIF files.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (Phase 34 entry)
@.planning/REQUIREMENTS.md (DOC-04)
@.planning/phases/34-user-facing-docs-refresh/34-CONTEXT.md
@.planning/phases/34-user-facing-docs-refresh/34-RESEARCH.md
@.planning/phases/32-demo-suite-polish/32-01-PLAN.md
@.planning/phases/32-demo-suite-polish/32-02-PLAN.md
@.planning/phases/32-demo-suite-polish/32-03-PLAN.md

@demos/_common.py
@demos/suturing_demo.py
@demos/knot_tying_demo.py
@demos/needle_passing_demo.py
@demos/_omp_compat.py
@demos/_platform_guard.py
@scenes/suturing_demo.json
@scenes/knot_tying_demo.json
@scenes/needle_passing_demo.json
</context>

<tasks>
  <task id="1">
    <read_first>
      <file>demos/_common.py</file>
      <file>demos/suturing_demo.py</file>
      <file>demos/_omp_compat.py</file>
      <file>demos/_platform_guard.py>
    </read_first>
    <action>
      Create `docs/demos/` directory if it does not exist.
      Create `demos/capture_demo_gif.py` that imports `_omp_compat` and `_platform_guard` first,
      then imports `resolve_scene` and `DEFAULT_TRAINING_CONFIG` from `demos._common`, and provides
      a `main()` that loads a task scene, builds the matching reward (reusing helper functions from
      the corresponding demo script where possible), constructs a `SurgicalEnv` with MuJoCo backend,
      steps a PPO-trained or scripted policy for ~300 frames at 10 FPS, collects `rgb_array` frames,
      and writes them to the requested `--output` path using `imageio.mimsave()`.
    </action>
    <acceptance_criteria>
      `python demos/capture_demo_gif.py --help` prints usage and exits 0.
      `python demos/capture_demo_gif.py --task suturing --output docs/demos/suturing.gif --frames 30`
      runs and produces a GIF file with size > 0 bytes.
    </acceptance_criteria>
    <verify>
      `python -c "import demos.capture_demo_gif"` succeeds without importing PySide6.
      `grep -E 'imageio|mimsave|resolve_scene|_omp_compat' demos/capture_demo_gif.py` exits 0.
    </verify>
  </task>

  <task id="2">
    <read_first>
      <file>demos/capture_demo_gif.py</file>
      <file>demos/knot_tying_demo.py</file>
      <file>demos/needle_passing_demo.py</file>
    </read_first>
    <action>
      Run the capture script for each of the three tasks to produce:
      `docs/demos/suturing.gif`, `docs/demos/knot_tying.gif`, `docs/demos/needle_passing.gif`.
      Use `--frames 300` and `--fps 10` as defaults so each GIF is ~30s of playback.
      If a task scene is missing, use the matching `tests/fixtures/scenes/{task}.json` fallback.
      If imageio is not installed, add it under the `[gui]` extra in `pyproject.toml`.
    </action>
    <acceptance_criteria>
      All three `docs/demos/*.gif` files exist and have size between 100 KB and 15 MB.
      Each file opens with `python -c "import imageio; imageio.imread('docs/demos/suturing.gif')"`
      (or PIL) and reports a frame count between 240 and 450.
    </acceptance_criteria>
    <verify>
      `ls -l docs/demos/*.gif` lists three files.
      `python -c "from pathlib import Path; files=[Path('docs/demos/suturing.gif'),Path('docs/demos/knot_tying.gif'),Path('docs/demos/needle_passing.gif')]; print(all(100_000<=f.stat().st_size<=15_000_000 for f in files))"` prints `True`.
    </verify>
  </task>

  <task id="3">
    <read_first>
      <file>demos/capture_demo_gif.py</file>
    </read_first>
    <action>
      Add a regression test in `tests/test_doc_assets.py` that asserts the three GIF files exist,
      are non-empty, and that `demos/capture_demo_gif.py` is syntactically valid.
    </action>
    <acceptance_criteria>
      The new test passes with `pytest tests/test_doc_assets.py -v` or is added to the
      existing `tests/test_demos.py` suite and passes there.
    </acceptance_criteria>
    <verify>
      `pytest tests/test_doc_assets.py -v` exits 0 (or `pytest tests/test_demos.py -v -k gif` exits 0).
    </verify>
  </task>
</tasks>

<verification>
  - Run `pytest tests/test_doc_assets.py -v` (or equivalent) and confirm all GIF checks pass.
  - Visually inspect each GIF by opening it in an image viewer or with `python -c "from PIL import Image; ..."` to confirm it contains recognisable simulation frames.
</verification>

<success_criteria>
  - `docs/demos/capture_demo_gif.py` exists and is importable without PySide6.
  - `docs/demos/{suturing,knot_tying,needle_passing}.gif` exist, are each 100 KB–15 MB, and contain 240–450 frames.
  - A regression test verifies the capture script smoke-run and the GIF file presence.
</success_criteria>
