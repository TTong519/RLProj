---
phase: 34-user-facing-docs-refresh
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/gui/viewport.png
  - docs/gui/tree_form.png
  - docs/gui/llm_panel.png
  - docs/gui/.gitkeep
autonomous: true
requirements:
  - DOC-05

must_haves:
  truths:
    - "`docs/gui/viewport.png`, `docs/gui/tree_form.png`, and `docs/gui/llm_panel.png` exist and are each between 5 and 50 KB"
    - "Each PNG is a valid image with height >= 200 pixels (tree/form panel may be narrow; viewport and LLM panel are wide)"
    - "The source files at `tests/gui/screenshots/{viewport,tree_form,llm_panel}.png` are copied to `docs/gui/` (not moved) so the Phase 33 test suite continues to pass"
    - "`docs/gui/` directory is created if missing"
  artifacts:
    - path: docs/gui/viewport.png
      provides: "GUI viewport screenshot for README"
      min_size_bytes: 5000
    - path: docs/gui/tree_form.png
      provides: "GUI tree/form editor screenshot for README"
      min_size_bytes: 5000
    - path: docs/gui/llm_panel.png
      provides: "GUI LLM panel screenshot for README"
      min_size_bytes: 5000
  key_links:
    - from: docs/gui/viewport.png
      to: tests/gui/screenshots/viewport.png
      via: "Copied from Phase 33 smoke-test screenshot"
      pattern: "N/A - file copy"
    - from: docs/gui/viewport.png
      to: README.md
      via: "Embedded via `![GUI viewport](docs/gui/viewport.png)`"
      pattern: "docs/gui/viewport.png"

threat_model:
  trust_boundaries:
    - "File copy operation inside the repo; no untrusted input"
  stride_register:
    - id: T-34-05
      category: Tampering
      component: docs/gui/*.png
      disposition: accept
      mitigation: "PNG files are static screenshots produced by Phase 33 QWidget.grab(); read-only after copy. Git history preserves originals."

---

<objective>
Publish the Phase 33 GUI screenshots to `docs/gui/` so the README can embed them.

Purpose: Phase 33 already captured screenshots via `QWidget.grab()` at
`tests/gui/screenshots/`. This plan copies them to the public `docs/gui/` location
expected by the README and verifies they are valid PNG images.

Output: `docs/gui/{viewport,tree_form,llm_panel}.png`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (Phase 34 entry)
@.planning/REQUIREMENTS.md (DOC-05)
@.planning/phases/34-user-facing-docs-refresh/34-CONTEXT.md
@.planning/phases/33-pyside6-scene-editor/33-07-PLAN.md

@tests/gui/screenshots/viewport.png
@tests/gui/screenshots/tree_form.png
@tests/gui/screenshots/llm_panel.png
</context>

<tasks>
  <task id="1">
    <read_first>
      <file>tests/gui/screenshots/viewport.png</file>
      <file>tests/gui/screenshots/tree_form.png</file>
      <file>tests/gui/screenshots/llm_panel.png</file>
    </read_first>
    <action>
      Create `docs/gui/` directory. Copy (do not move) the three PNG files from
      `tests/gui/screenshots/` to `docs/gui/`. Preserve file names.
    </action>
    <acceptance_criteria>
      `ls docs/gui/` lists `viewport.png`, `tree_form.png`, and `llm_panel.png`.
      `ls tests/gui/screenshots/` still lists the original three files.
    </acceptance_criteria>
    <verify>
      `python -c "from pathlib import Path; src=Path('tests/gui/screenshots'); dst=Path('docs/gui'); print(all((src/f).exists() and (dst/f).exists() for f in ['viewport.png','tree_form.png','llm_panel.png']))"` prints `True`.
    </verify>
  </task>

  <task id="2">
    <read_first>
      <file>docs/gui/viewport.png</file>
      <file>docs/gui/tree_form.png</file>
      <file>docs/gui/llm_panel.png</file>
    </read_first>
    <action>
      Add a regression test in `tests/test_doc_assets.py` (or extend `tests/test_gui_scaffold.py`)
      that asserts each `docs/gui/*.png` is a valid PNG with width and height >= 200 px.
    </action>
    <acceptance_criteria>
      `pytest tests/test_doc_assets.py -v -k gui_screenshot` exits 0.
    </acceptance_criteria>
    <verify>
      `python -c "from pathlib import Path; import struct; sizes=[struct.unpack('>II', (Path(f'docs/gui/{n}.png').read_bytes()[data.find(b'IHDR')+4:data.find(b'IHDR')+12])) for n,data in [(n,Path(f'docs/gui/{n}.png').read_bytes()) for n in ['viewport','tree_form','llm_panel']]]; print(all(h>=200 for _,h in sizes))"` prints `True`.
    </verify>
  </task>
</tasks>

<verification>
  - Run the screenshot regression test.
  - Open each PNG with `python -c "from PIL import Image; Image.open('docs/gui/viewport.png').show()"` (optional) to visually confirm content.
</verification>

<success_criteria>
  - `docs/gui/{viewport,tree_form,llm_panel}.png` exist and are valid PNGs with height >= 200 px.
  - Source files in `tests/gui/screenshots/` remain in place.
  - A regression test guards the public docs/gui screenshots.
</success_criteria>
