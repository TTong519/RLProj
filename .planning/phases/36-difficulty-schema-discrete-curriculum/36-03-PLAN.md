---
phase: 36-difficulty-schema-discrete-curriculum
plan: 03
type: tdd
wave: 3
depends_on:
  - 02
files_modified:
  - src/surg_rl/dynamics/curriculum.py
  - tests/test_discrete_curriculum.py
autonomous: true
requirements:
  - TASK-07
  - TASK-09
user_setup: []

must_haves:
  truths:
    - "CurriculumScheduler has an additive progression_mode (default 'continuous') that, when set to 'discrete', advances EASY→MEDIUM→HARD via set_difficulty_level / advance_level"
    - "advance_level at HARD returns False (terminal — D-12); set_difficulty_level sets _current_level directly (manual override)"
    - "advance_level reuses the success-rate gate via the extracted _meets_success_threshold helper (corrected D-11), called with curriculum_config.min_success_rate — no new episode-count-per-level config field"
    - "The continuous advance_stage path is byte-identical to v0.5.0 (advance_stage / _should_advance observable output unchanged; _should_advance refactored to call _meets_success_threshold(stage_cfg.success_threshold) as a pure refactor)"
    - "current_difficulty branches on progression_mode: 'continuous' returns the existing stage scalar; 'discrete' returns float(self._current_level.value)"
    - "The full v0.4.0 + v0.4.2 curriculum + difficulty test suite passes unchanged (additive-regression gate — TASK-09 / SC#4)"
  artifacts:
    - path: "src/surg_rl/dynamics/curriculum.py"
      provides: "progression_mode + discrete_config fields on CurriculumConfig; _current_level state; set_difficulty_level / advance_level / _meets_success_threshold methods; current_difficulty mode branch"
      contains: "def advance_level"
    - path: "tests/test_discrete_curriculum.py"
      provides: "TDD RED→GREEN gate for discrete progression + advance_stage parity + current_difficulty mode branch"
      contains: "test_advance_stage_unchanged"
  key_links:
    - from: "src/surg_rl/dynamics/curriculum.py"
      to: "src/surg_rl/dynamics/difficulty_wiring.py"
      via: "late bottom-of-file import of DiscreteCurriculumConfig to bind the forward ref on CurriculumConfig.discrete_config"
      pattern: "from surg_rl\\.dynamics\\.difficulty_wiring import"
    - from: "tests/test_discrete_curriculum.py"
      to: "src/surg_rl/dynamics/curriculum.py"
      via: "from surg_rl.dynamics.curriculum import CurriculumConfig, CurriculumScheduler"
      pattern: "from surg_rl\\.dynamics\\.curriculum import"
---

<objective>
Add the discrete EASY→MEDIUM→HARD progression axis to `CurriculumScheduler` as a strictly additive `progression_mode`, while leaving the validated continuous `advance_stage` path byte-identical to v0.5.0. Lands the corrected D-11 shared success-rate helper (`_meets_success_threshold`), the new `set_difficulty_level` / `advance_level` methods, the `current_difficulty` mode branch, and the `progression_mode` + `discrete_config` fields on `CurriculumConfig`. Verified by a TDD RED→GREEN test file plus a continuous-path parity test that anchors SC#3 / SC#4.

Purpose: This is the last dependency in the leaf→wiring→curriculum chain. After it lands, the phase gate (`PYTHONPATH=src pytest tests/ -v`) is green and the additive-regression gate (TASK-09) is satisfied.
Output: `src/surg_rl/dynamics/curriculum.py` edited additively; `tests/test_discrete_curriculum.py` created with passing discrete + parity tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-difficulty-schema-discrete-curriculum/36-CONTEXT.md
@.planning/phases/36-difficulty-schema-discrete-curriculum/36-RESEARCH.md
@.planning/phases/36-difficulty-schema-discrete-curriculum/36-PATTERNS.md
@.planning/phases/36-difficulty-schema-discrete-curriculum/36-VALIDATION.md
@.planning/phases/36-difficulty-schema-discrete-curriculum/36-01-SUMMARY.md
@.planning/phases/36-difficulty-schema-discrete-curriculum/36-02-SUMMARY.md
@src/surg_rl/dynamics/curriculum.py
@src/surg_rl/scene_definition/schema.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Failing tests for discrete progression + advance_stage parity + current_difficulty mode branch</name>
  <files>tests/test_discrete_curriculum.py</files>
  <read_first>
    - tests/test_dynamics.py (lines 336-484 — TestCurriculumScheduler analog: construction, advance_stage assertions, auto-advancement shape; copy the suite-parity idiom)
    - src/surg_rl/dynamics/curriculum.py (current_difficulty lines 206-210; advance_stage lines 228-242; _should_advance lines 470-508; CurriculumConfig dataclass lines 64-86; __init__ lines 163-199 — the surfaces the new tests assert against)
    - .planning/phases/36-difficulty-schema-discrete-curriculum/36-RESEARCH.md (Pitfall 3 _meets_success_threshold extraction; Validation Architecture SC#3 test design)
    - .planning/phases/36-difficulty-schema-discrete-curriculum/36-PATTERNS.md (File 5 analog — test_discrete_curriculum.py shape from test_dynamics.py:336-484)
  </read_first>
  <behavior>
    - set_difficulty_level: scheduler.set_difficulty_level(DifficultyLevel.HARD) sets _current_level to HARD and current_difficulty returns 1.0 in discrete mode.
    - advance_level transitions: in discrete mode, advance_level walks EASY→MEDIUM→HARD returning True at each non-terminal step and False at HARD (D-12). current_difficulty returns 0.0 / 0.5 / 1.0 at each step.
    - current_difficulty mode branch: with progression_mode="continuous" (default), current_difficulty returns the existing stage scalar (0.25 at EASY stage — the v0.5.0 value); with progression_mode="discrete", current_difficulty returns float(_current_level.value).
    - advance_stage parity (SC#3 / SC#4): in continuous mode, advance_stage transitions EASY→MEDIUM→HARD→False identically to test_dynamics.py::TestCurriculumScheduler::test_stage_progression; the existing test_dynamics.py suite stays green unchanged.
    - _meets_success_threshold (corrected D-11): feeding a performance history with success_rate >= curriculum_config.min_success_rate returns True; below returns False. (Covered via advance_level auto-advance behavior OR a direct unit test of the helper.)
  </behavior>
  <action>
    Create tests/test_discrete_curriculum.py. Module docstring: "Tests for the additive discrete progression_mode on CurriculumScheduler (Phase 36, TASK-07 / SC#3) and the advance_stage continuous-path parity gate (SC#4)."

    Imports: `import pytest`, `from surg_rl.dynamics.curriculum import CurriculumConfig, CurriculumScheduler, CurriculumStage`, `from surg_rl.rl.difficulty import DifficultyLevel`. Verify the exact import names by reading tests/test_dynamics.py:1-30 at implement time.

    Class `TestDiscreteProgression` with:
    - test_init_discrete_defaults: `cfg = CurriculumConfig(progression_mode="discrete")`; `scheduler = CurriculumScheduler(curriculum_config=cfg)`; assert `scheduler._current_level == DifficultyLevel.EASY`; assert `scheduler.current_difficulty == 0.0`.
    - test_set_difficulty_level: scheduler (discrete mode) → `scheduler.set_difficulty_level(DifficultyLevel.HARD)` → assert `scheduler._current_level == DifficultyLevel.HARD` and `scheduler.current_difficulty == 1.0`.
    - test_advance_level_transitions: scheduler (discrete mode) → assert `scheduler.advance_level() is True` and `_current_level == DifficultyLevel.MEDIUM` and `current_difficulty == 0.5`; assert `scheduler.advance_level() is True` and `_current_level == DifficultyLevel.HARD` and `current_difficulty == 1.0`; assert `scheduler.advance_level() is False` (D-12 terminal) and `_current_level == DifficultyLevel.HARD`.
    - test_current_difficulty_mode_branch_continuous: default `CurriculumConfig()` (progression_mode="continuous") → `scheduler = CurriculumScheduler()` → assert `scheduler.current_difficulty == 0.25` (the v0.5.0 EASY stage scalar).
    - test_advance_level_auto_advances_on_success_rate: `cfg = CurriculumConfig(progression_mode="discrete", auto_advance=True, advancement_window=10, min_success_rate=0.7)`; scheduler; call `scheduler.start()`; for enough episodes call `scheduler.reset()` + `scheduler.episode_end({"success": 1, "reward": 100}, simulator=None)`; assert `_current_level` eventually advances beyond EASY. (Mirror test_dynamics.py:393-411 auto-advancement shape.)

    Class `TestAdvanceStageParity` with:
    - test_advance_stage_unchanged: construct a default continuous `CurriculumScheduler()` and assert the full EASY→MEDIUM→HARD→(EXPERT→)False transition sequence and `current_difficulty` scalars match the v0.5.0 baseline captured in `test_dynamics.py::TestCurriculumScheduler::test_stage_progression` (read that test at implement time and assert the same stage + scalar sequence). This is the SC#3 continuous-path parity anchor.

    Run `PYTHONPATH=src pytest tests/test_discrete_curriculum.py -v` — tests MUST fail (RED) because `progression_mode`, `set_difficulty_level`, `advance_level`, `_current_level` do not yet exist (AttributeError / TypeError). Commit: `test(36-03): add failing RED tests for discrete progression + advance_stage parity`.
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/test_discrete_curriculum.py -v 2>&1 | grep -E "(AttributeError|TypeError)" exits 0; PYTHONPATH=src pytest tests/test_dynamics.py -v exits 0 (existing suite still green — no edits)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class TestDiscreteProgression' tests/test_discrete_curriculum.py` returns 1
    - `grep -c 'class TestAdvanceStageParity' tests/test_discrete_curriculum.py` returns 1
    - `grep -c 'test_advance_level_transitions' tests/test_discrete_curriculum.py` returns 1
    - `grep -c 'test_advance_stage_unchanged' tests/test_discrete_curriculum.py` returns 1
    - `grep -c 'test_current_difficulty_mode_branch_continuous' tests/test_discrete_curriculum.py` returns 1
    - `PYTHONPATH=src pytest tests/test_discrete_curriculum.py -v` exits non-zero (RED — missing attributes)
    - `PYTHONPATH=src pytest tests/test_dynamics.py -v` exits 0 (existing suite untouched)
  </acceptance_criteria>
  <done>Discrete + parity tests added and RED; existing test_dynamics.py suite still GREEN; commit landed as `test(36-03): ...`.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Add progression_mode + discrete level methods to CurriculumScheduler (additive)</name>
  <files>src/surg_rl/dynamics/curriculum.py</files>
  <read_first>
    - src/surg_rl/dynamics/curriculum.py (FULL READ — current_difficulty lines 206-210; advance_stage lines 228-242; _should_advance lines 470-508; _should_regress lines 564-568 for hysteresis idiom; CurriculumConfig dataclass lines 64-86; __init__ lines 163-199; top imports lines 1-23)
    - src/surg_rl/scene_definition/schema.py (lines 5, 12-19, 1491-1506 — `from __future__ import annotations` + late-import + model_rebuild pattern reference; NOTE curriculum.py is a dataclass not Pydantic so no model_rebuild call — Pitfall 4)
    - .planning/phases/36-difficulty-schema-discrete-curriculum/36-RESEARCH.md (Pitfall 3 _meets_success_threshold extraction recipe; Pitfall 4 dataclass field syntax; Pattern 1 cycle pattern)
    - .planning/phases/36-difficulty-schema-discrete-curriculum/36-PATTERNS.md (File 3 analog — exact excerpts for current_difficulty / advance_stage / _should_advance / CurriculumConfig / __init__ / model_rebuild adaptation)
    - .planning/phases/36-difficulty-schema-discrete-curriculum/36-02-SUMMARY.md (DiscreteCurriculumConfig location + signature from Plan 02)
  </read_first>
  <behavior>
    - All RED tests in tests/test_discrete_curriculum.py flip to GREEN (exit 0).
    - `progression_mode` defaults to "continuous"; `discrete_config` defaults to None (v0.5.0 advance_stage output byte-identical).
    - advance_level walks EASY→MEDIUM→HARD→False (D-12) using the shared `_meets_success_threshold(curriculum_config.min_success_rate)` gate (corrected D-11).
    - `_should_advance` observable output unchanged (pure refactor: it now calls `_meets_success_threshold(stage_cfg.success_threshold)`).
    - `current_difficulty` returns the existing stage scalar in continuous mode and `float(self._current_level.value)` in discrete mode.
    - No Pydantic cross-package cycle; `from __future__ import annotations` added at top; late bottom-of-file import binds `DiscreteCurriculumConfig` for the forward-ref annotation on `CurriculumConfig.discrete_config`.
  </behavior>
  <action>
    Edit src/surg_rl/dynamics/curriculum.py additively. Step-by-step:

    1. Add `from __future__ import annotations` as the FIRST line of the file (above the module docstring is fine — mirror schema.py:5). Add `Literal` to the existing `from typing import Any` import line so it becomes `from typing import Any, Literal`. Add `Optional` there too if not already present.

    2. In the `@dataclass` `CurriculumConfig` (lines 64-86), add two new fields AFTER the existing `stage_configs` field using DATACLASS field syntax (Pitfall 4 — NOT pydantic.Field):
       - `progression_mode: Literal["continuous", "discrete"] = "continuous"`
       - `discrete_config: Optional["DiscreteCurriculumConfig"] = None`
       Default "continuous" + None keeps v0.5.0 advance_stage output byte-identical (SC#4).

    3. In `CurriculumScheduler.__init__` (lines 163-199), add alongside the existing `_current_stage` / `_stage_entry_episode` initialization:
       - `self._current_level: DifficultyLevel = DifficultyLevel.EASY`
       - `self._level_entry_episode: int = 0`
       Mirror the `_stage_entry_episode` pattern at line 186. Do NOT touch `_current_stage` initialization.

    4. Refactor `_should_advance` (lines 470-508) as a PURE refactor: extract the success-rate computation (slice `self._performance_history[-self.curriculum_config.advancement_window :]`, compute `success_rate = sum(m.get("success", 0) for m in recent) / len(recent)`, compare `success_rate >= threshold`) into a new method `_meets_success_threshold(self, threshold: float) -> bool` that returns False if the recent slice is empty, else `success_rate >= threshold`. Then `_should_advance` calls `self._meets_success_threshold(stage_cfg.success_threshold)` in place of the inline computation — its observable output MUST be unchanged (regression-safe; Pitfall 3). Do NOT change the episode_threshold / _stage_order checks in _should_advance — only the success-rate comparison is extracted.

    5. Add `current_difficulty` mode branch (lines 206-210): keep the existing body as the "continuous" branch; add `if self.curriculum_config.progression_mode == "discrete": return float(self._current_level.value)` as the FIRST branch of the property, then the existing body runs for "continuous". Do NOT edit the continuous body's logic.

    6. Add new methods (additive, near `advance_stage`):
       - `def set_difficulty_level(self, level: DifficultyLevel) -> None:` that sets `self._current_level = level` and `self._level_entry_episode = self._episode` (D-12 manual override).
       - `def advance_level(self) -> bool:` mirroring `advance_stage`'s structure (lines 228-242) but operating on `_level_order = [DifficultyLevel.EASY, DifficultyLevel.MEDIUM, DifficultyLevel.HARD]` and `self._current_level` (D-10 — separate state, never share `_current_stage`). If `_current_level == DifficultyLevel.HARD`: return False (D-12 terminal). Else if `self.curriculum_config.auto_advance` is False: return False. Else if `self._meets_success_threshold(self.curriculum_config.min_success_rate)` (corrected D-11 — NOT a direct call to the stage-coupled `_should_advance`): advance to the next level (`self._level_entry_episode = self._episode`, `self._current_level = next_level`), return True. Else return False.

    7. At the BOTTOM of curriculum.py (after all class definitions, mirroring schema.py:1491-1506), add a late import block to bind the `DiscreteCurriculumConfig` forward ref used by `CurriculumConfig.discrete_config`:
       `from surg_rl.dynamics.difficulty_wiring import DiscreteCurriculumConfig  # noqa: E402, F401`
       Do NOT call `model_rebuild()` — `CurriculumConfig` is a stdlib `@dataclass`, not a Pydantic BaseModel (Pitfall 4 / 36-PATTERNS.md File 3 nuance). The string forward ref resolves at runtime via PEP 563 (`from __future__ import annotations`) + this late binding. `DiscreteCurriculumConfig` itself needs no rebuild (its `levels` field references only DifficultyLevel + DifficultyLevelConfig, both eagerly imported in difficulty_wiring.py).

    Do NOT edit `advance_stage`, `_should_regress`, `CurriculumStage`, `CurriculumStageConfig`, or any continuous-path scalar. Do NOT edit rewards.py or task_reward_router.py. Run `PYTHONPATH=src pytest tests/test_discrete_curriculum.py tests/test_difficulty_config.py tests/test_difficulty_levels.py tests/test_dynamics.py -v` — ALL MUST pass (GREEN). Commit: `feat(36-03): add additive progression_mode + set/advance_level to CurriculumScheduler`.
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/test_discrete_curriculum.py tests/test_difficulty_config.py tests/test_difficulty_levels.py tests/test_dynamics.py -v exits 0; PYTHONPATH=src pytest tests/ -v exits 0</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 src/surg_rl/dynamics/curriculum.py` returns `from __future__ import annotations`
    - `grep -c 'progression_mode: Literal' src/surg_rl/dynamics/curriculum.py` returns 1
    - `grep -c 'discrete_config: Optional' src/surg_rl/dynamics/curriculum.py` returns 1
    - `grep -c 'def set_difficulty_level' src/surg_rl/dynamics/curriculum.py` returns 1
    - `grep -c 'def advance_level' src/surg_rl/dynamics/curriculum.py` returns 1
    - `grep -c 'def _meets_success_threshold' src/surg_rl/dynamics/curriculum.py` returns 1
    - `grep -c '_meets_success_threshold(stage_cfg.success_threshold)' src/surg_rl/dynamics/curriculum.py` returns 1 (pure refactor of _should_advance)
    - `grep -c '_meets_success_threshold(self.curriculum_config.min_success_rate)' src/surg_rl/dynamics/curriculum.py` returns 1 (corrected D-11 in advance_level)
    - `grep -c 'from surg_rl.dynamics.difficulty_wiring import DiscreteCurriculumConfig' src/surg_rl/dynamics/curriculum.py` returns 1 (late import)
    - `grep -c 'model_rebuild' src/surg_rl/dynamics/curriculum.py` returns 0 (Pitfall 4 — no model_rebuild on dataclass)
    - `PYTHONPATH=src pytest tests/test_discrete_curriculum.py -v` exits 0 (GREEN — all discrete + parity tests pass)
    - `PYTHONPATH=src pytest tests/test_dynamics.py -v` exits 0 (additive-regression gate — continuous path unchanged, TASK-09 / SC#4)
    - `PYTHONPATH=src pytest tests/test_difficulty_levels.py -v` exits 0 (v0.4.0+v0.4.2 difficulty suite unchanged)
    - `PYTHONPATH=src pytest tests/ -v` exits 0 (full phase gate green)
    - `python -c "import surg_rl.dynamics.curriculum, surg_rl.scene_definition.schema, surg_rl.dynamics.difficulty_wiring"` exits 0 (no Pydantic cross-package cycle — SC#5)
  </acceptance_criteria>
  <done>All discrete + parity tests GREEN; existing v0.4.0+v0.4.2 suite GREEN; full suite GREEN; no cycle; advance_stage byte-identical; commit landed as `feat(36-03): ...`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config → scheduler state | progression_mode + discrete_config cross from authored config into scheduler state; _current_level is settable via set_difficulty_level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-06 | Tampering | set_difficulty_level(level) | accept | Manual override of _current_level is a researcher-facing control, not an untrusted input; level is a DifficultyLevel enum (Pydantic-validated upstream in Phase 37 scene JSON; in-phase callers pass the enum directly) |
| T-36-07 | Denial of Service | CurriculumConfig.discrete_config forward ref + late import | mitigate | `from __future__ import annotations` + bottom-of-file late import of DiscreteCurriculumConfig (Pitfall 4 / Pattern 1) — no model_rebuild on dataclass; verified by import-both-modules acceptance check |
| T-36-08 | Tampering | advance_level success-rate gate | mitigate | corrected D-11: uses shared `_meets_success_threshold(curriculum_config.min_success_rate)` — does NOT inherit continuous stage's episode_threshold/success_threshold (Pitfall 3) |
| T-36-09 | Tampering | continuous advance_stage path | mitigate | additive-only edits; _should_advance is a pure refactor (extracted helper, observable output unchanged); verified by test_dynamics.py + test_advance_stage_unchanged parity (SC#4) |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | No package installs — only stdlib (dataclasses, typing, Literal) + already-pinned pydantic |
</threat_model>

<verification>
- `PYTHONPATH=src pytest tests/test_discrete_curriculum.py -v` exits 0 (SC#3 covered).
- `PYTHONPATH=src pytest tests/test_dynamics.py tests/test_difficulty_levels.py -v` exits 0 (SC#4 / TASK-09 additive-regression gate).
- `PYTHONPATH=src pytest tests/ -v` exits 0 (full phase gate green).
- `python -c "import surg_rl.dynamics.curriculum, surg_rl.scene_definition.schema, surg_rl.dynamics.difficulty_wiring"` exits 0 (SC#5 — no Pydantic cross-package cycle).
</verification>

<success_criteria>
- CurriculumScheduler has additive `progression_mode` + `set_difficulty_level` / `advance_level` with EASY→MEDIUM→HARD→False transitions (SC#3, TASK-07).
- The continuous `advance_stage` path is byte-identical to v0.5.0 (parity test + existing test_dynamics.py suite green) (SC#3, SC#4, TASK-09).
- `_meets_success_threshold` extracted as a pure refactor; `advance_level` uses `curriculum_config.min_success_rate` (corrected D-11).
- `current_difficulty` branches on progression_mode (SC#3).
- Full v0.4.0 + v0.4.2 curriculum + difficulty suite passes unchanged (SC#4, TASK-09).
- No Pydantic cross-package cycle (SC#5).
</success_criteria>

<output>
Create `.planning/phases/36-difficulty-schema-discrete-curriculum/36-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces (Plan 03)

- `CurriculumConfig.progression_mode` (dataclass field, `src/surg_rl/dynamics/curriculum.py`) — `Literal["continuous", "discrete"] = "continuous"`
- `CurriculumConfig.discrete_config` (dataclass field, `src/surg_rl/dynamics/curriculum.py`) — `Optional["DiscreteCurriculumConfig"] = None` (forward ref resolved via late import + PEP 563)
- `CurriculumScheduler._current_level` (instance attribute) — `DifficultyLevel.EASY` default
- `CurriculumScheduler._level_entry_episode` (instance attribute) — `int = 0`
- `CurriculumScheduler.set_difficulty_level` (method) — signature `(self, level: DifficultyLevel) -> None`
- `CurriculumScheduler.advance_level` (method) — signature `(self) -> bool`; EASY→MEDIUM→HARD→False (D-12)
- `CurriculumScheduler._meets_success_threshold` (method) — signature `(self, threshold: float) -> bool`; shared success-rate gate (corrected D-11)
- `CurriculumScheduler.current_difficulty` (modified property) — adds `progression_mode == "discrete"` branch returning `float(self._current_level.value)`; continuous branch unchanged
- late bottom-of-file import `from surg_rl.dynamics.difficulty_wiring import DiscreteCurriculumConfig` in `curriculum.py`
- `tests/test_discrete_curriculum.py` (new test file path) — `TestDiscreteProgression`, `TestAdvanceStageParity` classes