---
phase: 36-difficulty-schema-discrete-curriculum
plan: 02
type: tdd
wave: 2
depends_on:
  - 01
files_modified:
  - src/surg_rl/dynamics/difficulty_wiring.py
  - tests/test_difficulty_config.py
autonomous: true
requirements:
  - TASK-06
user_setup: []

must_haves:
  truths:
    - "ABSTRACT_TO_CONCRETE maps each of the 6 task_type literals to its per-abstract-field concrete PARAM_BOUNDS key, exactly matching D-05 (corrected D-03: keyed by task_type, not TaskConfig.name)"
    - "compose_difficulty_overrides computes interpolate_params(level.value) first, then replaces ONLY the mapped concrete key for each SET override field with the absolute override value (D-06); unoverridden keys retain the interpolated value"
    - "An override field with no mapping for the loaded task logs a warning and keeps the interpolated value (D-04 — does not raise)"
    - "DiscreteCurriculumConfig is a Pydantic BaseModel with levels: dict[DifficultyLevel, DifficultyLevelConfig] defaulting to an empty dict (pure interpolate_params baseline)"
    - "dynamics.difficulty_wiring imports only DifficultyLevel + DifficultyLevelConfig from rl.difficulty (one-way edge; no cycle; no reward-surface edits)"
    - "D-01: the four override field names are abstract aliases — only tissue_stiffness and time_limit coincide with real PARAM_BOUNDS keys (and not on every task); target_precision_tolerance and tool_position_noise match no task's keys. ABSTRACT_TO_CONCRETE resolves each abstract name to the concrete PARAM_BOUNDS key for the loaded task"
    - "D-02: the mapping is a pure-data dict in the non-leaf wiring module (src/surg_rl/dynamics/difficulty_wiring.py), co-located with DiscreteCurriculumConfig, keeping DifficultyLevelConfig a true import-free leaf"
  artifacts:
    - path: "src/surg_rl/dynamics/difficulty_wiring.py"
      provides: "ABSTRACT_TO_CONCRETE registry dict + DiscreteCurriculumConfig Pydantic model + compose_difficulty_overrides helper"
      contains: "ABSTRACT_TO_CONCRETE"
    - path: "tests/test_difficulty_config.py"
      provides: "Truth-table test (SC#2) + unmapped-override-warns test (D-04) — extends Plan 01's file"
      contains: "test_compose_truth_table"
  key_links:
    - from: "src/surg_rl/dynamics/difficulty_wiring.py"
      to: "src/surg_rl/rl/difficulty.py"
      via: "from surg_rl.rl.difficulty import DifficultyLevel, DifficultyLevelConfig"
      pattern: "from surg_rl\\.rl\\.difficulty import"
    - from: "src/surg_rl/dynamics/difficulty_wiring.py"
      to: "src/surg_rl/rl/rewards.py"
      via: "reward_cls.interpolate_params(level.value) — read-only call on the passed-in reward class"
      pattern: "interpolate_params\\("
    - from: "tests/test_difficulty_config.py"
      to: "src/surg_rl/rl/task_reward_router.py"
      via: "TASK_REWARD_REGISTRY resolves reward class per task_type in the truth-table test"
      pattern: "TASK_REWARD_REGISTRY"
---

<objective>
Land the wiring layer that turns `DifficultyLevelConfig` from a passive schema into an additive composition over `interpolate_params()`. Creates `dynamics/difficulty_wiring.py` holding the D-05 `ABSTRACT_TO_CONCRETE` registry (keyed by `task_type` per the corrected D-03), the `DiscreteCurriculumConfig` Pydantic wrapper, and the `compose_difficulty_overrides` helper. Verified by a parametrized truth-table test that asserts SC#2: overriding one field changes only that field; unoverridden fields retain the interpolated value; empty `levels` == pure interpolation.

Purpose: This module is the one-way bridge from the leaf to the scheduler. It owns every piece of logic that needs both the override model and the reward-class `interpolate_params` baseline, so neither the leaf nor `curriculum.py` has to know about reward classes.
Output: `src/surg_rl/dynamics/difficulty_wiring.py` created; `tests/test_difficulty_config.py` extended with passing truth-table + D-04 tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-difficulty-schema-discrete-curriculum/36-CONTEXT.md
@.planning/phases/36-difficulty-schema-discrete-curriculum/36-RESEARCH.md
@.planning/phases/36-difficulty-schema-discrete-curriculum/36-PATTERNS.md
@.planning/phases/36-difficulty-schema-discrete-curriculum/36-VALIDATION.md
@.planning/phases/36-difficulty-schema-discrete-curriculum/36-01-SUMMARY.md
@src/surg_rl/rl/difficulty.py
@src/surg_rl/rl/task_reward_router.py
@src/surg_rl/rl/rewards.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Failing truth-table + unmapped-override-warns tests</name>
  <files>tests/test_difficulty_config.py</files>
  <read_first>
    - tests/test_difficulty_config.py (current state from Plan 01 — append new classes; do not edit existing tests)
    - src/surg_rl/rl/task_reward_router.py (TASK_REWARD_REGISTRY — the 6 task_type keys + reward class mapping; use to resolve reward_cls in the truth-table test)
    - src/surg_rl/rl/rewards.py (PARAM_BOUNDS + interpolate_params classmethod — the additive baseline; DO NOT EDIT)
    - .planning/phases/36-difficulty-schema-discrete-curriculum/36-RESEARCH.md (D-05 mapping table; Validation Architecture SC#2 truth-table design; Open Q3 recommendation to use TASK_REWARD_REGISTRY in tests)
    - .planning/phases/36-difficulty-schema-discrete-curriculum/36-PATTERNS.md (File 4 analog — parametrize shape from test_difficulty_levels.py:83-167)
  </read_first>
  <behavior>
    - Truth table (SC#2): for each (task_type, level in {EASY,MEDIUM,HARD}, single-field-override) tuple, the composed dict differs from pure reward_cls.interpolate_params(level.value) ONLY on the mapped concrete key for that overridden abstract field; every other key is equal. The override value is the absolute replacement (D-06), not a delta.
    - Empty levels (SC#2): compose_difficulty_overrides(task_type, level, config=DifficultyLevelConfig() with all None, reward_cls) == reward_cls.interpolate_params(level.value) (pure interpolation baseline).
    - D-04 unmapped override: setting tissue_stiffness on a suturing scene (which has no tissue_stiffness mapping per D-05) logs a warning and the composed dict equals pure interpolation for that field — no raise, no KeyError.
    - D-05 coverage: the truth table exercises all 6 task_type keys (suturing, dissection, needle_insertion, knot_tying, grasping, cutting) and all 4 abstract fields.
  </behavior>
  <action>
    Extend tests/test_difficulty_config.py with two new test classes (do NOT modify Plan 01's existing classes).

    Imports to add at the top of the file (alongside Plan 01's imports): `import warnings`, `from surg_rl.dynamics.difficulty_wiring import ABSTRACT_TO_CONCRETE, DiscreteCurriculumConfig, compose_difficulty_overrides`, `from surg_rl.rl.task_reward_router import TASK_REWARD_REGISTRY`. Import all 6 reward classes from `surg_rl.rl.rewards` (SuturingReward, DissectionReward, NeedlePassingReward, KnotTyingReward, GraspingReward, CuttingReward).

    Class `TestComposeDifficultyOverrides` with:
    - `test_compose_truth_table`: a `@pytest.mark.parametrize` over `(task_type, level, abstract_field, override_value)` tuples covering all 6 task_types × {DifficultyLevel.EASY, DifficultyLevel.MEDIUM, DifficultyLevel.HARD} × each abstract field that has a mapping for that task_type (per D-05). For each tuple: look up `reward_cls = TASK_REWARD_REGISTRY[task_type]`; compute `baseline = reward_cls.interpolate_params(level.value)`; build `cfg = DifficultyLevelConfig(**{abstract_field: override_value})`; call `composed = compose_difficulty_overrides(task_type, level, cfg, reward_cls)`; look up the expected concrete key via `ABSTRACT_TO_CONCRETE[task_type][abstract_field]`; assert `composed[concrete_key] == override_value`; assert for every other key in baseline, `composed[key] == baseline[key]`. Use override values inside the verified D-07 bounds (e.g. tissue_stiffness=120.0, target_precision_tolerance=0.02, tool_position_noise=0.04, time_limit=90.0).
    - `test_compose_empty_levels_equals_interpolation`: for a parametrized (task_type, level), build `cfg = DifficultyLevelConfig()` (all None) and assert `compose_difficulty_overrides(task_type, level, cfg, TASK_REWARD_REGISTRY[task_type]) == TASK_REWARD_REGISTRY[task_type].interpolate_params(level.value)`.
    - `test_unmapped_override_warns`: set tissue_stiffness=120.0 on task_type="suturing" (D-05 has no tissue_stiffness mapping for suturing). Use `pytest.warns(UserWarning)` (or the project logger's warning category — verify against task_reward_router.py:93-94) and assert the composed dict equals `SuturingReward.interpolate_params(level.value)` for every key (the override is a no-op). Also assert no KeyError is raised.

    Run `PYTHONPATH=src pytest tests/test_difficulty_config.py -v` — the new tests MUST fail (RED) at import because `surg_rl.dynamics.difficulty_wiring` does not yet exist (ModuleNotFoundError). Commit: `test(36-02): add failing RED truth-table + D-04 tests for compose_difficulty_overrides`.
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/test_difficulty_config.py -v 2>&1 | grep -E "(ModuleNotFoundError|ImportError)" exits 0 for difficulty_wiring; Plan 01 tests still pass (DifficultyLevelConfig exists)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class TestComposeDifficultyOverrides' tests/test_difficulty_config.py` returns 1
    - `grep -c 'test_compose_truth_table' tests/test_difficulty_config.py` returns >= 1
    - `grep -c 'test_compose_empty_levels_equals_interpolation' tests/test_difficulty_config.py` returns 1
    - `grep -c 'test_unmapped_override_warns' tests/test_difficulty_config.py` returns 1
    - `grep -c 'TASK_REWARD_REGISTRY' tests/test_difficulty_config.py` returns >= 2
    - `PYTHONPATH=src pytest tests/test_difficulty_config.py::TestDifficultyLevelConfig tests/test_difficulty_config.py::TestLeafImportAudit -v` exits 0 (Plan 01 tests still GREEN)
    - `PYTHONPATH=src pytest tests/test_difficulty_config.py::TestComposeDifficultyOverrides -v` exits non-zero (RED — difficulty_wiring missing)
  </acceptance_criteria>
  <done>Truth-table + D-04 tests added and RED; Plan 01 tests still GREEN; commit landed as `test(36-02): ...`.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Implement difficulty_wiring module — ABSTRACT_TO_CONCRETE + DiscreteCurriculumConfig + compose_difficulty_overrides</name>
  <files>src/surg_rl/dynamics/difficulty_wiring.py</files>
  <read_first>
    - src/surg_rl/rl/task_reward_router.py (registry dict shape lines 7-35; warning-on-miss idiom lines 93-94; logger setup lines 23-25)
    - src/surg_rl/rl/rewards.py (interpolate_params classmethod lines 676-681 et al. — the additive baseline to call, NOT edit)
    - src/surg_rl/rl/difficulty.py (DifficultyLevel + DifficultyLevelConfig from Plan 01 — the imports to use)
    - .planning/phases/36-difficulty-schema-discrete-curriculum/36-RESEARCH.md (D-05 mapping table; Pitfall 2 task_type keying; Pattern 2 compose recipe; Open Q3 wiring-module path rationale)
    - .planning/phases/36-difficulty-schema-discrete-curriculum/36-PATTERNS.md (File 2 analog — registry + composer excerpts)
  </read_first>
  <behavior>
    - All RED truth-table + D-04 tests flip to GREEN (exit 0).
    - ABSTRACT_TO_CONCRETE has exactly the 6 task_type keys (suturing, dissection, needle_insertion, knot_tying, grasping, cutting) with the D-05 cell values.
    - compose_difficulty_overrides returns a dict equal to reward_cls.interpolate_params(level.value) except on mapped overridden keys, which hold the absolute override value.
    - No reward surface (rewards.py, task_reward_router.py) is edited.
  </behavior>
  <action>
    Create src/surg_rl/dynamics/difficulty_wiring.py. Module docstring: "Wiring layer for per-level difficulty overrides (Phase 36, TASK-06). Holds the abstract→concrete PARAM_BOUNDS mapping (D-05), the DiscreteCurriculumConfig wrapper (D-08), and the additive compose_difficulty_overrides helper (D-06). Imports DifficultyLevel + DifficultyLevelConfig from rl.difficulty one-way (no cycle)."

    Imports: `from typing import Optional`, `from pydantic import BaseModel, Field`, `from surg_rl.rl.difficulty import DifficultyLevel, DifficultyLevelConfig`, `from surg_rl.utils.logging import get_logger`, `logger = get_logger(__name__)`. Do NOT import curriculum or schema here (one-way edge: dynamics.difficulty_wiring → rl.difficulty only). Do NOT import task_reward_router — the composition helper receives `reward_cls` as a parameter (RESEARCH.md Open Q3).

    Define module-level dict `ABSTRACT_TO_CONCRETE: dict[str, dict[str, str]]` with EXACTLY the D-05 cells (keyed by task_type per corrected D-03 — NOT TaskConfig.name; 36-RESEARCH.md Pitfall 2):
    - "suturing": {"target_precision_tolerance": "needle_position_tolerance", "time_limit": "time_limit"}
    - "dissection": {"tissue_stiffness": "tissue_stiffness", "target_precision_tolerance": "incision_path_tolerance", "time_limit": "time_limit"}
    - "needle_insertion": {"target_precision_tolerance": "needle_alignment_tolerance", "tool_position_noise": "action_noise", "time_limit": "time_limit"}
    - "knot_tying": {"tissue_stiffness": "tissue_stiffness", "target_precision_tolerance": "loop_deviation_tolerance", "tool_position_noise": "action_noise", "time_limit": "time_limit"}
    - "grasping": {"target_precision_tolerance": "approach_tolerance", "tool_position_noise": "action_noise", "time_limit": "time_limit"}
    - "cutting": {"tissue_stiffness": "tissue_stiffness", "target_precision_tolerance": "cut_path_accuracy", "time_limit": "time_limit"}

    Define `class DiscreteCurriculumConfig(BaseModel)` with `levels: dict[DifficultyLevel, DifficultyLevelConfig] = Field(default_factory=dict)` and a docstring noting "default empty == pure interpolate_params(level.value) baseline (D-08)".

    Define `def compose_difficulty_overrides(task_type: str, level: DifficultyLevel, config: DifficultyLevelConfig, reward_cls: type) -> dict[str, float]:` that:
    1. Computes `baseline = reward_cls.interpolate_params(level.value)` (D-06 — interpolate FIRST).
    2. For each of the four abstract fields on `config` whose value is not None: look up `concrete_key = ABSTRACT_TO_CONCRETE.get(task_type, {}).get(abstract_field)`. If `concrete_key is None`: `logger.warning(f"override field {abstract_field!r} has no mapping for task_type={task_type!r}; keeping interpolated value")` (D-04) and continue. Else set `baseline[concrete_key] = override_value` (absolute replacement, D-06 — NOT a delta/multiplier).
    3. Return the composed dict (a copy of baseline with replacements applied; unoverridden keys retain the interpolated value).

    Run `PYTHONPATH=src pytest tests/test_difficulty_config.py -v` — ALL tests (Plan 01 + Plan 02) MUST pass (GREEN). Commit: `feat(36-02): implement difficulty_wiring module (ABSTRACT_TO_CONCRETE + DiscreteCurriculumConfig + compose_difficulty_overrides)`.
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/test_difficulty_config.py -v exits 0; PYTHONPATH=src pytest tests/test_difficulty_levels.py tests/test_dynamics.py -v exits 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ABSTRACT_TO_CONCRETE' src/surg_rl/dynamics/difficulty_wiring.py` returns >= 2 (definition + uses)
    - `grep -c 'class DiscreteCurriculumConfig(BaseModel):' src/surg_rl/dynamics/difficulty_wiring.py` returns 1
    - `grep -c 'def compose_difficulty_overrides' src/surg_rl/dynamics/difficulty_wiring.py` returns 1
    - `grep -c 'interpolate_params(level.value)' src/surg_rl/dynamics/difficulty_wiring.py` returns 1
    - `grep -c 'logger.warning' src/surg_rl/dynamics/difficulty_wiring.py` returns 1 (D-04 warning path)
    - `grep -c 'from surg_rl.dynamics.curriculum' src/surg_rl/dynamics/difficulty_wiring.py` returns 0 (no reverse edge)
    - `grep -c 'from surg_rl.scene_definition' src/surg_rl/dynamics/difficulty_wiring.py` returns 0 (no cycle)
    - `grep -c '"suturing": {"target_precision_tolerance": "needle_position_tolerance"' src/surg_rl/dynamics/difficulty_wiring.py` returns 1 (D-05 cell)
    - `PYTHONPATH=src pytest tests/test_difficulty_config.py -v` exits 0 (GREEN — Plan 01 + Plan 02 tests pass)
    - `PYTHONPATH=src pytest tests/test_difficulty_levels.py tests/test_dynamics.py -v` exits 0 (additive-regression gate)
    - `python -c "import surg_rl.dynamics.curriculum, surg_rl.scene_definition.schema, surg_rl.dynamics.difficulty_wiring"` exits 0 (no Pydantic cross-package cycle re-introduced — SC#5)
  </acceptance_criteria>
  <done>All truth-table + D-04 tests GREEN; existing suite GREEN; no cycle; commit landed as `feat(36-02): ...`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| researcher authoring → compose_difficulty_overrides | override-field values + task_type string cross into the composer; task_type is used as a dict key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03 | Tampering | compose_difficulty_overrides task_type lookup | mitigate | `ABSTRACT_TO_CONCRETE.get(task_type, {})` — unknown task_type yields empty mapping; all override fields warn-and-no-op (D-04), never raise KeyError |
| T-36-04 | Tampering | override value range | mitigate | values already validated at DifficultyLevelConfig schema time (Plan 01 field_validator — ASVS V5) |
| T-36-05 | Denial of Service | dynamics.difficulty_wiring → rl import edge | mitigate | one-way import verified by acceptance grep (no curriculum/schema import); no Pydantic cross-package cycle |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | No package installs — only stdlib + already-pinned pydantic |
</threat_model>

<verification>
- `PYTHONPATH=src pytest tests/test_difficulty_config.py -v` exits 0 (SC#1, SC#2, D-04, SC#5 covered by Plan 01 + Plan 02).
- `PYTHONPATH=src pytest tests/test_difficulty_levels.py tests/test_dynamics.py -v` exits 0 (additive-regression gate).
- `python -c "import surg_rl.dynamics.curriculum, surg_rl.scene_definition.schema, surg_rl.dynamics.difficulty_wiring"` exits 0 (no cross-package cycle).
</verification>

<success_criteria>
- ABSTRACT_TO_CONCRETE registry matches D-05 cell-for-cell, keyed by task_type (corrected D-03).
- compose_difficulty_overrides is additive over interpolate_params (SC#2 truth table GREEN) and warns on unmapped overrides (D-04).
- DiscreteCurriculumConfig wraps the levels dict with an empty default (D-08).
- No reward surface edited; no Pydantic cross-package cycle re-introduced (SC#5).
</success_criteria>

<output>
Create `.planning/phases/36-difficulty-schema-discrete-curriculum/36-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (Plan 02)

- `ABSTRACT_TO_CONCRETE` (module-level dict, `src/surg_rl/dynamics/difficulty_wiring.py`) — 6 task_type keys × per-abstract-field concrete PARAM_BOUNDS key strings (D-05)
- `DiscreteCurriculumConfig` (class, `src/surg_rl/dynamics/difficulty_wiring.py`) — Pydantic BaseModel with `levels: dict[DifficultyLevel, DifficultyLevelConfig]`
- `compose_difficulty_overrides` (function, `src/surg_rl/dynamics/difficulty_wiring.py`) — signature `(task_type: str, level: DifficultyLevel, config: DifficultyLevelConfig, reward_cls: type) -> dict[str, float]`
- `tests/test_difficulty_config.py` additions: `TestComposeDifficultyOverrides` class (test_compose_truth_table, test_compose_empty_levels_equals_interpolation, test_unmapped_override_warns)