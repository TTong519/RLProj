---
phase: 07-real-time-rendering
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified: [
  "src/surg_rl/cli.py",
  "tests/unit/test_cli.py",
  "tests/unit/test_rendering.py",
]
autonomous: true
requirements: ["RENDER-04", "RENDER-05"]
user_setup: []

must_haves:
  truths:
    - "CLI surg-rl train --render-human --render-fps 60 passes flags through to TrainingConfig"
    - "CLI prints user-facing message when viewer is enabled"
    - "Backward compat: existing flags unchanged"
    - "test_rendering.py covers viewer survives reset, step overhead <5ms, FPS throttle, clean shutdown"
  artifacts:
    - path: "src/surg_rl/cli.py"
      provides: "--render-human and --render-fps Typer flags on train()"
      min_lines: 10
    - path: "tests/unit/test_cli.py"
      provides: "CLI flag parsing tests"
      min_lines: 30
    - path: "tests/unit/test_rendering.py"
      provides: "Full rendering test suite (all RENDER requirements)"
      min_lines: 150
  key_links:
    - from: "CLI train()"
      to: "TrainingConfig"
      via: "render_mode='human', render_fps=... passed to constructor"
      pattern: "render_mode|render_fps"

---

<objective>
Add user-facing CLI flags for real-time rendering and lock in correctness with a comprehensive test suite covering all Phase 7 requirements.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-real-time-rendering/07-real-time-rendering-CONTEXT.md

# Wave 1 and 2 dependencies
## From 07-01-PLAN.md
- BaseSimulator.start_viewer() / stop_viewer() abstract methods
- RenderThread in src/surg_rl/render_thread.py
- MuJoCoSimulator viewer with launch_passive + macOS guard
- PyBullet render_mode wired at construction time

## From 07-02-PLAN.md
- SurgicalEnvConfig.render_fps field (default 30.0)
- SurgicalEnv eagerly starts viewer when render_mode == "human"
- Headless fallback: warning, continue without viewer
- SIGINT + atexit handlers on SurgicalEnv
- TrainingConfig.render_mode and TrainingConfig.render_fps fields
- TrainingManager passes them to SurgicalEnvConfig
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Add --render-human and --render-fps flags to CLI train</name>
  <files>
    src/surg_rl/cli.py
    tests/unit/test_cli.py
  </files>
  <behavior>
    - --render-human: bool flag opens live viewer during training
    - --render-fps: float flag (default 30.0) controls viewer FPS
    - Flags passed to TrainingConfig constructor
    - Console prints "Live viewer: enabled (30 FPS)" or similar
    - Backward compatible: existing train() calls unchanged
  </behavior>
  <action>
    1. In cli.py train() signature, add after backend:
       ```python
       render_human: bool = typer.Option(
           False, "--render-human",
           help="Open a live 3D viewer during training (requires display)"
       ),
       render_fps: float = typer.Option(
           30.0, "--render-fps",
           help="Target FPS for the live viewer (default: 30)"
       ),
       ```

    2. In train() body, when constructing TrainingConfig:
       ```python
       config = TrainingConfig(
           # ... existing fields ...
           render_mode="human" if render_human else None,
           render_fps=render_fps,
           backend=backend_enum,
       )
       ```

    3. Add console output:
       ```python
       if render_human:
           console.print(f"  • Live viewer: enabled ({render_fps} FPS)")
       ```
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/unit/test_cli.py::test_train_render_flags -x -m "not integration"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>task 2: Create comprehensive rendering test suite</name>
  <files>
    tests/unit/test_rendering.py
    tests/conftest.py
  </files>
  <behavior>
    - Test viewer survives reset (mocked)
    - Test step() overhead <5ms with viewer active (mocked)
    - Test FPS throttle maintains ~target interval (mocked perf_counter)
    - Test clean shutdown (stop_viewer called, no active threads)
    - Test headless fallback (start_viewer returns False, warning log)
    - Test macOS mjpython check (mocked platform)
    - Test PyBullet GUI returns True, DIRECT returns False
    - Test rgb_array after human mode works (independent renderer)
  </behavior>
  <action>
    Create tests/unit/test_rendering.py with classes:
    - TestRenderThread: daemon, FPS throttle, stop lifecycle
    - TestMuJoCoViewer: start/stop, headless fallback, macOS guard
    - TestPyBulletViewer: GUI True, DIRECT False, no-op stop_viewer
    - TestSurgicalEnvViewer: eager start, render() branching, close cleanup
    - TestRenderingIntegration: viewer survives reset (mocked), SIGINT handler
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/unit/test_rendering.py -x -m "not integration"</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user→CLI | Typer parses flags |
| CLI→TrainingConfig | render_mode/render_fps passed |
| test→viewer | MagicMock prevents real window creation |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | DoS | Mocked test hangs | mitigate | All tests use MagicMock; no real viewer opened |
| T-07-08 | Tampering | Typer type validation | mitigate | float/bool types enforced by Typer |
</threat_model>

<verification>
After wave 3 (full phase):
- PYTHONPATH=src pytest tests/unit/test_rendering.py -x passes
- PYTHONPATH=src pytest tests/unit/test_cli.py -k render -x passes
- PYTHONPATH=src pytest tests/ -m "not integration" -v passes
- PYTHONPATH=src pytest tests/ -x passes (full suite)
</verification>

<output>
After completion, create .planning/phases/07-real-time-rendering/07-03-SUMMARY.md
</output>
