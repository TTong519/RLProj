---
phase: 13-kubernetes-deployment
plan: 04
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - k8s/base/configmap.yaml
  - k8s/base/secret.yaml
  - k8s/base/pvc.yaml
  - k8s/base/rbac.yaml
autonomous: true
requirements:
  - K8S-04
  - K8S-05
---

<objective>
Create K8s ConfigMap, Secret, PVC, and RBAC manifests. ConfigMap injects scene definition JSON as a file mount. Secret injects API keys as env vars via `secretKeyRef`. PVC provides persistent checkpoint storage. RBAC enables namespace-scoped pod listing for the training service account.

Purpose: Complete the infrastructure manifests needed by the training Job and RayCluster. Configurable via Kustomize overlays.

Output: 4 new manifest files in `k8s/base/`
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-kubernetes-deployment/13-RESEARCH.md
@k8s/base/training-job.yaml (references configmap + secret + pvc)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: create ConfigMap manifest</name>
  <files>k8s/base/configmap.yaml</files>
  <read_first>
    - .planning/phases/13-kubernetes-deployment/13-RESEARCH.md (ConfigMap pattern)
  </read_first>
  <action>
Create `k8s/base/configmap.yaml`:
```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: surg-rl-scene
  labels:
    app: surg-rl
data:
  scene.json: |
    {
      "metadata": {"name": "k8s-training-scene"},
      "robots": [],
      "tissues": [],
      "instruments": [],
      "physics": {"timestep": 0.002, "gravity": [0, 0, -9.81]}
    }
```
  </action>
  <verify>
    <automated>
      kubectl apply -f k8s/base/configmap.yaml --dry-run=client 2>&1 | head -3
    </automated>
  </verify>
  <done>ConfigMap with placeholder scene JSON; overridden per-environment via Kustomize overlays.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: create Secret manifest</name>
  <files>k8s/base/secret.yaml</files>
  <read_first>
    - .planning/phases/13-kubernetes-deployment/13-RESEARCH.md (Secret pattern — never hardcode in pod spec)
  </read_first>
  <action>
Create `k8s/base/secret.yaml`:
```yaml
apiVersion: v1
kind: Secret
metadata:
  name: surg-rl-secrets
  labels:
    app: surg-rl
type: Opaque
stringData:
  llm-api-key: "change-me-production"
  model-registry-token: "change-me-production"
```
  </action>
  <verify>
    <automated>
      kubectl apply -f k8s/base/secret.yaml --dry-run=client 2>&1 | head -3
    </automated>
  </verify>
  <done>Secret with placeholder values; production secrets injected via Kustomize secretGenerator or external secrets manager.</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: create PVC manifest</name>
  <files>k8s/base/pvc.yaml</files>
  <read_first>
    - .planning/phases/13-kubernetes-deployment/13-RESEARCH.md (PVC pattern, ReadWriteOnce constraint)
  </read_first>
  <action>
Create `k8s/base/pvc.yaml`:
```yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: surg-rl-checkpoints
  labels:
    app: surg-rl
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: standard
```
  </action>
  <verify>
    <automated>
      kubectl apply -f k8s/base/pvc.yaml --dry-run=client 2>&1 | head -3
    </automated>
  </verify>
  <done>50Gi PVC with ReadWriteOnce access for checkpoint and TensorBoard log persistence.</done>
</task>

<task type="auto" tdd="true">
  <name>task 4: create RBAC manifest</name>
  <files>k8s/base/rbac.yaml</files>
  <read_first>
    - .planning/phases/13-kubernetes-deployment/13-RESEARCH.md (RBAC minimal pattern)
  </read_first>
  <action>
Create `k8s/base/rbac.yaml`:
```yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: surg-rl
  labels:
    app: surg-rl
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: surg-rl
  labels:
    app: surg-rl
rules:
  - apiGroups: [""]
    resources: ["pods", "pods/log"]
    verbs: ["get", "list"]
  - apiGroups: ["batch"]
    resources: ["jobs"]
    verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: surg-rl
  labels:
    app: surg-rl
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: surg-rl
subjects:
  - kind: ServiceAccount
    name: surg-rl
```
  </action>
  <verify>
    <automated>
      kubectl apply -f k8s/base/rbac.yaml --dry-run=client 2>&1 | head -3
    </automated>
  </verify>
  <done>ServiceAccount + namespace-scoped Role (get/list pods, jobs only) + RoleBinding.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-13-07 | Information Disclosure | Secret committed in git | accept | base has placeholder values; production uses kustomize secretGenerator or SOPS |
| T-13-08 | Elevation | RBAC overly permissive | mitigate | Namespace-scoped Role with minimal verbs (get/list pods/jobs only) |
| T-13-09 | DoS | PVC unbounded | mitigate | 50Gi initial request; cluster-level quota enforces cap |
</threat_model>

<verification>
1. All 4 manifests pass `kubectl apply -f ... --dry-run=client`
2. Secret uses `stringData` for placeholder readability (production overlays use `data: base64`)
3. RBAC is namespace-scoped, minimal verbs
4. PVC accessMode is ReadWriteOnce (documented constraint in RESEARCH.md)
</verification>

<success_criteria>
- ConfigMap with scene.json placeholder (overridable via overlays)
- Secret with placeholder values (production: kustomize secretGenerator)
- PVC with 50Gi ReadWriteOnce for checkpoint persistence
- RBAC: ServiceAccount + namespace-scoped Role with minimal permissions
</success_criteria>

<output>
After completion, create `.planning/phases/13-kubernetes-deployment/13-04-SUMMARY.md`
</output>
