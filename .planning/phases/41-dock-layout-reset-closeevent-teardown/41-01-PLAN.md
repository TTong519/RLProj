---
phase: 41-dock-layout-reset-closeevent-teardown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/surg_rl/editor/dock_state.py
  - src/surg_rl/editor/main_window.py
  - src/surg_rl/editor/viewport.py
  - src/surg_rl/editor/tree_view.py
  - tests/test_dock_state.py
autonomous: true
requirements: [GUI-18]
user_setup: []

must_haves:
  truths:
    # SC#1 — Reset Layout restores factory-default arrangement (incl. tabification/floating/closed)
    - "User clicks View → Reset Layout and the dock panels restore to the factory-default arrangement captured at first showEvent, including tabification/floating/closed state — bug #3 closed (per D-01, D-02)"
    # SC#2 — rearrange→close→reopen restores saved layout (widget-recreation fix)
    - "User rearranges docks, loads a new scene via New/Open/LLM-accept, closes the editor, and reopens it — the saved layout restores on rerun (not reset to a broken state), because _refresh_viewport_and_tree now calls update_scene() instead of recreating widgets (per D-03, D-06)"
    # SC#4 — every dock has unique objectName
    - "Every QDockWidget child of EditorWindow has a non-empty, unique objectName so saveState()/restoreState() round-trip correctly (per D-07)"
    - "_refresh_viewport_and_tree calls ViewportPanel.update_scene and SceneTreeView.update_scene instead of constructing new widgets / setCentralWidget / setWidget"
  artifacts:
    - "src/surg_rl/editor/dock_state.py — DockStateManager class with capture_factory_default(window) (guarded by _captured bool, Pitfall 5), reset_to_default(window) -> bool (primary: restoreState(factory QByteArray); fallback: _rebuild_default_layout re-adds docks then re-captures), and _rebuild_default_layout(window) (the crude re-addDockWidget body preserved ONLY as fallback per D-01)"
    - "src/surg_rl/editor/viewport.py — ViewportPanel.update_scene(scene) method (closes old simulator via the stop() suppress pattern, sets _simulator=None so _tick reloads, swaps _scene, calls reset_camera())"
    - "src/surg_rl/editor/tree_view.py — SceneTreeView.update_scene(scene) method (swaps _scene, clears _model, re-sets horizontal header labels, re-runs _build_tree, expandAll; re-wires selectionModel().currentChanged after clear() if needed)"
    - "src/surg_rl/editor/main_window.py — _action_reset_layout body replaced with self._dock_state.reset_to_default(self); new showEvent override calling self._dock_state.capture_factory_default(self); _refresh_viewport_and_tree body replaced with update_scene() calls on viewport + tree (no widget recreation, no node_selected re-connect); __init__ adds self._dock_state = DockStateManager()"
    - "tests/test_dock_state.py — offscreen harness (QT_QPA_PLATFORM=offscreen module-top, pytestmark skipif not _HAVE_PYSIDE6, qapp + isolated_home fixtures) + TestDockObjectNames (SC#4) + TestDockRoundTrip (SC#1/#2)"
  key_links:
    - "EditorWindow._action_reset_layout → self._dock_state.reset_to_default(self) (D-02)"
    - "EditorWindow.showEvent (first show) → self._dock_state.capture_factory_default(self) (D-01, guarded)"
    - "EditorWindow._refresh_viewport_and_tree → ViewportPanel.update_scene + SceneTreeView.update_scene (D-06)"
    - "DockStateManager._factory_state ← QMainWindow.saveState() QByteArray captured once at first showEvent (D-01)"
  prohibitions:
    # descriptor-less, flagged-unverified (PROHIB_ABSENT specless fallback)
    - "Must NOT persist the factory-default layout QByteArray to QSettings (D-01 — recomputed each launch from the first showEvent; only the user's saved layout round-trips via the existing window/state key)"
    - "Must NOT recreate ViewportPanel or SceneTreeView widgets in _refresh_viewport_and_tree (D-06 — widget recreation is the bug #3 root cause; use in-place update_scene swap)"
    - "Must NOT capture the factory-default snapshot on every showEvent (Pitfall 5 — capture must be guarded by a one-shot bool or it overwrites the factory snapshot with the user's rearranged layout)"
  flagged_assumptions:
    - "GUI-18 edge probe returned 'unclassified/unresolved' (the requirement is a high-level user story, not a sharp edge). The defensible acceptance criteria are the 4 ROADMAP success criteria SC#1–4 mapped to the VALIDATION.md test classes TestDockObjectNames (SC#4), TestDockRoundTrip (SC#1/#2), TestCloseMidCallMockSlow + TestCloseMidCallRealProvider (SC#3, in Plan 02). No silent drop."

---

<objective>
Deliver the dock-layout half of GUI-18: a `DockStateManager` that captures the factory-default dock layout at first `showEvent` and restores it on "Reset Layout" (including tabification/floating/closed state that the current crude re-`addDockWidget` reset ignores), plus in-place `update_scene(scene)` on `ViewportPanel` and `SceneTreeView` so New/Open/LLM-accept/undo/redo stop destroying and recreating widgets (the widget recreation that resets dock geometry on rerun).

Purpose: Close bug #3 (dock panels not reset on rerun) and satisfy SC#1, SC#2, SC#4. Establishes the `DockStateManager` + `update_scene` contract that later GUI phases (42–51) build on.
Output: `src/surg_rl/editor/dock_state.py` (new), modified `main_window.py`/`viewport.py`/`tree_view.py`, new `tests/test_dock_state.py` with TestDockObjectNames + TestDockRoundTrip.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-dock-layout-reset-closeevent-teardown/41-CONTEXT.md
@.planning/phases/41-dock-layout-reset-closeevent-teardown/41-RESEARCH.md
@.planning/phases/41-dock-layout-reset-closeevent-teardown/41-PATTERNS.md
@.planning/phases/41-dock-layout-reset-closeevent-teardown/41-VALIDATION.md
@src/surg_rl/editor/main_window.py
@src/surg_rl/editor/viewport.py
@src/surg_rl/editor/tree_view.py
@src/surg_rl/editor/__init__.py
@src/surg_rl/editor/undo_stack.py
@tests/test_viewport.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 (Wave 0): Create offscreen test scaffold + TestDockObjectNames + TestDockRoundTrip stubs (RED)</name>
  <files>tests/test_dock_state.py</files>
  <read_first>
    - .planning/phases/41-dock-layout-reset-closeevent-teardown/41-CONTEXT.md (D-07, D-08 locked decisions)
    - .planning/phases/41-dock-layout-reset-closeevent-teardown/41-PATTERNS.md (§tests/test_dock_state.py — verbatim offscreen harness from tests/gui/conftest.py:1-35 + standalone single-file variant from tests/test_viewport.py:13-67; objectName introspection pattern; the CURRENT method bodies the tests exercise)
    - .planning/phases/41-dock-layout-reset-closeevent-teardown/41-RESEARCH.md (§Code Examples — objectName introspection test)
    - tests/test_viewport.py (lines 13-67 and 142-153 — the standalone single-file offscreen harness + EditorWindow construction pattern to mirror)
    - src/surg_rl/editor/main_window.py (the _build_dock_widgets objectNames at ~L91-117, _action_reset_layout at ~L256-262, _refresh_viewport_and_tree at ~L306-319, _restore_geometry at ~L364-371)
  </read_first>
  <action>
    Create `tests/test_dock_state.py` following the standalone single-file offscreen pattern (mirror `tests/test_viewport.py:13-67`): module-top `os.environ.setdefault("QT_QPA_PLATFORM", "offscreen")`, `_HAVE_PYSIDE6` try/except import guard, module-level `pytestmark = pytest.mark.skipif(not _HAVE_PYSIDE6, reason="PySide6 not installed")`, a session-scoped `qapp` fixture returning `QApplication.instance() or QApplication(sys.argv)`, and an `isolated_home` fixture that `monkeypatch.setenv("HOME", str(tmp_path))` + `monkeypatch.setenv("XDG_CONFIG_HOME", str(tmp_path / ".config"))` and yields `tmp_path`. The `isolated_home` fixture is mandatory for any test constructing `EditorWindow` so QSettings does not pollute the developer's real `~/Library/Preferences/com.SurgRL.SceneEditor.plist`.

    Add test class `TestDockObjectNames` (SC#4, D-07): `test_every_dock_has_unique_nonempty_objectname(qapp, isolated_home)` builds `EditorWindow()`, calls `w.show()` + `qapp.processEvents()`, collects `w.findChildren(QDockWidget)`, asserts `all(names)` and `len(names) == len(set(names))`, then `w.close()`. This test must PASS immediately against the current 3 docks (`dock_scene_tree`, `dock_properties`, `dock_llm`) — it is the regression guard for future phases.

    Add test class `TestDockRoundTrip` (SC#1/#2, D-08) with two tests that are RED on this task (they import `DockStateManager` from `surg_rl.editor.dock_state`, which does not exist yet — the import error is the expected RED state; do NOT add `pytest.skip` or `xfail` — let the ImportError surface as the honest RED baseline):
    - `test_reset_layout_restores_factory_default(qapp, isolated_home)`: build `EditorWindow`, `w.show()` + processEvents, capture `factory = w.saveState()`, rearrange docks via the API (e.g. `w.tabifyDockWidget(w._tree_dock, w._properties_dock)` + processEvents), invoke `w._action_reset_layout()`, processEvents, then assert `w.saveState() == factory` (the arrangement round-trips through the factory QByteArray). Per D-01 the factory snapshot is captured at first showEvent; the assertion verifies reset restores tabification/floating/closed state, not just area assignment.
    - `test_rearrange_close_reopen(qapp, isolated_home)`: build `EditorWindow`, show, rearrange docks, trigger a scene load via `w._refresh_viewport_and_tree()` (call it directly with `w._scene` already set or a `_empty_scene_stub()`), then `w.close()` (calls closeEvent → saves state via EditorSettings.save_window), construct a fresh `EditorWindow` (same `isolated_home` so QSettings persists), and assert `restoreState` restored the rearranged arrangement (e.g. the tabified dock is still tabified). This is the SC#2 regression: widget recreation via the refresh path must NOT reset dock geometry on rerun.

    Do NOT create `src/surg_rl/editor/dock_state.py` in this task — Task 2 does that. The RoundTrip ImportError is the RED baseline.
  </action>
  <verify>
    <automated>PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_dock_state.py::TestDockObjectNames -v</automated>
    <automated>PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_dock_state.py::TestDockRoundTrip -v 2>&amp;1 | grep -E "(ImportError|ModuleNotFoundError|dock_state)" | grep -v '^#' | grep -c . ; echo "expect TestDockRoundTrip RED (ImportError) >=1"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_dock_state.py` exists and declares `pytestmark = pytest.mark.skipif(not _HAVE_PYSIDE6, reason="PySide6 not installed")` at module level
    - `tests/test_dock_state.py` contains `class TestDockObjectNames` with a test that asserts every `QDockWidget` child of `EditorWindow` has a non-empty unique `objectName`
    - `tests/test_dock_state.py` contains `class TestDockRoundTrip` with `test_reset_layout_restores_factory_default` and `test_rearrange_close_reopen` that import from `surg_rl.editor.dock_state`
    - `PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_dock_state.py::TestDockObjectNames -v` exits 0 (the 3 existing docks already have objectNames)
    - `PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_dock_state.py::TestDockRoundTrip -v` fails to collect/error (RED baseline — `dock_state.py` not yet created)
  </acceptance_criteria>
  <done>
    Test scaffold file exists with offscreen harness (qapp + isolated_home) and TestDockObjectNames + TestDockRoundTrip classes. TestDockObjectNames passes against the current 3 docks. TestDockRoundTrip is RED (ImportError on dock_state) — honest baseline for Task 2's GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DockStateManager + Reset Layout wiring + showEvent capture (GREEN for SC#1/#4)</name>
  <files>src/surg_rl/editor/dock_state.py, src/surg_rl/editor/main_window.py</files>
  <read_first>
    - .planning/phases/41-dock-layout-reset-closeevent-teardown/41-CONTEXT.md (D-01, D-02, D-03 locked)
    - .planning/phases/41-dock-layout-reset-closeevent-teardown/41-RESEARCH.md (§Pattern 1 — DockStateManager skeleton; §Pitfalls 1, 5 — objectName silent no-op + capture-once guard)
    - .planning/phases/41-dock-layout-reset-closeevent-teardown/41-PATTERNS.md (§dock_state.py — LazyImport discipline from editor/__init__.py:31-42, class shape from undo_stack.py:50-77; §main_window.py — _action_reset_layout CURRENT body at L256-262 to REPLACE, _restore_geometry CURRENT body at L364-371 to KEEP per D-03, __init__ extension points at L51-85, showEvent NEW override, imports pattern at L10-20)
    - src/surg_rl/editor/__init__.py (the LazyImport proxies — import Qt via `from surg_rl.editor import QtCore, QtWidgets`, NOT `from PySide6 import ...`)
    - src/surg_rl/editor/undo_stack.py (the canonical small state-manager class shape + module-level constant + private guard pattern)
    - src/surg_rl/editor/main_window.py (current _action_reset_layout L256-262, _build_dock_widgets L91-117, __init__ L51-85, _restore_geometry L364-371)
    - tests/test_dock_state.py (the RED tests written in Task 1 — these are the GREEN targets)
  </read_first>
  <behavior>
    - capture_factory_default(window) on a fresh DockStateManager: first call sets _factory_state to window.saveState() and _captured=True; second call (same instance) is a no-op (does not overwrite the snapshot) — Pitfall 5 guard
    - reset_to_default(window) when _factory_state is None (never captured): invokes _rebuild_default_layout (the crude re-addDockWidget fallback), then re-captures via saveState(), returns True
    - reset_to_default(window) when _factory_state is a valid QByteArray and window.restoreState returns True: returns True without invoking the fallback
    - reset_to_default(window) when window.restoreState returns False (corrupt/missing): invokes _rebuild_default_layout, re-captures, returns True
    - TestDockRoundTrip::test_reset_layout_restores_factory_default passes (w.saveState() == factory after _action_reset_layout)
    - TestDockObjectNames continues to pass (no dock identity changed)
  </behavior>
  <action>
    Create `src/surg_rl/editor/dock_state.py` following the LazyImport discipline: `from __future__ import annotations`, `from typing import TYPE_CHECKING`, `from surg_rl.editor import QtCore, QtWidgets` (the LazyImport proxies — NOT `from PySide6 import ...` at module top). Define `class DockStateManager` with `__init__` setting `self._factory_state: QtCore.QByteArray | None = None` and `self._captured: bool = False` (Pitfall 5 guard). Methods: `capture_factory_default(self, window: QtWidgets.QMainWindow) -> None` (guarded by `if self._captured: return`; captures `self._factory_state = window.saveState()`; sets `self._captured = True`), `reset_to_default(self, window: QtWidgets.QMainWindow) -> bool` (primary: `if self._factory_state is not None and window.restoreState(self._factory_state): return True`; fallback: `self._rebuild_default_layout(window); self._factory_state = window.saveState(); self._captured = True; return True`), and `_rebuild_default_layout(self, window: QtWidgets.QMainWindow) -> None` (the ONLY place the crude re-`addDockWidget` body from current `main_window.py:256-262` is preserved — re-add `self._tree_dock`/`self._properties_dock`/`self._llm_dock` to their factory areas + show them; kept as the D-01 fallback, NOT the primary path). The factory default is NOT persisted to QSettings (D-01). If a logger is needed, add `from surg_rl.utils.logging import get_logger` + `logger = get_logger(__name__)` per the viewport.py:21-23 convention.

    Modify `src/surg_rl/editor/main_window.py`:
    1. Add `from surg_rl.editor.dock_state import DockStateManager` to the module-top imports (same-package; verify `dock_state.py` imports Qt via the LazyImport proxies so this stays cheap).
    2. In `__init__` (around `self._settings = EditorSettings()` at L~51-85), add `self._dock_state = DockStateManager()`.
    3. Replace the body of `_action_reset_layout` (current L256-262, the crude re-`addDockWidget`) with a single call: `self._dock_state.reset_to_default(self)`. The old body is preserved ONLY as `DockStateManager._rebuild_default_layout` (the fallback).
    4. Add a `showEvent(self, event: QtGui.QCloseEvent) -> None` override decorated `# noqa: N802` that calls `self._dock_state.capture_factory_default(self)` then `super().showEvent(event)`. The guard is inside the manager (Pitfall 5).

    Do NOT change `_restore_geometry` (D-03 — the existing restore path is correct; the bug is widget recreation, not restore timing). Do NOT add an `aboutToClose` signal or modify `closeEvent` — that is Plan 02. Do NOT touch `_refresh_viewport_and_tree` — that is Task 3.

    Run TestDockRoundTrip → GREEN. Run TestDockObjectNames → still GREEN.
  </action>
  <verify>
    <automated>PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_dock_state.py::TestDockObjectNames tests/test_dock_state.py::TestDockRoundTrip -v</automated>
  </verify>
  <acceptance_criteria>
    - `src/surg_rl/editor/dock_state.py` exists and imports Qt via `from surg_rl.editor import QtCore, QtWidgets` (NOT `from PySide6 import ...` at module top) — `grep -n "from PySide6" src/surg_rl/editor/dock_state.py` returns 0 matches (excluding lines starting with `#`)
    - `src/surg_rl/editor/dock_state.py` contains `class DockStateManager` with methods `capture_factory_default`, `reset_to_default`, `_rebuild_default_layout`
    - `src/surg_rl/editor/dock_state.py` contains a `_captured` guard so `capture_factory_default` is idempotent (Pitfall 5)
    - `grep -n "restoreState" src/surg_rl/editor/dock_state.py | grep -v '^#' | grep -c .` returns >= 1 (the primary reset path uses restoreState)
    - `src/surg_rl/editor/main_window.py` `_action_reset_layout` body calls `self._dock_state.reset_to_default(self)` and no longer contains direct `addDockWidget` calls in that method body
    - `src/surg_rl/editor/main_window.py` defines a `showEvent` override that calls `self._dock_state.capture_factory_default(self)` and `super().showEvent(event)`
    - `src/surg_rl/editor/main_window.py` `_restore_geometry` is unchanged (D-03) — still calls `self._settings.load_window()` + `restoreState`
    - `PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_dock_state.py::TestDockObjectNames tests/test_dock_state.py::TestDockRoundTrip -v` exits 0
  </acceptance_criteria>
  <done>
    DockStateManager implemented with capture-once guard + restore primary + rebuild fallback. _action_reset_layout delegates to reset_to_default. showEvent captures factory default at first show. TestDockObjectNames + TestDockRoundTrip green. SC#1 and SC#4 satisfied.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: update_scene in-place swap on ViewportPanel + SceneTreeView + rewire _refresh_viewport_and_tree (SC#2 fix)</name>
  <files>src/surg_rl/editor/viewport.py, src/surg_rl/editor/tree_view.py, src/surg_rl/editor/main_window.py, tests/test_dock_state.py</files>
  <read_first>
    - .planning/phases/41-dock-layout-reset-closeevent-teardown/41-CONTEXT.md (D-06 locked — minimal: ViewportPanel + SceneTreeView only; PropertyForm NOT folded in; undo/redo keep calling _refresh_viewport_and_tree, now safe)
    - .planning/phases/41-dock-layout-reset-closeevent-teardown/41-RESEARCH.md (§Pattern 4 — in-place update_scene; §Pitfall 7 — leak old simulator; §A2 — selectionModel wiring; §A3 — reset camera; §Open Question 1 — update_scene resets camera)
    - .planning/phases/41-dock-layout-reset-closeevent-teardown/41-PATTERNS.md (§viewport.py — stop() simulator-close pattern at L169-180, _tick lazy-load at L189-209, __init__ state at L126-163, reset_camera at L390-397; §tree_view.py — __init__ model + signal wiring at L56-69, _build_tree at L71-96, the clear()-drops-headers NOTE and selectionModel re-wire NOTE; §main_window.py — _refresh_viewport_and_tree CURRENT body at L306-319 to REPLACE)
    - src/surg_rl/editor/viewport.py (current stop() L169-180, _tick L189-209, reset_camera L390-397 — the patterns update_scene reuses; note the 1-line uncommitted type-annotation change on main does not affect this)
    - src/surg_rl/editor/tree_view.py (current __init__ L56-69, _build_tree L71-96 — the wiring update_scene must preserve)
    - src/surg_rl/editor/main_window.py (_refresh_viewport_and_tree current body L306-319 — the widget recreation to replace)
  </read_first>
  <behavior>
    - ViewportPanel.update_scene(scene): old simulator closed via the stop() suppress pattern (contextlib.suppress(AttributeError, OSError)); self._simulator set to None so _tick reloads via _on_load_simulator on next tick; self._scene swapped to the new scene; reset_camera() called (A3); _running stays True (render loop continues across the swap); NO new ViewportPanel constructed; NO setCentralWidget call
    - SceneTreeView.update_scene(scene): self._scene swapped; self._model.clear() then re-set horizontal header labels (clear drops them); _build_tree() re-run; expandAll() called; selectionModel().currentChanged connection intact (if clear() resets selectionModel, re-wire it — A2 verify)
    - _refresh_viewport_and_tree no longer constructs new ViewportPanel/SceneTreeView, no longer calls setCentralWidget/setWidget, no longer re-connects node_selected (the widget identity survives, so the __init__ connection survives)
    - TestDockRoundTrip::test_rearrange_close_reopen passes (SC#2 — the refresh path no longer resets dock geometry on rerun)
  </behavior>
  <action>
    Add `update_scene(self, scene: SceneDefinition) -> None` to `ViewportPanel` in `src/surg_rl/editor/viewport.py` per RESEARCH.md Pattern 4 + Pitfall 7 + A3: `with contextlib.suppress(AttributeError, OSError): if self._simulator is not None: self._simulator.close()`; `self._simulator = None` (forces `_tick` to reload via `_on_load_simulator` on the next tick — viewport.py:189-209); `self._scene = scene`; `self.reset_camera()` (reuses viewport.py:390-397, A3 — new scene = fresh view). Do NOT create a new `ViewportPanel`. Do NOT call `setCentralWidget`. Do NOT halt `_running` (the render loop continues; next `_tick` reloads the simulator for the new scene). `contextlib` is already imported in viewport.py (used by stop() at L169-180).

    Add `update_scene(self, scene: SceneDefinition) -> None` to `SceneTreeView` in `src/surg_rl/editor/tree_view.py` per RESEARCH.md Pattern 4 + A2: `self._scene = scene`; `self._model.clear()`; `self._model.setHorizontalHeaderLabels(["Scene Elements"])` (clear() drops headers — verified against the __init__ order at tree_view.py:56-69); `self._build_tree()`; `self.expandAll()`. After `clear()`, verify whether `selectionModel().currentChanged` still fires (A2): if `clear()` reset the selection model, re-wire `self.selectionModel().currentChanged.connect(self._on_selection_changed)`. Do NOT create a new `SceneTreeView`. Do NOT call `setWidget`. Do NOT re-wire `customContextMenuRequested` (it's on `self`, preserved — A2).

    Replace the body of `EditorWindow._refresh_viewport_and_tree` (current main_window.py:306-319) with in-place swap calls: `self._tree_view.update_scene(self._scene or _empty_scene_stub())`; `self._viewport_panel.update_scene(self._scene or _empty_scene_stub())`. Remove the `from surg_rl.editor.tree_view import SceneTreeView` and `from surg_rl.editor.viewport import ViewportPanel` local imports from this method body (no longer constructing widgets). Remove the `new SceneTreeView(...)`, `self._tree_dock.setWidget(...)`, `self._tree_view.node_selected.connect(self._on_node_selected)`, `new ViewportPanel(...)`, `self.setCentralWidget(...)`, and `old_panel.stop()` lines — none are needed with in-place swap (the widget identity survives, so the `node_selected` connection made in `_build_dock_widgets` survives; `old_panel.stop()` is replaced by update_scene's internal simulator close). Keep the function signature and the `_empty_scene_stub()` fallback.

    Add a new test class `TestUpdateScene` to `tests/test_dock_state.py` (or fold into TestDockRoundTrip — implementer's discretion, but a dedicated class is cleaner) verifying the in-place swap contract offscreen:
    - `test_update_scene_does_not_recreate_viewport(qapp, isolated_home)`: build EditorWindow, show, capture `vp_id = id(w._viewport_panel)`, call `w._refresh_viewport_and_tree()`, processEvents, assert `id(w._viewport_panel) == vp_id` (same widget instance — no recreation).
    - `test_update_scene_does_not_recreate_tree(qapp, isolated_home)`: same for `w._tree_view` — assert `id` unchanged across `_refresh_viewport_and_tree()`.
    These tests are the SC#2 regression guard: they prove the refresh path no longer recreates widgets (the bug #3 root cause).

    Run the full phase test file → GREEN. Run the full suite to confirm no regression in the 1,513-test baseline.
  </action>
  <verify>
    <automated>PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_dock_state.py -v</automated>
    <automated>PYTHONPATH=src pytest tests/ -v 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/surg_rl/editor/viewport.py` contains `def update_scene(self, scene` with `self._simulator = None` and `self.reset_camera()` in its body (Pitfall 7 + A3)
    - `src/surg_rl/editor/viewport.py` update_scene body does NOT contain `setCentralWidget` or `ViewportPanel(` construction
    - `src/surg_rl/editor/tree_view.py` contains `def update_scene(self, scene` with `self._model.clear()` and `self._build_tree()` and `self.expandAll()` in its body
    - `src/surg_rl/editor/tree_view.py` update_scene body does NOT contain `setWidget` or `SceneTreeView(` construction
    - `src/surg_rl/editor/main_window.py` `_refresh_viewport_and_tree` body contains `self._tree_view.update_scene` and `self._viewport_panel.update_scene` and does NOT contain `setCentralWidget`, `setWidget`, or `new ViewportPanel`/`SceneTreeView` construction (`grep -E "setCentralWidget|setWidget|ViewportPanel\(|SceneTreeView\(" src/surg_rl/editor/main_window.py | grep -v '^#' | grep -c .` returns 0 — wait, the imports + other methods may reference these; scope the check to the _refresh_viewport_and_tree method body specifically)
    - `tests/test_dock_state.py` contains a test asserting `id(w._viewport_panel)` and `id(w._tree_view)` are unchanged across `w._refresh_viewport_and_tree()`
    - `PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_dock_state.py -v` exits 0 (all 4 SC#1/#2/#4 test classes + update_scene tests green)
    - `PYTHONPATH=src pytest tests/ -v` exits 0 (no regression in the 1,513-test baseline)
  </acceptance_criteria>
  <done>
    ViewportPanel.update_scene and SceneTreeView.update_scene implemented in-place (no widget recreation). _refresh_viewport_and_tree calls update_scene. SC#2 regression test (id unchanged across refresh) green. Full suite green. SC#1, SC#2, SC#4 all satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan touches only local Qt-client dock geometry + in-place widget state swap. No auth, network, or untrusted-input boundaries beyond what already exists in the editor. The LLM HTTP call (in Plan 02) is the only external boundary and is out of scope here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Information Disclosure | error dialogs surfaced from teardown/reset paths | low | accept | Any user-facing error string from a reset/teardown path must route through `safe_error_message()` (existing pattern in `_safe_error.py` + `llm_panel.py:38,144`). This plan adds no new user-facing error strings (reset_to_default returns bool, showEvent/capture are silent). |
| T-41-02 | Tampering | QSettings INI store (window/geometry, window/state keys) | low | accept | The factory-default QByteArray is NOT persisted to QSettings (D-01 — recomputed each launch from the first showEvent). Only the user's saved layout round-trips via the existing keys, which are non-sensitive (geometry + dock arrangement). No-secrets convention in `_settings.py:8-9` upheld. |
| T-41-SC | Tampering | pip/ npm/ cargo installs | low | accept | This phase installs ZERO external packages (GUI-18 is pure code on the existing PySide6 dep, verified in RESEARCH.md §Package Legitimacy Audit). No install tasks; no slopcheck needed. |
</threat_model>

<verification>
- `PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_dock_state.py -v` — all phase tests green (TestDockObjectNames, TestDockRoundTrip, TestUpdateScene).
- `PYTHONPATH=src pytest tests/ -v` — full suite green (no regression in the 1,513-test baseline).
- `ruff check src/surg_rl/editor/dock_state.py src/surg_rl/editor/main_window.py src/surg_rl/editor/viewport.py src/surg_rl/editor/tree_view.py tests/test_dock_state.py` clean.
- `mypy src/surg_rl/editor` clean (type hints required per CLAUDE.md).
- Manual (optional, post-verification): launch the editor offscreen-capable or on a display, rearrange docks, click View → Reset Layout, confirm the factory arrangement (incl. tabification) restores.
</verification>

<success_criteria>
- SC#1: Reset Layout restores the factory-default arrangement (incl. tabification/floating/closed) — TestDockRoundTrip::test_reset_layout_restores_factory_default green.
- SC#2: rearrange → load scene → close → reopen restores the saved layout — TestDockRoundTrip::test_rearrange_close_reopen green + TestUpdateScene id-unchanged tests green (no widget recreation).
- SC#4: every QDockWidget has a non-empty unique objectName — TestDockObjectNames green.
- Full suite green (1,513-test baseline + new phase tests).
</success_criteria>

<output>
Create `.planning/phases/41-dock-layout-reset-closeevent-teardown/41-01-SUMMARY.md` when done.

## Artifacts this phase produces

**New symbols (this plan):**
- `src/surg_rl/editor/dock_state.py`: `class DockStateManager` with methods `capture_factory_default(window)`, `reset_to_default(window) -> bool`, `_rebuild_default_layout(window)`, and private state `_factory_state: QByteArray | None`, `_captured: bool`.
- `src/surg_rl/editor/viewport.py`: `ViewportPanel.update_scene(scene)` method.
- `src/surg_rl/editor/tree_view.py`: `SceneTreeView.update_scene(scene)` method.
- `src/surg_rl/editor/main_window.py`: `EditorWindow.showEvent(event)` override; `self._dock_state` instance attribute; rewired `_action_reset_layout` and `_refresh_viewport_and_tree` bodies (no new symbols, just behavior change).
- `tests/test_dock_state.py`: test classes `TestDockObjectNames`, `TestDockRoundTrip`, `TestUpdateScene`.

**New symbols (Plan 02, for drift-verification exclusion):**
- `EditorWindow.aboutToClose` signal; `EditorWindow.closeEvent` extended (behavior change, not a new symbol).
- `LLMPanel.stop()` method.
- Test classes `TestCloseMidCallMockSlow`, `TestCloseMidCallRealProvider`.

The plan-review-convergence source-grounding pass should exclude these newly-created symbols from drift verification (they did not exist before this phase).
</output>