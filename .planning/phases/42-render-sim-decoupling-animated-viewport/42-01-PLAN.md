---
phase: 42-render-sim-decoupling-animated-viewport
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/surg_rl/editor/sim_step_worker.py
  - src/surg_rl/editor/render_poll_loop.py
  - tests/test_sim_step_worker.py
  - tests/test_render_poll_loop.py
autonomous: true
requirements: [GUI-11]
user_setup: []

must_haves:
  truths:
    # SC#1 — preview animates (bug #1 root cause: _tick never called step())
    - "SC#1 (GUI-11): SimStepWorker calls simulator.step(None) at ~50 Hz via the fixed-step accumulator (accum += wall_dt * speed; while accum >= sim_dt=0.02: step(None); accum -= sim_dt) and publishes State snapshots via the queued snapshot_ready signal — physics advances, the preview is no longer a frozen single frame (per D-01, D-03)"
    # SC#2 — offscreen proxy (cadence) + backstop (real-fps)
    - "SC#2 proxy (GUI-11): RenderPollLoop's self-rescheduling QTimer.singleShot has a _FRAME_INTERVAL_MS=33 cadence and, with an instant-render MockSimulator, fires at >=30 Hz — the render-poll yields to the event loop between frames (per D-01)"
    - statement: "On a real macOS display, a typical scene animates at >30 fps"
      verification: backstop
    # SC#3 — pause/resume/step-one
    - "SC#3 (GUI-11): set_paused(True) stops the accumulator QTimer (no step() calls while paused); set_paused(False) resumes; step_one() increments MockSimulator.step_count by exactly 1 while paused and publishes exactly 1 snapshot (per D-07)"
    # SC#4 — render/sim decoupled + ~30 Hz publish cap
    - "SC#4 (GUI-11): a slow render does NOT slow physics (MockSimulator render sleeps 80 ms; step_count still advances at ~50 Hz because step() runs on the worker QThread); a fast sim does NOT flood the UI — snapshot_ready fired <= ~3 times in 100 ms wall for a 50 Hz sim with the 30 Hz publish cap (per D-01)"
    # D-09 — speed scaling
    - "D-09: at 2x speed, step_count after 100 ms wall ~= 2x the 1x count; at 0.5x ~= half (within QTimer jitter). Speed scales wall_dt (accum += wall_dt * speed), NOT sim_dt — sim_dt stays fixed at 1/50 (Pitfall 5, per D-09)"
    # step-one renders while paused (Pitfall 6)
    - "Pitfall 6: after step_one() while paused, the render-poll's next tick renders the new snapshot — RenderPollLoop stays alive at ~30 Hz while paused (pause ONLY stops the worker's accumulator QTimer), so camera orbit still works and a step-one snapshot gets rendered on the next poll (per D-12)"
    # Edge probe flagged assumptions (boundary + precision) — resolved in task <action> text
    - "FLAGGED ASSUMPTION (boundary, unresolved edge for GUI-11): the accumulator threshold boundary is `accum >= sim_dt` (INCLUSIVE — one step executes at exactly accum == sim_dt); one step either side: at accum = sim_dt - epsilon no step, at sim_dt + epsilon one step. The ~30 Hz publish cap boundary is `now - last_publish >= 1.0/_PUBLISH_HZ` (INCLUSIVE — a snapshot is published at exactly 30 Hz); at 31 Hz the cap still limits to ~30 publishes/wall-second. The wait(3000) teardown timeout is 3000 ms; `wait()` returns True if the thread exits within 3000 ms, False on timeout (log and proceed, NEVER terminate). The 5 speed values are exactly 0.25 / 0.5 / 1 / 2 / 4. Resolved in SimStepWorker._tick action."
    - "FLAGGED ASSUMPTION (precision, unresolved edge for GUI-11): accumulator float-drift contract — wall_dt is RECOMPUTED from time.monotonic() each tick (NOT accumulated) so wall-clock skew does not compound; the while loop subtracts the fixed sim_dt=0.02 each step; when steps == _MAX_STEPS_PER_TICK=8 (spiral-of-death cap) accum is RESET to 0.0 to discard backed-up debt. frame_id is a monotonic int counter (no float precision loss). Step-count scaling at speed multipliers: `accum += wall_dt * speed` scales the accumulator INPUT — at 2x the accumulator fills 2x faster and the while loop runs floor(accum / sim_dt) steps per tick (the while condition is the floor). Resolved in SimStepWorker._tick action."
  artifacts:
    - "src/surg_rl/editor/sim_step_worker.py — SimStepWorker(QtCore.QObject) with class-level Signal `snapshot_ready = QtCore.Signal(object)` (payload = _Snapshot wrapper dataclass with .state and .frame_id); slots @QtCore.Slot() start, @QtCore.Slot(bool) set_paused, @QtCore.Slot(float) set_speed, @QtCore.Slot() step_one, @QtCore.Slot(object) bind_scene; stop() method (cancel flag + timer.stop, NO thread.quit/wait — controller owns that); _tick accumulator (sim_dt=1/50=0.02, _PUBLISH_HZ=30, _MAX_STEPS_PER_TICK=8 spiral cap); _Snapshot dataclass (state, frame_id: int); _cancelled bool, _paused bool (default True per D-11), _speed float (default 1.0), _frame_id int monotonic"
    - "src/surg_rl/editor/render_poll_loop.py — RenderPollLoop(QtCore.QObject) with on_snapshot slot (stores _latest_snapshot), start() (sets _running=True, QTimer.singleShot(0, _tick)), stop() (sets _running=False — does NOT close simulator), _tick (self-rescheduling QTimer.singleShot(_FRAME_INTERVAL_MS=33, _tick) with _running guard at top AND before reschedule; reads _latest_snapshot, skips render when snapshot.frame_id == _last_rendered_id, renders initial static frame when snapshot is None, pushes _editor_camera_* into simulator before render, calls simulator.render(mode='rgb_array', w, h, camera_name), updates _last_rendered_id, calls on_fps_update)"
    - "tests/test_sim_step_worker.py — MockSimulator (step_delay, step_count, get_state->State(time=float(step_count)), render->np.zeros, close) + offscreen harness (QT_QPA_PLATFORM=offscreen module-top, _HAVE_PYSIDE6 try/except, pytestmark skipif, qapp session fixture, isolated_home fixture) + classes TestSimStepWorkerAccumulator (SC#1), TestPauseResumeStepOne (SC#3), TestDecouplingAndPublishCap (SC#4), TestSpeedScaling (D-09)"
    - "tests/test_render_poll_loop.py — MockSimulator + offscreen harness + classes TestRenderPollCadence (SC#2 proxy — assert render-poll fires >=30 Hz with instant-render mock), TestStepOneRendersWhilePaused (Pitfall 6), TestSkipNoNewSnapshot, TestRunningGuard"
  key_links:
    - "SimStepWorker.snapshot_ready (queued Signal(object)) --[Qt.ConnectionType.QueuedConnection]--> RenderPollLoop.on_snapshot (the decoupling boundary, D-03; State is pure-data, safe across threads per A3)"
    - "SimStepWorker._tick QTimer (created in start() slot, affinity = worker QThread) --> simulator.step(None) + simulator.get_state() (D-01, D-03; step(None) is the physics-only advance, both backends guard `if action is not None`)"
    - "RenderPollLoop._tick QTimer.singleShot(_FRAME_INTERVAL_MS=33, _tick) (UI thread, self-rescheduling) --> simulator.render(mode='rgb_array', w, h, camera_name) (D-01; render() ONLY on the UI thread — GL/software-renderer contexts are thread-affine, Pitfall 2)"
    - "SimStepWorker.stop() sets _cancelled=True + _timer.stop() (controller calls thread.quit()+thread.wait(3000) — mirror LLMPanel.stop template, Phase 41 D-05)"
  prohibitions:
    # descriptor-less, flagged-unverified (PROHIB_ABSENT specless fallback) — kept values/safety prohibitions from Stage 2
    - statement: "Must NOT call simulator.render() from the SimStepWorker worker thread (GL/software-renderer contexts are thread-affine — render() ONLY on the UI thread via RenderPollLoop; the worker only calls step() + get_state())"
      status: unverified
      flagged: true
    - statement: "Must NOT add a second QTimer.singleShot chain on the main thread for stepping (D-02 — ONE render timer on the UI thread + ONE sim loop on the QThread worker; two singleShot chains on the main thread re-enter render() mid-framebuffer-acquire → GL context contention + the <10fps stutter returns)"
      status: unverified
      flagged: true
    - statement: "Must NOT call thread.terminate() on a wait() timeout (D-04 — risks leaving simulator physics state inconsistent; log a warning and proceed with close)"
      status: unverified
      flagged: true
    - statement: "Must NOT call thread.deleteLater() before thread.wait() (Pitfall 4 — 'Deleting a running QThread will result in a program crash'; thread.finished -> thread.deleteLater is the ONLY delete path, wired in EditorWindow.__init__)"
      status: unverified
      flagged: true
    - statement: "Must NOT write speed/playback state to SceneDefinition or EditorSettings (D-05/D-10 — session-only, panel-local; this phase writes nothing to the schema/QSettings to avoid silently corrupting undo/save/revert)"
      status: unverified
      flagged: true
  flagged_assumptions:
    - "GUI-11 edge probe returned 2 unresolved edges (boundary + precision). Both are surfaced as FLAGGED ASSUMPTION truths above and resolved with concrete thresholds/contracts in the SimStepWorker._tick action text (sim_dt=0.02, publish cap 30 Hz, wait(3000), speed values 0.25/0.5/1/2/4, inclusive `>=` boundaries, monotonic int frame_id, _MAX_STEPS_PER_TICK=8 spiral cap, wall_dt recomputed from time.monotonic() each tick). No silent drop."

---

<objective>
Deliver the decoupling core of GUI-11: two new editor modules — `SimStepWorker(QObject)` (QThread worker-object with a fixed-step ~50 Hz accumulator, ~30 Hz publish-capped `snapshot_ready` signal, pause/resume/step-one/speed/bind slots, cooperative `stop()`) and `RenderPollLoop(QObject)` (UI-thread self-rescheduling ~30 Hz render of the latest published snapshot, skip-when-no-new, `_running` guard). Both are built and tested in isolation with a `MockSimulator` + controllable clock offscreen — no `EditorWindow`/`ViewportPanel` integration yet (that is Plan 02, Wave 2).

Purpose: Close bug #1 (immobile preview — physics never advanced) and bug #2 (<10fps — synchronous render blocked the event loop) at the component level first, so Plan 02's integration is pure wiring. SC#1, SC#2 proxy, SC#3, SC#4, and D-09 are satisfied here at the unit/integration-offscreen level.
Output: `src/surg_rl/editor/sim_step_worker.py` (new), `src/surg_rl/editor/render_poll_loop.py` (new), `tests/test_sim_step_worker.py` (new), `tests/test_render_poll_loop.py` (new).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-render-sim-decoupling-animated-viewport/42-CONTEXT.md
@.planning/phases/42-render-sim-decoupling-animated-viewport/42-RESEARCH.md
@.planning/phases/42-render-sim-decoupling-animated-viewport/42-PATTERNS.md
@.planning/phases/42-render-sim-decoupling-animated-viewport/42-VALIDATION.md
@.planning/phases/41-dock-layout-reset-closeevent-teardown/41-CONTEXT.md
@src/surg_rl/editor/llm_panel.py
@src/surg_rl/editor/viewport.py
@src/surg_rl/editor/__init__.py
@src/surg_rl/simulators/base_simulator.py
@tests/test_dock_state.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0 / RED): Create test_sim_step_worker.py + test_render_poll_loop.py scaffolds with MockSimulator + offscreen harness + RED tests for accumulator/publish-cap/step-one/speed/teardown/render-poll</name>
  <files>tests/test_sim_step_worker.py, tests/test_render_poll_loop.py</files>
  <read_first>
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-CONTEXT.md (D-01, D-03, D-07, D-09, D-11 locked)
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-RESEARCH.md (§Pattern 1 SimStepWorker accumulator skeleton; §Pattern 2 RenderPollLoop; §Code Examples — MockSimulator shape VERIFIED against base_simulator.py:90-111 State + 114-135 StepResult; §Validation Architecture → Test Map — the exact test classes + SC mapping; §Pitfalls 4, 6, 8 — spiral cap, step-one-renders-while-paused, _loaded guard)
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-PATTERNS.md (§tests/test_dock_state.py — VERBATIM offscreen harness at :19-55; §MockSimulator pattern; §tests/test_sim_step_worker.py / test_render_poll_loop.py assignments)
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-VALIDATION.md (§Per-Task Verification Map — TestSimStepWorkerAccumulator=SC#1, TestRenderPollCadence=SC#2 proxy, TestPauseResumeStepOne=SC#3, TestDecouplingAndPublishCap=SC#4, TestSpeedScaling=D-09, TestStepOneRendersWhilePaused=Pitfall 6)
    - src/surg_rl/simulators/base_simulator.py (State at :90-111, StepResult at :114-135 — the shapes MockSimulator mirrors)
    - src/surg_rl/editor/llm_panel.py (the QThread worker-object + Signal pattern these tests construct)
    - tests/test_dock_state.py (lines 19-55 — the standalone offscreen harness to mirror: QT_QPA_PLATFORM=offscreen module-top, _HAVE_PYSIDE6 try/except, pytestmark skipif, qapp session fixture, isolated_home fixture; lines 277-329 — the TestCloseMidCallMockSlow pattern for the teardown test)
  </read_first>
  <behavior>
    - MockSimulator.step(None) increments step_count by 1 and returns a StepResult; step(delay) sleeps step_delay
    - MockSimulator.get_state() returns State(time=float(step_count))
    - MockSimulator.render(...) returns np.zeros((h, w, 3)) with NO delay by default (instant render for the cadence proxy) and a configurable render_delay for the decoupling test
    - TestSimStepWorkerAccumulator: after ~100 ms wall with the worker unpaused at 1x, step_count is ~5 (50 Hz); snapshot_ready fired (the publish cap is tested in TestDecouplingAndPublishCap)
    - TestPauseResumeStepOne: set_paused(True) stops step calls (step_count does not advance); step_one() increments step_count by EXACTLY 1 and publishes EXACTLY 1 snapshot; set_paused(False) resumes
    - TestDecouplingAndPublishCap: with MockSimulator(render_delay=0.08) (slow render on the UI thread does not affect the worker), step_count still advances at ~50 Hz; with render_delay=0, snapshot_ready fired <= ~3 times in 100 ms (30 Hz cap)
    - TestSpeedScaling: at 2x, step_count after 100 ms ~= 2x the 1x count; at 0.5x ~= half (within QTimer jitter)
    - TestRenderPollCadence: with an instant-render MockSimulator, RenderPollLoop fires >=30 Hz (count QTimer.singleShot callbacks in a 100 ms window)
    - TestStepOneRendersWhilePaused: render-poll stays alive while the worker is paused; after step_one(), the next render-poll tick renders the new snapshot (render called once for the new frame_id)
    - TestSkipNoNewSnapshot: render() NOT called when snapshot.frame_id == _last_rendered_id
    - TestRunningGuard: after stop(), _tick early-returns and does NOT reschedule
  </behavior>
  <action>
    Create `tests/test_sim_step_worker.py` and `tests/test_render_poll_loop.py` as standalone single-file offscreen test modules mirroring `tests/test_dock_state.py:19-55` VERBATIM in harness shape: module-top `os.environ.setdefault("QT_QPA_PLATFORM", "offscreen")`, `_HAVE_PYSIDE6` try/except import guard, `pytestmark = pytest.mark.skipif(not _HAVE_PYSIDE6, reason="PySide6 not installed")`, a session-scoped `qapp` fixture (`QApplication.instance() or QApplication(sys.argv)`), and an `isolated_home` fixture (`monkeypatch.setenv("HOME", str(tmp_path))` + `monkeypatch.setenv("XDG_CONFIG_HOME", str(tmp_path / ".config"))` → yields tmp_path). Duplicate the small fixtures in each file (per 42-PATTERNS.md test assignment).

    Define a shared `MockSimulator` (duplicate in each file, or place in a small `tests/_mock_sim.py` helper and import — implementer's discretion). The MockSimulator shape is VERIFIED in 42-RESEARCH.md §Code Examples: `step_delay: float = 0.0`, `step_count: int = 0`, `_loaded = True`, `timestep = 0.02`, `frame_skip = 1`; `step(action)` sleeps `step_delay` then `step_count += 1` and returns `StepResult(observation=Observation(), reward=0.0, terminated=False, truncated=False)`; `get_state()` returns `State(time=float(self.step_count))`; `render(mode="rgb_array", width=None, height=None, camera_name=None)` returns `np.zeros((height or 480, width or 640, 3), dtype=np.uint8)`; `close()` is a no-op. Import `State`, `StepResult`, `Observation` from `surg_rl.simulators.base_simulator`.

    In `tests/test_sim_step_worker.py` write these pytest classes (RED on this task — they import `SimStepWorker` from `surg_rl.editor.sim_step_worker`, which does not exist yet; let the ImportError surface as the honest RED baseline — do NOT add `pytest.skip`/`xfail`):
    - `TestSimStepWorkerAccumulator` (SC#1): construct `SimStepWorker`, `moveToThread` a real `QThread`, `thread.started.connect(worker.start)`, `thread.start()`, `worker.bind_scene.emit(MockSimulator())` (queued), `worker.set_paused.emit(False)` (queued — the worker loads paused per D-11), `time.sleep(0.1)`, `qapp.processEvents()`, assert `mock.step_count >= 3` (50 Hz for 100 ms ≈ 5; allow jitter down to 3). Use `Qt.QueuedConnection` for cross-thread slot calls.
    - `TestPauseResumeStepOne` (SC#3): bind a MockSimulator, keep paused (D-11 default), call `step_one` (queued), processEvents, assert `mock.step_count == 1` and exactly one `snapshot_ready` emission (use a Qt signal spy or a counter slot connected with `Qt.QueuedConnection`). Set paused False, sleep 100 ms, assert step_count advanced. Set paused True, sleep 100 ms, assert step_count did NOT advance further.
    - `TestDecouplingAndPublishCap` (SC#4): bind `MockSimulator(step_delay=0)`; spy on `snapshot_ready`; unpause for 100 ms; assert `mock.step_count >= 3` (50 Hz sim) AND `snapshot_ready` fired `<= 4` times in 100 ms (30 Hz cap → ~3, allow 4 for jitter). Then a second test with `MockSimulator(render_delay=0.08)` on a RenderPollLoop (render sleeps 80 ms) — assert the WORKER's `step_count` still advanced at ~50 Hz (the slow render does not slow physics — the worker is on its own QThread).
    - `TestSpeedScaling` (D-09): at 2x, step_count after 100 ms ≈ 2× the 1x count; at 0.5x ≈ half. Use `worker.set_speed.emit(2.0)` / `0.5` (queued). Allow generous jitter bounds (e.g. 1.5x..2.5x and 0.3x..0.7x) since QTimer jitter is real.

    In `tests/test_render_poll_loop.py` write these pytest classes (RED — import `RenderPollLoop` from `surg_rl.editor.render_poll_loop`, which does not exist yet):
    - `TestRenderPollCadence` (SC#2 proxy): construct a `RenderPollLoop` with a `MockSimulator()` (instant render) and a frame-counter spy; `loop.start()`; `time.sleep(0.1)`; processEvents; assert the render callback fired `>= 3` times in 100 ms (30 Hz → ~3). This is the offscreen cadence proxy — real-fps is the backstop truth.
    - `TestStepOneRendersWhilePaused` (Pitfall 6): the render-poll stays alive while the worker is paused; inject a snapshot with `frame_id=1` via `loop.on_snapshot(_Snapshot(state, 1))`; processEvents; assert `MockSimulator.render` was called once for frame_id=1; inject `frame_id=2`; assert render called once for frame_id=2.
    - `TestSkipNoNewSnapshot`: inject `frame_id=1`; processEvents; assert render called. Inject `frame_id=1` again (same); processEvents; assert render NOT called again (skip-when-no-new).
    - `TestRunningGuard`: `loop.start()`; processEvents; `loop.stop()`; processEvents; assert no further render calls (already-queued `singleShot` callbacks early-return on `not _running`).

    Use a small `_Snapshot` test stub (state + frame_id) OR import the real one once Task 2 creates it — for RED, a local dataclass stub in the test file is fine so the tests fail on the `RenderPollLoop` import, not on `_Snapshot`. Add a teardown in each test that calls `worker.stop()` + `thread.quit()` + `thread.wait(3000)` + `loop.stop()` in a `try/finally` so no thread leaks across tests.
  </action>
  <verify>
    <automated>PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_sim_step_worker.py tests/test_render_poll_loop.py -v 2>&amp;1 | grep -E "(ImportError|ModuleNotFoundError|sim_step_worker|render_poll_loop)" | grep -v '^#' | grep -c . ; echo "expect RED (ImportError) >=1 for both modules"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_sim_step_worker.py` exists and declares `pytestmark = pytest.mark.skipif(not _HAVE_PYSIDE6, reason="PySide6 not installed")` at module level + sets `QT_QPA_PLATFORM=offscreen` at module top
    - `tests/test_sim_step_worker.py` defines `class MockSimulator` with `step(action)`, `get_state() -> State`, `render(...) -> np.ndarray`, `close()`, and a `step_count` attribute
    - `tests/test_sim_step_worker.py` contains classes `TestSimStepWorkerAccumulator`, `TestPauseResumeStepOne`, `TestDecouplingAndPublishCap`, `TestSpeedScaling` that import `SimStepWorker` from `surg_rl.editor.sim_step_worker`
    - `tests/test_render_poll_loop.py` contains classes `TestRenderPollCadence`, `TestStepOneRendersWhilePaused`, `TestSkipNoNewSnapshot`, `TestRunningGuard` that import `RenderPollLoop` from `surg_rl.editor.render_poll_loop`
    - Both test files have `qapp` (session) and `isolated_home` (function) fixtures
    - `PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_sim_step_worker.py tests/test_render_poll_loop.py -v` fails with ImportError on the not-yet-created modules (RED baseline — honest, no skip/xfail)
  </acceptance_criteria>
  <done>
    Both test scaffold files exist with offscreen harness, MockSimulator, and the VALIDATION-mapped test classes (RED on import). The ImportError is the honest TDD baseline for Task 2 (SimStepWorker) and Task 3 (RenderPollLoop) GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement SimStepWorker — fixed-step accumulator + ~30 Hz publish cap + step_one/set_speed/set_paused/bind_scene slots + cooperative stop()</name>
  <files>src/surg_rl/editor/sim_step_worker.py</files>
  <read_first>
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-CONTEXT.md (D-01, D-03, D-04, D-07, D-09, D-11 locked)
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-RESEARCH.md (§Pattern 1 — the recommended SimStepWorker skeleton with _SIM_HZ=50.0, _PUBLISH_HZ=30.0, _MAX_STEPS_PER_TICK=8; §Pitfalls 4, 5, 8 — spiral cap, scale wall_dt NOT sim_dt, _loaded guard; §Assumptions A1 step(None) safe off UI thread, A3 State safe across threads; §Resolving D-12 — NOT this task, but note the _Snapshot wrapper)
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-PATTERNS.md (§sim_step_worker.py — LazyImport discipline from editor/__init__.py:31-42; QObject worker + Signal declaration from llm_panel.py:17-26; QThread wiring from llm_panel.py:117-128; stop() cooperative-teardown template from llm_panel.py:130-158; _running guard from viewport.py:189-191; simulator.step(None) VERIFIED mujoco_simulator.py:220-221 + pybullet_simulator.py:946-947)
    - .planning/phases/41-dock-layout-reset-closeevent-teardown/41-CONTEXT.md (D-05 — stop() = cancel flag + thread.quit() + thread.wait(3000) + log-on-timeout, NEVER terminate; the controller owns thread.quit/wait, the worker only flags cancel + stops the timer)
    - src/surg_rl/editor/__init__.py (LazyImport proxies — import Qt via `from surg_rl.editor import QtCore`, NOT `from PySide6 import ...`)
    - src/surg_rl/editor/llm_panel.py (TextParserWorker QObject + Signal shape at :17-26; QThread wiring at :117-128; stop() template at :130-158)
    - src/surg_rl/editor/viewport.py (_running guard pattern at :142, :189-191, :289-290; stop() suppress at :169-180)
    - src/surg_rl/simulators/base_simulator.py (step at :219, get_state at :252, State at :90-111)
    - tests/test_sim_step_worker.py (the RED tests from Task 1 — these are the GREEN targets)
  </read_first>
  <behavior>
    - SimStepWorker.snapshot_ready is a QtCore.Signal(object) declared at class-body top (payload = _Snapshot(state, frame_id))
    - start() slot creates the QTimer (affinity = worker thread), sets PreciseTimer, connects timeout -> _tick, starts at int(1000/_SIM_HZ)=20 ms; resets _last_wall = time.monotonic()
    - bind_scene(simulator) slot sets self._simulator, resets _accum=0.0, _last_wall=monotonic(), _last_publish=_last_wall, _frame_id=0
    - set_paused(True) stops the timer (no step calls while paused); set_paused(False) resets _last_wall and starts the timer
    - set_speed(speed) sets self._speed (scales wall_dt in the accumulator, NOT sim_dt — Pitfall 5)
    - step_one() runs exactly one simulator.step(None) + increments _frame_id + emits snapshot_ready (even while paused — D-07; does NOT resume the timer)
    - _tick: guard `if self._cancelled or self._simulator is None: return`; now=time.monotonic(); wall_dt=now-self._last_wall; _last_wall=now; accum += wall_dt * self._speed; sim_dt=1.0/_SIM_HZ=0.02; steps=0; while accum >= sim_dt and steps < _MAX_STEPS_PER_TICK=8: simulator.step(None); accum -= sim_dt; steps += 1; if steps == _MAX_STEPS_PER_TICK: accum = 0.0 (spiral cap); if now - _last_publish >= 1.0/_PUBLISH_HZ (>= INCLUSIVE): _frame_id += 1; snapshot_ready.emit(_Snapshot(get_state(), _frame_id)); _last_publish = now
    - stop(): _cancelled=True; if _timer: _timer.stop() (does NOT call thread.quit/wait — the controller owns that, mirroring LLMPanel.stop)
    - TestSimStepWorkerAccumulator, TestPauseResumeStepOne, TestDecouplingAndPublishCap, TestSpeedScaling all pass
  </behavior>
  <action>
    Create `src/surg_rl/editor/sim_step_worker.py` following the LazyImport discipline: `from __future__ import annotations`, `import time`, `from dataclasses import dataclass`, `from typing import TYPE_CHECKING`, `from surg_rl.editor import QtCore` (the LazyImport proxy — NOT `from PySide6 import ...` at module top), `from surg_rl.utils.logging import get_logger`, `logger = get_logger(__name__)`. Under `if TYPE_CHECKING:` import `BaseSimulator` from `surg_rl.simulators.base_simulator` (and `State` for the dataclass type hint).

    Define module constants `_SIM_HZ: float = 50.0`, `_PUBLISH_HZ: float = 30.0`, `_MAX_STEPS_PER_TICK: int = 8` (spiral-of-death cap — Pitfall 4). Define `@dataclass class _Snapshot: state: object  # State; frame_id: int` (the wrapper that gives the render-poll a monotonic "new snapshot?" check — implementer's discretion per CONTEXT but a frame_id is required by the render-poll's skip-when-no-new logic).

    Define `class SimStepWorker(QtCore.QObject)` with class-level `snapshot_ready = QtCore.Signal(object)` (the decoupling boundary D-03; payload = `_Snapshot`). `__init__(self) -> None`: `super().__init__()`; `self._simulator = None` (bound via `bind_scene`); `self._timer: QtCore.QTimer | None = None`; `self._paused: bool = True` (D-11 — load paused); `self._speed: float = 1.0` (D-09 default 1x); `self._accum: float = 0.0`; `self._last_wall: float = 0.0`; `self._last_publish: float = 0.0`; `self._frame_id: int = 0` (monotonic int counter — NO float precision loss); `self._cancelled: bool = False`.

    Slots (decorate each with the matching `@QtCore.Slot(...)`):
    - `@QtCore.Slot() def start(self) -> None`: create `self._timer = QtCore.QTimer()` HERE (affinity = worker thread because `start` runs on the worker thread via `thread.started`), `setTimerType(QtCore.Qt.TimerType.PreciseTimer)`, `timeout.connect(self._tick)`, `start(int(1000 / _SIM_HZ))` (=20 ms); `self._last_wall = time.monotonic()`.
    - `@QtCore.Slot(object) def bind_scene(self, simulator) -> None`: `self._simulator = simulator`; `self._accum = 0.0`; `self._last_wall = time.monotonic()`; `self._last_publish = self._last_wall`; `self._frame_id = 0`. (Called queued from `update_scene` on the UI thread — runs on the worker thread after any in-flight `_tick`.)
    - `@QtCore.Slot(bool) def set_paused(self, paused: bool) -> None`: `self._paused = paused`; if `self._timer is None: return`; if paused: `self._timer.stop()`; else: `self._last_wall = time.monotonic()` (reset so wall_dt does not spike across the pause), `self._timer.start(int(1000 / _SIM_HZ))`.
    - `@QtCore.Slot(float) def set_speed(self, speed: float) -> None`: `self._speed = speed`.
    - `@QtCore.Slot() def step_one(self) -> None`: D-07 — if `self._simulator is None: return`; `self._simulator.step(None)` (one physics timestep, even while paused — does NOT resume the timer); `self._frame_id += 1`; `self.snapshot_ready.emit(self._publish())`.
    - `def _tick(self) -> None`: `if self._cancelled or self._simulator is None: return` (Pitfall 8 — _loaded guard via bind_scene); `now = time.monotonic()`; `wall_dt = now - self._last_wall`; `self._last_wall = now`; `self._accum += wall_dt * self._speed` (Pitfall 5 — scale wall_dt, NOT sim_dt); `sim_dt = 1.0 / _SIM_HZ` (=0.02); `steps = 0`; `while self._accum >= sim_dt and steps < _MAX_STEPS_PER_TICK: self._simulator.step(None); self._accum -= sim_dt; steps += 1`; `if steps == _MAX_STEPS_PER_TICK: self._accum = 0.0` (discard backed-up debt — Pitfall 4); `if now - self._last_publish >= 1.0 / _PUBLISH_HZ:` (`>=` INCLUSIVE — 30 Hz cap boundary) `self._frame_id += 1; self.snapshot_ready.emit(self._publish()); self._last_publish = now`.
    - `def _publish(self) -> _Snapshot`: `return _Snapshot(state=self._simulator.get_state(), frame_id=self._frame_id)`.
    - `def stop(self) -> None`: D-04 / Phase 41 D-05 template — `self._cancelled = True`; `if self._timer is not None: self._timer.stop()`. Do NOT call `thread.quit()`/`thread.wait()` here — the controller (`EditorWindow`, Plan 02) owns the QThread and calls those, mirroring `llm_panel.py:130-158` where `LLMPanel.stop()` owns both `_worker` and `_thread`. Keep the separation identical to Phase 41 so the teardown ordering is provably the same. NEVER `thread.terminate()` (D-04). NEVER `deleteLater()` here (Pitfall 4 — `thread.finished -> thread.deleteLater` is the only delete path, wired by the controller).

    Do NOT create `render_poll_loop.py` — that is Task 3. Do NOT touch `viewport.py`/`main_window.py` — that is Plan 02.

    Run the four test classes from Task 1 → GREEN. The `step(None)` advance is backend-agnostic (both simulators guard `if action is not None` — verified mujoco_simulator.py:220-221 + pybullet_simulator.py:946-947).
  </action>
  <verify>
    <automated>PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_sim_step_worker.py::TestSimStepWorkerAccumulator tests/test_sim_step_worker.py::TestPauseResumeStepOne tests/test_sim_step_worker.py::TestDecouplingAndPublishCap tests/test_sim_step_worker.py::TestSpeedScaling -v</automated>
  </verify>
  <acceptance_criteria>
    - `src/surg_rl/editor/sim_step_worker.py` exists and imports Qt via `from surg_rl.editor import QtCore` (NOT `from PySide6 import ...` at module top) — `grep -n "from PySide6" src/surg_rl/editor/sim_step_worker.py | grep -v '^#' | grep -c .` returns 0
    - `src/surg_rl/editor/sim_step_worker.py` defines `class SimStepWorker(QtCore.QObject)` with class-level `snapshot_ready = QtCore.Signal(object)`
    - `src/surg_rl/editor/sim_step_worker.py` defines `_SIM_HZ = 50.0`, `_PUBLISH_HZ = 30.0`, `_MAX_STEPS_PER_TICK = 8` module constants
    - `SimStepWorker` has slots `start`, `bind_scene`, `set_paused`, `set_speed`, `step_one` and a `stop()` method
    - `_tick` contains `while self._accum >= sim_dt and steps < _MAX_STEPS_PER_TICK:` and `self._simulator.step(None)` (NOT `step(0)` or `step(zeros)`)
    - `_tick` scales `wall_dt * self._speed` (Pitfall 5 — `grep -n "wall_dt \* self._speed" src/surg_rl/editor/sim_step_worker.py | grep -v '^#' | grep -c .` returns >=1; `grep -n "sim_dt \* self._speed" src/surg_rl/editor/sim_step_worker.py | grep -v '^#' | grep -c .` returns 0)
    - `stop()` sets `_cancelled=True` and stops the timer; does NOT call `thread.quit`/`thread.wait`/`terminate`/`deleteLater` — `grep -nE "thread\.(quit|wait|terminate|deleteLater)" src/surg_rl/editor/sim_step_worker.py | grep -v '^#' | grep -c .` returns 0
    - `PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_sim_step_worker.py::TestSimStepWorkerAccumulator tests/test_sim_step_worker.py::TestPauseResumeStepOne tests/test_sim_step_worker.py::TestDecouplingAndPublishCap tests/test_sim_step_worker.py::TestSpeedScaling -v` exits 0
  </acceptance_criteria>
  <done>
    SimStepWorker implemented with the fixed-step accumulator (sim_dt=0.02, ~50 Hz), 30 Hz publish cap, step_one/set_speed/set_paused/bind_scene slots, cooperative stop(). All four worker test classes green. SC#1, SC#3, SC#4, D-09 satisfied at the component level.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (GREEN): Implement RenderPollLoop — UI-thread ~30 Hz render of the latest snapshot, skip-when-no-new, _running guard, step-one renders while paused</name>
  <files>src/surg_rl/editor/render_poll_loop.py</files>
  <read_first>
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-CONTEXT.md (D-01, D-02, D-05, D-12 locked)
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-RESEARCH.md (§Pattern 2 — RenderPollLoop skeleton with _FRAME_INTERVAL_MS=33, on_snapshot, start/stop, _tick; §Pitfall 2 — render() ONLY on UI thread; §Pitfall 6 — render-pool stays alive while paused; §Pitfall 8 — _running guard at both ends)
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-PATTERNS.md (§render_poll_loop.py — LazyImport discipline; self-rescheduling QTimer.singleShot + _running guard from viewport.py:165-180, 189-191, 287-290; camera-offset push viewport.py:220-236; render() + framebuffer retry viewport.py:237-274; _display_array + _maybe_update_fps viewport.py:292-353; skip-when-no-new is NEW but trivially derived)
    - src/surg_rl/editor/__init__.py (LazyImport proxies — `from surg_rl.editor import QtCore, QtGui, QtWidgets`)
    - src/surg_rl/editor/viewport.py (_tick at :189-290 the monolithic loop being split — the render half moves here; _running guard at :142, :190-191, :289-290; stop() at :169-180; _display_array at :306-353; _maybe_update_fps at :292-304; camera push at :220-236; _FRAME_INTERVAL_MS=50 at :31 — replaced by 33)
    - src/surg_rl/simulators/base_simulator.py (render at :231 — `render(mode, width, height, camera_name) -> np.ndarray | None`)
    - tests/test_render_poll_loop.py (the RED tests from Task 1 — these are the GREEN targets)
  </read_first>
  <behavior>
    - RenderPollLoop.on_snapshot(snapshot) stores self._latest_snapshot = snapshot (connected queued to SimStepWorker.snapshot_ready)
    - start() sets _running=True and schedules QTimer.singleShot(0, _tick)
    - stop() sets _running=False (does NOT close the simulator — ViewportPanel.stop/update_scene closes the shared simulator after pausing the worker)
    - _tick: `if not self._running: return` at the top; reads _latest_snapshot; if snapshot is None and sim is not None → render the initial static frame once (sentinel _last_rendered_id=-2) so the user sees the scene on load; if snapshot.frame_id != _last_rendered_id → push _editor_camera_* into sim, render(mode="rgb_array", w, h, camera_name), set _last_rendered_id, call on_fps_update; if snapshot.frame_id == _last_rendered_id → skip render; before reschedule check `if self._running: QTimer.singleShot(_FRAME_INTERVAL_MS=33, _tick)`
    - The render-poll NEVER calls simulator.step() (D-02) and NEVER blocks on physics — it only reads the latest snapshot and calls render() on the UI thread
    - TestRenderPollCadence, TestStepOneRendersWhilePaused, TestSkipNoNewSnapshot, TestRunningGuard all pass
  </behavior>
  <action>
    Create `src/surg_rl/editor/render_poll_loop.py` following the LazyImport discipline: `from __future__ import annotations`, `import contextlib`, `from collections.abc import Callable`, `from typing import TYPE_CHECKING`, `from surg_rl.editor import QtCore` (LazyImport proxy — NOT `from PySide6 import ...`), `from surg_rl.utils.logging import get_logger`, `logger = get_logger(__name__)`. Under `if TYPE_CHECKING:` import `BaseSimulator` from `surg_rl.simulators.base_simulator`.

    Define module constant `_FRAME_INTERVAL_MS: int = 33` (~30 Hz render poll — replaces the old `_tick`'s 50 ms / 20 Hz).

    Define `class RenderPollLoop(QtCore.QObject)`. `__init__(self, simulator_ref: Callable[[], object], canvas, camera_offset_ref: Callable[[], dict], on_fps_update: Callable[[float], None] | None = None, width: int = 640, height: int = 480, camera_name: str | None = None) -> None`: `super().__init__()`; `self._simulator_ref = simulator_ref` (a callable returning the CURRENT shared simulator — the live simulator is swapped in-place by `ViewportPanel.update_scene`, so the loop always reads the latest); `self._canvas = canvas`; `self._camera_offset_ref = camera_offset_ref`; `self._on_fps_update = on_fps_update`; `self._width = width`; `self._height = height`; `self._camera_name = camera_name`; `self._running: bool = True`; `self._latest_snapshot = None`; `self._last_rendered_id: int = -1` (sentinel -1 = "no frame rendered yet"; -2 = "initial static frame rendered"). Mirror the fps-counter state from `viewport.py:137-138, 292-304` (`_frame_count: int = 0`, `_last_fps_check: float`, `_fps: float = 0.0`).

    Methods:
    - `def on_snapshot(self, snapshot) -> None`: `self._latest_snapshot = snapshot` (connected queued to `SimStepWorker.snapshot_ready` — the thread boundary D-03).
    - `def bind_simulator(self, simulator) -> None`: update the callable ref OR set a direct attr (implementer's discretion — `ViewportPanel.update_scene` calls this in Plan 02). If using a direct attr, `self._simulator = simulator` and the ref callable becomes optional. Pick ONE and keep it consistent with Plan 02.
    - `def start(self) -> None`: `self._running = True`; `QtCore.QTimer.singleShot(0, self._tick)` (kick off the self-rescheduling chain).
    - `def stop(self) -> None`: `self._running = False` (the `_running` guard — already-queued `singleShot` callbacks early-return at the top of `_tick`). Do NOT close the simulator here — `ViewportPanel.stop()` / `update_scene` closes the shared simulator AFTER pausing the worker (Pitfall 3 ordering).
    - `def _tick(self) -> None`: `if not self._running: return` (top guard — Pitfall 8). `sim = self._simulator_ref()` (or `self._simulator`). `snap = self._latest_snapshot`. Branch:
      - `if sim is None:` skip to reschedule (no simulator bound yet).
      - `elif snap is None and self._last_rendered_id == -1:` render the initial static frame ONCE (so the user sees the scene on a paused load per D-11); set `self._last_rendered_id = -2` (sentinel: "initial frame rendered, only re-render on new frame_id").
      - `elif snap is not None and snap.frame_id != self._last_rendered_id:` push `_editor_camera_*` into `sim` via `object.__setattr__(sim, "_editor_camera_target", self._camera_offset_ref()["target"])` etc. (the VERBATIM `viewport.py:220-236` block — D-05 ephemeral camera, kept on the render side); call `arr = sim.render(mode="rgb_array", width=self._width, height=self._height, camera_name=self._camera_name)`; wrap in the `try/except Exception: # noqa: BLE001` framebuffer-retry pattern from `viewport.py:243-274` (route any user-facing string through `safe_error_message` from `surg_rl.editor._safe_error`); if `arr is not None:` display it via `self._canvas.set_image(arr)` (or the `_display_array` path from `viewport.py:306-353` — delegate to the canvas); `self._last_rendered_id = snap.frame_id`; `self._frame_count += 1`; `self._maybe_update_fps()`.
      - `elif snap is not None and snap.frame_id == self._last_rendered_id:` skip render (no new snapshot — saves CPU).
      - Before reschedule: `if self._running: QtCore.QTimer.singleShot(_FRAME_INTERVAL_MS, self._tick)` (bottom guard — Pitfall 8).
    - `def _maybe_update_fps(self) -> None`: mirror `viewport.py:292-304` (`time.monotonic()`, 1 s window); call `self._on_fps_update(self._fps)` if provided.

    The render-poll NEVER calls `simulator.step()` (D-02) and NEVER blocks on physics — it reads the latest snapshot and calls `render()` on the UI thread only (Pitfall 2 — GL thread-affinity). `render()` is the ONLY simulator call here.

    Run the four test classes from Task 1 → GREEN.
  </action>
  <verify>
    <automated>PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_render_poll_loop.py::TestRenderPollCadence tests/test_render_poll_loop.py::TestStepOneRendersWhilePaused tests/test_render_poll_loop.py::TestSkipNoNewSnapshot tests/test_render_poll_loop.py::TestRunningGuard -v</automated>
  </verify>
  <acceptance_criteria>
    - `src/surg_rl/editor/render_poll_loop.py` exists and imports Qt via `from surg_rl.editor import QtCore` (NOT `from PySide6 import ...` at module top) — `grep -n "from PySide6" src/surg_rl/editor/render_poll_loop.py | grep -v '^#' | grep -c .` returns 0
    - `src/surg_rl/editor/render_poll_loop.py` defines `_FRAME_INTERVAL_MS = 33` and `class RenderPollLoop(QtCore.QObject)` with `on_snapshot`, `start`, `stop`, `_tick`
    - `_tick` checks `if not self._running: return` at the top AND `if self._running: QtCore.QTimer.singleShot(_FRAME_INTERVAL_MS, self._tick)` before reschedule (Pitfall 8 — `grep -n "if not self._running" src/surg_rl/editor/render_poll_loop.py | grep -v '^#' | grep -c .` returns >=1)
    - `render_poll_loop.py` does NOT call `simulator.step(` anywhere — `grep -n "\.step(" src/surg_rl/editor/render_poll_loop.py | grep -v '^#' | grep -c .` returns 0 (D-02 — the render-poll never steps physics)
    - `stop()` only sets `_running=False` (does NOT call `simulator.close()` — `grep -n "\.close()" src/surg_rl/editor/render_poll_loop.py | grep -v '^#' | grep -c .` returns 0)
    - `PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_render_poll_loop.py::TestRenderPollCadence tests/test_render_poll_loop.py::TestStepOneRendersWhilePaused tests/test_render_poll_loop.py::TestSkipNoNewSnapshot tests/test_render_poll_loop.py::TestRunningGuard -v` exits 0
  </acceptance_criteria>
  <done>
    RenderPollLoop implemented with ~30 Hz UI-thread render, skip-when-no-new-snapshot, _running guard at both ends, initial-static-frame render, camera-offset push, and _maybe_update_fps. All four render-poll test classes green. SC#2 proxy and Pitfall 6 satisfied at the component level.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker QThread ↔ UI thread | `SimStepWorker.snapshot_ready(State)` queued signal crosses this boundary (the decoupling seam, D-03). `State` is pure-data (numpy arrays + dicts, no Qt/GL handles — verified `base_simulator.py:90-111`). |
| UI thread ↔ simulator GL context | `RenderPollLoop._tick` calls `simulator.render()` on the UI thread only (GL thread-affinity, Pitfall 2). The worker NEVER calls `render()`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Denial of Service (crash on close) | `SimStepWorker` QThread teardown | high | mitigate | D-04 cooperative `stop()` = cancel flag + `timer.stop()` + controller `thread.quit()` + `thread.wait(3000)` + log-on-timeout; NEVER `thread.terminate()` (risks leaving simulator physics state inconsistent); `aboutToClose` fires before `viewport.stop()` closes the shared simulator (Phase 41 ordering preserved). Verified by `TestCloseMidStepCleanExit` in Plan 02. |
| T-42-02 | Denial of Service (GL context corruption) | `SimStepWorker._tick` calling `render()` from the worker thread | high | mitigate | `render()` is ONLY called on the UI thread by `RenderPollLoop._tick` (Pitfall 2). The worker only calls `step(None)` + `get_state()` (CPU-only). Enforced by acceptance criterion: `render_poll_loop.py` has no `.step(` calls; `sim_step_worker.py` has no `render(` calls. |
| T-42-SC | Tampering | (no package installs this phase — pure code on existing PySide6) | high | accept | Zero external packages installed this phase (42-RESEARCH.md §Package Legitimacy Audit — PySide6 already pinned `>=6.8.0,<7.0` in the `[gui]` extra since v0.5.0). No `[ASSUMED]`/`[SUS]` packages to checkpoint. |
| T-42-03 | Information disclosure | status-bar hint leaks scene detail | low | accept | No status-bar hint in Plan 01 (that is Plan 02's D-12 hint). The worker reads no secrets and accepts no untrusted input; `State` carries no secrets. `safe_error_message()` is reused for any error string that reaches the canvas (existing `viewport.py` pattern). |

*ASVS L1, block_on: high. T-42-01 and T-42-02 are HIGH and mitigated (not accepted) — the mitigations are concrete code constraints enforced by acceptance criteria. No threat exceeds the block threshold after mitigation.*
</threat_model>

<verification>
- `PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_sim_step_worker.py tests/test_render_poll_loop.py -v` — all 8 test classes green (SC#1, SC#2 proxy, SC#3, SC#4, D-09, Pitfall 6)
- `grep -rn "singleShot" src/surg_rl/editor/ | grep -v '^#' | grep -c .` — the only `singleShot` chain in the editor package is the `RenderPollLoop` (ONE render chain on the UI thread — D-02; the worker uses a `QTimer.timeout` on its own thread, NOT a `singleShot` on the main thread)
- No new pip deps — `pip show PySide6` confirms 6.11.1 (no install step)
</verification>

<success_criteria>
- SimStepWorker advances physics at ~50 Hz via the fixed-step accumulator (SC#1 component-level)
- RenderPollLoop renders the latest snapshot at ~30 Hz cadence offscreen (SC#2 proxy); real-fps is the backstop truth
- Pause/resume/step-one work exactly (SC#3 component-level)
- Render/sim decoupled + 30 Hz publish cap verified with MockSimulator (SC#4 component-level)
- Speed scaling 0.25/0.5/1/2/4 verified (D-09 component-level)
- Both modules follow LazyImport discipline (no `from PySide6 import ...` at module top)
- No `render()` call in `sim_step_worker.py`; no `step()` call in `render_poll_loop.py` (D-02 + Pitfall 2)
- All 8 new test classes green offscreen
</success_criteria>

<output>
Create `.planning/phases/42-render-sim-decoupling-animated-viewport/42-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**New symbols (Plan 01 — this plan):**
- `src/surg_rl/editor/sim_step_worker.py`:
  - `class SimStepWorker(QtCore.QObject)` — QThread worker-object
  - `SimStepWorker.snapshot_ready` — `QtCore.Signal(object)` (the decoupling boundary, D-03; payload = `_Snapshot`)
  - `SimStepWorker.start` — `@QtCore.Slot()` (creates the accumulator QTimer on the worker thread)
  - `SimStepWorker.set_paused` — `@QtCore.Slot(bool)` (stops/starts the accumulator timer)
  - `SimStepWorker.set_speed` — `@QtCore.Slot(float)` (scales wall_dt in the accumulator)
  - `SimStepWorker.step_one` — `@QtCore.Slot()` (exactly one `step(None)` while paused, D-07)
  - `SimStepWorker.bind_scene` — `@QtCore.Slot(object)` (re-binds the worker to a new simulator)
  - `SimStepWorker.stop` — cooperative cancel flag + timer.stop (controller owns thread.quit/wait)
  - `SimStepWorker._tick` — the fixed-step accumulator (sim_dt=0.02, 30 Hz publish cap, _MAX_STEPS_PER_TICK=8)
  - `SimStepWorker._publish` — wraps `get_state()` in a `_Snapshot`
  - `_Snapshot` — dataclass (state, frame_id: int)
  - module constants `_SIM_HZ=50.0`, `_PUBLISH_HZ=30.0`, `_MAX_STEPS_PER_TICK=8`
- `src/surg_rl/editor/render_poll_loop.py`:
  - `class RenderPollLoop(QtCore.QObject)` — UI-thread render loop
  - `RenderPollLoop.on_snapshot` — slot storing `_latest_snapshot`
  - `RenderPollLoop.bind_simulator` — updates the simulator ref on scene swap
  - `RenderPollLoop.start` / `RenderPollLoop.stop` — `_running` guard control
  - `RenderPollLoop._tick` — self-rescheduling `QTimer.singleShot(33, _tick)`, skip-when-no-new, initial-static-frame, camera push, render, `_maybe_update_fps`
  - `RenderPollLoop._maybe_update_fps` — 1 s window fps counter (relocated from `viewport.py:292-304`)
  - module constant `_FRAME_INTERVAL_MS=33`
- `tests/test_sim_step_worker.py` — `MockSimulator`, `TestSimStepWorkerAccumulator`, `TestPauseResumeStepOne`, `TestDecouplingAndPublishCap`, `TestSpeedScaling`
- `tests/test_render_poll_loop.py` — `TestRenderPollCadence`, `TestStepOneRendersWhilePaused`, `TestSkipNoNewSnapshot`, `TestRunningGuard`

(Plan 02 produces: `SimStepWorker`/`RenderPollLoop` wiring in `EditorWindow`, `ViewportPanel.update_scene` re-bind, `_scene_has_dynamics`, `QToolBar` `toolbar_playback`, `QComboBox` `combo_playback_speed`, 5th `QLabel` `_status_playback`, `Space`/`.` `QShortcut`s, `tests/test_viewport_playback.py`, `TestDockObjectNames` QToolBar extension.)