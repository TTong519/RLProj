---
phase: 42-render-sim-decoupling-animated-viewport
plan: 02
type: execute
wave: 2
depends_on:
  - 42-01
files_modified:
  - src/surg_rl/editor/viewport.py
  - src/surg_rl/editor/main_window.py
  - tests/test_viewport_playback.py
  - tests/test_dock_state.py
autonomous: true
requirements: [GUI-11]
user_setup: []

must_haves:
  truths:
    # D-11 — load paused on every update_scene
    - "D-11: after ViewportPanel.update_scene(scene), sim_worker._paused == True (the accumulator timer is stopped), the toolbar Play button is unchecked, and the status bar shows '⏸ paused'. The SimStepWorker is created/bound but does NOT start stepping until the user presses Play (or Space). This avoids surprise CPU/GPU load on open for heavy scenes."
    # D-06 — playback toolbar + shortcuts
    - "D-06: a QToolBar with objectName='toolbar_playback' is docked at TopToolBarArea with a Play/Pause toggle QAction (setCheckable True), a Step-one QAction, and a speed QComboBox with objectName='combo_playback_speed' containing exactly the 5 items '0.25x','0.5x','1x','2x','4x' (default currentText '1x' per D-10). QShortcut Space → toggle play/pause; QShortcut '.' → step-one. R (Ctrl+R, camera reset, main_window.py:182) stays unchanged."
    # D-08 — status-bar playback segment
    - "D-08: a 5th permanent QLabel _status_playback is added to the status bar (same Panel/Sunken frame shape as the existing 4 labels at main_window.py:204-213) and reflects '▶ playing {speed}x' when playing, '⏸ paused' when paused, and '⏸ paused (static scene — no dynamics)' when paused on a static scene (D-12). The existing _status_fps label keeps showing render fps."
    # D-12 — static-scene hint (CORRECTED field paths)
    - "D-12: _scene_has_dynamics(scene) returns True when scene.robots (schema.py:1402) is non-empty, OR scene.tissues (schema.py:1403) is non-empty, OR scene.fluid (schema.py:1442 — a DIRECT field on SceneDefinition, NOT on EnvironmentConfig) is not None. Returns False otherwise (instruments-only is treated as static — instruments without robots have no actuated joints). The predicate is a structural schema-level check called once in update_scene — NOT a runtime step-delta heuristic and NOT an auto-pause. The worker keeps stepping harmlessly when the user hits Play on a static scene; the hint is informational only."
    # D-04 — close mid-step clean exit
    - "D-04: closing the editor mid-step fires aboutToClose → sim_worker.stop() (cancel flag + timer.stop) then the controller calls thread.quit() + thread.wait(3000) → sim_thread.isRunning()==False; no segfault; no 'RuntimeError: Internal C++ object already deleted'. aboutToClose fires at main_window.py:400 BEFORE viewport.stop() at :407 — the worker is cancelled BEFORE the shared simulator is closed (Pitfall 3 ordering preserved)."
    # Phase 41 D-06 — in-place swap (NO widget recreation)
    - "Phase 41 D-06: ViewportPanel.update_scene re-binds the SimStepWorker + RenderPollLoop to the new scene through the in-place swap (pause worker → close old sim → swap _scene + reset_camera → load new sim on UI thread → render_loop.bind_simulator → sim_worker.bind_scene.emit(new_sim) → set_paused(True)). NO ViewportPanel is constructed, NO setCentralWidget is called — dock geometry survives the swap."
    # Phase 41 D-07 extension — QToolBar objectName
    - "Phase 41 D-07 extension: EditorWindow.findChildren(QToolBar) is non-empty and every QToolBar has a non-empty unique objectName; 'toolbar_playback' is present. saveState()/restoreState() round-trip the toolbar correctly (Pitfall 7 — setObjectName BEFORE addToolBar)."
    # Pitfall 3 — pause worker before closing sim
    - "Pitfall 3: ViewportPanel.stop() and update_scene() pause the worker (stop the accumulator timer) BEFORE closing the shared simulator — the worker is never mid-step() while simulator.close() runs on the UI thread."
  artifacts:
    - "src/surg_rl/editor/viewport.py — _tick SPLIT (the render half delegates to self._render_loop; the step responsibility is on self._sim_worker; _tick is removed or reduced to a no-op/thin delegator); update_scene(scene) EXTENDED (pause worker → close old sim via contextlib.suppress → swap _scene + reset_camera → load new sim on UI thread via _on_load_simulator → render_loop.bind_simulator(new_sim) → sim_worker.bind_scene.emit(new_sim) queued → set_paused(True) D-11 → evaluate _scene_has_dynamics for the hint); stop() EXTENDED (render_loop.stop() + sim_worker.set_paused(True) BEFORE the contextlib.suppress sim.close() block); _scene_has_dynamics(scene) -> bool pure function (CORRECTED paths: scene.robots, scene.tissues, scene.fluid direct field)"
    - "src/surg_rl/editor/main_window.py — __init__ creates self._sim_thread (QtCore.QThread), self._sim_worker (SimStepWorker), self._render_loop (RenderPollLoop) and wires moveToThread + thread.started.connect(sim_worker.start) + thread.finished.connect(thread.deleteLater) + snapshot_ready.connect(render_loop.on_snapshot, Qt.QueuedConnection) + thread.start(); aboutToClose.connect(self._sim_worker.stop) mirrored after line 139; _build_playback_toolbar() method (QToolBar objectName='toolbar_playback', Play/Pause QAction setCheckable, Step-one QAction, QComboBox objectName='combo_playback_speed' with the 5 speed items, default '1x'); _wire_playback_shortcuts() extension (Space, '.' QShortcuts); _build_status_bar() extension (5th permanent QLabel _status_playback); _update_playback_status(playing, speed, static=False) helper; _toggle_play_pause / _on_step_one / _on_speed_changed handlers; _stop_sim_worker() controller teardown (sim_worker.stop + thread.quit + thread.wait(3000) + log-on-timeout, mirroring llm_panel.py:130-158); import of SimStepWorker + RenderPollLoop"
    - "tests/test_viewport_playback.py — offscreen harness (QT_QPA_PLATFORM=offscreen, qapp, isolated_home, MockSimulator) + TestPlaybackToolbar (D-06/D-09), TestPlaybackStatus (D-08), TestLoadPaused (D-11), TestStaticSceneHint (D-12), TestCloseMidStepCleanExit (D-04)"
    - "tests/test_dock_state.py — TestDockObjectNames extended (or new TestToolbarObjectNames sibling) collecting QToolBar children and asserting 'toolbar_playback' has a non-empty unique objectName (Phase 41 D-07 extension)"
  key_links:
    - "EditorWindow.aboutToClose (main_window.py:59) --connect--> self._sim_worker.stop (mirror main_window.py:139 — D-04 teardown contract; fires BEFORE viewport.stop at :407)"
    - "EditorWindow.__init__: self._sim_worker.snapshot_ready --[Qt.QueuedConnection]--> self._render_loop.on_snapshot (the decoupling boundary D-03, wired by the controller)"
    - "EditorWindow.__init__: self._sim_thread.started --> self._sim_worker.start; self._sim_thread.finished --> self._sim_thread.deleteLater (ONLY delete path — Pitfall 4)"
    - "ViewportPanel.update_scene --> self._sim_worker.set_paused(True) + self._render_loop.bind_simulator(new_sim) + self._sim_worker.bind_scene.emit(new_sim) (D-11 re-bind + load-paused)"
    - "ViewportPanel.stop --> self._render_loop.stop() + self._sim_worker.set_paused(True) BEFORE self._simulator.close() (Pitfall 3)"
    - "_act_play_pause.toggled --> sim_worker.set_paused; _act_step_one.triggered --> sim_worker.step_one; _speed_combo.currentTextChanged --> sim_worker.set_speed (D-06/D-09)"
    - "QShortcut('Space') --> _toggle_play_pause; QShortcut('.') --> _on_step_one (D-06; Ctrl+R at main_window.py:182 unchanged)"
  prohibitions:
    # descriptor-less, flagged-unverified (PROHIB_ABSENT specless fallback) — kept values/safety prohibitions from Stage 2
    - statement: "Must NOT recreate ViewportPanel on scene swap (Phase 41 D-06 — update_scene re-binds the worker + render-loop in place; widget recreation is the bug #3 root cause and resets dock geometry on rerun)"
      status: unverified
      flagged: true
    - statement: "Must NOT close the shared simulator while the SimStepWorker is mid-step() (Pitfall 3 — pause the worker via set_paused(True) BEFORE simulator.close() in both stop() and update_scene(); aboutToClose already fires before viewport.stop() in closeEvent — preserve that ordering)"
      status: unverified
      flagged: true
    - statement: "Must NOT call simulator.render() from the SimStepWorker worker thread (Pitfall 2 — render() ONLY on the UI thread via RenderPollLoop; the worker only calls step() + get_state())"
      status: unverified
      flagged: true
    - statement: "Must NOT call thread.terminate() on a wait() timeout (D-04 — log a warning and proceed with close; terminate risks leaving simulator physics state inconsistent)"
      status: unverified
      flagged: true
    - statement: "Must NOT write playback state (playing/paused/speed) to SceneDefinition or EditorSettings/QSettings (D-05/D-10 — session-only, panel-local; speed resets to 1x on launch; this phase writes nothing to the schema/QSettings to avoid silently corrupting undo/save/revert)"
      status: unverified
      flagged: true
    - statement: "Must NOT add a second QTimer.singleShot chain on the main thread for stepping (D-02 — ONE render timer on the UI thread via RenderPollLoop + ONE sim loop on the QThread worker; grep src/surg_rl/editor/ for singleShot must find the RenderPollLoop chain as the only render chain)"
      status: unverified
      flagged: true
  flagged_assumptions:
    - "GUI-11 edge probe (boundary + precision) flagged assumptions are resolved in Plan 01's SimStepWorker._tick action text (sim_dt=0.02, publish cap 30 Hz, wait(3000), speed values, inclusive boundaries, monotonic frame_id, _MAX_STEPS_PER_TICK=8 spiral cap). Plan 02 consumes those contracts via the Plan 01 artifacts. No new flagged assumptions introduced in Plan 02."
    - "D-12 instruments-only resolution (from 42-PATTERNS.md Open Question): a scene with ONLY instruments (no robots/tissues/fluid) is treated as STATIC — _scene_has_dynamics returns False — because instruments without robots have no actuated joints. The hint is informational only (D-12), so the risk is LOW either way. Flagged for implementer confirmation during Task 1."

---

<objective>
Deliver the integration half of GUI-11: wire the `SimStepWorker` + `RenderPollLoop` (built in Plan 01) into `ViewportPanel` and `EditorWindow`, split the monolithic `_tick()` along the decoupling seam, extend `update_scene()` to re-bind the worker + render-loop and load paused (D-11), add the playback `QToolBar` + `Space`/`.` `QShortcut`s + 5th status-bar label, implement the `_scene_has_dynamics` static-scene hint (D-12, corrected field paths), and plug `SimStepWorker.stop()` into the Phase 41 `aboutToClose` teardown harness (D-04). Also write the integration tests (toolbar/shortcuts/status-bar/load-paused/teardown/static-scene) and extend `TestDockObjectNames` for `QToolBar`.

Purpose: Close bugs #1 + #2 at the integration level and deliver SC#3 (pause/resume/step-one affordances) + D-04/D-06/D-08/D-11/D-12. After this plan, opening a scene shows a paused preview; pressing Play animates it at >30 fps (backstop); the user can pause/resume/step-one/speed via the toolbar or shortcuts; closing mid-step exits cleanly.
Output: modified `src/surg_rl/editor/viewport.py` + `src/surg_rl/editor/main_window.py`, new `tests/test_viewport_playback.py`, extended `tests/test_dock_state.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-render-sim-decoupling-animated-viewport/42-CONTEXT.md
@.planning/phases/42-render-sim-decoupling-animated-viewport/42-RESEARCH.md
@.planning/phases/42-render-sim-decoupling-animated-viewport/42-PATTERNS.md
@.planning/phases/42-render-sim-decoupling-animated-viewport/42-VALIDATION.md
@.planning/phases/42-render-sim-decoupling-animated-viewport/42-01-PLAN.md
@.planning/phases/41-dock-layout-reset-closeevent-teardown/41-CONTEXT.md
@src/surg_rl/editor/viewport.py
@src/surg_rl/editor/main_window.py
@src/surg_rl/editor/llm_panel.py
@src/surg_rl/editor/sim_step_worker.py
@src/surg_rl/editor/render_poll_loop.py
@src/surg_rl/editor/_safe_error.py
@src/surg_rl/scene_definition/schema.py
@tests/test_dock_state.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0 / RED + predicate GREEN): Create test_viewport_playback.py scaffold with RED integration tests + extend test_dock_state.py for QToolBar + implement _scene_has_dynamics predicate (TDD GREEN, pure function)</name>
  <files>tests/test_viewport_playback.py, tests/test_dock_state.py, src/surg_rl/editor/viewport.py</files>
  <read_first>
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-CONTEXT.md (D-06, D-08, D-09, D-11, D-12 locked)
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-RESEARCH.md (§Validation Architecture → Test Map — TestPlaybackToolbar=D-06/D-09, TestPlaybackStatus=D-08, TestLoadPaused=D-11, TestStaticSceneHint=D-12, TestCloseMidStepCleanExit=D-04; §Resolving D-12 — the schema-level predicate approach; §Code Examples — offscreen teardown test pattern)
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-PATTERNS.md (§tests/test_dock_state.py — VERBATIM offscreen harness at :19-55; §Static-scene predicate — CORRECTED field paths: `scene.robots` schema.py:1402, `scene.tissues` :1403, `scene.fluid` :1442 DIRECT on SceneDefinition NOT on EnvironmentConfig; instruments-only treated as static; §TestDockObjectNames extension pattern — also collect QToolBar children)
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-VALIDATION.md (§Per-Task Verification Map — the exact test classes + SC/decision mapping)
    - src/surg_rl/scene_definition/schema.py (lines 1397-1442 — SceneDefinition fields: environment :1397, robots :1402, tissues :1403, instruments :1406, task :1411, fluid :1442 DIRECT field; confirm NO `fluid` on EnvironmentConfig at :990-1009)
    - tests/test_dock_state.py (lines 19-55 offscreen harness; lines 58-85 TestDockObjectNames — the QDockWidget introspection to EXTEND for QToolBar; lines 277-329 TestCloseMidCallMockSlow — the mock-slow close-mid-call pattern to mirror for TestCloseMidStepCleanExit)
    - src/surg_rl/editor/viewport.py (current _tick :189-290, stop :169-180, update_scene :399-415 — the integration points Task 2 + 3 will modify; place _scene_has_dynamics as a module-level function OR a staticmethod on ViewportPanel)
  </read_first>
  <behavior>
    - _scene_has_dynamics(scene) with an empty SceneDefinition (no robots/tissues/fluid) returns False
    - _scene_has_dynamics(scene) with scene.robots non-empty returns True
    - _scene_has_dynamics(scene) with scene.tissues non-empty returns True
    - _scene_has_dynamics(scene) with scene.fluid is not None returns True (fluid is a DIRECT field on SceneDefinition at schema.py:1442, NOT on EnvironmentConfig)
    - _scene_has_dynamics(scene) with ONLY instruments non-empty (no robots/tissues/fluid) returns False (instruments-only is static — instruments without robots have no actuated joints)
    - TestPlaybackToolbar, TestPlaybackStatus, TestLoadPaused, TestStaticSceneHint, TestCloseMidStepCleanExit are RED on this task (they assert wiring that Task 3 has not added yet — toolbar_playback QToolBar, combo_playback_speed QComboBox, _status_playback QLabel, load-paused behavior, sim_thread teardown)
    - TestDockObjectNames (extended) is RED for the QToolBar assertion until Task 3 adds the toolbar (the existing QDockWidget assertions stay GREEN — regression guard for Phase 41 SC#4)
  </behavior>
  <action>
    First, implement `_scene_has_dynamics` as a TDD GREEN (the pure function is fully testable in isolation). Add a module-level function `_scene_has_dynamics(scene: SceneDefinition) -> bool` to `src/surg_rl/editor/viewport.py` (or a small helper — keep it in viewport.py so update_scene can call it directly). Use the CORRECTED field paths from 42-PATTERNS.md (the RESEARCH.md `env.fluid` reading is WRONG): `if getattr(scene, "robots", None): return True`; `if getattr(scene, "tissues", None): return True`; `if getattr(scene, "fluid", None) is not None: return True` (schema.py:1442 — DIRECT field on SceneDefinition); `return False`. Do NOT read `scene.environment.fluid` (EnvironmentConfig at schema.py:990-1009 has lights/cameras/ground_plane — NO fluid). Instruments-only (no robots/tissues/fluid) returns False — instruments without robots have no actuated joints (the hint is informational only per D-12, LOW risk either way — flagged in flagged_assumptions).

    Create `tests/test_viewport_playback.py` mirroring `tests/test_dock_state.py:19-55` VERBATIM in offscreen harness shape (module-top `os.environ.setdefault("QT_QPA_PLATFORM", "offscreen")`, `_HAVE_PYSIDE6` try/except, `pytestmark = pytest.mark.skipif(not _HAVE_PYSIDE6, reason="PySide6 not installed")`, session `qapp` fixture, `isolated_home` fixture). Duplicate the `MockSimulator` from `tests/test_sim_step_worker.py` (Plan 01) in this file (or import from a shared helper). Add a `monkeypatch.setattr(vp, "_default_load_simulator", lambda scene: MockSimulator())` fixture so `EditorWindow` constructs without real GL/physics.

    Write these pytest classes (RED on this task — they assert wiring Task 3 adds):
    - `TestPlaybackToolbar` (D-06/D-09): build `EditorWindow`, `w.show()`, processEvents; `tbs = w.findChildren(QToolBar)`; assert `len(tbs) >= 1`; `names = [t.objectName() for t in tbs]`; assert `"toolbar_playback" in names`; find the `QComboBox` with `objectName == "combo_playback_speed"`; assert its item texts are exactly `["0.25x", "0.5x", "1x", "2x", "4x"]` and `currentText() == "1x"` (D-10 default); assert a Play/Pause `QAction` (setCheckable) and a Step-one `QAction` exist on the toolbar.
    - `TestPlaybackStatus` (D-08): build `EditorWindow`, show; `labels = w.findChildren(QLabel)` filtered to permanent status-bar children; assert a `_status_playback` label exists (by `objectName` or by text starting with "⏸" / "▶"); trigger play (`w._toggle_play_pause()` or `w._act_play_pause.setChecked(True)`); processEvents; assert `_status_playback.text()` contains "▶" and "1x"; trigger pause; assert text contains "⏸".
    - `TestLoadPaused` (D-11): build `EditorWindow` with the MockSimulator loader; load a scene via `w._refresh_viewport_and_tree()` (or `w._viewport_panel.update_scene(scene)`); processEvents; assert `w._sim_worker._paused == True`; assert `w._act_play_pause.isChecked() == False`; assert `w._status_playback.text()` contains "⏸".
    - `TestStaticSceneHint` (D-12): construct a `SceneDefinition` with ONLY instruments (no robots/tissues/fluid) — assert `_scene_has_dynamics(scene) == False` (this part is GREEN from the predicate above); load it via `update_scene`; processEvents; assert `w._status_playback.text()` contains "static scene" (the D-12 informational hint). Construct a scene with a robot; assert `_scene_has_dynamics(scene) == True` and the hint is absent.
    - `TestCloseMidStepCleanExit` (D-04): mirror `tests/test_dock_state.py:277-329` TestCloseMidCallMockSlow — monkeypatch `_default_load_simulator` to `MockSimulator()`, build `EditorWindow`, show, `w._sim_worker.set_paused.emit(False)` (start stepping), `time.sleep(0.05)`, processEvents, `w.close()` (full closeEvent path: aboutToClose → sim_worker.stop → viewport.stop), processEvents; assert `not w._sim_thread.isRunning()` ("SimStepWorker thread still running after close (D-04)"); no segfault, no `RuntimeError: Internal C++ object already deleted`. Use `try/finally` with `contextlib.suppress(Exception): w.close()`.

    Extend `tests/test_dock_state.py::TestDockObjectNames` (add a second test method OR a new sibling class `TestToolbarObjectNames` — keep the existing QDockWidget test intact as the Phase 41 regression guard): `from PySide6.QtWidgets import QToolBar`; `tbs = w.findChildren(QToolBar)`; assert `tbs` non-empty; `names = [t.objectName() for t in tbs]`; assert `all(names)`, `len(names) == len(set(names))`, and `"toolbar_playback" in names` (RED until Task 3 adds the toolbar).

    The `_scene_has_dynamics` predicate tests (the GREEN part of this task) go in `tests/test_viewport_playback.py::TestStaticSceneHint` — the pure-function assertions pass immediately; the integration assertion (status-bar hint text) stays RED until Task 2 + 3.
  </action>
  <verify>
    <automated>PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_viewport_playback.py::TestStaticSceneHint -v 2>&amp;1 | grep -E "passed|failed" | tail -1 ; echo "predicate pure-function assertions GREEN; integration assertions RED until Task 2+3"</automated>
    <automated>PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_dock_state.py::TestDockObjectNames -v 2>&amp;1 | grep -E "passed|failed" | tail -1 ; echo "QDockWidget assertions GREEN (regression); QToolBar assertion RED until Task 3"</automated>
  </verify>
  <acceptance_criteria>
    - `src/surg_rl/editor/viewport.py` defines `_scene_has_dynamics(scene: SceneDefinition) -> bool` reading `scene.robots`, `scene.tissues`, `scene.fluid` (DIRECT field, NOT `scene.environment.fluid`) — `grep -n "environment.fluid" src/surg_rl/editor/viewport.py | grep -v '^#' | grep -c .` returns 0
    - `tests/test_viewport_playback.py` exists with offscreen harness (qapp + isolated_home + MockSimulator) and classes TestPlaybackToolbar, TestPlaybackStatus, TestLoadPaused, TestStaticSceneHint, TestCloseMidStepCleanExit
    - `tests/test_viewport_playback.py::TestStaticSceneHint` pure-function assertions on `_scene_has_dynamics` PASS (empty scene -> False; robots -> True; tissues -> True; fluid -> True; instruments-only -> False)
    - `tests/test_dock_state.py` TestDockObjectNames (or a new TestToolbarObjectNames) collects `QToolBar` children and asserts `"toolbar_playback" in names` (RED until Task 3)
    - The existing TestDockObjectNames QDockWidget assertions still PASS (Phase 41 SC#4 regression guard intact)
    - `PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_viewport_playback.py tests/test_dock_state.py -v` shows the predicate GREEN and the integration/toolbar assertions RED (honest baseline for Task 2+3)
  </acceptance_criteria>
  <done>
    `_scene_has_dynamics` predicate implemented (TDD GREEN) with the corrected field paths (scene.fluid direct, NOT environment.fluid). test_viewport_playback.py scaffold exists with the 5 VALIDATION-mapped integration test classes (RED baseline). test_dock_state.py extended for QToolBar (RED baseline for the toolbar assertion; existing QDockWidget assertions stay GREEN). Task 2 + 3 turn the integration tests GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN for D-11/Pitfall 3): ViewportPanel integration — split _tick delegating to RenderPollLoop + SimStepWorker, update_scene re-bind + load-paused, stop() extended to pause worker before closing sim</name>
  <files>src/surg_rl/editor/viewport.py</files>
  <read_first>
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-CONTEXT.md (D-01, D-02, D-05, D-11 locked; the decoupling seam)
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-RESEARCH.md (§Pattern 4 — update_scene re-bind skeleton: pause worker → close old sim → swap scene + reset_camera → load new sim on UI thread → render_loop.bind_simulator → sim_worker.bind_scene.emit(new_sim) queued → set_paused(True) D-11; §Pitfall 3 — pause worker BEFORE closing sim; §Pitfall 6 — render-poll stays alive while paused; §A1/A2 — load+probe on UI thread, step on worker thread)
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-PATTERNS.md (§viewport.py — _tick CURRENT body :189-290 SPLIT (render half → RenderPollLoop; step → SimStepWorker); stop() CURRENT :169-180 GENERALIZE (render_loop.stop + sim_worker.set_paused(True) BEFORE sim.close); update_scene CURRENT :399-415 EXTEND; reset_camera :390-397 UNCHANGED; _default_load_simulator :418-482 UNCHANGED — loader probes GL on UI thread, hand live sim to worker)
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-01-PLAN.md (the SimStepWorker + RenderPollLoop APIs from Plan 01 — snapshot_ready signal, bind_scene slot, set_paused slot, bind_simulator method, on_snapshot slot, start/stop)
    - src/surg_rl/editor/viewport.py (current _tick :189-290, stop :169-180, update_scene :399-415, _display_array :306-353, _maybe_update_fps :292-304, camera push :220-236, _running guard :142, _default_load_simulator :418-482, reset_camera :390-397)
    - src/surg_rl/editor/render_poll_loop.py (the RenderPollLoop API from Plan 01 — on_snapshot, bind_simulator, start, stop, _tick)
    - src/surg_rl/editor/sim_step_worker.py (the SimStepWorker API from Plan 01 — snapshot_ready, bind_scene, set_paused, set_speed, step_one, start, stop)
    - tests/test_viewport_playback.py (the RED tests from Task 1 — TestLoadPaused, TestStaticSceneHint are the GREEN targets here; TestPlaybackToolbar/Status/CloseMidStep need Task 3 too)
  </read_first>
  <behavior>
    - ViewportPanel holds references to the SimStepWorker and RenderPollLoop (passed in via __init__ OR set by EditorWindow after construction — implementer's discretion, but the loop + worker are owned by EditorWindow per Plan 01's controller pattern)
    - The monolithic _tick (viewport.py:189-290) is REMOVED from ViewportPanel — the render half is delegated to self._render_loop (which has its own _tick); the step responsibility is on self._sim_worker. ViewportPanel no longer calls simulator.step() or simulator.render() directly.
    - update_scene(scene): pause worker (sim_worker.set_paused(True)) → close old sim via contextlib.suppress(AttributeError, OSError) → _simulator=None → _scene=scene → reset_camera() → load new sim on UI thread via _on_load_simulator → _simulator=new_sim → render_loop.bind_simulator(new_sim) → sim_worker.bind_scene.emit(new_sim) queued → set_paused(True) D-11 → evaluate _scene_has_dynamics for the hint
    - stop(): render_loop.stop() + sim_worker.set_paused(True) BEFORE the contextlib.suppress sim.close() block (Pitfall 3 — worker never mid-step while sim closes)
    - TestLoadPaused passes (sim_worker._paused == True after update_scene; the worker does not start stepping)
    - TestStaticSceneHint integration part passes (status hint reflects _scene_has_dynamics — requires the playback-status callback from Task 3; OR the ViewportPanel sets a panel-local flag Task 3 reads)
  </behavior>
  <action>
    Modify `src/surg_rl/editor/viewport.py`:

    1. Add `self._sim_worker = None` and `self._render_loop = None` to `__init__` (the EditorWindow in Task 3 sets these after constructing the worker + loop — OR add a `set_playback(self, sim_worker, render_loop)` method; pick ONE and keep it consistent with Task 3's wiring). Keep `self._running`, `self._simulator`, `self._scene`, `self._camera_offset` as today.

    2. SPLIT `_tick` (current :189-290): REMOVE the method body from ViewportPanel — the render half (camera push :220-236, render() :237-242, framebuffer retry :243-274, _display_array :306-353, _maybe_update_fps :292-304, self-reschedule :289-290 with _running guard) is now in `RenderPollLoop._tick` (built in Plan 01); the step responsibility is on `SimStepWorker._tick` (Plan 01). ViewportPanel no longer has a `_tick` method (OR reduce it to a thin no-op/delegator — pick ONE, but NO `simulator.step()` or `simulator.render()` calls remain in ViewportPanel). Keep the lazy-load path (:193-209) on the UI thread — `_default_load_simulator` probes GL via `sim.render()` (:467-472) so loading MUST stay on the UI thread; the loaded simulator is then handed to the worker via `bind_scene` (queued).

    3. EXTEND `update_scene(self, scene: SceneDefinition) -> None` (current :399-415) per 42-RESEARCH.md Pattern 4 with the order: (a) `self._sim_worker.set_paused.emit(True)` (pause worker BEFORE closing the old sim — Pitfall 3); (b) `with contextlib.suppress(AttributeError, OSError): if self._simulator is not None: self._simulator.close()` (the existing close pattern); (c) `self._simulator = None`; (d) `self._scene = scene`; (e) `self.reset_camera()`; (f) `new_sim = self._on_load_simulator(self._scene)` (load + GL-probe on the UI thread); (g) `self._simulator = new_sim`; (h) `self._render_loop.bind_simulator(new_sim)` (update the loop's simulator ref); (i) `self._sim_worker.bind_scene.emit(new_sim)` (queued — the worker binds on its own thread after any in-flight _tick); (j) `self._sim_worker.set_paused.emit(True)` (D-11 — load paused; belt-and-braces after bind_scene); (k) evaluate `static_hint = not _scene_has_dynamics(scene)` and store it on a panel-local attr (e.g. `self._static_scene = static_hint`) for Task 3's status-bar callback to read. NO widget recreation, NO setCentralWidget (Phase 41 D-06).

    4. EXTEND `stop(self) -> None` (current :169-180) per Pitfall 3: BEFORE the `contextlib.suppress(...)` `simulator.close()` block, call `self._render_loop.stop()` (sets _running=False) and `self._sim_worker.set_paused.emit(True)` (halt the accumulator so the worker is not mid-step() when the sim closes). Keep the existing `with contextlib.suppress(AttributeError, OSError): self._simulator.close(); self._simulator = None` block VERBATIM. The `aboutToClose` signal already fired in `closeEvent` (main_window.py:400) and already triggered `sim_worker.stop()` via the Task 3 wiring — `viewport.stop()` is the belt-and-braces for the `update_scene` swap path.

    5. Keep `reset_camera` (:390-397) and `_default_load_simulator` (:418-482) UNCHANGED — the loader probes GL on the UI thread, the worker reuses whatever simulator the loader returns.

    Do NOT add the QToolBar/shortcuts/status-bar label — that is Task 3 (main_window.py). Do NOT create the QThread/SimStepWorker/RenderPollLoop — that is Task 3 (EditorWindow owns them).

    Run TestLoadPaused → GREEN (sim_worker._paused == True after update_scene). Run TestStaticSceneHint integration part → requires Task 3's status-bar callback for the hint text; the `_scene_has_dynamics` pure-function part is already GREEN from Task 1.
  </action>
  <verify>
    <automated>PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_viewport_playback.py::TestLoadPaused -v</automated>
    <automated>PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_sim_step_worker.py tests/test_render_poll_loop.py -v 2>&amp;1 | tail -3 ; echo "Plan 01 component tests still GREEN (no regression from the _tick split)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/surg_rl/editor/viewport.py` no longer calls `simulator.step(` or `simulator.render(` directly in `_tick` — the render half is delegated to `RenderPollLoop` and the step responsibility is on `SimStepWorker` (`grep -nE "self\._simulator\.(step|render)\(" src/surg_rl/editor/viewport.py | grep -v '^#' | grep -v "_default_load_simulator" | grep -c .` returns 0 in the _tick/stop/update_scene method bodies; the `_default_load_simulator` GL-probe render at :467-472 is allowed)
    - `update_scene` calls `self._sim_worker.set_paused.emit(True)` BEFORE the `contextlib.suppress` `simulator.close()` block (Pitfall 3 — `grep -n "set_paused" src/surg_rl/editor/viewport.py | grep -v '^#' | grep -c .` returns >=2 for update_scene + stop)
    - `update_scene` calls `self._render_loop.bind_simulator(new_sim)` and `self._sim_worker.bind_scene.emit(new_sim)` (queued re-bind — D-11)
    - `update_scene` does NOT construct a new ViewportPanel and does NOT call `setCentralWidget` (Phase 41 D-06 — `grep -n "setCentralWidget" src/surg_rl/editor/viewport.py | grep -v '^#' | grep -c .` returns 0)
    - `stop()` calls `self._render_loop.stop()` + `self._sim_worker.set_paused.emit(True)` BEFORE the `simulator.close()` block (Pitfall 3)
    - `_scene_has_dynamics` reads `scene.fluid` (DIRECT field) NOT `scene.environment.fluid` (`grep -n "environment.fluid" src/surg_rl/editor/viewport.py | grep -v '^#' | grep -c .` returns 0)
    - `PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_viewport_playback.py::TestLoadPaused -v` exits 0
    - Plan 01 component tests (`test_sim_step_worker.py` + `test_render_poll_loop.py`) still PASS (no regression from the _tick split)
  </acceptance_criteria>
  <done>
    ViewportPanel._tick is split (render → RenderPollLoop, step → SimStepWorker). update_scene re-binds the worker + render-loop in place and loads paused (D-11). stop() pauses the worker before closing the sim (Pitfall 3). _scene_has_dynamics predicate uses the corrected field paths. TestLoadPaused green. Plan 01 component tests still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (GREEN for D-04/D-06/D-08/D-09/D-12 + Phase 41 D-07): EditorWindow wiring — create QThread/SimStepWorker/RenderPollLoop, aboutToClose.connect, _build_playback_toolbar, _wire_playback_shortcuts, 5th status-bar label, _stop_sim_worker teardown, extend TestDockObjectNames GREEN</name>
  <files>src/surg_rl/editor/main_window.py</files>
  <read_first>
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-CONTEXT.md (D-04, D-06, D-08, D-09, D-10, D-11 locked)
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-RESEARCH.md (§System Architecture Diagram — EditorWindow.__init__ wiring; §Pattern 3 — playback QToolBar + QShortcut + status-bar segment skeleton; §Pitfall 7 — toolbar objectName BEFORE addToolBar; §Security Domain — safe_error_message for the static-scene hint)
    - .planning/phases/42-render-sim-decoupling-animated-viewport/42-PATTERNS.md (§main_window.py — CURRENT imports :10-20 (add SimStepWorker + RenderPollLoop imports); _viewport_panel creation :80-84 (EXTEND to create QThread + worker + loop); aboutToClose.connect(_llm_panel.stop) :139 (MIRROR for sim_worker.stop); _wire_shortcuts :180-183 (EXTEND with Space + "."); _build_status_bar :204-213 (EXTEND with 5th QLabel _status_playback); _build_dock_widgets objectName convention :119-132 (extend to toolbar_<slug>); _refresh_viewport_and_tree :323-332 (UNCHANGED — update_scene handles D-11); closeEvent :394-411 (UNCHANGED — aboutToClose already fires))
    - .planning/phases/41-dock-layout-reset-closeevent-teardown/41-CONTEXT.md (D-04 aboutToClose registry; D-05 stop() = cancel + thread.quit + thread.wait(3000) + log-on-timeout, NEVER terminate; D-07 objectName discipline)
    - src/surg_rl/editor/main_window.py (current __init__ :51-85, _build_dock_widgets :119-132, aboutToClose :59, aboutToClose.connect(_llm_panel.stop) :139, _wire_shortcuts :180-183, _build_status_bar :204-213, _update_fps_status :221-228, _refresh_viewport_and_tree :323-332, closeEvent :394-411)
    - src/surg_rl/editor/llm_panel.py (QThread wiring :117-128, stop() template :130-158 — mirror for _stop_sim_worker)
    - src/surg_rl/editor/sim_step_worker.py (SimStepWorker API from Plan 01)
    - src/surg_rl/editor/render_poll_loop.py (RenderPollLoop API from Plan 01)
    - src/surg_rl/editor/viewport.py (the modified ViewportPanel from Task 2 — the worker/loop refs it expects)
    - tests/test_viewport_playback.py (RED tests from Task 1 — TestPlaybackToolbar, TestPlaybackStatus, TestCloseMidStepCleanExit are the GREEN targets; TestStaticSceneHint integration part also needs the status-bar callback)
    - tests/test_dock_state.py (the QToolBar extension from Task 1 — GREEN target)
  </read_first>
  <behavior>
    - EditorWindow.__init__ creates self._sim_thread (QtCore.QThread), self._sim_worker (SimStepWorker), self._render_loop (RenderPollLoop); wires moveToThread + thread.started.connect(sim_worker.start) + thread.finished.connect(thread.deleteLater) + sim_worker.snapshot_ready.connect(render_loop.on_snapshot, Qt.QueuedConnection) + thread.start(); passes the worker + loop refs to ViewportPanel
    - aboutToClose.connect(self._sim_worker.stop) is wired (mirror line 139) — the controller-side _stop_sim_worker calls sim_worker.stop() (cancel + timer.stop) then thread.quit() + thread.wait(3000) + log-on-timeout (mirror llm_panel.py:130-158); NEVER terminate
    - _build_playback_toolbar creates a QToolBar with objectName="toolbar_playback" BEFORE addToolBar(TopToolBarArea); Play/Pause QAction setCheckable(True); Step-one QAction; QComboBox objectName="combo_playback_speed" with items ["0.25x","0.5x","1x","2x","4x"], currentText "1x" (D-10 default); toggled/triggered/currentTextChanged connect to handlers emitting queued signals to the worker
    - _wire_playback_shortcuts adds QShortcut("Space") → _toggle_play_pause and QShortcut(".") → _on_step_one (Ctrl+R at :182 unchanged)
    - _build_status_bar adds a 5th permanent QLabel _status_playback with Panel/Sunken frame (same as the existing 4 labels at :204-213); _update_playback_status(playing, speed, static=False) sets the text ("▶ playing {speed}x" / "⏸ paused" / "⏸ paused (static scene — no dynamics)")
    - TestPlaybackToolbar, TestPlaybackStatus, TestCloseMidStepCleanExit, TestStaticSceneHint (integration part), TestDockObjectNames (QToolBar extension) all PASS
  </behavior>
  <action>
    Modify `src/surg_rl/editor/main_window.py`:

    1. Add imports: `from surg_rl.editor.sim_step_worker import SimStepWorker` and `from surg_rl.editor.render_poll_loop import RenderPollLoop` (same-package; verify both import Qt via `from surg_rl.editor import ...` LazyImport proxies). Add `from PySide6.QtWidgets import QToolBar, QComboBox` OR use the LazyImport proxies if the module already imports via `from surg_rl.editor import QtWidgets` (prefer the LazyImport path for consistency).

    2. In `__init__` (after `self._viewport_panel = ViewportPanel(...)` at :80-84 and `setCentralWidget`), add the playback infrastructure per 42-RESEARCH.md System Architecture Diagram:
       - `self._sim_thread = QtCore.QThread()`
       - `self._sim_worker = SimStepWorker()`
       - `self._sim_worker.moveToThread(self._sim_thread)`
       - `self._sim_thread.started.connect(self._sim_worker.start)` (the `start` slot creates the accumulator QTimer on the worker thread)
       - Construct `self._render_loop = RenderPollLoop(...)` on the UI thread with refs to `self._viewport_panel` (canvas + camera_offset + simulator ref + `on_fps_update=self._update_fps_status`). Pass the `simulator_ref` as a callable returning `self._viewport_panel._simulator` (the live simulator swapped in-place by update_scene) OR wire `bind_simulator` directly — match what Task 2's ViewportPanel expects.
       - `self._sim_worker.snapshot_ready.connect(self._render_loop.on_snapshot, QtCore.Qt.ConnectionType.QueuedConnection)` (the decoupling boundary D-03 — queued because it crosses the worker→UI thread boundary; State is pure-data per A3)
       - `self._sim_thread.finished.connect(self._sim_thread.deleteLater)` (the ONLY delete path — Pitfall 4; do NOT call deleteLater anywhere else)
       - Hand the worker + loop refs to ViewportPanel: `self._viewport_panel.set_playback(self._sim_worker, self._render_loop)` (OR set attrs directly — match Task 2's choice).
       - `self._sim_thread.start()` (runs the worker's event loop — NOT a blocking `run()`)
       - `self._render_loop.start()` (kick off the UI-thread render-poll chain)

    3. Mirror line 139: add `self.aboutToClose.connect(self._stop_sim_worker)` immediately after `self.aboutToClose.connect(self._llm_panel.stop)`. Define `_stop_sim_worker(self) -> None`: `self._sim_worker.stop()` (sets cancel + stops the accumulator timer); `self._sim_thread.quit()`; `if not self._sim_thread.wait(3000): logger.warning("SimStepWorker thread did not exit within 3s; proceeding with close")`. NEVER `self._sim_thread.terminate()` (D-04). NEVER `self._sim_thread.deleteLater()` here (Pitfall 4 — `thread.finished -> thread.deleteLater` is wired in __init__). Mirror `llm_panel.py:130-158` shape. Best-effort — do not block close on timeout.

    4. Add `_build_playback_toolbar(self) -> None` (call it from __init__ after `_build_dock_widgets`): `tb = QtWidgets.QToolBar("Playback")`; `tb.setObjectName("toolbar_playback")` BEFORE `self.addToolBar(QtCore.Qt.ToolBarArea.TopToolBarArea, tb)` (Pitfall 7 — saveState identifies toolbars by objectName); `self._act_play_pause = tb.addAction("▶ Play")`; `self._act_play_pause.setCheckable(True)`; `self._act_step_one = tb.addAction("⏭ Step")`; `tb.addSeparator()`; `tb.addWidget(QtWidgets.QLabel("Speed:"))`; `self._speed_combo = QtWidgets.QComboBox()`; `self._speed_combo.setObjectName("combo_playback_speed")`; for `s in ("0.25x", "0.5x", "1x", "2x", "4x"): self._speed_combo.addItem(s)`; `self._speed_combo.setCurrentText("1x")` (D-10 default 1x, session-only); `tb.addWidget(self._speed_combo)`. Connect: `self._act_play_pause.toggled.connect(self._on_play_pause_toggled)`, `self._act_step_one.triggered.connect(self._on_step_one)`, `self._speed_combo.currentTextChanged.connect(self._on_speed_changed)`.

    5. Extend `_wire_shortcuts` (current :180-183): after the existing `Ctrl+R` shortcut, add `QtGui.QShortcut(QtGui.QKeySequence("Space"), self).activated.connect(self._toggle_play_pause)` and `QtGui.QShortcut(QtGui.QKeySequence("."), self).activated.connect(self._on_step_one)`. Keep `Ctrl+R` (camera reset) UNCHANGED (per Phase 33 D-12, shortcuts on the main window, not per-widget).

    6. Extend `_build_status_bar` (current :204-213): add `self._status_playback = QtWidgets.QLabel("⏸ paused")` to the tuple of permanent widgets (same `Panel`/`Sunken` frame shape — `setFrameShape(QtWidgets.QFrame.Shape.Panel)`, `setFrameShadow(QtWidgets.QFrame.Shape.Sunken)`, `bar.addPermanentWidget(self._status_playback)`). Keep the existing 4 labels (`_status_path`, `_status_sim`, `_status_fps`, `_status_validation`) UNCHANGED.

    7. Add handler methods:
       - `_on_play_pause_toggled(self, checked: bool) -> None`: `self._sim_worker.set_paused.emit(not checked)` (checked = playing → paused False); `self._update_playback_status(playing=checked, speed=self._current_speed())`.
       - `_toggle_play_pause(self) -> None`: `self._act_play_pause.setChecked(not self._act_play_pause.isChecked())` (toggles → triggers `_on_play_pause_toggled`).
       - `_on_step_one(self) -> None`: `self._sim_worker.step_one.emit()` (queued — runs one step on the worker even while paused, D-07).
       - `_on_speed_changed(self, text: str) -> None`: parse the float from `text` (strip the trailing "x"); `self._sim_worker.set_speed.emit(float(speed))`; `self._update_playback_status(playing=self._act_play_pause.isChecked(), speed=float(speed), static=self._viewport_panel._static_scene)`.
       - `_current_speed(self) -> float`: parse `self._speed_combo.currentText().rstrip("x")` to float.
       - `_update_playback_status(self, playing: bool, speed: float, static: bool = False) -> None`: if `static`: `self._status_playback.setText("⏸ paused (static scene — no dynamics)")` (D-12; generic string — no `safe_error_message` redaction needed per Security Domain V5, but route through `safe_error_message` if scene detail ever leaks in); elif `playing`: `self._status_playback.setText(f"▶ playing {speed}x")`; else: `self._status_playback.setText("⏸ paused")`.

    8. `closeEvent` (:394-411) is UNCHANGED — `aboutToClose.emit()` at :400 already triggers `_stop_sim_worker` (wired in step 3); `viewport.stop()` at :407 pauses the worker + stops the render-loop before closing the sim (Task 2). The ordering `aboutToClose` → `viewport.stop()` is the Pitfall 3 guarantee — preserve it.

    Do NOT modify `_refresh_viewport_and_tree` (:323-332) — `update_scene` (Task 2) handles D-11 load-paused internally.

    Run TestPlaybackToolbar, TestPlaybackStatus, TestCloseMidStepCleanExit, TestStaticSceneHint (integration), TestDockObjectNames (QToolBar extension) → GREEN.
  </action>
  <verify>
    <automated>PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_viewport_playback.py tests/test_dock_state.py::TestDockObjectNames -v</automated>
    <automated>PYTHONPATH=src pytest tests/ -v 2>&amp;1 | tail -5 ; echo "full suite — no regression in the 1,513-test v0.6.0 baseline + Phase 41"</automated>
  </verify>
  <acceptance_criteria>
    - `src/surg_rl/editor/main_window.py` imports `SimStepWorker` and `RenderPollLoop` via `from surg_rl.editor.sim_step_worker import SimStepWorker` / `from surg_rl.editor.render_poll_loop import RenderPollLoop`
    - `__init__` creates `self._sim_thread`, `self._sim_worker`, `self._render_loop`, wires `moveToThread` + `thread.started.connect(sim_worker.start)` + `snapshot_ready.connect(render_loop.on_snapshot, Qt.QueuedConnection)` + `thread.finished.connect(thread.deleteLater)`, and calls `thread.start()` + `render_loop.start()`
    - `aboutToClose.connect(self._stop_sim_worker)` is wired (mirror line 139) — `grep -n "aboutToClose.connect" src/surg_rl/editor/main_window.py | grep -v '^#' | grep -c .` returns >=2 (llm_panel + sim_worker)
    - `_stop_sim_worker` calls `sim_worker.stop()` + `thread.quit()` + `thread.wait(3000)` + log-on-timeout; NEVER `thread.terminate()` — `grep -n "terminate()" src/surg_rl/editor/main_window.py | grep -v '^#' | grep -c .` returns 0
    - `_build_playback_toolbar` creates a `QToolBar` with `objectName="toolbar_playback"` set BEFORE `addToolBar`, a Play/Pause `QAction` (`setCheckable(True)`), a Step-one `QAction`, and a `QComboBox` with `objectName="combo_playback_speed"` and exactly the 5 items "0.25x","0.5x","1x","2x","4x" with `currentText()=="1x"`
    - `_wire_playback_shortcuts` adds `QShortcut("Space")` and `QShortcut(".")` (Ctrl+R unchanged)
    - `_build_status_bar` adds a 5th permanent `QLabel` `_status_playback` with Panel/Sunken frame
    - `_update_playback_status` sets "▶ playing {speed}x" / "⏸ paused" / "⏸ paused (static scene — no dynamics)"
    - `closeEvent` (:394-411) is UNCHANGED (aboutToClose already fires at :400, viewport.stop at :407)
    - `PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_viewport_playback.py tests/test_dock_state.py::TestDockObjectNames -v` exits 0
    - `PYTHONPATH=src pytest tests/ -v` full suite exits 0 (no regression in the 1,513-test baseline + Phase 41 additions)
  </acceptance_criteria>
  <done>
    EditorWindow wires the SimStepWorker QThread + RenderPollLoop, connects aboutToClose → _stop_sim_worker (D-04), builds the playback toolbar (objectName="toolbar_playback", combo "combo_playback_speed" with 5 speeds), adds Space/"." shortcuts, adds the 5th _status_playback label, and _update_playback_status reflects playing/paused/static (D-08/D-12). All integration tests green. Full suite green (no regression). Phase 42 complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker QThread ↔ UI thread | `SimStepWorker.snapshot_ready(State)` queued signal crosses this boundary (D-03). `State` is pure-data (no Qt/GL handles). |
| UI thread ↔ simulator GL context | `RenderPollLoop._tick` calls `simulator.render()` on the UI thread only (Pitfall 2). The worker NEVER calls `render()`. |
| UI thread ↔ worker accumulator | `set_paused`/`set_speed`/`step_one`/`bind_scene` are queued slot invocations from the UI thread to the worker (D-06/D-09). |
| closeEvent → aboutToClose → worker teardown | `aboutToClose` fires before `super().closeEvent()`; `_stop_sim_worker` cancels the worker + `thread.quit()` + `wait(3000)` before `viewport.stop()` closes the shared simulator (Pitfall 3, D-04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Denial of Service (crash on close) | `EditorWindow.closeEvent` / `_stop_sim_worker` / `SimStepWorker` QThread teardown | high | mitigate | D-04 cooperative `stop()` = cancel flag + `timer.stop()` + controller `thread.quit()` + `thread.wait(3000)` + log-on-timeout; NEVER `thread.terminate()` (D-04/D-05). `aboutToClose` fires before `viewport.stop()` closes the shared simulator (Pitfall 3 ordering). Verified by `TestCloseMidStepCleanExit`. |
| T-42-02 | Denial of Service (GL context corruption) | `SimStepWorker` calling `render()` from the worker thread | high | mitigate | `render()` ONLY on the UI thread via `RenderPollLoop._tick` (Pitfall 2). The worker only calls `step(None)` + `get_state()`. Enforced by Plan 01 acceptance criteria; Plan 02 does not add any `render()` call to the worker path. |
| T-42-03 | Tampering (simulator closed while worker mid-step) | `ViewportPanel.update_scene` / `ViewportPanel.stop` closing the simulator before pausing the worker | high | mitigate | Pitfall 3 — `update_scene` and `stop()` call `sim_worker.set_paused.emit(True)` BEFORE the `contextlib.suppress` `simulator.close()` block. `aboutToClose` already fires before `viewport.stop()` in `closeEvent`. Enforced by acceptance criterion: `set_paused` appears before `simulator.close()` in both methods. |
| T-42-04 | Information disclosure | status-bar hint leaks scene detail | low | mitigate | `_update_playback_status` keeps the static-scene hint generic ("static scene — no dynamics" — no scene content). Route through `safe_error_message()` if scene detail ever leaks in (defensive default per Security Domain V5). |
| T-42-SC | Tampering | (no package installs this phase) | high | accept | Zero external packages installed this phase (42-RESEARCH.md §Package Legitimacy Audit — PySide6 already pinned in the `[gui]` extra). No `[ASSUMED]`/`[SUS]` packages to checkpoint. |
| T-42-05 | Elevation (widget recreation resets dock state — bug #3 regression) | `ViewportPanel.update_scene` recreating the widget | medium | mitigate | Phase 41 D-06 — `update_scene` re-binds the worker + render-loop in place; NO `setCentralWidget`, NO new `ViewportPanel`. Enforced by acceptance criterion: `grep setCentralWidget src/surg_rl/editor/viewport.py` returns 0. |

*ASVS L1, block_on: high. T-42-01, T-42-02, T-42-03 are HIGH and mitigated (concrete code constraints enforced by acceptance criteria). No threat exceeds the block threshold after mitigation.*
</threat_model>

<verification>
- `PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_viewport_playback.py tests/test_dock_state.py::TestDockObjectNames -v` — all integration tests green (D-04, D-06, D-08, D-09, D-11, D-12, Phase 41 D-07 extension)
- `PYTHONPATH=src QT_QPA_PLATFORM=offscreen pytest tests/test_sim_step_worker.py tests/test_render_poll_loop.py -v` — Plan 01 component tests still green (no regression from the _tick split)
- `grep -rn "singleShot" src/surg_rl/editor/ | grep -v '^#' | grep -c .` — ONE render chain (the `RenderPollLoop`) on the UI thread; the worker uses `QTimer.timeout` on its own thread (D-02)
- `grep -n "terminate()" src/surg_rl/editor/main_window.py src/surg_rl/editor/sim_step_worker.py | grep -v '^#' | grep -c .` returns 0 (D-04 — NEVER terminate)
- `PYTHONPATH=src pytest tests/ -v` — full suite green (no regression in the 1,513-test v0.6.0 baseline + Phase 41)
- SC#2 real-fps is the `verification: backstop` truth (acknowledged in 42-VALIDATION.md §Manual-Only Verifications — the verifier abstains → `human_needed` when no display evidence; never a silent pass)
</verification>

<success_criteria>
- Opening a scene shows the preview paused (D-11); pressing Play (or Space) animates it — physics steps live in the viewport (SC#1 at the integration level)
- The viewport animates at >30 fps on a real display (SC#2 backstop — offscreen proxy `TestRenderPollCadence` confirms the cadence; real-fps deferred to human verification)
- The user can pause/resume/step-one via the toolbar or Space/"." shortcuts (SC#3, D-06, D-07)
- Render rate and sim rate are decoupled; snapshot publish capped at ~30 Hz (SC#4, D-01)
- Playback toolbar (objectName="toolbar_playback") + speed combo (objectName="combo_playback_speed", 5 values) + status-bar segment (D-06, D-08, D-09)
- Static-scene hint (D-12) with the corrected field paths (scene.fluid direct, instruments-only static)
- Close mid-step exits cleanly (D-04, Pitfall 3); aboutToClose → sim_worker.stop → thread.quit + wait(3000)
- QToolBar objectName discipline (Phase 41 D-07 extension); saveState round-trip intact
- Full suite green (no regression)
</success_criteria>

<output>
Create `.planning/phases/42-render-sim-decoupling-animated-viewport/42-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**New symbols (Plan 02 — this plan):**
- `src/surg_rl/editor/viewport.py`:
  - `_scene_has_dynamics(scene: SceneDefinition) -> bool` — module-level pure function (D-12, corrected paths: scene.robots, scene.tissues, scene.fluid direct)
  - `ViewportPanel.set_playback(self, sim_worker, render_loop)` (OR equivalent attrs) — receives the worker + loop refs from EditorWindow
  - `ViewportPanel._tick` — REMOVED/split (render → RenderPollLoop, step → SimStepWorker)
  - `ViewportPanel.update_scene` — EXTENDED (pause worker → close old sim → swap → load new sim on UI thread → render_loop.bind_simulator → sim_worker.bind_scene.emit → set_paused(True) D-11 → _scene_has_dynamics hint)
  - `ViewportPanel.stop` — EXTENDED (render_loop.stop + sim_worker.set_paused(True) BEFORE sim.close)
  - `ViewportPanel._static_scene` — panel-local bool (the D-12 hint flag for the status-bar callback)
- `src/surg_rl/editor/main_window.py`:
  - `EditorWindow._sim_thread` — `QtCore.QThread`
  - `EditorWindow._sim_worker` — `SimStepWorker`
  - `EditorWindow._render_loop` — `RenderPollLoop`
  - `EditorWindow._stop_sim_worker(self)` — controller teardown (sim_worker.stop + thread.quit + thread.wait(3000) + log-on-timeout)
  - `EditorWindow._build_playback_toolbar(self)` — creates `QToolBar` objectName="toolbar_playback"
  - `EditorWindow._act_play_pause` — Play/Pause `QAction` (setCheckable)
  - `EditorWindow._act_step_one` — Step-one `QAction`
  - `EditorWindow._speed_combo` — `QComboBox` objectName="combo_playback_speed" (5 items)
  - `EditorWindow._wire_playback_shortcuts` extension — `QShortcut("Space")`, `QShortcut(".")`
  - `EditorWindow._status_playback` — 5th permanent `QLabel`
  - `EditorWindow._update_playback_status(self, playing, speed, static=False)` — status text helper
  - `EditorWindow._toggle_play_pause(self)` / `_on_step_one(self)` / `_on_speed_changed(self, text)` / `_on_play_pause_toggled(self, checked)` / `_current_speed(self)` — handlers
  - `aboutToClose.connect(self._stop_sim_worker)` — the D-04 teardown wiring (mirror of :139)
- `tests/test_viewport_playback.py` — `TestPlaybackToolbar`, `TestPlaybackStatus`, `TestLoadPaused`, `TestStaticSceneHint`, `TestCloseMidStepCleanExit`
- `tests/test_dock_state.py` — `TestDockObjectNames` extended (or new `TestToolbarObjectNames`) for `QToolBar` objectName introspection