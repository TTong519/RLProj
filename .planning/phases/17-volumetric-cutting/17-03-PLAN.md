---
phase: 17-volumetric-cutting
plan: 03
type: execute
wave: 3
depends_on: [17-01, 17-02]
files_modified:
  - src/surg_rl/scene_definition/schema.py
  - src/surg_rl/rl/action.py
  - src/surg_rl/rl/environment.py
  - tests/test_cutting.py
autonomous: true
requirements: [CUT-04]
---

<objective>
Define CutAction Pydantic schema, register in ActionBuilder, wire cut application into environment step() with discrete trigger + cooldown enforcement. Cutting is a discrete event, not continuous control — use a trigger mechanism to prevent spam cutting.

Output: Updated schema.py with CutAction, updated action.py with cut processing, updated environment.py with cut wiring, extended test_cutting.py.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/17-volumetric-cutting/17-RESEARCH.md#Pattern 3, Pitfall 4
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: CutAction schema + action registration</name>
  <files>src/surg_rl/scene_definition/schema.py, src/surg_rl/rl/action.py, tests/test_cutting.py</files>
  <read_first>schema.py (existing CuttingProperties at ~line 611), action.py (ActionBuilder.process flow)</read_first>
  <behavior>
    - Test 1: CutAction validates and normalizes direction to unit vector
    - Test 2: CutAction with depth>0.05 raises ValidationError
    - Test 3: should_cut=True consumes cut params; should_cut=False ignores them
    - Test 4: Cooldown enforcement: second cut within 500ms is rejected
  </behavior>
  <action>
    **Step A:** Add CutAction model to schema.py.
    **Step B:** Register cut processing in ActionBuilder — if should_cut=1 and cooldown passed, extract CutAction params.
    **Step C:** Add TestCutAction class to test_cutting.py (4 tests).
    **Step D:** Run `PYTHONPATH=src pytest tests/test_cutting.py -v` — must pass.
  </action>
  <verify><automated>PYTHONPATH=src pytest tests/test_cutting.py -v -x</automated></verify>
  <done>CutAction schema with direction normalization, depth validation, discrete trigger, cooldown. 4 tests pass.</done>
</task>

<task type="auto">
  <name>task 2: wire cut into environment step</name>
  <files>src/surg_rl/rl/environment.py</files>
  <read_first>environment.py::step() (understand current action processing flow)</read_first>
  <behavior>
    - When should_cut=1 and cooldown passed, call simulator._apply_cut() before the physics step
    - When should_cut=0 or cooldown not passed, skip cutting and proceed with normal step
    - Cut state (cut faces, mesh stats) observable in observation.custom
  </behavior>
  <action>
    **Step A:** Add cut detection + routing in environment step() — after action processing, before sim.step().
    **Step B:** Track last_cut_time per tissue, enforce 500ms cooldown.
    **Step C:** Expose cut state in observation.custom["cut_state"].
    **Step D:** Run full test suite — no regressions.
  </action>
  <verify><automated>PYTHONPATH=src pytest tests/ -m "not integration" -q</automated></verify>
  <done>Environment step() routes CutAction to simulator before physics. Cooldown enforced. Full test suite green.</done>
</task>

</tasks>

<threat_model>
| Threat | Category | Mitigation |
|--------|----------|------------|
| Spam cutting from continuous action | DoS | Cooldown (500ms minimum between cuts); should_cut default=0 |
| Invalid cut plane (zero-length direction) | Tampering | Pydantic validator: |normal| < 1e-6 → reject |
| Cut into non-existent tissue | Tampering | Validate tissue_name against simulator's known tissues |
</threat_model>

<success_criteria>
- [ ] CutAction in schema.py with direction normalization + depth validation
- [ ] ActionBuilder routes should_cut → CutAction extraction
- [ ] Environment step() enforces cooldown + applies cut before physics
- [ ] All tests pass
</success_criteria>
