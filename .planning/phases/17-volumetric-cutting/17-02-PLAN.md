---
phase: 17-volumetric-cutting
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - src/surg_rl/simulators/mujoco_simulator.py
  - src/surg_rl/simulators/pybullet_simulator.py
autonomous: true
requirements: [CUT-03]
---

<objective>
Integrate the cutting engine (Plan 01) with both simulator backends. MuJoCo: save qpos/qvel → rebuild model with new mesh → restore state. PyBullet: optimize the existing resetSimulation + loadSoftBody reload path to only reload modified tissues.

Output: Updated mujoco_simulator.py and pybullet_simulator.py with _apply_cut() methods.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/17-volumetric-cutting/17-RESEARCH.md#Pitfall 2, Pitfall 3

Key constraints:
- MuJoCo: mjModel is immutable after compilation → need full model swap
- PyBullet: removeBody() unsafe for soft bodies → use resetSimulation + reload
- Must preserve simulator state (qpos, qvel) across cuts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: MuJoCo cut integration via model swap</name>
  <files>src/surg_rl/simulators/mujoco_simulator.py</files>
  <read_first>mujoco_simulator.py (find _model, _data references, understand current MJCF generation via scene_builder)</read_first>
  <behavior>
    - Test: Calling _apply_cut() on a flex body tissue saves qpos/qvel, rebuilds model with new mesh, restores state
    - Validate that uncut tissues/robots/instruments are unchanged after model swap
  </behavior>
  <action>
    **Step A:** Add `_apply_cut(self, cut_action)` method to `MuJoCoSimulator`.
    Flow: get current mesh from flex body → run cut_tetrahedral_mesh() → write new node/ele files → call scene_builder to regenerate MJCF → mj_deleteData(old) → mj_deleteModel(old) → mj_loadXML(new) → mj_makeData(new) → copy qpos/qvel.
    **Step B:** Handle case where cut tissue is rigid (no-op or raise).
    **Step C:** Test via `PYTHONPATH=src pytest tests/test_simulators.py -k "mujoco" -v` — no regressions.
  </action>
  <verify><automated>PYTHONPATH=src pytest tests/test_simulators.py -k "mujoco" -v -x</automated></verify>
  <done>MuJoCo _apply_cut() with model swap + state preservation. Existing tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: PyBullet cut integration via optimized reload</name>
  <files>src/surg_rl/simulators/pybullet_simulator.py</files>
  <read_first>pybullet_simulator.py::_load_soft_body_tissue (line ~583), reset() (line ~791)</read_first>
  <behavior>
    - Test: Cutting a soft body tissue reloads only that tissue (not entire scene)
    - Test: Backward compat — scenes without cutting still use full reset path
  </behavior>
  <action>
    **Step A:** Add `_apply_cut(self, cut_action)` method to `PyBulletSimulator`.
    Flow: get current mesh from soft body → run cut_tetrahedral_mesh() → write new VTK → resetSimulation(RESET_USE_DEFORMABLE_WORLD) → reload non-soft bodies → loadSoftBody with new mesh for cut tissue → restore other soft bodies unchanged.
    **Step B:** Test via `PYTHONPATH=src pytest tests/test_simulators.py -k "pybullet" -v` — no regressions.
  </action>
  <verify><automated>PYTHONPATH=src pytest tests/test_simulators.py -k "pybullet" -v -x</automated></verify>
  <done>PyBullet _apply_cut() with optimized single-tissue reload. Backward compat preserved.</done>
</task>

</tasks>

<threat_model>
| Threat | Category | Mitigation |
|--------|----------|------------|
| DOF mismatch after model swap | DoS | Verify new model flex_vertnum matches old; abort cut if changed |
| State loss during MuJoCo swap | Data loss | Save qpos/qvel pre-delete; validate shapes before restore |
| PyBullet reload performance | DoS | Cache unchanged body meshes; skip reload for non-soft bodies |
</threat_model>

<success_criteria>
- [ ] `mujoco_simulator.py` has `_apply_cut()` with model swap + state preservation
- [ ] `pybullet_simulator.py` has `_apply_cut()` with optimized single-tissue reload
- [ ] All existing MuJoCo + PyBullet tests pass (no regressions)
</success_criteria>
