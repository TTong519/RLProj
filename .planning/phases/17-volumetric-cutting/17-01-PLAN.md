---
phase: 17-volumetric-cutting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/surg_rl/cutting/__init__.py
  - src/surg_rl/cutting/engine.py
  - src/surg_rl/cutting/intersection.py
  - tests/test_cutting.py
autonomous: true
requirements: [CUT-01, CUT-02]
---

<objective>
Implement the core tetrahedral mesh cutting engine: signed distance computation, tet-plane intersection classification (5 canonical cases), element subdivision (1-3 and 2-2 split patterns), vertex deduplication, and boundary face extraction. Pure NumPy/SciPy — no simulator integration yet (that's Plan 02).

Output: New `src/surg_rl/cutting/` module with engine and intersection submodules, new `tests/test_cutting.py` test file.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/17-volumetric-cutting/17-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: scaffold cutting module + signed distance</name>
  <files>src/surg_rl/cutting/__init__.py, src/surg_rl/cutting/intersection.py, tests/test_cutting.py</files>
  <read_first>17-RESEARCH.md#Example 1, Example 2</read_first>
  <behavior>
    - Test 1: `compute_signed_distances(vertices, origin, normal)` returns (N,) array with correct signs
    - Test 2: `edge_intersection(v_i, v_j, d_i, d_j)` returns correct midpoint when d_i = -d_j
    - Test 3: `classify_tet_case(d0, d1, d2, d3, eps)` returns correct case number (0/1/2/3/4)
  </behavior>
  <action>
    **Step A:** Create `src/surg_rl/cutting/__init__.py` with public exports.
    **Step B:** Create `src/surg_rl/cutting/intersection.py` with `compute_signed_distances`, `edge_intersection`, `classify_tet_case`.
    **Step C:** Create `tests/test_cutting.py` with TestIntersection class (3 tests). 
    **Step D:** Run `PYTHONPATH=src pytest tests/test_cutting.py -v` — must pass.
  </action>
  <verify><automated>PYTHONPATH=src pytest tests/test_cutting.py -v -x</automated></verify>
  <done>intersection.py with compute_signed_distances, edge_intersection, classify_tet_case. 3 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement cut_tetrahedral_mesh() engine</name>
  <files>src/surg_rl/cutting/engine.py, tests/test_cutting.py</files>
  <read_first>17-RESEARCH.md#Pattern 2 (full pseudocode)</read_first>
  <behavior>
    - Test 1: Cutting a unit tetrahedron at its centroid produces 2+ valid child tets
    - Test 2: Cutting a tetrahedralized cube (8 verts, 6 face triangulations) produces valid output with correct vertex count
    - Test 3: Cut boundary faces extracted from cut surface are non-empty
    - Test 4: Uncut tets (plane misses) are preserved unchanged in output
    - Test 5: Vertex dedup: two adjacent tets sharing an edge produce only 1 intersection point on that edge
  </behavior>
  <action>
    **Step A:** Extend `tests/test_cutting.py` with TestCutEngine class (5 tests).
    **Step B:** Create `src/surg_rl/cutting/engine.py` with `cut_tetrahedral_mesh()`, `_subdivide_3_1()`, `_subdivide_2_2()`, `_extract_cut_boundary()`.
    **Step C:** Run `PYTHONPATH=src pytest tests/test_cutting.py -v` — all must pass.
  </action>
  <verify><automated>PYTHONPATH=src pytest tests/test_cutting.py -v -x</automated></verify>
  <done>engine.py with cut_tetrahedral_mesh() implementing all 5 cases. 5 engine tests pass.</done>
</task>

</tasks>

<threat_model>
| Threat | Category | Mitigation |
|--------|----------|------------|
| Degenerate tets after multiple cuts | DoS | Track per-tet generation counter; retetrahedralize at gen>3 |
| Zero-length edges from vertex snap | Tampering | Assert edge length > 1e-12 before subdivision |
| NaN in intersection due to zero denominator | DoS | Guard abs(d_i)+abs(d_j) > 1e-15 |
</threat_model>

<success_criteria>
- [ ] `src/surg_rl/cutting/` module exists with __init__.py, intersection.py, engine.py
- [ ] `cut_tetrahedral_mesh(verts, tets, origin, normal)` returns (new_verts, new_tets, cut_faces)
- [ ] All 8 tests pass
</success_criteria>
