---
phase: 40.1-close-gap-dmv3-08-thread-checkpoint-dir-into-find-latest-che
verified: 2026-07-15T00:00:00Z
status: passed
score: 14/14 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 40.1: Close gap — DMV3-08 thread checkpoint_dir + Phase 38 advisory cleanups — Verification Report

**Phase Goal:** Close the DMV3-08 resume-path gap (thread `checkpoint_dir` into `_find_latest_checkpoint` so the parent resume path finds custom-dir checkpoints) and land the Phase 38 advisory cleanups (CR-01 3D force-unit magnitude regression test + implement real TWO_WAY obstacle-velocity feedback and substep loop so `coupling_mode`/`coupling_substeps` behave as documented instead of being inert). Two parallel plans with disjoint module sets (`dreamer/` vs `fluids/`).

**Verified:** 2026-07-15
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

Both workstreams delivered. The DMV3-08 parent-side resume path is now wired and CPU-verified; the Phase 38 WR-01/WR-02 inert-knob advisory is closed (real TWO_WAY feedback + substep loop), and the CR-01 magnitude regression test pins the already-committed `np.gradient` spacing fix.

### Observable Truths

Plan 01 (dreamer, requirements D-01, D-02, D-03, D-09):

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | `_find_latest_checkpoint` accepts an additive `checkpoint_dir` parameter and returns the latest `.ckpt` from that dir when given, falling back to the default `models/dreamerv3/{task}_{obs_type}` dir when `checkpoint_dir` is None (D-01) | VERIFIED | `src/surg_rl/dreamer/training.py:183-209` — signature `task: str, obs_type: str, checkpoint_dir: str \| None = None` (line 184); candidate-list `[Path(checkpoint_dir) if checkpoint_dir else None, Path(f"models/dreamerv3/{task}_{obs_type}")]` searched in order, first dir with `.ckpt` wins, else `None` (lines 199-209); `*.ckpt` glob + max-by-mtime reused verbatim |
| 2 | Both call sites (eval_only branch at training.py:282 and resume branch at training.py:299) pass the parent-resolved `checkpoint_dir` to `_find_latest_checkpoint` (D-01) | VERIFIED | `grep -c '_find_latest_checkpoint(task, obs_type, checkpoint_dir)' src/surg_rl/dreamer/training.py == 2` — call site 1 at `training.py:293` (eval_only), call site 2 at `training.py:310` (resume branch) |
| 3 | `run_dreamer_training(resume=True, checkpoint_dir=<custom>)` calls `subprocess.load_checkpoint` with the custom-dir `.ckpt` path, verified by a CPU-runnable subprocess-stub test with no GPU/JAX/skipif (D-03) | VERIFIED (behavioral) | `tests/dreamer/test_checkpoint_dir_resume_cpu.py::test_resume_custom_checkpoint_dir_calls_load_checkpoint` PASSES — uses `MagicMock` stub (`fake_sp.train.return_value=iter([])`, `evaluate.return_value={}`), monkeypatches `check_spike_status`/`_create_scene_for_task`/`_create_env`, asserts `fake_sp.load_checkpoint.assert_called_once_with(str(ckpt_dir / "checkpoint.ckpt"))` (line 100); ran in the verification suite (10 passed, 1 xpassed) |
| 4 | The child `cp.load_or_save()` at subprocess.py:217 remains active as defense-in-depth alongside the now-wired parent path (D-02) | VERIFIED | `grep -n 'load_or_save' src/surg_rl/dreamer/subprocess.py` returns 5 matches including line 217 `cp.load_or_save()  # resume if checkpoint.ckpt exists, else save initial state`; `git log --name-only 6576f40..HEAD` shows `subprocess.py` NOT modified by any 40.1 commit |
| 5 | Two parallel plans with disjoint module sets (dreamer/ vs fluids/) run in a single wave (D-09) | VERIFIED | `git diff --name-only HEAD~4 HEAD` — plan 01 commits (`f0d193f`, `8664ac9`) touched only `training.py` + `tests/dreamer/test_checkpoint_dir_resume_cpu.py`; plan 02 commits (`0c9e017`, `2cd882c`) touched only `fluid_simulator.py`, `schema.py`, `test_3d_coupling.py`, `test_force_computation.py`; no crossover |

Plan 02 (fluids, requirements D-04, D-05, D-06, D-07, D-08, D-09):

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 6 | A linear y-ramp pressure fixture produces `fy = -(1/0.3) * V_mask` within 1% tolerance, pinning that the CR-01 `np.gradient` spacing fix produces Newtons not cell-index units (D-04) | VERIFIED (behavioral) | `tests/test_fluids/test_force_computation.py::TestComputeObstacleForces3D::test_3d_force_y_axis_physical_magnitude` PASSES — linear ramp `arr[:,j,:] = j/16.0` on 0.3m/16-cell domain, hand-computes `dy=0.3/16`, `cell_vol=dy^3`, `V_mask=N_mask*cell_vol`, `expected_fy=-(1/0.3)*V_mask`, asserts `abs(fy-expected_fy)/abs(expected_fy) < 0.01` (lines 134-176); ran in the verification suite |
| 7 | `FluidSimulator.step` runs a substep loop for BOTH ONE_WAY and TWO_WAY with `sub_dt = dt / coupling_substeps` on the 3D obstacle path (D-06) | VERIFIED (behavioral) | `src/surg_rl/fluids/fluid_simulator.py:295-333` — `sub_dt = dt / self.config.coupling_substeps` (line 296), `for _ in range(self.config.coupling_substeps):` (line 298), loop runs unconditionally for both modes (no ONE_WAY skip); `tests/test_fluids/test_3d_coupling.py::Test3DCouplingOneWay::test_one_way_stable_n100` PASSES (4x substepping, finiteness held) |
| 8 | TWO_WAY `step()` passes a moving `Obstacle` (velocity != 0) to `make_incompressible`, diverging from ONE_WAY's stationary `Obstacle` (D-05, D-08) | VERIFIED (behavioral) | `fluid_simulator.py:188-218` — `_build_obstacles_arg` branches on `coupling_mode`: TWO_WAY returns `Obstacle(merged, velocity=(float(v[0]), float(v[1]), float(v[2])))` (lines 210-217), ONE_WAY returns `Obstacle(merged)` stationary (line 218); `tests/test_fluids/test_3d_coupling.py::Test3DCouplingTwoWayOptIn::test_two_way_takes_feedback_branch` PASSES — asserts `captured["obstacles"][0].is_moving is True` |
| 9 | TWO_WAY obstacle velocity is integrated from prior-substep forces via `v_obs += (F / m_obs) * sub_dt` (D-08, added-mass feedback) | VERIFIED (behavioral) | `fluid_simulator.py:220-235` — `_integrate_obstacle_velocity(forces, sub_dt)` with `m_obs = 1e-3` (line 231), `self._obstacle_velocities[name] = self._obstacle_velocities[name] + (f / m_obs) * sub_dt` (lines 233-234); called inside the 3D loop gated on TWO_WAY (line 328-329); per-obstacle state `_obstacle_velocities` declared in `__init__` (line 101), registered in `add_obstacle` (line 117), cleared in `clear_obstacles` (line 122) |
| 10 | The structural test observes the feedback branch via `obstacle.is_moving` on the captured `Obstacle` arg (D-07a) | VERIFIED (behavioral) | `test_two_way_takes_feedback_branch` (lines 133-193) — monkeypatches `phi.physics.fluid.make_incompressible` with a spy capturing the `obstacles` arg, seeds nonzero initial velocity (`StaggeredGrid(0.1, ...)`, line 178) so the pressure solve develops forces, calls `fs.step()`, asserts `captured["obstacles"][0].is_moving` (line 190); PASSES |
| 11 | The thin-instrument xfail test genuinely exercises the unstable added-mass path now that TWO_WAY diverges from ONE_WAY (D-07b) | VERIFIED (behavioral) | `test_two_way_thin_instrument_unstable` (lines 205+) — renamed from `test_two_way_opt_in_documented_unstable` (old name `grep -c == 0`, new name `grep -c == 1`), keeps `@pytest.mark.xfail(strict=False)` + same docstring, runs N=100 thin-instrument loop with `_assert_velocity_pressure_finite` per step; ran as XPASS in the verification suite (strict=False accepts xpass — genuinely exercises the path; an xpass is a real signal the tested dims happen to be stable, still "not a guarantee" per the docstring) |
| 12 | `coupling_substeps` field description text matches `dt / coupling_substeps` semantics (D-06) | VERIFIED | `src/surg_rl/scene_definition/schema.py:1550` — description text is `"each substep uses sub_dt = dt / coupling_substeps."`; `grep -c 'dt / coupling_substeps' == 1`, `grep -c 'reuses substep_dt as per-substep dt' == 0` (old text removed); `ge=1`, `le=16`, `default=4` unchanged |
| 13 | The 2D step branch stays byte-identical to v0.5.0 (SC#1 — substep loop runs only on the 3D path) | VERIFIED (behavioral) | `fluid_simulator.py:259-293` — 2D path early-returns before the 3D substep loop (`if not self.config.dim_3d:` at line 260, `return forces` at line 293); exact single-solve sequence (single `advect.mac_cormack`, single `make_incompressible` with `[Obstacle(merged)]`, single `compute_obstacle_forces`, `self._sim_time += dt`); `tests/test_fluids/test_2d_baseline.py` PASSES (2 passed — `test_2d_velocity_pressure_pinned_to_baseline` + `test_2d_per_step_finite_n10`) |
| 14 | ONE_WAY stability test `test_one_way_stable_n100` still passes under 4x substepping (smaller `sub_dt` is more stable, D-06) | VERIFIED (behavioral) | `tests/test_fluids/test_3d_coupling.py::Test3DCouplingOneWay::test_one_way_stable_n100` PASSES in the verification suite |

**Score:** 14/14 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `src/surg_rl/dreamer/training.py` | `_find_latest_checkpoint` signature gains `checkpoint_dir: str \| None = None`; both call sites pass `checkpoint_dir` | VERIFIED | Line 184 signature; lines 293 + 310 call sites; candidate-list body at 199-209 |
| `tests/dreamer/test_checkpoint_dir_resume_cpu.py` | NEW CPU-runnable test file with 2 test functions, no `pytestmark` skipif | VERIFIED | 2 module-level test functions (`test_find_latest_checkpoint_custom_dir` line 28, `test_resume_custom_checkpoint_dir_calls_load_checkpoint` line 49); no `pytestmark =` assignment, no `skipif(` call (only docstring mentions for the deliberate contrast); no `import jax`/`import dreamerv3` |
| `src/surg_rl/fluids/fluid_simulator.py` | `_obstacle_velocities` dict state, `_build_obstacles_arg`, `_integrate_obstacle_velocity`, restructured `step()` with 3D substep loop, updated docstring | VERIFIED | `_obstacle_velocities` at :101; `_build_obstacles_arg` at :188; `_integrate_obstacle_velocity` at :220; `step()` at :237 with 2D early-return :259-293 + 3D substep loop :295-333; docstring at :238-253 |
| `src/surg_rl/scene_definition/schema.py` | `coupling_substeps` description updated to `dt / coupling_substeps` semantics | VERIFIED | Line 1550; bounds + default unchanged |
| `tests/test_fluids/test_force_computation.py` | `test_3d_force_y_axis_physical_magnitude` added to `TestComputeObstacleForces3D` | VERIFIED | Lines 134-176; added to existing class (no new class) |
| `tests/test_fluids/test_3d_coupling.py` | `test_two_way_takes_feedback_branch` (structural) + `test_two_way_thin_instrument_unstable` (renamed xfail) | VERIFIED | Structural at :133-193; renamed xfail at :205+; `test_two_way_opt_in_accepted` kept at :117-131 |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `run_dreamer_training(resume=True, checkpoint_dir=X)` | `_find_latest_checkpoint(task, obs_type, X)` | additive third param | WIRED | `training.py:310` — `_find_latest_checkpoint(task, obs_type, checkpoint_dir)` |
| `_find_latest_checkpoint` (custom dir hit) | `subprocess.load_checkpoint(X/*.ckpt)` | `resume_checkpoint = latest` → `subprocess.load_checkpoint(resume_checkpoint)` | WIRED | `training.py:310-314` |
| `FluidSimulator.step` (3D path) | `make_incompressible` | `_build_obstacles_arg` → `Obstacle(merged, velocity=...)` for TWO_WAY | WIRED | `fluid_simulator.py:303` → `:188-218` → `make_incompressible` at :306 |
| `FluidSimulator.step` (TWO_WAY 3D) | `_integrate_obstacle_velocity` | `_compute_obstacle_forces_3d` → forces → `v_obs += (F/m_obs)*sub_dt` | WIRED | `fluid_simulator.py:315-326` (forces) → `:328-329` (integrate call) → `:220-235` (body) |
| `_integrate_obstacle_velocity` | `_obstacle_velocities` state | `self._obstacle_velocities[name] = ...` | WIRED | `:233-234` writes; `_build_obstacles_arg` reads at `:211` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `_find_latest_checkpoint` | `checkpoint_dir` param | `run_dreamer_training` checkpoint_dir arg (resolved at :247-250) | Yes — real filesystem path, `glob("*.ckpt")` returns real files | FLOWING |
| `_build_obstacles_arg` (TWO_WAY) | `_obstacle_velocities[name]` | `_integrate_obstacle_velocity` accumulates from `_compute_obstacle_forces_3d` forces | Yes — real per-substep forces integrated into velocity | FLOWING |
| `_integrate_obstacle_velocity` | `forces` dict | `_compute_obstacle_forces_3d(self._velocity, self._pressure, ...)` | Yes — real pressure-gradient forces (pinned by D-04 magnitude test) | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| DMV3-08 resume path wiring (D-01/D-03) | `PYTHONPATH=src python -m pytest tests/dreamer/test_checkpoint_dir_resume_cpu.py -q` | 2 passed | PASS |
| CR-01 magnitude regression (D-04) | `PYTHONPATH=src python -m pytest tests/test_fluids/test_force_computation.py::TestComputeObstacleForces3D -q` | 5 passed | PASS |
| TWO_WAY feedback branch + ONE_WAY stability + xfail (D-05/D-06/D-07/D-08) | `PYTHONPATH=src python -m pytest tests/test_fluids/test_3d_coupling.py -q` | 3 passed, 1 xpassed | PASS |
| SC#1 2D byte-identical baseline | `PYTHONPATH=src python -m pytest tests/test_fluids/test_2d_baseline.py -q` | 2 passed | PASS |
| Combined verification suite | `PYTHONPATH=src python -m pytest tests/dreamer/test_checkpoint_dir_resume_cpu.py tests/test_fluids/test_force_computation.py::TestComputeObstacleForces3D tests/test_fluids/test_3d_coupling.py -q --tb=short` | 10 passed, 1 xpassed (448.59s) | PASS |

### Probe Execution

No probe scripts declared in this phase's PLAN/SUMMARY. N/A.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| D-01 | 01 | Thread `checkpoint_dir` into `_find_latest_checkpoint` | SATISFIED | `training.py:183-209` signature + candidate-list; both call sites :293, :310 |
| D-02 | 01 | Keep both resume layers (parent wired + child `cp.load_or_save()` at subprocess.py:217) | SATISFIED | `subprocess.py:217` unchanged; not in any 40.1 commit |
| D-03 | 01 | CPU-runnable subprocess-stub test, no GPU/JAX/skipif | SATISFIED | `tests/dreamer/test_checkpoint_dir_resume_cpu.py` — 2 tests pass; no `pytestmark=`/`skipif(`; `MagicMock` stub asserts `load_checkpoint` called with custom path |
| D-04 | 02 | CR-01 physical-magnitude regression test (fy = -(1/0.3)*V_mask within 1%) | SATISFIED | `test_3d_force_y_axis_physical_magnitude` PASSES; hand-computed expected value, 1% tolerance, unclamped \|fy\| ≈ 0.0084 N well below 1e4 clamp |
| D-05 | 02 | IMPLEMENT real TWO_WAY (do NOT remove knobs) | SATISFIED | `FluidCouplingMode` + `coupling_mode`/`coupling_substeps` fields kept; `_build_obstacles_arg` branches on coupling_mode |
| D-06 | 02 | Substep loop for BOTH modes, `sub_dt = dt / coupling_substeps`; update field description | SATISFIED | `fluid_simulator.py:295-333` loop runs unconditionally; `schema.py:1550` description updated |
| D-07 | 02 | Split: structural test (D-07a) + renamed xfail (D-07b) | SATISFIED | `test_two_way_takes_feedback_branch` (stable, PASSES) + `test_two_way_thin_instrument_unstable` (xfail strict=False, ran as xpass); old name removed |
| D-08 | 02 | TWO_WAY feedback into `make_incompressible` boundary; `v_obs += (F/m_obs)*sub_dt` | SATISFIED | `Obstacle(merged, velocity=(float,float,float))` at :213-217; `_integrate_obstacle_velocity` at :220-235 with m_obs=1e-3 |
| D-09 | 01 + 02 | Two parallel plans, disjoint module sets (dreamer/ vs fluids/) | SATISFIED | git diff confirms plan 01 = dreamer-only, plan 02 = fluids-only; no crossover; `.ckpt` glob unchanged, no `.pt` shim |

REQUIREMENTS.md cross-reference: `DMV3-08` is mapped to Phase 40 (Complete). This phase 40.1 closes the residual resume-path gap surfaced by the v0.6.0 milestone audit; `FLUID-02` (3D coupling, two-way opt-in) is mapped to Phase 38 — this phase implements the real TWO_WAY behavior the opt-in promised.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| (none) | — | — | — | No TBD/FIXME/XXX debt markers, no placeholder returns, no hardcoded empty data in production code. The `MagicMock` in the CPU resume test is an intentional test double (D-03), not a production stub. The seeded `StaggeredGrid(0.1, ...)` in the structural test is a documented test fixture (necessary for the is_moving assertion to be observable — zero initial velocity produces zero forces and is_moving=False), not a stub. |

### Prohibitions Verified (Must-NOT checks)

| Prohibition | Status | Evidence |
| --- | --- | --- |
| Do NOT remove/modify `subprocess.py:217 cp.load_or_save()` (D-02) | HONORED | Line 217 unchanged; `subprocess.py` not in any 40.1 commit (`git log --name-only 6576f40..HEAD \| grep subprocess` empty) |
| Do NOT add `pytestmark` skipif to the new CPU resume test (D-03) | HONORED | No `pytestmark =` assignment, no `skipif(` call in `test_checkpoint_dir_resume_cpu.py` (only docstring text explaining the deliberate contrast) |
| Do NOT touch any `fluids/` file, `force_computation.py`, `fluid_simulator.py`, or `schema.py` in Plan 01 (D-09) | HONORED | Plan 01 commits touched only `training.py` + `tests/dreamer/test_checkpoint_dir_resume_cpu.py` |
| Do NOT touch `training.py`, `subprocess.py`, or any `dreamer/` file in Plan 02 (D-09) | HONORED | Plan 02 commits touched only `fluid_simulator.py`, `schema.py`, `test_3d_coupling.py`, `test_force_computation.py` |
| Do NOT change the `.ckpt` glob pattern or add a `.pt` shim (D-09) | HONORED | `glob("*.ckpt")` at `training.py:206` unchanged; no `.pt` references added |
| Do NOT re-fix CR-01 — `force_computation.py:67-69` spacing fix stays (D-04) | HONORED | `grep -c 'np.gradient' src/surg_rl/fluids/force_computation.py == 4` (baseline); `force_computation.py` not in any 40.1 commit (only the TEST file `test_force_computation.py` was modified) |
| Do NOT substep the 2D branch — byte-identical to v0.5.0 (SC#1) | HONORED | 2D path early-returns at `fluid_simulator.py:259-293`; `tests/test_fluids/test_2d_baseline.py` PASSES (2 passed) |
| Do NOT remove `coupling_mode`/`coupling_substeps`/`FluidCouplingMode` or their tests (D-05) | HONORED | All fields/enum/tests retained; `test_two_way_opt_in_accepted` kept at `test_3d_coupling.py:117-131` |
| Do NOT pass Obstacle velocity as `np.ndarray` (PhiFlow 3.4.0 landmine 7) | HONORED | `fluid_simulator.py:215` — `velocity=(float(v[0]), float(v[1]), float(v[2]))` (tuple of Python floats) |
| Do NOT silently special-case ONE_WAY to skip the substep loop (D-06) | HONORED | Loop at `:298` runs `for _ in range(self.config.coupling_substeps)` unconditionally; only the `_integrate_obstacle_velocity` call is TWO_WAY-gated (`:328`) |
| Do NOT build a magnitude-test ramp whose unclamped force exceeds 1e4 | HONORED | Test docstring + asserted value: `\|fy\| ≈ 0.0084 N` (6 orders below the 1e4 per-axis clamp) |

### Human Verification Required

None. All behavior-dependent truths (D-03 resume wiring, D-04 magnitude, D-06 substep loop, D-07a feedback branch, D-07b thin-instrument xfail, D-08 velocity integration, SC#1 2D byte-identical) have passing behavioral tests in the verification suite. No truth is left PRESENT_BEHAVIOR_UNVERIFIED.

Note (unchanged from CONTEXT): The GPU runtime resume (DMV3-07 runtime, DMV3-08 resume runtime, DMV3-10 CI GREEN) remains a separate ops USER ACTION ITEM gated on standalone GitHub-GPU-runner enablement — this phase's CPU resume test verifies parent-path wiring, NOT GPU runtime. That deferral is intentional and out of scope for phase 40.1; it is not a gap.

### Gaps Summary

No gaps. All 14 truths verified, all artifacts present + substantive + wired + data-flowing, all 11 prohibitions honored, all 9 requirement IDs (D-01..D-09) satisfied, all behavioral spot-checks PASS (10 passed + 1 xpassed across the combined suite; 2D baseline 2 passed for SC#1).

---

_Verified: 2026-07-15_
_Verifier: Claude (gsd-verifier)_