---
phase: 35-advanced-tech-debt
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/k8s/test_pvc_e2e.py
  - pytest.ini
autonomous: true
requirements:
  - DEBT-06
must_haves:
  truths:
    - "`tests/k8s/test_pvc_e2e.py` is committed with a `[k8s]` pytest marker, a `kind`-cluster skip, and a stub test body containing a TODO plus a v0.6.0 deferral rationale"
    - "`pytest.ini` registers the `[k8s]` marker so pytest does not emit unknown-marker warnings"
  artifacts:
    - path: tests/k8s/test_pvc_e2e.py
      provides: "Stub K8s PVC end-to-end test"
      contains: "pytest\.mark\.k8s|kind|TODO|v0\.6\.0"
    - path: pytest.ini
      provides: "Registered k8s marker"
      contains: "k8s: marks tests as Kubernetes e2e tests"
  key_links: []
---

<objective>
Create the K8s PVC end-to-end scaffolding that v0.4.2 deferred. Because a real
PVC e2e test requires a live Kubernetes cluster (e.g. `kind`) and persistent
volume provider, this plan commits a properly marked stub that documents the
deferral and the shape of the future test. The stub validates the marker
registry, the skip condition, and the project convention for K8s tests without
requiring cluster infrastructure in v0.5.0 CI.

Purpose: The v0.4.2 milestone identified K8s PVC e2e as out of scope. v0.5.0
acknowledges the work by laying down the test file, marker, and skip logic so
that v0.6.0 can fill in the body without restructuring the test suite.

Output: One new test file and an updated `pytest.ini` marker registry.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/35-advanced-tech-debt/35-CONTEXT.md
@.planning/ROADMAP.md
@tests/test_kubernetes_manifests.py
@tests/conftest.py
@pytest.ini
</context>

<tasks>

<task id="1">
  <name>Create K8s test directory and stub test</name>
  <read_first>
    - tests/test_kubernetes_manifests.py
    - pytest.ini
    - k8s/base/training-job.yaml (to reference PVC name)
  </read_first>
  <action>
    Create directory `tests/k8s/` if it does not exist. Add a new file
    `tests/k8s/test_pvc_e2e.py` containing:
    - Module docstring explaining the test is a stub deferred to v0.6.0.
    - Imports: `pytest`, `subprocess`, `shutil`.
    - A module-level helper `_kind_cluster_available() -> bool` that returns
      `True` only if `shutil.which("kind")` is truthy AND
      `subprocess.run(["kind", "get", "clusters"], capture_output=True,
      text=True, check=False).returncode == 0` and the output is non-empty.
    - A `@pytest.mark.k8s` decorated test function `test_pvc_read_write_stub`.
    - Inside the test, call `pytest.skip("No local kind cluster available       (K8s PVC e2e deferred to v0.6.0)")` if `_kind_cluster_available()` is
      `False`.
    - If a cluster is available, the test body should be a TODO stub:
      ```python
      # TODO(v0.6.0): implement PVC create/write/read/delete cycle.
      #   1. kubectl apply -f k8s/base/pvc.yaml (or an e2e overlay).
      #   2. Launch a Job that writes a checkpoint file to the PVC.
      #   3. Launch a second Job that reads the checkpoint back and verifies
      #      contents.
      #   4. Clean up PVC + Jobs.
      pass
      ```
    - Add `tests/k8s/__init__.py` as an empty package marker.
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/k8s/test_pvc_e2e.py -v || echo "Expected skip or import check"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/k8s/__init__.py` exists.
    - `tests/k8s/test_pvc_e2e.py` exists.
    - The test function is decorated with `@pytest.mark.k8s`.
    - The test skips when no local `kind` cluster is available.
    - The test body contains a TODO and a v0.6.0 deferral rationale.
    - The helper detects `kind` binary and non-empty cluster list.
  </acceptance_criteria>
  <done>
    The K8s PVC stub test is on disk and either skips or passes a no-op check
    when run in isolation.
  </done>
</task>

<task id="2">
  <name>Register the `[k8s]` marker in pytest.ini</name>
  <read_first>
    - pytest.ini
  </read_first>
  <action>
    Add a new marker line under the `[pytest]` `markers =` block:
    `k8s: marks tests as Kubernetes e2e tests (deselect with '-m "not k8s"')`
    Preserve the existing markers and formatting.
  </action>
  <verify>
    <automated>grep -q "k8s: marks tests as Kubernetes e2e tests" pytest.ini && echo "marker registered"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest.ini` contains the new `k8s` marker definition.
    - Existing `integration` and `slow` markers are unchanged.
  </acceptance_criteria>
  <done>
    The `[k8s]` marker is registered and pytest no longer warns about an unknown
    marker.
  </done>
</task>

<task id="3">
  <name>Verify marker and skip behavior</name>
  <read_first>
    - tests/k8s/test_pvc_e2e.py
    - pytest.ini
  </read_first>
  <action>
    Run the test file and the deselect command:
    - `PYTHONPATH=src python -m pytest tests/k8s/test_pvc_e2e.py -v`
    - `PYTHONPATH=src python -m pytest tests/ -m "not k8s" -q`
    Confirm the first invocation reports `skipped` (1 skipped) and the second
    invocation does not collect the new test. If `kind` is installed and a
    cluster exists, the test should still be a no-op `pass`.
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/k8s/test_pvc_e2e.py -v -rs 2>&1 | grep -E "SKIPPED|PASSED|k8s" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/k8s/test_pvc_e2e.py` reports 1 skipped or 1 passed.
    - `pytest tests/ -m "not k8s"` does not collect `test_pvc_e2e.py`.
  </acceptance_criteria>
  <done>
    The marker and skip logic behave as designed.
  </done>
</task>

</tasks>

<verification>
- [ ] `tests/k8s/__init__.py` exists.
- [ ] `tests/k8s/test_pvc_e2e.py` exists and is decorated with `@pytest.mark.k8s`.
- [ ] `pytest.ini` registers the `k8s` marker.
- [ ] `PYTHONPATH=src pytest tests/k8s/test_pvc_e2e.py -v` reports 1 passed or 1 skipped.
- [ ] `PYTHONPATH=src pytest tests/ -m "not k8s" -q` does not collect the new test.
- [ ] `ruff check tests/k8s/test_pvc_e2e.py` exits 0.
- [ ] `black --check tests/k8s/test_pvc_e2e.py` exits 0.
</verification>

<success_criteria>
- [ ] A `[k8s]` pytest marker is registered in `pytest.ini`.
- [ ] `tests/k8s/test_pvc_e2e.py` is committed with a `kind`-cluster skip and a stub test body.
- [ ] The test either skips transparently or passes a no-op check when a kind cluster exists.
- [ ] The v0.6.0 deferral rationale is present in the test body.
</success_criteria>

<output>
After completion, create `.planning/phases/35-advanced-tech-debt/35-03-SUMMARY.md`
following the project summary template. Cite:
- The new `tests/k8s/` files.
- The `pytest.ini` marker addition.
- The observed local skip/pass status.
- The TODO text for the v0.6.0 implementation.
</output>
