---
phase: 35-advanced-tech-debt
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/research/organ-mesh-licensing.md
  - .planning/STATE.md
autonomous: true
requirements:
  - DEBT-06
must_haves:
  truths:
    - "`docs/research/organ-mesh-licensing.md` exists and documents candidate sources (surgtoolloc/SARAS, MakeHuman, BodyParts3D) with license constraints and a v0.6.0 deferral recommendation"
    - "`.planning/STATE.md` records that the organ-mesh licensing question is researched and deferred to v0.6.0"
  artifacts:
    - path: docs/research/organ-mesh-licensing.md
      provides: "Organ mesh licensing research spike"
      contains: "surgtoolloc|MakeHuman|BodyParts3D|v0\.6\.0"
  key_links: []
---

<objective>
Complete the organ mesh licensing research spike that v0.4.0 identified as a
blocker for real surgical assets. This plan produces a documented decision:
survey at least three candidate sources, compare their licenses, and recommend
deferring asset procurement/generation to v0.6.0. The deliverable is a research
markdown file and an updated `STATE.md` deferred-items table.

Purpose: Realistic organ meshes are out of scope for v0.5.0, but the project
needs a written record of the licensing landscape so v0.6.0 can make an informed
choice between permissive datasets, CC0 proxies, procedural generation, or
attribution-required assets.

Output: `docs/research/organ-mesh-licensing.md` plus an updated `STATE.md` row.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/35-advanced-tech-debt/35-CONTEXT.md
@.planning/phases/35-advanced-tech-debt/35-RESEARCH.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task id="1">
  <name>Create research document directory and file</name>
  <read_first>
    - .planning/STATE.md (deferred items related to organ meshes)
    - .planning/research/ARCHITECTURE-v0.5.0.md (if organ-mesh references exist)
  </read_first>
  <action>
    Create directory `docs/research/` if it does not exist. Add
    `docs/research/organ-mesh-licensing.md` with the following sections:
    - `# Organ Mesh Licensing Research Spike`
    - `## Objective` — purpose and scope.
    - `## Candidates` — subsections for at least:
      1. **SurgToolLoc / SARAS** — academic/research license, likely
         non-commercial, redistribution restrictions.
      2. **MakeHuman** — CC0 exported assets, AGPLv3 application; good for
         body-shell proxies, internal organs need extra modeling.
      3. **BodyParts3D / Anatomography** — CC BY 2.1 JP, requires attribution,
         high-quality segmented organs, conversion needed.
    - `## License Comparison` — a markdown table with columns: Source, License,
      Commercial OK, Redistribute OK, Attribution, Organs Quality.
    - `## Recommendation` — defer asset decision to v0.6.0; keep procedural
      fallback as default; evaluate procedural generation vs. MakeHuman CC0
      vs. BodyParts3D-with-attribution in the next milestone.
    - `## Action Items` — v0.5.0: no assets added, only this doc; v0.6.0:
      re-open decision.
    Keep all content factual and cite license names; do not copy full license
    text.
  </action>
  <verify>
    <automated>test -f docs/research/organ-mesh-licensing.md && grep -c "## " docs/research/organ-mesh-licensing.md | grep -qE "^[5-9]$" && echo "doc structure OK"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/research/organ-mesh-licensing.md` exists.
    - It contains subsections for SurgToolLoc/SARAS, MakeHuman, and BodyParts3D.
    - It contains a license comparison table.
    - It explicitly recommends deferring the asset decision to v0.6.0.
  </acceptance_criteria>
  <done>
    The research document is on disk with the required sections and candidates.
  </done>
</task>

<task id="2">
  <name>Update STATE.md deferred-items table</name>
  <read_first>
    - .planning/STATE.md
    - docs/research/organ-mesh-licensing.md
  </read_first>
  <action>
    In `.planning/STATE.md`, locate the Deferred Items table row for
    "Assets | Organ mesh source licensing (surgtoolloc or procedural)". Update
    the `Status` column to "Researched; deferred to v0.6.0". If the row does
    not exist, append a new row with `Category=Assets`, `Item=Organ mesh
    source licensing research spike`, `Status=Researched; deferred to v0.6.0`,
    `Deferred At=v0.4.0`.
  </action>
  <verify>
    <automated>grep -q "Organ mesh source licensing" .planning/STATE.md && grep -q "Researched; deferred to v0.6.0" .planning/STATE.md && echo "state updated"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` contains an updated row for the organ-mesh licensing
      item with status "Researched; deferred to v0.6.0".
  </acceptance_criteria>
  <done>
    The project state records the research outcome and v0.6.0 deferral.
  </done>
</task>

<task id="3">
  <name>Validate document rendering</name>
  <read_first>
    - docs/research/organ-mesh-licensing.md
  </read_first>
  <action>
    Run a lightweight markdown sanity check: ensure the file parses as markdown
    (no broken tables, balanced headers). If `markdown-it-py` is installed,
    optionally render to plain text and verify it contains the candidate names
    and recommendation. Otherwise, eyeball the file for structure.
  </action>
  <verify>
    <automated>grep -E "^\| Source \|" docs/research/organ-mesh-licensing.md && grep -E "SurgToolLoc|MakeHuman|BodyParts3D" docs/research/organ-mesh-licensing.md && echo "render check OK"</automated>
  </verify>
  <acceptance_criteria>
    - The document contains a markdown table with a `Source` header.
    - The document mentions all three candidate names in body text or table.
  </acceptance_criteria>
  <done>
    Research doc is structurally sound and mentions all required candidates.
  </done>
</task>

</tasks>

<verification>
- [ ] `docs/research/organ-mesh-licensing.md` exists.
- [ ] The doc covers SurgToolLoc/SARAS, MakeHuman, and BodyParts3D.
- [ ] The doc contains a license comparison table.
- [ ] The doc recommends deferring to v0.6.0.
- [ ] `.planning/STATE.md` records the research outcome.
- [ ] No new lint/type issues are introduced (docs only).
</verification>

<success_criteria>
- [ ] `docs/research/organ-mesh-licensing.md` documents candidate sources and license constraints.
- [ ] The document defers the license/asset decision to v0.6.0.
- [ ] `.planning/STATE.md` reflects the research outcome.
- [ ] No procedural generation or asset import work is started in v0.5.0.
</success_criteria>

<output>
After completion, create `.planning/phases/35-advanced-tech-debt/35-04-SUMMARY.md`
following the project summary template. Cite:
- The path to the research document.
- The three candidate sources and the chosen recommendation.
- The `STATE.md` row that was updated.
</output>
