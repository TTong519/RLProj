---
phase: 35-advanced-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/test_suturing_hard_env_construction.py
autonomous: true
requirements:
  - DEBT-06
must_haves:
  truths:
    - "`tests/integration/test_suturing_hard_env_construction.py` exists and loads `tests/fixtures/scenes/suturing_difficulty_hard.json` into a `SurgicalEnv`, calls `reset()`, calls `step()` with a sampled action, and asserts no exception is raised"
    - "The test is marked with `@pytest.mark.integration` and `@pytest.mark.slow`, and skips cleanly when MuJoCo is unavailable"
  artifacts:
    - path: tests/integration/test_suturing_hard_env_construction.py
      provides: "End-to-end integration test for HARD-fixture env construction"
      contains: "suturing_difficulty_hard\.json|SurgicalEnv|SurgicalEnvConfig|reset\(|step\(|pytest\.mark\.integration"
  key_links: []
---

<objective>
Close DEBT-06 and Phase 29 code-review finding WR-02 by adding an end-to-end
integration test that exercises the complete chain from the HARD-fixture
suturing scene JSON through `SurgicalEnv` construction, reset, and a single
simulation step. This proves that the wiring from `SceneLoader` lazy import to
reward construction to simulator load is functional for the hardest difficulty
preset shipped in v0.4.2.

Purpose: The HARD fixture existed in v0.4.2 but was only tested at the
`SceneLoader.load()` layer. WR-02 identified that no test actually constructed a
`SurgicalEnv` from it. This plan closes that coverage gap.

Output: One new integration test file and a green run of the targeted test
command.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/35-advanced-tech-debt/35-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/29-task-02-3-difficulty-levels/29-REVIEW.md
@AGENTS.md
</context>

<tasks>

<task id="1">
  <name>Create integration test directory and stub</name>
  <read_first>
    - tests/test_difficulty_levels.py (existing difficulty tests for style)
    - tests/conftest.py (shared fixtures, skip helpers)
    - tests/fixtures/scenes/suturing_difficulty_hard.json
    - src/surg_rl/rl/environment.py
    - AGENTS.md
  </read_first>
  <action>
    Create directory `tests/integration/` if it does not exist. Add a new file
    `tests/integration/test_suturing_hard_env_construction.py` containing:
    - Module docstring referencing DEBT-06 / WR-02.
    - Imports: `pytest`, `numpy as np`, `SurgicalEnv`, `SurgicalEnvConfig`,
      `Path`.
    - A `@pytest.mark.integration` + `@pytest.mark.slow` decorated test function
      named `test_hard_fixture_env_constructs_reset_and_steps`.
    - Inside the test, resolve the fixture path relative to `__file__`:
      `Path(__file__).resolve().parents[2] / "tests" / "fixtures" / "scenes" / "suturing_difficulty_hard.json"`.
    - Construct `SurgicalEnv(SurgicalEnvConfig(scene_path=str(fixture),
      simulator_type="mujoco", use_curriculum=False,
      use_adaptive_difficulty=False, render_mode=None))` inside a `try` block.
    - Call `env.reset()` and unpack the return values.
    - Sample an action from `env.action_space` and call `env.step(action)`.
    - Assert that `obs` is not None and that `step()` returns a 5-tuple.
    - Ensure `env.close()` is called in a `finally` block.
    - Add a skip guard at module level or via fixture that catches
      `ImportError` for `mujoco` and `pytest.skip`s with reason
      "MuJoCo not available".
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/integration/test_suturing_hard_env_construction.py -v --timeout=120 -m integration || echo "Test may skip if MuJoCo unavailable; check output for skip reason"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/__init__.py` exists (empty package marker).
    - `tests/integration/test_suturing_hard_env_construction.py` exists.
    - The test function is decorated with both `@pytest.mark.integration` and `@pytest.mark.slow`.
    - The test loads `tests/fixtures/scenes/suturing_difficulty_hard.json` by absolute path.
    - The test constructs `SurgicalEnv`, calls `reset()`, calls `step()` with a sampled action, and asserts no exception.
    - `env.close()` is guaranteed via `try/finally`.
    - MuJoCo-import failures are converted to `pytest.skip`, not a hard test failure.
  </acceptance_criteria>
  <done>
    Integration test stub is on disk and imports cleanly; the test either passes
    or skips for the documented reason when run in isolation.
  </done>
</task>

<task id="2">
  <name>Run targeted verification</name>
  <read_first>
    - tests/integration/test_suturing_hard_env_construction.py
    - pytest.ini
  </read_first>
  <action>
    Run the targeted integration test command:
    `PYTHONPATH=src python -m pytest tests/integration/test_suturing_hard_env_construction.py -v --timeout=120`
    If MuJoCo is unavailable locally, confirm the skip path is exercised and
    record the skip reason. If the test fails with a simulator/asset error,
    file the error message as a blocker and do not commit a broken test; instead
    add a narrower construction-only assertion or a skip guard for the
    specific failure mode.
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/integration/test_suturing_hard_env_construction.py -v --timeout=120 -m "integration"</automated>
  </verify>
  <acceptance_criteria>
    - The test exits with `passed` or `skipped` (no `failed` or `error`).
    - If skipped, the reason mentions MuJoCo availability.
    - `pytest.ini` still contains the `integration` and `slow` marker definitions.
  </acceptance_criteria>
  <done>
    Targeted test command runs cleanly and the test either passes on the local
    MuJoCo backend or skips transparently.
  </done>
</task>

<task id="3">
  <name>Update STATE.md to close WR-02</name>
  <read_first>
    - .planning/STATE.md
    - tests/integration/test_suturing_hard_env_construction.py
  </read_first>
  <action>
    In `.planning/STATE.md`, locate the Deferred Items table rows for Phase 29
    WR-02 ("End-to-end SurgicalEnv-construction integration test for HARD
    fixture scene"). Update the `Status` column for that row to
    "Closed in v0.5.0". If a short footnote is helpful, add a comment that the
    test lives at `tests/integration/test_suturing_hard_env_construction.py`.
  </action>
  <verify>
    <automated>grep -q "Closed in v0.5.0" .planning/STATE.md && grep -q "test_suturing_hard_env_construction" .planning/STATE.md && echo "WR-02 state updated"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` contains a row with WR-02 text and status
      "Closed in v0.5.0".
    - The test file path is referenced in or near that row.
  </acceptance_criteria>
  <done>
    The deferred Phase 29 WR-02 item is explicitly marked closed in project state.
  </done>
</task>

</tasks>

<verification>
- [ ] `tests/integration/__init__.py` exists.
- [ ] `tests/integration/test_suturing_hard_env_construction.py` imports cleanly.
- [ ] `PYTHONPATH=src pytest tests/integration/test_suturing_hard_env_construction.py -v --timeout=120` reports `passed` or `skipped` (0 failures).
- [ ] The test file contains the fixture path, `SurgicalEnv`, `reset()`, `step()`, and both `integration`/`slow` markers.
- [ ] `ruff check tests/integration/test_suturing_hard_env_construction.py` exits 0.
- [ ] `black --check tests/integration/test_suturing_hard_env_construction.py` exits 0.
- [ ] `.planning/STATE.md` marks WR-02 as Closed in v0.5.0.
- [ ] `git diff --stat` shows only the expected new files plus `STATE.md`.
</verification>

<success_criteria>
- [ ] DEBT-06 closed: an integration test loads the HARD fixture, constructs `SurgicalEnv`, resets, steps, and asserts no exception.
- [ ] WR-02 explicitly marked "Closed in v0.5.0" in `STATE.md`.
- [ ] The test is properly marked and skips cleanly when MuJoCo is unavailable.
- [ ] No regression in the existing test suite.
</success_criteria>

<output>
After completion, create `.planning/phases/35-advanced-tech-debt/35-01-SUMMARY.md`
following the project summary template. Cite:
- The exact fixture path used.
- Whether the test passed or skipped locally and the skip reason if applicable.
- The new files created.
- The STATE.md row that was updated.
</output>
