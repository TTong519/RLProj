---
phase: 40-real-dreamerv3-integration-sentinel-flip
plan: 02
type: tdd
wave: 2
depends_on:
  - 40-01
files_modified:
  - src/surg_rl/dreamer/subprocess.py
  - tests/dreamer/test_dreamerv3_subprocess_e2e.py
autonomous: false
requirements:
  - DMV3-07
  - DMV3-09
  - DMV3-10
user_setup: []

must_haves:
  truths:
    # DMV3-07 (the other 4 stubs)
    - "_train_loop drives a manual embodied.Driver + agent.train() batch loop (NOT embodied.run.train) and yields METRICS per batch via the _JsonStdout pipe (D-07)"
    - "_evaluate runs agent.policy rollouts and returns a dict matching the existing EVAL handler's expected shape (D-08 — handler unchanged)"
    - "_save_checkpoint delegates to cp.save() and _load_checkpoint delegates to cp.load() on the bundle's embodied.Checkpoint (D-09)"
    - "The embodied logger writes to stderr (NOT stdout) so the JSON-over-stdio pipe stays clean (SC#5 / D-logger-stderr)"
    # DMV3-09 (sentinel flip half)
    - "tests/dreamer/test_dreamerv3_subprocess_e2e.py no longer contains the negative stub-reality pytest.raises blocks; the two test methods assert positive real-agent completion (training runs, checkpoint.ckpt exists, METRICS received)"
    - "The module-level pytestmark skipif stays (GPU + dreamerv3 + jax gate per INV-8); on macOS the flipped tests SKIP not ERROR"
    # DMV3-10 (structural-only assertions live in the flipped test)
    - "The flipped E2E test asserts structural properties only: loss is finite (not NaN/inf), loss is non-explosive (last <= first * tolerance), checkpoint.ckpt exists, training completes — NO convergence threshold like MSE<0.01"
  artifacts:
    - "src/surg_rl/dreamer/subprocess.py _train_loop / _evaluate / _save_checkpoint / _load_checkpoint real implementations"
    - "tests/dreamer/test_dreamerv3_subprocess_e2e.py flipped negative→positive (2 test methods renamed + assertions changed)"
  key_links:
    - "_run_subprocess_loop TRAIN handler (subprocess.py:86-94) → _train_loop(bundle, total_steps, eval_every) → yields METRICS → parent train() generator"
    - "_run_subprocess_loop EVAL handler (subprocess.py:96-103) → _evaluate(bundle, checkpoint, n_episodes) → EVAL_RESULT"
    - "_run_subprocess_loop CHECKPOINT handler (subprocess.py:105-113) → _save_checkpoint / _load_checkpoint → cp.save() / cp.load()"
    - "Flipped E2E test → run_dreamer_training(task='suturing', obs_type='state', total_steps=1000, eval_every=500) → asserts positive completion + structural properties"
  prohibitions:
    - "DO NOT call embodied.run.train as the primary training path (D-07 — it double-checkpoints); it is a documented fallback only if the manual Driver loop proves insufficient"
    - "DO NOT change the EVAL handler's expected return shape (D-08 — _evaluate must match it, not vice versa)"
    - "DO NOT add convergence thresholds (MSE<0.01, reward_mae<0.5) to the flipped test — DMV3-10 explicitly excludes them"
    - "DO NOT delete the module-level pytestmark skipif (line 42) — the flipped runtime tests stay GPU-gated"
    - "DO NOT modify _JsonStdout, DreamerSubprocess, _subprocess_main, _run_subprocess_loop message dispatch, XLA env vars (SC#1)"
    - "DO NOT add module-top imports of jax/dreamerv3/embodied in subprocess.py (SC#5 — keep inside function bodies)"
    - "DO NOT modify the test_dreamerv3_dreamer_color_constant test (line 55-59 — unchanged per D-10/RESEARCH)"
    - "DO NOT touch _build_agent (owned by 40-01) or training.py glob (owned by 40-03)"
    - "DO NOT use embodied.wrappers.FromGym (D-05)"
---

<objective>
Real training loop, evaluation, checkpoint save/load + the Phase 30 sentinel flip (negative→positive).

Purpose: Complete the 5-stub replacement (DMV3-07) by implementing the 4 remaining stubs that
40-01 did not touch, and invert the Phase 30 E2E test from a negative stub assertion
(RuntimeError "Agent not configured") to a positive real-agent completion assertion with
structural-only properties (DMV3-09 + DMV3-10). The manual Driver + agent.train() loop owns
the JSON pipe directly (D-07 — no embodied.run.train double-checkpointing). The embodied
logger is redirected to stderr so stdout stays clean for the JSON pipe (SC#5).

This plan is autonomous:false because the GREEN verification (positive assertions pass with
a real agent) requires a GPU + dreamerv3 + jax — the CI GPU job in 40-04 satisfies GREEN on
the merge-to-main runner. Locally on macOS the flipped tests SKIP per INV-8 (the RED scaffold
verifies the skipif fires on CPU, not that the positive assertions pass).

Output: Real `_train_loop` / `_evaluate` / `_save_checkpoint` / `_load_checkpoint` in
`src/surg_rl/dreamer/subprocess.py`, flipped `tests/dreamer/test_dreamerv3_subprocess_e2e.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-real-dreamerv3-integration-sentinel-flip/40-CONTEXT.md
@.planning/phases/40-real-dreamerv3-integration-sentinel-flip/40-RESEARCH.md
@.planning/phases/40-real-dreamerv3-integration-sentinel-flip/40-AI-SPEC.md
@.planning/phases/40-real-dreamerv3-integration-sentinel-flip/40-01-PLAN.md
@src/surg_rl/dreamer/subprocess.py
@src/surg_rl/dreamer/wrapper.py
@src/surg_rl/dreamer/training.py
@tests/dreamer/test_dreamerv3_subprocess_e2e.py
@tests/dreamer/test_dreamerv3_regression_guard.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED scaffold): Flip the Phase 30 sentinel E2E test negative→positive</name>
  <files>tests/dreamer/test_dreamerv3_subprocess_e2e.py</files>
  <read_first>
    - tests/dreamer/test_dreamerv3_subprocess_e2e.py (the FULL current file — lines 42-49 skipif, lines 55-59 color constant test which stays unchanged, lines 61-79 the first negative assertion block to flip, lines 81-104 the second negative assertion block + final.pt negative checks to flip)
    - 40-RESEARCH.md "Sentinel Inversion Design for DMV3-09" section (the exact test-method-by-test-method flip table; the renaming map)
    - 40-CONTEXT.md D-10 (the two pytest.raises blocks ~lines 72, 91 and the final.pt NOT-written negative assertion ~lines 99-101 become positive; the module-level pytestmark skipif line 42 STAYS)
    - 40-RESEARCH.md "CI GPU Smoke Test Design for DMV3-10" section (the structural-only assertions table: finite loss, non-explosive loss, checkpoint exists, training completes, METRICS received)
    - src/surg_rl/dreamer/training.py (run_dreamer_training lines 199-359 — what the flipped test calls; the metrics_log shape with 'training_curves' + 'eval_results' keys; the checkpoint_path / final_checkpoint conventions)
  </read_first>
  <behavior>
    - The module-level pytestmark skipif (line 42-49) is UNCHANGED — on macOS the module SKIPS with the existing remediation message.
    - test_e2e_dreamer_color_constant (lines 55-59) is UNCHANGED.
    - test_e2e_run_dreamer_training_against_stub (lines 61-79) is RENAMED to test_e2e_run_dreamer_training_real_agent and its body flips: the pytest.raises(RuntimeError, match=...) block is replaced by a positive call — metrics = run_dreamer_training(...); assert metrics is not None; assert 'training_curves' in metrics; assert 'eval_results' in metrics; assert at least one METRICS step was collected (len(metrics['training_curves']['total_loss']) > 0); assert every total_loss value is finite (not NaN, not inf); assert the last total_loss <= first total_loss * tolerance (tolerance=2.0 — non-explosive, NOT a convergence threshold).
    - test_e2e_checkpoint_files_not_written_in_stub_state (lines 81-104) is RENAMED to test_e2e_checkpoint_files_written and its body flips: the pytest.raises block is replaced by a positive call — run_dreamer_training(...); assert (ckpt_dir / 'checkpoint.ckpt').exists() OR (ckpt_dir / 'final.pt').exists() (the .pt may still appear via training.py's final_checkpoint line 333 — 40-03 retires that; assert training_metrics.json exists (training.py line 345 writes it); remove the two 'assert not ... .exists()' negative checks.
    - No convergence threshold (MSE<0.01 / reward_mae<0.5) appears anywhere in the flipped file.
  </behavior>
  <action>
    Edit tests/dreamer/test_dreamerv3_subprocess_e2e.py. KEEP the module-level pytestmark
    skipif block (lines 42-49) and the _has_module / _gpu_available helpers (lines 16-39) and
    the color-constant test (lines 55-59) byte-identical — these are the INV-8 gate and the
    unchanged constant test. RENAME test_e2e_run_dreamer_training_against_stub to
    test_e2e_run_dreamer_training_real_agent and replace the negative pytest.raises block
    (the with-context that expects the stub-era RuntimeError) with a positive call:
    metrics = run_dreamer_training(task='suturing', obs_type='state', total_steps=1000,
    eval_every=500, checkpoint_dir=str(tmp_path / 'checkpoints')). Then assert metrics is not
    None, 'training_curves' in metrics, 'eval_results' in metrics, the total_loss list is
    non-empty, every entry in metrics['training_curves']['total_loss'] is finite (use
    math.isfinite and reject math.isnan / math.isinf), and the last total_loss <= first
    total_loss * 2.0 (non-explosive structural bound — NOT a convergence threshold). Add a
    brief docstring noting this is the DMV3-09 sentinel flip (negative→positive) and that
    structural-only assertions are per DMV3-10.

    RENAME test_e2e_checkpoint_files_not_written_in_stub_state to
    test_e2e_checkpoint_files_written and replace the negative pytest.raises block with a
    positive call: run_dreamer_training(task='suturing', obs_type='state', total_steps=1000,
    eval_every=500, checkpoint_dir=str(ckpt_dir)). Then assert at least one of
    (ckpt_dir / 'checkpoint.ckpt') or (ckpt_dir / 'final.pt') exists (the .pt path comes from
    training.py line 333 and is retired by 40-03; the .ckpt path is the embodied.Checkpoint
    native format from 40-01's _build_agent). Assert (ckpt_dir / 'training_metrics.json')
    .exists() (training.py line 345 writes it on completion). REMOVE the two negative
    'assert not ... .exists()' lines that currently guard the stub-reality invariant.

    Do NOT add or remove any import of pytest (already imported). Do NOT touch the skipif or
    the color test. Do NOT add any convergence-threshold constant (no MSE<0.01, no
    DEFAULT_THRESHOLDS reference). The flipped file must still parse and collect under pytest.

    Run pytest --collect-only tests/dreamer/test_dreamerv3_subprocess_e2e.py to confirm the
    renamed tests collect. Run pytest tests/dreamer/test_dreamerv3_subprocess_e2e.py -v on
    macOS to confirm the module SKIPs cleanly (skipif fires — RED scaffold on CPU = SKIP not
    ERROR, per the TDD contribution note). Commit with
    test(40-02): flip Phase 30 sentinel negative→positive (RED scaffold).
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest --collect-only tests/dreamer/test_dreamerv3_subprocess_e2e.py 2>&1 | grep -c 'test_e2e_run_dreamer_training_real_agent\|test_e2e_checkpoint_files_written' && PYTHONPATH=src pytest tests/dreamer/test_dreamerv3_subprocess_e2e.py -v -rs 2>&1 | grep -c 'skipped'</automated>
  </verify>
  <acceptance_criteria>
    - tests/dreamer/test_dreamerv3_subprocess_e2e.py contains no pytest.raises block matching the stub-era RuntimeError sentinel (grep -v '^#' file | grep -c 'pytest.raises(RuntimeError' == 0)
    - the file contains the renamed test functions test_e2e_run_dreamer_training_real_agent and test_e2e_checkpoint_files_written (pytest --collect-only shows both)
    - the file still contains the module-level pytestmark skipif (grep -v '^#' file | grep -c 'pytest.mark.skipif' == 1) and the color-constant test (grep -c 'test_e2e_dreamer_color_constant' == 1)
    - the file contains no convergence-threshold literal (grep -c 'MSE<0.01\|reconstruction_mse.*0.01\|DEFAULT_THRESHOLDS' file == 0)
    - the flipped test body asserts math.isfinite on total_loss entries (grep -c 'math.isfinite' file >= 1)
    - PYTHONPATH=src pytest tests/dreamer/test_dreamerv3_subprocess_e2e.py -v -rs on macOS shows SKIPPED (not ERROR, not FAILED) — the skipif fires cleanly
  </acceptance_criteria>
  <done>
    The two Phase 30 sentinel test methods are renamed and flipped to positive real-agent
    assertions with structural-only properties (finite + non-explosive loss, checkpoint
    exists, training completes). The skipif and color test are unchanged. The module SKIPs
    cleanly on macOS. RED scaffold committed (GREEN requires the real train/eval/save/load
    impls from Task 2 + a GPU host).
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Real _train_loop / _evaluate / _save_checkpoint / _load_checkpoint</name>
  <files>src/surg_rl/dreamer/subprocess.py</files>
  <read_first>
    - src/surg_rl/dreamer/subprocess.py (the 4 stubs at lines 132-149; the dispatch that calls them at lines 86-113; the bundle returned by 40-01's _build_agent carrying agent/env/checkpoint/replay/step)
    - 40-01-PLAN.md must_haves.truths + the bundle shape (agent + env + checkpoint + replay + step keys)
    - 40-AI-SPEC.md Section 4 "Core Pattern (manual embodied.Driver + agent.train() batch loop — D-07)" — the loop structure: Driver warmup → agent.init_train(batch_size) → while step < total_steps: data = next(replay.dataset(...)); carry, metrics = agent.train(carry, data); step.increment(...); yield METRICS; cp.save() at step % eval_every == 0
    - 40-RESEARCH.md "PyPI 1.5.0 embodied.run.train signature" + "Checkpoint API" (cp.save() / cp.load(); agent.policy for rollouts; agent.init_train / init_policy recurrent state)
    - 40-CONTEXT.md D-07 (manual Driver loop, owns JSON pipe, no embodied.run.train), D-08 (_evaluate return shape matches existing EVAL handler — do not change handler), D-logger-stderr (logger→stderr not stdout)
    - src/surg_rl/dreamer/training.py run_dreamer_training (lines 296-348 — the parent loop that consumes METRICS, calls save_checkpoint at step % eval_every, and expects 'step' / 'reconstruction_loss' / 'reward_loss' / 'total_loss' keys in each METRICS dict)
    - tests/dreamer/test_dreamerv3_subprocess_e2e.py (the flipped test from Task 1 — the GREEN must make the positive assertions pass on a GPU host)
  </read_first>
  <behavior>
    - _train_loop(agent_bundle, total_steps, eval_every) yields METRICS dicts with keys step / reconstruction_loss / reward_loss / total_loss (matching what run_dreamer_training's parent loop reads at lines 299-302) per training batch, and calls cp.save() at step % eval_every == 0 for DMV3-08 persistence.
    - _train_loop does NOT call embodied.run.train (D-07); it drives embodied.Driver + agent.train() manually.
    - _evaluate(agent_bundle, checkpoint, n_episodes) runs agent.policy rollouts over the wrapped env and returns a dict matching the EVAL handler's expected shape (the handler at line 103 wraps it as {"type": "EVAL_RESULT", "metrics": metrics} — return a dict with reconstruction_mse / reward_mae / success_rate keys to match run_dreamer_training's eval_consumption + evaluate_checkpoint's expectations).
    - _save_checkpoint(agent_bundle, path) delegates to agent_bundle['checkpoint'].save() (embodied.Checkpoint.save).
    - _load_checkpoint(agent_bundle, path) delegates to agent_bundle['checkpoint'].load() (embodied.Checkpoint.load).
    - All jax / dreamerv3 / embodied imports are INSIDE the four function bodies (SC#5) — no new module-top imports.
    - The embodied logger is configured to write to sys.stderr (NOT sys.stdout) inside _train_loop before any training step, so the _JsonStdout pipe stays clean.
  </behavior>
  <action>
    Replace the bodies of _train_loop, _evaluate, _save_checkpoint, _load_checkpoint in
    src/surg_rl/dreamer/subprocess.py (currently lines 132-149). Keep their signatures
    unchanged — the dispatch at lines 86-113 calls them positionally with (agent, ...) where
    'agent' is the bundle dict/namespace returned by 40-01's _build_agent. Inside each
    function body, import embodied / dreamerv3 lazily (child-only, SC#5) and unpack the
    bundle: agent = agent_bundle['agent']; env = agent_bundle['env']; cp =
    agent_bundle['checkpoint']; replay = agent_bundle['replay']; step = agent_bundle['step']
    (adjust for dict-vs-namespace per 40-01's chosen shape).

    _train_loop: configure the embodied logger to stderr FIRST (redirect TerminalOutput to
    sys.stderr, or suppress TerminalOutput and rely on JSONL file output + the METRICS pipe —
    VERIFY the exact embodied.Logger API against the installed package per research A1). Then
    construct embodied.Driver(env) and warmup the replay buffer with initial episodes (driver
    on a callable that adds to replay). Initialize the recurrent training state via
    agent.init_train(batch_size) (VERIFY the exact method name). Loop while int(step) <
    total_steps: pull a batch from replay.dataset(...) (VERIFY iterator API), call carry,
    metrics = agent.train(carry, data) (VERIFY return arity), increment step (step.increment
    or step += batch_steps per the Counter API), yield a METRICS dict with keys step
    (int(step)), reconstruction_loss, reward_loss, total_loss (extract from the metrics dict
    returned by agent.train, coercing to float; if agent.train returns keys with different
    names, map them to the three keys run_dreamer_training reads at lines 300-302). On step
    % eval_every == 0, call cp.save() for DMV3-08 persistence. Do NOT call embodied.run.train
    (D-07 — it would double-checkpoint via its internal cadence).

    _evaluate: initialize the policy recurrent state via agent.init_policy() (VERIFY), run
    n_episodes rollouts over env using agent.policy (VERIFY the rollout callable signature —
    typically agent.policy(obs, carry) -> (action, carry)). Collect reconstruction_mse (from
    the world-model forward on observed transitions), reward_mae (predicted-vs-actual reward),
    success_rate (fraction of episodes ending with is_terminal True), and mean_reward /
    mean_episode_length if cheap. Return a dict with at minimum reconstruction_mse, reward_mae,
    success_rate keys (matching what run_dreamer_training's eval consumption and
    evaluate_checkpoint expect at training.py lines 404-411). Do NOT change the EVAL handler
    at line 103 (D-08).

    _save_checkpoint: call cp.save() on agent_bundle's embodied.Checkpoint. The 'path' arg
    from the parent is the parent-side checkpoint path (e.g. models/dreamerv3/{task}_{obs_type}
    /checkpoint_500.pt) — if it differs from the Checkpoint's registered path, either ignore
    it (the Checkpoint already points at the canonical .ckpt path) or re-register the
    Checkpoint at the parent-provided path before save (Claude's discretion — but the .ckpt
    native format is the target per D-09, so prefer the registered path and document the
    mismatch in the summary). _load_checkpoint: call cp.load() (or cp.load_or_save()) on the
    bundle's Checkpoint.

    All four functions: no module-top imports of jax/dreamerv3/embodied. After writing, run
    the regression guard + JAX-leak guard from 40-01 (must still pass). Run the CPU suite
    (GPU-gated E2E tests skip on macOS). The GREEN for the flipped E2E test requires a GPU
    host — note in the summary that GREEN is satisfied by the 40-04 CI dreamer-gpu job at
    merge time. Commit with feat(40-02): real train/eval/save/load (manual Driver loop,
    D-07) + logger→stderr.
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/dreamer/test_dreamerv3_regression_guard.py tests/test_dreamer_subprocess.py::TestProcessIsolationImport -v && PYTHONPATH=src pytest tests/ -m 'not integration' -v -q</automated>
  </verify>
  <acceptance_criteria>
    - src/surg_rl/dreamer/subprocess.py _train_loop contains 'embodied.Driver' and 'agent.train' (manual Driver loop, D-07) and does NOT contain 'embodied.run.train' as a call (grep -v '^#' file | grep -c 'embodied.run.train(' == 0)
    - src/surg_rl/dreamer/subprocess.py _train_loop yields a dict containing the key 'total_loss' (matches run_dreamer_training line 302) and calls cp.save (grep -v '^#' file | grep -c 'cp.save\|checkpoint.save\|\.save()' >= 1)
    - src/surg_rl/dreamer/subprocess.py _evaluate contains 'agent.policy' or 'agent.act' (policy rollouts, D-08) and returns a dict with reconstruction_mse / reward_mae / success_rate keys
    - src/surg_rl/dreamer/subprocess.py _save_checkpoint and _load_checkpoint delegate to the bundle's checkpoint (grep -c 'checkpoint' in those function bodies >= 2)
    - src/surg_rl/dreamer/subprocess.py has NO new module-top 'import jax' / 'import dreamerv3' / 'import embodied' (40-01's JAX-leak guard still passes)
    - src/surg_rl/dreamer/subprocess.py _train_loop configures the logger to stderr (grep -v '^#' file | grep -c 'stderr' >= 1 in _train_loop, OR the summary documents that the installed logger defaults to stderr / was suppressed)
    - PYTHONPATH=src pytest tests/dreamer/test_dreamerv3_regression_guard.py tests/test_dreamer_subprocess.py::TestProcessIsolationImport -v exits 0
    - PYTHONPATH=src pytest tests/ -m 'not integration' -v exits 0 (CPU suite green; GPU-gated dreamer E2E tests skip on macOS)
  </acceptance_criteria>
  <done>
    The 4 remaining stubs are real implementations: _train_loop drives a manual Driver +
    agent.train() loop yielding METRICS per batch + cp.save() at eval_every; _evaluate runs
    agent.policy rollouts; _save_checkpoint / _load_checkpoint delegate to embodied.Checkpoint.
    The embodied logger writes to stderr. All JAX/dreamerv3/embodied imports stay inside
    function bodies. CPU suite green; GPU GREEN for the flipped E2E test is deferred to the
    40-04 CI dreamer-gpu job.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent↔child subprocess pipe | The METRICS / EVAL_RESULT / CHECKPOINT_SAVED messages cross here as JSON. No new boundary — the handlers are unchanged (SC#1). |
| child↔disk (models/dreamerv3/) | cp.save() writes checkpoint.ckpt; cp.load() reads it back on resume. Local disk, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-04 | Tampering | checkpoint.ckpt resume load path | low | mitigate | cp.load() deserializes a pickle of numpy arrays from local disk. Only load from the canonical models/dreamerv3/{task}_{obs_type}/checkpoint.ckpt path registered in _build_agent; do NOT load arbitrary parent-provided paths without re-registering the Checkpoint. Same trust profile as Phase 24/30 (vetted). |
| T-40-05 | Information Disclosure | embodied logger writes to stdout (pipe corruption) | medium | mitigate | Configure logger to sys.stderr (or suppress TerminalOutput) inside _train_loop before any training step — stdout is the _JsonStdout JSON pipe; logger output there corrupts the protocol (Pitfall 4). Acceptance criterion greps for stderr configuration. |
| T-40-06 | Denial of Service | METRICS pipe flood | low | accept | ~100 METRICS messages per 1000 steps (D-03 budget) — negligible pipe traffic; the parent's train() generator drains them synchronously. Not a DoS vector at the smoke budget. |
</threat_model>

<verification>
- Regression guard (CPU, from 40-01): PYTHONPATH=src pytest tests/dreamer/test_dreamerv3_regression_guard.py -v → GREEN
- JAX-leak guard (CPU, from 40-01): PYTHONPATH=src pytest tests/test_dreamer_subprocess.py::TestProcessIsolationImport -v → GREEN
- Flipped E2E test (CPU): PYTHONPATH=src pytest tests/dreamer/test_dreamerv3_subprocess_e2e.py -v -rs → SKIPPED on macOS (skipif fires)
- Full CPU suite: PYTHONPATH=src pytest tests/ -m 'not integration' -v → GREEN
- GPU GREEN (deferred to 40-04 CI job): the flipped E2E test passes on a GPU runner with dreamerv3 + jax[cuda12] installed — asserts finite + non-explosive loss, checkpoint.ckpt exists, training completes.
- Source assertions: _train_loop uses embodied.Driver + agent.train (not embodied.run.train); _evaluate uses agent.policy; _save_checkpoint / _load_checkpoint delegate to the bundle's embodied.Checkpoint; logger → stderr.
</verification>

<success_criteria>
- DMV3-07 (complete): all 5 stubs replaced with real implementations against the PyPI 1.5.0 object API; _train_loop drives a manual Driver loop (D-07); _evaluate matches the EVAL handler (D-08); save/load delegate to embodied.Checkpoint (D-09).
- DMV3-09 (sentinel flip): the two Phase 30 test methods are renamed and flipped to positive real-agent completion assertions; the skipif stays; the regression guard from 40-01 protects against None-valued return regression.
- DMV3-10 (structural-only): the flipped E2E test asserts finite + non-explosive loss + checkpoint exists + training completes — NO MSE<0.01 convergence threshold.
- SC#1 preserved: _JsonStdout / DreamerSubprocess / _subprocess_main / _run_subprocess_loop dispatch / XLA env vars unchanged.
- SC#5 preserved: no module-top jax/dreamerv3/embodied imports; logger → stderr.
- CPU suite green on macOS; GPU GREEN deferred to 40-04 CI job.
</success_criteria>

<output>
Create .planning/phases/40-real-dreamerv3-integration-sentinel-flip/40-02-SUMMARY.md when done.
</output>

## Artifacts this phase produces

### Plan 40-02 (this plan)
| File | Symbol | Kind |
|------|--------|------|
| src/surg_rl/dreamer/subprocess.py | _train_loop (real — manual Driver + agent.train loop, METRICS per batch, cp.save at eval_every) | MODIFIED — stub → real |
| src/surg_rl/dreamer/subprocess.py | _evaluate (real — agent.policy rollouts, returns reconstruction_mse/reward_mae/success_rate) | MODIFIED — stub → real |
| src/surg_rl/dreamer/subprocess.py | _save_checkpoint (real — delegates to bundle checkpoint .save()) | MODIFIED — stub → real |
| src/surg_rl/dreamer/subprocess.py | _load_checkpoint (real — delegates to bundle checkpoint .load()) | MODIFIED — stub → real |
| tests/dreamer/test_dreamerv3_subprocess_e2e.py | test_e2e_run_dreamer_training_real_agent (renamed from _against_stub, flipped negative→positive) | MODIFIED test — sentinel flip |
| tests/dreamer/test_dreamerv3_subprocess_e2e.py | test_e2e_checkpoint_files_written (renamed from _not_written_in_stub_state, flipped negative→positive) | MODIFIED test — sentinel flip |

### Symbols downstream plans should treat as newly-created (not pre-existing API surface)
- The METRICS dict shape yielded by _train_loop: {step, reconstruction_loss, reward_loss, total_loss} — 40-03's resume test reads these via run_dreamer_training's metrics_log.
- The _evaluate return shape: {reconstruction_mse, reward_mae, success_rate, ...} — 40-03's resume test and 40-04's CI smoke consume this.
- The bundle-unpacking convention inside _train_loop / _evaluate / _save_checkpoint / _load_checkpoint (agent/env/checkpoint/replay/step keys) — established by 40-01, consumed here.