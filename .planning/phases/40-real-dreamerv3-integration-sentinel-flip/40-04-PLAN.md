---
phase: 40-real-dreamerv3-integration-sentinel-flip
plan: 04
type: execute
wave: 3
depends_on:
  - 40-02
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - DMV3-10
user_setup:
  - service: github-actions-gpu-runner
    why: "Run the real-agent DreamerV3 smoke test on a GPU host (DMV3-10) — the regression guard + JAX-leak guard run on CPU per PR, but the positive real-agent assertions (flipped E2E test + resume test) require jax[cuda12] + dreamerv3 on a GPU runner."
    dashboard_config:
      - task: "Enable GitHub-hosted GPU Actions runners on the repo account (ubuntu-latest-4-core-gpu or similar). If not enabled, the dreamer-gpu job will be queued/blocked — document the gap in the PR per D-01 and mark DMV3-10 as pending GPU enablement (the tests are added; they just cannot run yet — same as Phase 30's status)."
        location: "GitHub organizational billing settings → Actions → GPU runners"
      - task: "Confirm the dreamer-gpu job's workflow_dispatch trigger is manually runnable from the Actions tab after merge."
        location: "GitHub repo → Actions → CI → Run workflow"

must_haves:
  truths:
    # DMV3-10
    - ".github/workflows/ci.yml contains a dreamer-gpu job that runs on a GitHub-hosted GPU Actions runner (e.g. ubuntu-latest-4-core-gpu) per D-01"
    - "The dreamer-gpu job fires on push to main + release tags + workflow_dispatch (NOT on every PR) per D-02"
    - "The dreamer-gpu job installs jax[cuda12]~=0.4.20 + the [dreamer] extra + optax>=0.1.7, then runs pytest tests/dreamer/ -v per D-01/D-03"
    - "The dreamer-gpu job runs with total_steps=1000 (the smoke budget per D-03, ~3-5 min on a GPU runner) — either via the test default or an env var"
    - "The dreamer-gpu job asserts structural-only properties (the flipped E2E test + resume test from 40-02/40-03 assert finite loss, non-explosive loss, checkpoint.ckpt exists) — NOT the v0.4.0 spike's MSE<0.01 convergence threshold per DMV3-10"
    - "On macOS local runs (no GPU), the dreamer tests SKIP cleanly per INV-8 (the regression guard + JAX-leak guard run on CPU in the existing test matrix; the flipped E2E + resume tests skipif fires)"
  artifacts:
    - ".github/workflows/ci.yml dreamer-gpu job (NEW)"
  key_links:
    - "dreamer-gpu job → pytest tests/dreamer/ -v (runs test_dreamerv3_subprocess_e2e.py flipped positive + test_dreamerv3_checkpoint_resume.py + test_dreamerv3_regression_guard.py)"
    - "dreamer-gpu job → install jax[cuda12]~=0.4.20 BEFORE pip install -e '.[dev,dreamer]' so pip resolves jaxlib against the CUDA build (AI-SPEC Section 3)"
    - "dreamer-gpu job → runs-on: ubuntu-latest-4-core-gpu (GitHub-hosted GPU runner per D-01)"
  prohibitions:
    - "DO NOT use a self-hosted GPU runner or a Docker/CUDA container job (D-01 — keep infra on GitHub-hosted GPU runners)"
    - "DO NOT fire the dreamer-gpu job on every PR (D-02 — merge-to-main + release tags + manual dispatch only; PRs get the CPU-only regression guard)"
    - "DO NOT add convergence-threshold assertions to the CI job (DMV3-10 — structural-only; the tests themselves assert finite/non-explosive loss + checkpoint exists)"
    - "DO NOT change the dreamerv3~=1.5.0 / jax~=0.4.20 / optax>=0.1.7 pins in pyproject.toml (D-04 — UNCHANGED)"
    - "DO NOT modify the existing test / docker-ci / k8s-e2e jobs (additive — a new dreamer-gpu job only)"
    - "DO NOT extend Dockerfile.cuda with [dreamer] + jax[cuda12] (deferred per CONTEXT.md deferred-ideas — the GitHub GPU runner path is the target; Dockerfile.cuda extension is a fallback only if the GitHub GPU runner is unavailable)"
---

<objective>
CI GPU smoke job (DMV3-10).

Purpose: Stand up the `dreamer-gpu` GitHub Actions job that runs the flipped E2E test
(40-02) + the restart-then-continue resume test (40-03) + the regression guard (40-01) on a
GitHub-hosted GPU Actions runner, so the real-agent smoke test actually executes (addressing
the STATE.md blocker's "100%-skipped audit failure" risk). The job fires on merge-to-main +
release tags + manual `workflow_dispatch` (NOT per-PR) to control metered GPU-runner spend
(D-02); PR-time signal comes from the CPU-runnable regression guard + JAX-leak guard in the
existing test matrix. Structural-only assertions (finite + non-explosive loss,
checkpoint.ckpt exists) live in the tests themselves (40-02/40-03) — this job just runs them
with `total_steps=1000` (D-03). macOS local runs skip cleanly per INV-8.

Output: NEW `dreamer-gpu` job in `.github/workflows/ci.yml`. No source/test changes (the
structural assertions are already in the tests from 40-02/40-03).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-real-dreamerv3-integration-sentinel-flip/40-CONTEXT.md
@.planning/phases/40-real-dreamerv3-integration-sentinel-flip/40-RESEARCH.md
@.planning/phases/40-real-dreamerv3-integration-sentinel-flip/40-AI-SPEC.md
@.planning/phases/40-real-dreamerv3-integration-sentinel-flip/40-02-PLAN.md
@.planning/phases/40-real-dreamerv3-integration-sentinel-flip/40-03-PLAN.md
@.github/workflows/ci.yml
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dreamer-gpu job to ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the FULL current file — the on: triggers at lines 3-7 (push branches [main] + pull_request branches [main]); the existing test job matrix at lines 10-111; the docker-ci job at lines 113-178; the k8s-e2e job at lines 180-206; note there is NO existing GPU/dreamer job)
    - pyproject.toml (the [dreamer] extra pins: jax~=0.4.20, optax>=0.1.7, dreamerv3~=1.5.0 — UNCHANGED per D-04; the [dev] extra for pytest/ruff/black/mypy)
    - 40-CONTEXT.md D-01 (GitHub-hosted GPU Actions runner, e.g. ubuntu-latest-4-core-gpu; install jax[cuda12]~=0.4.20 + [dreamer] + optax>=0.1.7), D-02 (merge-to-main + release tags + workflow_dispatch, NOT per-PR), D-03 (total_steps=1000, eval_every=500, ~3-5 min)
    - 40-AI-SPEC.md Section 3 "Installation" (GPU host install order: pip install "jax[cuda12]==0.4.20" BEFORE pip install -e ".[dev,dreamer]" so pip resolves jaxlib against the CUDA build, not the CPU default)
    - 40-02-PLAN.md + 40-03-PLAN.md (the flipped E2E test + resume test — the structural-only assertions they make; the tests run with total_steps=1000 via the run_dreamer_training default or their explicit call args)
  </read_first>
  <!-- planner-discipline-allow: pull_request -->
  <action>
    Edit .github/workflows/ci.yml. Add a NEW dreamer-gpu job as a top-level job key
    (sibling of test / docker-ci / k8s-e2e). The job:

    - name: DreamerV3 GPU smoke
    - runs-on: ubuntu-latest-4-core-gpu (GitHub-hosted GPU Actions runner per D-01; if the
      exact label differs on the account, use the available GitHub GPU runner label —
      document the chosen label in the summary)
    - timeout-minutes: 15 (the smoke budget is ~3-5 min per D-03; 15 min gives headroom for
      jax/dreamerv3 install + first-time JIT compilation)
    - The job's trigger gate: the job should only run on merge-to-main + release tags +
      manual dispatch, NOT on every PR (D-02). Implement this with an if: condition on the
      job — e.g. if: (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
      startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'. The
      top-level on: block must be extended to add workflow_dispatch: (with an empty or named
      input) and to keep the existing push.branches:[main] + pull_request.branches:[main]
      triggers (the PR trigger stays for the CPU test job; the dreamer-gpu job's if: gate
      excludes PRs). Add tags: ['v*'] to the push trigger so release tags fire the GPU job.
    - steps:
      1. actions/checkout@v4
      2. actions/setup-python@v5 with python-version: "3.11" (match the existing test
         matrix's primary Python)
      3. Cache pip dependencies (actions/cache@v4) keyed on pyproject.toml + runner.os, same
         pattern as the existing test job lines 35-41.
      4. Install CUDA-enabled jax FIRST: pip install --upgrade pip && pip install
         "jax[cuda12]~=0.4.20" (the ~=0.4.20 line matches the pyproject pin; install BEFORE
         the project extra so pip resolves jaxlib against the CUDA build per AI-SPEC Section 3).
      5. Install the project with the dreamer + dev extras: pip install -e ".[dev,dreamer]"
         (this installs dreamerv3~=1.5.0 + optax>=0.1.7 + the dev tooling; jax is already
         installed with CUDA from step 4 so pip keeps the CUDA build).
      6. Run the dreamer smoke tests: pytest tests/dreamer/ -v -rs
         (the -rs flag shows skip reasons; on the GPU runner the flipped E2E test +
         resume test RUN (not skip); the regression guard also runs. The tests use
         total_steps=1000 / eval_every=500 per their own call args per D-03 — no env var
         needed unless a test reads one, in which case set DREAMER_TOTAL_STEPS=1000 and
         DREAMER_EVAL_EVERY=500 as env: on the step.)
      7. (Optional) Upload the models/dreamerv3/ checkpoint artifact on failure for
         debugging — actions/upload-artifact@v4 with path: models/dreamerv3/ if-no-files-
         found: ignore. (Claude's discretion — useful for post-mortem but not required.)

    Do NOT modify the existing test / docker-ci / k8s-e2e jobs (additive). Do NOT change
    the pyproject pins (D-04). Do NOT add a Dockerfile.cuda extension (deferred per CONTEXT).
    Do NOT add convergence-threshold assertions to the job (the tests assert structural-only
    properties themselves). After editing, validate the YAML syntax with python -c 'import
    yaml; yaml.safe_load(open(".github/workflows/ci.yml"))'. Commit with
    ci(40-04): add dreamer-gpu job for DMV3-10 smoke (merge-to-main + manual dispatch).
  </action>
  <verify>
    <automated>python -c "import yaml; y=yaml.safe_load(open('.github/workflows/ci.yml')); assert 'dreamer-gpu' in y['jobs'], 'no dreamer-gpu job'; j=y['jobs']['dreamer-gpu']; assert 'gpu' in j['runs-on'], 'not a GPU runner'; assert 'workflow_dispatch' in y[True] if True in y else 'workflow_dispatch' in y.get('on',{}), 'no workflow_dispatch trigger'; print('ok')" && PYTHONPATH=src pytest tests/dreamer/test_dreamerv3_regression_guard.py tests/test_dreamer_subprocess.py::TestProcessIsolationImport -v</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml parses as valid YAML (python -c 'import yaml; yaml.safe_load(...)' succeeds)
    - the jobs map contains a 'dreamer-gpu' key (grep -c 'dreamer-gpu' .github/workflows/ci.yml >= 1)
    - the dreamer-gpu job runs-on contains 'gpu' (grep -A2 'dreamer-gpu' .github/workflows/ci.yml | grep -c 'gpu' >= 1)
    - the top-level on: block contains workflow_dispatch (grep -c 'workflow_dispatch' .github/workflows/ci.yml >= 1)
    - the dreamer-gpu job has an if: gate that excludes pull_request events (the condition references github.event_name and excludes 'pull_request' — grep -c 'pull_request' on the dreamer-gpu job block == 0, OR the if: condition explicitly checks for push/tags/workflow_dispatch)
    - the dreamer-gpu job install step contains 'jax[cuda12]' and '[dev,dreamer]' (grep -c 'jax\[cuda12\]' .github/workflows/ci.yml >= 1 and grep -c 'dev,dreamer' .github/workflows/ci.yml >= 1)
    - the dreamer-gpu job runs pytest tests/dreamer/ (grep -c 'pytest tests/dreamer' .github/workflows/ci.yml >= 1)
    - the dreamer-gpu job has timeout-minutes set (grep -c 'timeout-minutes' .github/workflows/ci.yml >= 1)
    - PYTHONPATH=src pytest tests/dreamer/test_dreamerv3_regression_guard.py tests/test_dreamer_subprocess.py::TestProcessIsolationImport -v exits 0 (CPU guards still green — the CI job does not affect them)
    - the existing test / docker-ci / k8s-e2e jobs are unchanged (git diff shows only an additive dreamer-gpu job + the on: trigger extension)
  </acceptance_criteria>
  <done>
    The dreamer-gpu job exists in ci.yml, runs on a GitHub-hosted GPU runner, fires on
    merge-to-main + release tags + workflow_dispatch (NOT per-PR), installs jax[cuda12] +
    [dev,dreamer], runs pytest tests/dreamer/ -v with the structural-only smoke assertions
    from 40-02/40-03. YAML valid; CPU guards still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner ↔ PyPI | pip install jax[cuda12] + dreamerv3 + optax from PyPI (package legitimacy vetted in 40-RESEARCH.md § Package Legitimacy Audit — all SUS due to unknown-downloads but verified via tarball inspection + established authors). |
| GitHub Actions runner ↔ repo | actions/checkout@v4 + actions/setup-python@v5 + actions/cache@v4 — standard GitHub-trusted actions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-09 | Tampering | pip install of jax/dreamerv3/optax from PyPI (supply chain) | high | mitigate | Package legitimacy audited in 40-RESEARCH.md § Package Legitimacy Audit: dreamerv3 verified via tarball inspection (danijar/dreamerv3, 3469 stars), jax from Google JAX team, optax from DeepMind. All SUS due to unknown-downloads but approved. Pin jax[cuda12]~=0.4.20 + the project's existing dreamerv3~=1.5.0 pin (D-04) so a malicious newer release is NOT auto-installed. No new checkpoint:human-verify needed — these are the established dreamerv3 ecosystem packages already pinned since Phase 24. |
| T-40-10 | Spoofing | workflow_dispatch trigger run by a non-maintainer | medium | mitigate | GitHub Actions workflow_dispatch requires repo write access by default; the GPU runner is metered spend — restrict via branch protection / required reviews on main so only maintainers can merge-to-main (the primary trigger). Manual dispatch is a maintainer-only operation by default. |
| T-40-SC | Tampering | npm/pip/cargo installs | high | mitigate | Pip installs are pinned (jax~=0.4.20, dreamerv3~=1.5.0, optax>=0.1.7) — no floating latest. Package legitimacy audited in 40-RESEARCH.md. No [SLOP] packages. (Same disposition as T-40-09 — pip supply chain, pinned.) |
</threat_model>

<verification>
- YAML validity: python -c 'import yaml; yaml.safe_load(open(".github/workflows/ci.yml"))' succeeds.
- dreamer-gpu job present with GPU runner, workflow_dispatch trigger, if:-gate excluding PRs, jax[cuda12] + [dev,dreamer] install, pytest tests/dreamer/ run step, timeout-minutes set.
- CPU guards unaffected: PYTHONPATH=src pytest tests/dreamer/test_dreamerv3_regression_guard.py tests/test_dreamer_subprocess.py::TestProcessIsolationImport -v → GREEN.
- Existing test / docker-ci / k8s-e2e jobs unchanged (additive diff).
- GPU GREEN (on merge): the dreamer-gpu job runs the flipped E2E test + resume test + regression guard on a GPU runner and passes (structural-only assertions: finite loss, non-explosive loss, checkpoint.ckpt exists, training completes, resume takes effect). This is the DMV3-10 closure signal.
- If the GitHub GPU runner is not enabled on the account: the job is queued/blocked — document the gap in the PR per D-01 and mark DMV3-10 as pending GPU enablement (the tests + job are added; they cannot run until the runner is enabled). Do NOT silently leave DMV3-10 as 100%-skipped.
</verification>

<success_criteria>
- DMV3-10 (complete): the dreamer-gpu CI job runs the real-agent smoke test on a GitHub-hosted GPU runner, asserts structural-only properties (finite + non-explosive loss, checkpoint.ckpt exists, training completes, resume takes effect) via the tests from 40-02/40-03, and fires on merge-to-main + release tags + workflow_dispatch (NOT per-PR). macOS local runs skip cleanly per INV-8.
- D-01/D-02/D-03 honored: GitHub-hosted GPU runner, merge-to-main + manual dispatch trigger, total_steps=1000 smoke budget.
- D-04 honored: pyproject pins unchanged (jax~=0.4.20, dreamerv3~=1.5.0, optax>=0.1.7); jax[cuda12] installed with the ~=0.4.20 line.
- DMV3-10 convergence exclusion honored: no MSE<0.01 / DEFAULT_THRESHOLDS assertions in the CI job or the tests it runs.
- If GPU runner not enabled: gap documented in the PR (ops enablement step, not a silent skip).
</success_criteria>

<output>
Create .planning/phases/40-real-dreamerv3-integration-sentinel-flip/40-04-SUMMARY.md when done.
</output>

## Artifacts this phase produces

### Plan 40-04 (this plan)
| File | Symbol | Kind |
|------|--------|------|
| .github/workflows/ci.yml | dreamer-gpu (NEW job) | MODIFIED — additive GPU smoke job |
| .github/workflows/ci.yml | on: (top-level triggers) | MODIFIED — add workflow_dispatch + push.tags ['v*'] |

### Symbols downstream plans should treat as newly-created (not pre-existing API surface)
- The `dreamer-gpu` CI job name — referenced in milestone audit for DMV3-10 closure.
- The `workflow_dispatch` trigger on the CI workflow — enables manual GPU smoke runs from the Actions tab.