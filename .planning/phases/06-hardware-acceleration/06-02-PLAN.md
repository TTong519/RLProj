---
phase: 06-hardware-acceleration
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/surg_rl/simulators/base_simulator.py
  - src/surg_rl/simulators/mujoco_simulator.py
  - src/surg_rl/simulators/pybullet_simulator.py
  - src/surg_rl/cli.py
autonomous: true
requirements:
  - GPU-01
  - GPU-02
  - GPU-03
  - GPU-07
  - GPU-10
  - GPU-13
user_setup: []

must_haves:
  truths:
    - "BaseSimulator.__init__ accepts backend kwarg and stores it as self._backend"
    - "MuJoCoSimulator logs selected backend at INFO; does NOT add gl_context to Renderer (auto-detected)"
    - "PyBulletSimulator logs single WARNING when gpu_accelerated=True in DIRECT mode"
    - "surg-rl version --verbose displays GPU availability table for all 5 backends"
    - "MuJoCo Metal requirement (GPU-13) is handled as documentation note, not implementation"
  artifacts:
    - path: "src/surg_rl/simulators/base_simulator.py"
      provides: "backend kwarg acceptance"
      contains: "backend: HardwareBackend = HardwareBackend.auto"
    - path: "src/surg_rl/simulators/mujoco_simulator.py"
      provides: "backend logging + renderer setup"
      contains: "Selected backend"
    - path: "src/surg_rl/simulators/pybullet_simulator.py"
      provides: "GPU warning in DIRECT mode"
      contains: "DIRECT mode does not support explicit GPU"
    - path: "src/surg_rl/cli.py"
      provides: "version --verbose GPU table"
      contains: "--verbose"
  key_links:
    - from: "src/surg_rl/simulators/mujoco_simulator.py"
      to: "src/surg_rl/utils/gpu.py"
      via: "select_backend() call"
      pattern: "from surg_rl.utils.gpu import select_backend"
    - from: "src/surg_rl/simulators/pybullet_simulator.py"
      to: "src/surg_rl/utils/gpu.py"
      via: "select_backend() call"
      pattern: "from surg_rl.utils.gpu import select_backend"
    - from: "src/surg_rl/cli.py"
      to: "src/surg_rl/utils/gpu.py"
      via: "detect_backends() + get_cuda_version()"
      pattern: "from surg_rl.utils.gpu import detect_backends"
---

<objective>
Wire the backend selector into both simulator backends and add `surg-rl version --verbose` GPU availability display.

Purpose: Simulators must know the requested backend so they can log the active path and warn users about mismatches. The CLI version command must advertise GPU capabilities.
Output: Backend-aware simulators + version --verbose table.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-hardware-acceleration/06-hardware-acceleration-CONTEXT.md
@.planning/phases/06-hardware-acceleration/06-hardware-acceleration-RESEARCH.md
@.planning/phases/06-hardware-acceleration/06-01-PLAN.md

<interfaces>
From src/surg_rl/simulators/base_simulator.py:
```python
class BaseSimulator(ABC):
    def __init__(
        self,
        timestep: float = 0.002,
        frame_skip: int = 1,
        render_width: int = 640,
        render_height: int = 480,
    ):
    def render(self, mode="rgb_array", **kwargs):
```

From src/surg_rl/simulators/mujoco_simulator.py:
```python
class MuJoCoSimulator(BaseSimulator):
    def __init__(self, timestep=0.002, frame_skip=1, render_width=640, render_height=480, assets_dir=None):
    def _check_mujoco(self) -> None:
    def _check_renderer_available(self) -> bool:
```
(see RESEARCH.md: MuJoCo Python Renderer has NO gl_context parameter; GPU is auto-detected)

From src/surg_rl/simulators/pybullet_simulator.py:
```python
class PyBulletSimulator(BaseSimulator):
    def __init__(self, timestep=0.002, frame_skip=1, render_width=640, render_height=480, assets_dir=None, render_mode="DIRECT"):
    def _check_pybullet(self) -> None:
```
(PyBullet GPU is implicit via OpenGL context; no explicit GPU flag exists)

From src/surg_rl/utils/gpu.py (from Plan 01):
```python
def select_backend(requested: HardwareBackend) -> HardwareBackend:
def detect_backends() -> tuple[HardwareBackend, ...]:
def get_cuda_version() -> str | None:
def get_rocm_version() -> str | None:
```

From src/surg_rl/cli.py:
```python
def version() -> None:
def train(...):
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: wire backend into BaseSimulator, MuJoCoSimulator, and PyBulletSimulator</name>
  <files>
    src/surg_rl/simulators/base_simulator.py
    src/surg_rl/simulators/mujoco_simulator.py
    src/surg_rl/simulators/pybullet_simulator.py
  </files>
  <read_first>
    - src/surg_rl/simulators/base_simulator.py
    - src/surg_rl/simulators/mujoco_simulator.py
    - src/surg_rl/simulators/pybullet_simulator.py
    - src/surg_rl/utils/gpu.py (from Plan 01)
    - .planning/phases/06-hardware-acceleration/06-hardware-acceleration-RESEARCH.md (MuJoCo Renderer section)
  </read_first>
  <behavior>
    - Test 1: `BaseSimulator(backend=HardwareBackend.cuda)._backend == HardwareBackend.cuda`
    - Test 2: `MuJoCoSimulator()` constructs without error and logs INFO containing backend name
    - Test 3: `PyBulletSimulator(render_mode="DIRECT", backend=HardwareBackend.cuda)` logs WARNING about DIRECT mode
    - Test 4: `PyBulletSimulator(render_mode="GUI", backend=HardwareBackend.cuda)` does NOT log the warning
    - Test 5: No `gl_context` kwarg appears in MuJoCoSimulator renderer creation
  </behavior>
  <action>
    1. In `src/surg_rl/simulators/base_simulator.py`:
       - Add import: `from surg_rl.scene_definition.schema import HardwareBackend`
       - Modify `__init__` signature to accept `backend: HardwareBackend = HardwareBackend.auto` as the **last** keyword argument (to preserve positional arg compatibility for existing callers).
       - Store: `self._backend = backend`
       - No changes to `render()` signature or other methods.

    2. In `src/surg_rl/simulators/mujoco_simulator.py`:
       - Add import: `from surg_rl.utils.gpu import select_backend` and `from surg_rl.scene_definition.schema import HardwareBackend`
       - Modify `__init__` to accept `backend: HardwareBackend = HardwareBackend.auto` and pass it to `super().__init__(..., backend=backend)`.
       - After `self._check_mujoco()`, call: `self._active_backend = select_backend(backend)` and log: `logger.info("MuJoCo simulator: selected backend=%s", self._active_backend.value)`.
       - **CRITICAL (per RESEARCH.md):** Do NOT add `gl_context` parameter to `mujoco.Renderer()`. The Python API auto-detects GPU context. Keep existing renderer creation: `self._renderer = self._mujoco.Renderer(self._model, height=self.render_height, width=self.render_width)`.
       - In `_check_renderer_available()`, after confirming renderer works, add a log line: `logger.info("MuJoCo renderer available (backend=%s)", self._active_backend.value)`.
       - On macOS, if `self._active_backend == HardwareBackend.metal`, log INFO: "Metal rendering requested but MuJoCo uses OpenGL-to-Metal translation layer. No explicit Metal context available." (per CONTEXT.md D-03: Metal is NOT supported as a separate path).

    3. In `src/surg_rl/simulators/pybullet_simulator.py`:
       - Add import: `from surg_rl.utils.gpu import select_backend` and `from surg_rl.scene_definition.schema import HardwareBackend`
       - Modify `__init__` to accept `backend: HardwareBackend = HardwareBackend.auto` and pass to `super().__init__(..., backend=backend)`.
       - After `self._check_pybullet()`, call `select_backend(backend)` and store as `self._active_backend`.
       - **CRITICAL (per RESEARCH.md):** PyBullet has NO explicit GPU flag. GPU rendering is implicit when running GUI mode on a GPU-attached display.
       - If `self.render_mode == "DIRECT"` AND `self._active_backend in (HardwareBackend.cuda, HardwareBackend.rocm)`, log a single WARNING: `logger.warning("PyBullet DIRECT mode does not support explicit GPU acceleration. GPU rendering requires GUI mode with display connected to GPU.")`.
       - If `self.render_mode == "GUI"`, log INFO: `logger.info("PyBullet simulator: selected backend=%s (GPU rendering implicit via OpenGL)", self._active_backend.value)`.
       - Log INFO in all other cases: `logger.info("PyBullet simulator: selected backend=%s", self._active_backend.value)`.
  </action>
  <verify>
    <automated>
      PYTHONPATH=src pytest tests/test_simulators.py -v -k "backend" -x || PYTHONPATH=src python -c "
from surg_rl.simulators.mujoco_simulator import MuJoCoSimulator
from surg_rl.simulators.pybullet_simulator import PyBulletSimulator
from surg_rl.scene_definition.schema import HardwareBackend
m = MuJoCoSimulator(backend=HardwareBackend.auto)
p = PyBulletSimulator(render_mode='DIRECT', backend=HardwareBackend.auto)
print('MuJoCo backend:', m._active_backend.value)
print('PyBullet backend:', p._active_backend.value)
"
    </automated>
  </verify>
  <done>
    - `BaseSimulator.__init__` has `backend` parameter with default `HardwareBackend.auto`
    - `MuJoCoSimulator` logs selected backend at INFO; no `gl_context` kwarg added
    - `PyBulletSimulator` logs WARNING in DIRECT mode when GPU backend requested
    - `PyBulletSimulator` does NOT add any `--gpu` connect option (no such option exists)
  </done>
</task>

<task type="auto">
  <name>task 2: add version --verbose GPU availability table</name>
  <files>
    src/surg_rl/cli.py
  </files>
  <read_first>
    - src/surg_rl/cli.py (version() function)
    - src/surg_rl/utils/gpu.py (detect_backends, get_cuda_version, get_rocm_version)
    - src/surg_rl/__init__.py (version string)
  </read_first>
  <action>
    1. Modify `src/surg_rl/cli.py` `version()` function:
       - Change signature to: `def version(verbose: bool = typer.Option(False, "--verbose", "-v", help="Show detailed system info including GPU availability")) -> None:`
       - Keep existing version print: `typer.echo(f"surg-rl version {__version__}")`
       - When `verbose` is True, after printing version, create a Rich `Table` with columns: "Backend", "Available", "Version / Info".
       - For each backend in `[HardwareBackend.cuda, HardwareBackend.rocm, HardwareBackend.metal, HardwareBackend.intel, HardwareBackend.cpu]`:
         - Call `_has_*()` functions or use `detect_backends()` membership.
         - CUDA row: available = _has_cuda(), version = get_cuda_version() or "N/A"
         - ROCm row: available = _has_rocm(), version = get_rocm_version() or "N/A"
         - Metal row: available = _has_metal(), info = "Apple Silicon" if Apple Silicon else "Intel Mac" if on macOS else "N/A (Linux/Windows)"
         - Intel row: available = _has_intel(), info = "oneAPI / XPU" or "N/A"
         - CPU row: always available = True, info = "Always available"
       - Print the table using `typer.echo()` or Rich console (the project already uses Rich).
       - Also print a single line: `f"Default backend: {select_backend(HardwareBackend.auto).value}"`

    2. Add necessary imports at top of `cli.py` if not already present:
       - `from rich.table import Table`
       - `from rich.console import Console`
       - `from surg_rl.utils.gpu import detect_backends, select_backend, get_cuda_version, get_rocm_version, _has_cuda, _has_rocm, _has_metal, _has_intel`
       - `from surg_rl.scene_definition.schema import HardwareBackend`

    3. Ensure `surg-rl version --verbose` works even when NO GPU is present (all backends show "No" except CPU).
  </action>
  <verify>
    <automated>
      PYTHONPATH=src python -m surg_rl.cli version --verbose
    </automated>
  </verify>
  <done>
    - `surg-rl version --verbose` prints version + Rich table with 5 rows
    - Table shows "Available: Yes/No" for each backend
    - CUDA row shows driver version when available; "N/A" when not
    - Command exits 0 even on CPU-only machine
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI output → user terminal | GPU info displayed to user; no secrets involved |
| Simulator logs → log files | Backend selection logged at INFO; not sensitive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Information Disclosure | version --verbose | accept | GPU availability is system info, not PII; research tool context |
| T-06-06 | Denial of Service | MuJoCo renderer init | mitigate | Existing `_check_renderer_available()` catches failures; backend logging is post-check |
| T-06-07 | Spoofing | PyBullet GPU warning | accept | Warning is advisory only; does not change behavior |
</threat_model>

<verification>
1. `grep -n "backend.*HardwareBackend.auto" src/surg_rl/simulators/base_simulator.py` returns match
2. `grep -n "selected backend" src/surg_rl/simulators/mujoco_simulator.py` returns match
3. `grep -n "DIRECT mode does not support" src/surg_rl/simulators/pybullet_simulator.py` returns match
4. `PYTHONPATH=src python -m surg_rl.cli version --verbose` exits 0 and prints table
5. `grep -v "gl_context" src/surg_rl/simulators/mujoco_simulator.py | grep -c "gl_context"` == 0 (no gl_context added)
</verification>

<success_criteria>
- BaseSimulator accepts backend kwarg and stores self._backend
- MuJoCoSimulator logs selected backend at INFO without modifying Renderer creation
- PyBulletSimulator warns in DIRECT mode when GPU backend requested
- version --verbose displays Rich table with all 5 backends and their availability
- No gl_context kwarg added to MuJoCo Renderer (compliance with RESEARCH.md)
- No --gpu flag added to PyBullet connect (compliance with RESEARCH.md)
- All code paths tested with mocked subprocess calls
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardware-acceleration/06-02-SUMMARY.md`
</output>
