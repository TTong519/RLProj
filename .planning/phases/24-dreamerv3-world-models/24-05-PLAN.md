---
phase: 24-dreamerv3-world-models
plan: 05
type: execute
wave: 5
depends_on: ["24-02"]
files_modified:
  - src/surg_rl/dreamer/training.py
autonomous: true
requirements: ["DMV3-02", "DMV3-04"]
user_setup: []
must_haves:
  truths:
    - "DreamerV3 training supports knot_tying task with appropriate instrument and tissue"
    - "DreamerV3 training supports needle_insertion task with appropriate instrument and tissue"
    - "DreamerV3 training supports dissection task with appropriate instrument and tissue"
    - "All 6 task types from Phase 21 work with dreamer-train CLI"
  artifacts:
    - path: "src/surg_rl/dreamer/training.py"
      provides: "Complete task support for all 6 surgical tasks in _create_scene_for_task"
      min_lines: 50 (added to existing)
  key_links:
    - from: "src/surg_rl/dreamer/training.py"
      to: "src/surg_rl/scene_definition/schema.py"
      via: "InstrumentType and TissueType enums for new tasks"
      pattern: "InstrumentType.KNOT_TIER|InstrumentType.NEEDLE|TissueType"
---

<objective>
Close the gap from UAT Test 12: Add support for the 3 missing DreamerV3 training task types — knot_tying, needle_insertion, and dissection — in the _create_scene_for_task function in training.py. These tasks already exist in Phase 21's task curriculum and the benchmark's TASK_SCENE_MAP but were not implemented in the DreamerV3 training orchestration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-dreamerv3-world-models/24-CONTEXT.md
@.planning/phases/24-dreamerv3-world-models/24-UAT.md
@src/surg_rl/dreamer/training.py
@src/surg_rl/scene_definition/schema.py
@src/surg_rl/benchmark/experiment_runner.py
</context>

<interfaces>
<!-- Key interfaces the executor needs - extracted from codebase -->

From src/surg_rl/scene_definition/schema.py (InstrumentType, TissueType):
```python
class InstrumentType(str, Enum):
    FORCEPS = "forceps"
    NEEDLE_DRIVER = "needle_driver"
    SCISSORS = "scissors"
    SCALPEL = "scalpel"
    RETRACTOR = "retractor"
    TROCAR = "trocar"
    CLAMP = "clamp"
    KNOT_TIER = "knot_tier"
    NEEDLE = "needle"
    CUSTOM = "custom"

class TissueType(str, Enum):
    SKIN = "skin"
    FAT = "fat"
    MUSCLE = "muscle"
    ORGAN = "organ"
    VESSEL = "vessel"
    NERVE = "nerve"
    CUSTOM = "custom"
```

From src/surg_rl/benchmark/experiment_runner.py (TASK_SCENE_MAP):
```python
TASK_SCENE_MAP = {
    "suturing": "scenes/simple_suturing.json",
    "knot_tying": "scenes/knot_tying.json",
    "needle_insertion": "scenes/needle_insertion.json",
    "grasping": "scenes/grasping.json",
    "cutting": "scenes/cutting.json",
    "dissection": "scenes/dissection.json",
}
```

From src/surg_rl/dreamer/training.py (existing _create_scene_for_task pattern):
The function currently handles: suturing, grasping, cutting → InstrumentType.NEEDLE_DRIVER, FORCEPS, SCISSORS
Need to add: knot_tying, needle_insertion, dissection
</interfaces>

<tasks>

<task type="auto">
  <name>task 1: add knot_tying, needle_insertion, dissection task support to _create_scene_for_task</name>
  <files>src/surg_rl/dreamer/training.py</files>
  <action>
Update _create_scene_for_task function in training.py to add the 3 missing task types:

1. **knot_tying task** (after grasping, before cutting):
   - Instrument: InstrumentType.KNOT_TIER with name "knot_tier", URDF at "assets/instruments/knot_tier.urdf"
   - Tissue: TissueType.ORGAN (or SKIN), name "suture_pad", mesh "assets/tissues/suture_pad_tet.vtk"
   - Target position: [0.0, 0.02, 0.02] (similar to suturing)
   - Use appropriate TissueType and mesh for knot tying training surface

2. **needle_insertion task** (after knot_tying):
   - Instrument: InstrumentType.NEEDLE with name "needle", URDF at "assets/instruments/needle.urdf"
   - Tissue: TissueType.ORGAN, name "organ_tissue", mesh "assets/tissues/organ_tissue_tet.vtk" (could reuse liver mesh)
   - Target position: [0.0, 0.0, 0.02] (needle entry point)
   - Stiffer tissue for needle penetration resistance

3. **dissection task** (after cutting):
   - Instrument: InstrumentType.SCISSORS (or SCALPEL) with name "dissection_scissors"
   - Tissue: TissueType.MUSCLE (or ORGAN/FAT layering), name "dissection_tissue", mesh "assets/tissues/dissection_tissue_tet.vtk"
   - Target position: [0.02, 0.02, 0.02]
   - Could use layered tissue representation

Follow the EXACT same pattern as existing tasks (suturing/grasping/cutting):
- Create InstrumentConfig with proper InstrumentType, name, urdf_path, pose
- Create TissueConfig with proper TissueType, geometry (MeshAsset with path), soft_body=True, physics (SoftBodyPhysics), pose
- Create TaskConfig with task_type, objectives, reward_shaping, success_threshold
- Return SceneDefinition with all components including DreamerConfig

Key: Use existing InstrumentType enum values (KNOT_TIER, NEEDLE) and TissueType enum values. The mesh paths may not exist as real files — the scene_builder will fall back to primitives per Phase 20 behavior. That's expected.
</action>
  <verify>
<automated>PYTHONPATH=src python -c "
from surg_rl.dreamer.training import _create_scene_for_task
for task in ['suturing', 'knot_tying', 'needle_insertion', 'grasping', 'cutting', 'dissection']:
    scene = _create_scene_for_task(task, 'state', (64, 64))
    print(f'{task}: {len(scene.instruments)} instruments, {len(scene.tissues)} tissues, task_type={scene.task.task_type if scene.task else None}')
"
</automated>
  </verify>
  <done>All 6 task types create valid SceneDefinitions with appropriate instrument types (NEEDLE_DRIVER, KNOT_TIER, NEEDLE, FORCEPS, SCISSORS, SCISSORS/SCALPEL) and tissue configurations, no fallback to CUSTOM for the 3 new tasks</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: add tests for new task types in training module</name>
  <files>tests/test_dreamer_training.py</files>
  <behavior>
    - Test 1: knot_tying task creates scene with KNOT_TIER instrument and proper tissue
    - Test 2: needle_insertion task creates scene with NEEDLE instrument and organ tissue
    - Test 3: dissection task creates scene with SCISSORS/SCALPEL instrument and muscle/fat tissue
    - Test 4: All 6 tasks produce scenes with unique task_type matching input
    - Test 5: Scene has DreamerConfig with correct obs_type and pixel_resolution
  </behavior>
  <action>
Create test file tests/test_dreamer_training.py that validates the 3 new task types are properly handled by _create_scene_for_task. Tests should verify:
1. Each task returns a SceneDefinition (not None)
2. Scene has exactly 1 instrument with correct InstrumentType
3. Scene has exactly 1 tissue with appropriate TissueType
4. Scene.task.task_type matches the input task name
5. DreamerConfig is present with expected fields
Run with: PYTHONPATH=src pytest tests/test_dreamer_training.py -v
  </action>
  <verify>
<automated>PYTHONPATH=src pytest tests/test_dreamer_training.py -v</automated>
  </verify>
  <done>Test file created with 5 test cases covering all 3 new task types, all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Task config → Scene creation | User-controlled task string drives scene generation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-16 | Tampering | Invalid task name | mitigate | Function handles unknown tasks gracefully with CUSTOM fallback; log warning |
| T-24-17 | Denial of Service | Missing mesh files | accept | Phase 20 fallback to primitives handles missing assets; no crash |

</threat_model>

<verification>
Overall phase checks for gap closure Plan 05:
- _create_scene_for_task handles all 6 task types: suturing, knot_tying, needle_insertion, grasping, cutting, dissection
- Each uses appropriate InstrumentType enum (not CUSTOM fallback)
- Each uses appropriate TissueType enum
- All 6 tasks work with surg-rl dreamer-train --task <task>
- Tests pass for all 6 task types
</verification>

<success_criteria>
- knot_tying, needle_insertion, dissection added to _create_scene_for_task with proper instrument/tissue configs
- No fallback to InstrumentType.CUSTOM for these 3 tasks
- New test file validates all 6 tasks
- UAT Test 12 passes: "Only 3 task types implemented" → "All 6 task types supported"
</success_criteria>

<output>
After completion, create `.planning/phases/24-dreamerv3-world-models/24-05-SUMMARY.md`
</output>