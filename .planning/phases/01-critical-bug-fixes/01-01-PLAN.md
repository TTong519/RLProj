---
phase: 01-critical-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/surg_rl/simulators/pybullet_simulator.py
  - src/surg_rl/scene_definition/schema.py
  - tests/test_simulators.py
autonomous: true
requirements: [BUG-01, BUG-02, BUG-03]
must_haves:
  truths:
    - "PyBullet primitive robot createMultiBody is called with [x, y, z, w] orientation"
    - "PyBullet reset() zeroes all joint positions and velocities, not just bodies"
    - "load_scene() does not crash when scene_definition.physics is None"
  artifacts:
    - path: "src/surg_rl/simulators/pybullet_simulator.py"
      provides: "BUG-01 quaternion fix, BUG-02 joint reset fix, BUG-03 physics=None guard"
    - path: "tests/test_simulators.py"
      provides: "Regression tests for all three bugs"
  key_links:
    - from: "tests/test_simulators.py"
      to: "src/surg_rl/simulators/pybullet_simulator.py"
      via: "pytest regression assertions"
---

<objective>
Fix three PyBullet simulator correctness bugs: quaternion order convention, episode joint state leakage, and `physics=None` crash. Each fix must be accompanied by a regression test that fails before the fix and passes after.

Purpose: The simulation layer must be correct before any feature work proceeds. These bugs silently corrupt robot orientation, leak state between episodes, and crash on minimal scenes.
Output: Bug-free `pybullet_simulator.py` + regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

Current code (from `src/surg_rl/simulators/pybullet_simulator.py`):
- `_load_robot()` calls `createMultiBody(..., baseOrientation=[w, x, y, z])` — PyBullet expects `[x, y, z, w]` (BUG-01)
- `reset()` calls `resetJointState(..., targetValue=0.0, targetVelocity=0.0)` but only when `_soft_body_ids` is empty; however it does not iterate over all joints when soft bodies are present, and the joint reset is inside the body loop but the body loop only resets positions/orientations, not the joint states (BUG-02).
- `load_scene()` accesses `scene_definition.physics.timestep` without checking if `physics` is `None` (line 94) or later `getattr(scene_definition, "physics", None)` but uses `physics is not None` — need to verify if `physics=None` path is actually safe (BUG-03)

Tests to add in `tests/test_simulators.py` under `TestPyBulletBugs`:
- `test_pybullet_primitive_robot_quaternion_order` (already in test file as a regression test — verify it passes after fix)
- `test_pybullet_reset_resets_joints` (already in test file)
- `test_pybullet_load_scene_without_physics` (already in test file)
</context>

<tasks>

<task type="auto">
  <name>task 1: fix PyBullet quaternion order and scene physics=None guard</name>
  <files>src/surg_rl/simulators/pybullet_simulator.py</files>
  <action>
1. Fix BUG-01: In `_load_robot()`, change `baseOrientation=` to pass `[x, y, z, w]` instead of `[w, x, y, z]`. The robot's `base_pose.orientation` has fields `.x`, `.y`, `.z`, `.w`. PyBullet's convention is `[x, y, z, w]`.
2. Fix BUG-03: In `load_scene()`, before accessing `scene_definition.physics.timestep`, guard with `if scene_definition.physics is not None:`. Also ensure the `else` branch at line 145 sets default gravity and timestep correctly, which it already does. Verify that when `physics=None`, the code does not raise `AttributeError`.
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/test_simulators.py::TestPyBulletBugs::test_pybullet_primitive_robot_quaternion_order tests/test_simulators.py::TestPyBulletBugs::test_pybullet_load_scene_without_physics -xvs</automated>
  </verify>
  <done>BUG-01: baseOrientation order is [x, y, z, w]; BUG-03: load_scene succeeds when physics=None</done>
</task>

<task type="auto">
  <name>task 2: fix PyBullet reset() to zero all joint states</name>
  <files>src/surg_rl/simulators/pybullet_simulator.py</files>
  <action>
Fix BUG-02: In `reset()`, ensure that for every body in `_body_ids`, every joint in `_joint_ids[body_name]` is reset to `targetValue=0.0, targetVelocity=0.0`. The current code has this inside the `for name, body_id in self._body_ids.items():` loop under the `if self._soft_body_ids:` branch and also in the `else` branch. Verify both branches (soft body reload and normal reset) correctly reset joint states. If soft body reload calls `load_scene()` again, joint states are implicitly reset by re-creation, but add an explicit loop in the non-soft-body branch for clarity and correctness.
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/test_simulators.py::TestPyBulletBugs::test_pybullet_reset_resets_joints -xvs</automated>
  </verify>
  <done>BUG-02: reset() resets all joint positions and velocities to 0.0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Simulator internal state → user observation | `reset()` corruption leaks into observations |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `_load_robot` quaternion | mitigate | Fix order to match PyBullet convention |
| T-01-02 | Information Disclosure | `reset()` joint state leakage | mitigate | Zero all joints on reset |
| T-01-03 | Denial of Service | `load_scene()` with `physics=None` | mitigate | Null-check before attribute access |
</threat_model>

<verification>
Run the full simulator regression suite:
```bash
PYTHONPATH=src pytest tests/test_simulators.py -x -q
```
All 60+ tests must pass (including existing tests, no regressions).
</verification>

<success_criteria>
- `test_pybullet_primitive_robot_quaternion_order` passes
- `test_pybullet_reset_resets_joints` passes
- `test_pybullet_load_scene_without_physics` passes
- No existing simulator tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/01-critical-bug-fixes/01-01-SUMMARY.md`
</output>
