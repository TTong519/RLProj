---
phase: 01-critical-bug-fixes
plan: 01
type: verify  # already fixed in source; verify-only
wave: 1
depends_on: []
files_modified:
  - tests/test_simulators.py
autonomous: true
requirements: [BUG-01, BUG-02, BUG-03]
must_haves:
  truths:
    - "PyBullet primitive robot createMultiBody is called with [x, y, z, w] orientation"
    - "PyBullet reset() zeroes all joint positions and velocities"
    - "load_scene() does not crash when scene_definition.physics is None"
  artifacts:
    - path: "tests/test_simulators.py"
      provides: "Regression tests for all three bugs (must all pass)"
  key_links:
    - from: "tests/test_simulators.py"
      to: "src/surg_rl/simulators/pybullet_simulator.py"
      via: "pytest regression assertions"
---

<objective>
Verify three PyBullet simulator correctness bugs are resolved: quaternion order convention, episode joint state leakage, and `physics=None` crash. Run regression tests that fail if the bugs reappear.

Purpose: The simulation layer must be correct before any feature work proceeds. The fixes already exist in the codebase; this plan ensures they are protected by tests.
Output: Confirmation all three regression tests pass.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

Current code (from `src/surg_rl/simulators/pybullet_simulator.py`):
- `_load_robot()` already passes `[x, y, z, w]` to `createMultiBody` (lines 271–276) — BUG-01 is fixed.
- `reset()` already resets joint states to `targetValue=0.0, targetVelocity=0.0` for every joint in `_joint_ids` (lines 705–713) — BUG-02 is fixed.
- `load_scene()` already guards `scene_definition.physics` with `hasattr` + `if ... and scene_definition.physics:` (line 93) and later `getattr(..., None)` + `if physics is not None:` (lines 133–147) — BUG-03 is fixed.

Tests to verify in `tests/test_simulators.py` under `TestPyBulletBugs`:
- `test_pybullet_primitive_robot_quaternion_order` (already in test file)
- `test_pybullet_reset_resets_joints` (already in test file)
- `test_pybullet_load_scene_without_physics` (already in test file)
</context>

<tasks>

<task type="verify">
  <name>task 1: verify all three PyBullet bug regression tests pass</name>
  <files>tests/test_simulators.py</files>
  <action>
Run the three regression tests to confirm the bugs remain fixed:

```bash
PYTHONPATH=src pytest tests/test_simulators.py::TestPyBulletBugs -xvs
```

All three must pass. If any fail, investigate whether a recent change regressed the fix.
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/test_simulators.py::TestPyBulletBugs::test_pybullet_primitive_robot_quaternion_order tests/test_simulators.py::TestPyBulletBugs::test_pybullet_reset_resets_joints tests/test_simulators.py::TestPyBulletBugs::test_pybullet_load_scene_without_physics -xvs</automated>
  </verify>
  <done>BUG-01, BUG-02, BUG-03 confirmed fixed via regression tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Simulator internal state → user observation | `reset()` corruption leaks into observations |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `_load_robot` quaternion | accepted | Already fixed; verified by test |
| T-01-02 | Information Disclosure | `reset()` joint state leakage | accepted | Already fixed; verified by test |
| T-01-03 | Denial of Service | `load_scene()` with `physics=None` | accepted | Already fixed; verified by test |
</threat_model>

<verification>
Run the full simulator regression suite:
```bash
PYTHONPATH=src pytest tests/test_simulators.py -x -q
```
All 60+ tests must pass (including existing tests, no regressions).
</verification>

<success_criteria>
- `test_pybullet_primitive_robot_quaternion_order` passes
- `test_pybullet_reset_resets_joints` passes
- `test_pybullet_load_scene_without_physics` passes
- No existing simulator tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/01-critical-bug-fixes/01-01-SUMMARY.md` noting that bugs were already fixed and verified.
</output>
