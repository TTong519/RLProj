---
phase: 38-3d-fluid-flag-dim-3d-true
plan: 03
type: tdd
wave: 2
depends_on: [38-01]
files_modified:
  - src/surg_rl/fluids/visualizer.py
  - src/surg_rl/fluids/__init__.py
  - tests/test_fluids/test_render_fluid_3d.py
autonomous: true
requirements: [FLUID-01]
tags: [fluid, visualization, 3d, refactor]

must_haves:
  truths:
    - "render_fluid_3d(pressure, config, z_layer=None, width=400, height=400) returns an (height, width, 3) uint8 image for a 3D pressure field"
    - "render_fluid_3d(None, ...) returns None"
    - "render_fluid_3d slices p.values.numpy('x,y,z')[:,:,layer] and delegates to a private _render_np_2d helper"
    - "render_fluid_2d public signature + 2D output stay byte-identical after extracting _render_np_2d (2D image-array-equality regression test guards the refactor)"
    - "fluids/__init__.py exports render_fluid_3d alongside render_fluid_2d"
  artifacts:
    - "src/surg_rl/fluids/visualizer.py: _render_np_2d(arr, width, height) -> np.ndarray | None (private helper extracted from render_fluid_2d)"
    - "src/surg_rl/fluids/visualizer.py: render_fluid_3d(pressure, config, z_layer=None, width=400, height=400)"
    - "src/surg_rl/fluids/visualizer.py: render_fluid_2d refactored to call _render_np_2d (2D behavior byte-identical)"
    - "src/surg_rl/fluids/__init__.py: render_fluid_3d added to imports + __all__"
    - "tests/test_fluids/test_render_fluid_3d.py: 3D z-layer slice test + null-pressure test + 2D image-array-equality guard"
  key_links:
    - "render_fluid_3d → _render_np_2d (private helper shared with render_fluid_2d)"
    - "fluids.__init__ → render_fluid_3d export (consumed by future env/CLI visualization)"
  prohibitions:
    - statement: "MUST NOT change render_fluid_2d's public signature or 2D output (SC#1 byte-identical)"
      status: resolved
      verification: "tests/test_fluids/test_render_fluid_3d.py includes a 2D image-array-equality assertion vs the pre-refactor output; PYTHONPATH=src pytest tests/test_fluids/test_fluid_simulator.py::TestFluidVisualization -v passes unchanged"
    - statement: "MUST NOT touch the 2D np.asarray fallback in render_fluid_2d's extraction path (lines 22-30 of visualizer.py)"
      status: resolved
      verification: "git diff shows the 2D extraction try/except block preserved; only the rendering body (lines 32-52) is extracted into _render_np_2d"
---

<objective>
Add `render_fluid_3d` — a 2D z-layer slice renderer that delegates to the existing `render_fluid_2d` machinery via a shared private `_render_np_2d` helper. The refactor extracting `_render_np_2d` from `render_fluid_2d` is guarded by a 2D image-array-equality test so the 2D public behavior stays byte-identical (SC#1).

Purpose: Delivers the FLUID-01 visualization slice (D-18). `render_fluid_3d` is a slice-of-3D fallback, NOT a true volume renderer (deferred per CONTEXT.md).
Output: Extended `visualizer.py`, updated `fluids/__init__.py` exports, new `test_render_fluid_3d.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-3d-fluid-flag-dim-3d-true/38-CONTEXT.md
@.planning/phases/38-3d-fluid-flag-dim-3d-true/38-RESEARCH.md
@.planning/phases/38-3d-fluid-flag-dim-3d-true/38-PATTERNS.md
@.planning/phases/38-3d-fluid-flag-dim-3d-true/38-01-PLAN.md
@src/surg_rl/fluids/visualizer.py
@src/surg_rl/fluids/__init__.py
@tests/test_fluids/test_fluid_simulator.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — write render_fluid_3d z-layer slice tests + 2D image-array-equality refactor guard</name>
  <files>tests/test_fluids/test_render_fluid_3d.py</files>
  <read_first>
    - src/surg_rl/fluids/visualizer.py (entire file — understand render_fluid_2d's extraction path lines 22-30 + rendering body lines 32-52; the body is what gets extracted into _render_np_2d)
    - tests/test_fluids/test_fluid_simulator.py lines 146-161 (TestFluidVisualization::test_render_2d_returns_image + test_render_null_pressure_returns_none — mirror style)
    - .planning/phases/38-3d-fluid-flag-dim-3d-true/38-PATTERNS.md § visualizer.py (concrete render_fluid_3d signature + _render_np_2d extraction plan)
    - .planning/phases/38-3d-fluid-flag-dim-3d-true/38-CONTEXT.md decision D-18
  </read_first>
  <behavior>
    - test_render_3d_returns_image: build a 3D StaggeredGrid + pressure field directly via PhiFlow (Box(x=0.3,y=0.3,z=0.3) + StaggeredGrid(0.0, extrapolation.ZERO, domain, x=16,y=16,z=16), run fluid.make_incompressible to get a 3D pressure), call render_fluid_3d(pressure, config, z_layer=8, width=100, height=80) → img.shape == (80, 100, 3) AND img.dtype == np.uint8
    - test_render_3d_default_layer: render_fluid_3d(pressure, config) (z_layer=None) uses the middle layer (nz//2) and returns a non-None image
    - test_render_3d_null_pressure_returns_none: render_fluid_3d(None, None) returns None
    - test_render_2d_image_byte_identical_after_refactor: render a 2D pressure field via render_fluid_2d before AND after the _render_np_2d extraction (this test pins the 2D output array — assert np.array_equal(img_before, img_after) OR pin against a hardcoded expected array hash); this test will FAIL (RED) until _render_np_2d is extracted and render_fluid_2d delegates to it
  </behavior>
  <action>
    Create tests/test_fluids/test_render_fluid_3d.py (NEW file). Import `render_fluid_2d, render_fluid_3d` from `surg_rl.fluids.visualizer` (render_fluid_3d does not yet exist → RED). Build a 3D pressure fixture directly via PhiFlow (do NOT depend on FluidSimulator 3D from Plan 02 — keep this plan parallel/independent). For the 2D byte-identical guard, render a known 2D pressure field (e.g. a 4x4 grid with values [[0,1,2,3],[...]] fed through render_fluid_2d) and assert the output array equals the expected pinned array (compute the expected array once from the CURRENT render_fluid_2d and hardcode it — this pins the refactor). Run — these MUST FAIL (RED) because render_fluid_3d is missing. Commit `test(38-03): add failing render_fluid_3d + 2D refactor guard tests`.
  </action>
  <verify>
    <automated>cd /Users/tt/Documents/RLProj && PYTHONPATH=src pytest tests/test_fluids/test_render_fluid_3d.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_fluids/test_render_fluid_3d.py` exists
    - `grep -c 'render_fluid_3d' tests/test_fluids/test_render_fluid_3d.py` returns >= 3 (imports + calls)
    - `grep -c 'test_render_2d_image_byte_identical_after_refactor' tests/test_fluids/test_render_fluid_3d.py` returns 1
    - Running the file produces ImportError (RED) referencing missing `render_fluid_3d`
    - `tests/test_fluids/test_fluid_simulator.py::TestFluidVisualization` still passes unchanged (no edits to it)
  </acceptance_criteria>
  <done>RED: render_fluid_3d tests fail with missing import; 2D refactor-guard test in place; committed.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — extract _render_np_2d + add render_fluid_3d + export</name>
  <files>src/surg_rl/fluids/visualizer.py, src/surg_rl/fluids/__init__.py</files>
  <read_first>
    - src/surg_rl/fluids/visualizer.py (the rendering body lines 32-52 — extract verbatim into _render_np_2d; keep the 2D extraction try/except lines 22-30 in render_fluid_2d unchanged)
    - src/surg_rl/fluids/__init__.py (existing __all__ — add render_fluid_3d additively)
    - .planning/phases/38-3d-fluid-flag-dim-3d-true/38-PATTERNS.md § visualizer.py + § __init__.py (concrete code)
    - .planning/phases/38-3d-fluid-flag-dim-3d-true/38-RESEARCH.md § Pitfall 1 (.numpy('x,y,z') explicit dim order for 3D)
    - .planning/phases/38-3d-fluid-flag-dim-3d-true/38-CONTEXT.md decision D-18
  </read_first>
  <action>
    **(a) visualizer.py — extract `_render_np_2d`:** Add a private `def _render_np_2d(arr: np.ndarray, width: int, height: int) -> np.ndarray | None:` containing the EXISTING rendering body (the `from skimage.transform import resize` block: normalize, build img, resize, blue channel mapping, return img; the `except Exception: return None`). Move that body verbatim from `render_fluid_2d` into `_render_np_2d`. Then in `render_fluid_2d`, after the existing 2D extraction (`p_vals` from `pressure.values.numpy()` / `np.asarray` fallback — UNCHANGED lines 22-30), replace the rendering body with `return _render_np_2d(p_vals, width, height)`. The 2D public signature + extraction path + output stay byte-identical (guarded by the 2D image-array-equality test).

    **(b) visualizer.py — add `render_fluid_3d` (D-18):** `def render_fluid_3d(pressure: Any | None, config: Any, z_layer: int | None = None, width: int = 400, height: int = 400) -> np.ndarray | None:` with docstring "Render a 2D z-layer slice of a 3D pressure field via the 2D renderer (slice-of-3D fallback; not a true volume renderer)." If `pressure is None: return None`. Extract `p_np = pressure.values.numpy('x,y,z')` (explicit dim order — Pitfall 1; wrap in try/except returning None on failure, mirroring the 2D extraction defensiveness). `nz = p_np.shape[2]`; `layer = z_layer if z_layer is not None else nz // 2`; clamp `layer` to `[0, nz-1]`; `slice_2d = p_np[:, :, layer]`; `return _render_np_2d(slice_2d, width, height)`. Do NOT modify `render_fluid_2d`'s 2D extraction path.

    **(c) fluids/__init__.py — export:** Change `from surg_rl.fluids.visualizer import render_fluid_2d` to `from surg_rl.fluids.visualizer import render_fluid_2d, render_fluid_3d` and add `"render_fluid_3d",` to `__all__` (additive — keep `render_fluid_2d` and existing entries).

    Run: RED tests MUST pass (GREEN); the 2D image-array-equality guard MUST pass (proving the refactor is byte-identical); existing `tests/test_fluids/test_fluid_simulator.py::TestFluidVisualization` MUST pass unchanged. Commit `feat(38-03): add render_fluid_3d z-layer slice + extract _render_np_2d`.
  </action>
  <verify>
    <automated>cd /Users/tt/Documents/RLProj && PYTHONPATH=src pytest tests/test_fluids/test_render_fluid_3d.py tests/test_fluids/test_fluid_simulator.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def _render_np_2d' src/surg_rl/fluids/visualizer.py` returns 1
    - `grep -c 'def render_fluid_3d' src/surg_rl/fluids/visualizer.py` returns 1
    - `grep -c "\.numpy('x,y,z')" src/surg_rl/fluids/visualizer.py` returns >= 1 (3D slice extraction)
    - `grep -c 'render_fluid_3d' src/surg_rl/fluids/__init__.py` returns 2 (import + __all__)
    - `render_fluid_2d`'s 2D extraction try/except (np.asarray fallback) preserved: `git diff src/surg_rl/fluids/visualizer.py` shows the lines 22-30 block unchanged
    - PYTHONPATH=src pytest tests/test_fluids/test_render_fluid_3d.py -v exits 0 (3D returns (80,100,3) uint8; null returns None; 2D byte-identical guard passes)
    - PYTHONPATH=src pytest tests/test_fluids/test_fluid_simulator.py::TestFluidVisualization -v exits 0 (2D visualization tests unchanged)
  </acceptance_criteria>
  <done>GREEN: render_fluid_3d returns (height,width,3) uint8 for a 3D field; _render_np_2d extracted; 2D byte-identical guard passes; render_fluid_3d exported; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PhiFlow pressure field → renderer | Numerical output crosses; malformed fields handled via try/except None |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-06 | DoS (NaN in image) | render_fluid_3d slice | low | mitigate | Defensive try/except returns None on extraction failure; _render_np_2d normalizes via p_norm with p_max>1e-12 guard (existing pattern) |
| T-38-07 | Tampering (z_layer out of range) | render_fluid_3d z_layer arg | low | mitigate | Clamp layer to [0, nz-1] before slicing |
</threat_model>

<verification>
PYTHONPATH=src pytest tests/test_fluids/test_render_fluid_3d.py tests/test_fluids/test_fluid_simulator.py -v exits 0. render_fluid_2d 2D output byte-identical (image-array-equality guard passes). render_fluid_3d exported in fluids.__all__.
</verification>

<success_criteria>
- render_fluid_3d(pressure, config, z_layer, width, height) returns (height, width, 3) uint8 for a 3D pressure field; None for null pressure
- _render_np_2d private helper extracted from render_fluid_2d's rendering body
- render_fluid_2d public signature + 2D output byte-identical (2D image-array-equality test + existing TestFluidVisualization pass unchanged)
- fluids/__init__.py exports render_fluid_3d
</success_criteria>

<output>
Create `.planning/phases/38-3d-fluid-flag-dim-3d-true/38-03-SUMMARY.md` when done
</output>

<artifacts_produced>
Symbols created by this plan (excluded from plan-review-convergence drift verification):
- `surg_rl.fluids.visualizer._render_np_2d` (private helper)
- `surg_rl.fluids.visualizer.render_fluid_3d` (new public function)
- `surg_rl.fluids.__init__.__all__` entry `render_fluid_3d`
- `tests/test_fluids/test_render_fluid_3d.py` (NEW file)
</artifacts_produced>