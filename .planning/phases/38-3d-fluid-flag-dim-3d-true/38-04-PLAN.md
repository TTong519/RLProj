---
phase: 38-3d-fluid-flag-dim-3d-true
plan: 04
type: execute
wave: 3
depends_on: [38-01, 38-02, 38-03]
files_modified:
  - tests/test_fluids/test_2d_baseline.py
  - tests/test_fluids/test_3d_coupling.py
  - tests/test_fluids/test_nan_regression.py
autonomous: true
requirements: [FLUID-01, FLUID-02, FLUID-03]
tags: [fluid, regression, nan, coupling, 2d-baseline]

must_haves:
  truths:
    - "SC#1: tests/test_fluids/test_2d_baseline.py pins 2D velocity+pressure output (array-equality or hash) for dim_3d=False against a v0.5.0 baseline — the 2D path is byte-identical"
    - "SC#2: tests/test_fluids/test_3d_coupling.py runs ONE_WAY N=100 steps via add_instrument on thin instruments and asserts no NaN/blow-up; TWO_WAY is opt-in and documented unstable"
    - "SC#4: tests/test_fluids/test_nan_regression.py is a SINGLE parametrized test over (dim_3d=False, dim_3d=True) x (single, overlapping) obstacles, N=50 steps, asserting finite velocity+pressure EVERY step"
    - "D-22 / SC#5: tests/test_fluid_step.py (5-test v0.5.0 suite) passes UNCHANGED — the fluid_step hook remains an env-driven no-op in both backends (fluid driven by env._fluid_simulator.step(), which becomes dim-aware); confirmed by running it as part of this plan's verification"
    - "All existing 2D fluid tests (test_fluid_simulator.py, test_schema.py, test_render_fluid_3d.py's 2D guard) pass unchanged"
  artifacts:
    - "tests/test_fluids/test_2d_baseline.py (NEW — SC#1 2D byte-identical baseline)"
    - "tests/test_fluids/test_3d_coupling.py (NEW — SC#2 ONE_WAY stability + TWO_WAY opt-in)"
    - "tests/test_fluids/test_nan_regression.py (NEW — SC#4 parametrized NaN regression)"
  key_links:
    - "test_2d_baseline → FluidSimulator 2D path (Plan 02) — pins byte-identical output"
    - "test_3d_coupling → FluidSimulator.add_instrument + ONE_WAY coupling (Plan 02)"
    - "test_nan_regression → FluidSimulator.step + union(*geoms) workaround for both dims (Plan 02)"
  prohibitions:
    - statement: "MUST NOT edit existing tests/test_fluid_step.py (5-test SC#5 suite) or existing 2D tests in test_fluid_simulator.py/test_schema.py"
      status: resolved
      verification: "git diff shows no changes to tests/test_fluid_step.py or existing 2D test classes; PYTHONPATH=src pytest tests/test_fluid_step.py -v exits 0 unchanged"
---

<objective>
Author the three regression-gate test files that close SC#1/SC#2/SC#4 (and confirm SC#5 by running the unchanged `test_fluid_step.py` suite). These are additive test files only — no production code changes. They exercise the 3D solver + force helper + add_instrument + render path delivered by Plans 01-03 and pin the 2D byte-identical baseline.

Purpose: Closes FLUID-01 (SC#1 2D byte-identical), FLUID-02 (SC#2 ONE_WAY stable + TWO_WAY opt-in), FLUID-03 (SC#4 NaN regression parametrized over both dims). Confirms SC#5 (fluid_step hook unchanged).
Output: Three NEW additive test files; the full fluid test suite + test_fluid_step.py green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-3d-fluid-flag-dim-3d-true/38-CONTEXT.md
@.planning/phases/38-3d-fluid-flag-dim-3d-true/38-RESEARCH.md
@.planning/phases/38-3d-fluid-flag-dim-3d-true/38-PATTERNS.md
@.planning/phases/38-3d-fluid-flag-dim-3d-true/38-VALIDATION.md
@.planning/phases/38-3d-fluid-flag-dim-3d-true/38-01-PLAN.md
@.planning/phases/38-3d-fluid-flag-dim-3d-true/38-02-PLAN.md
@.planning/phases/38-3d-fluid-flag-dim-3d-true/38-03-PLAN.md
@tests/test_fluids/test_fluid_simulator.py
@tests/test_fluid_step.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC#1 — 2D byte-identical baseline test</name>
  <files>tests/test_fluids/test_2d_baseline.py</files>
  <read_first>
    - tests/test_fluids/test_fluid_simulator.py lines 13-22 (basic_config fixture — copy verbatim into a NEW basic_config_2d fixture; do NOT edit the existing fixture)
    - tests/test_fluids/test_fluid_simulator.py lines 167-176 (test_velocity_finite_after_step — the finite-assert pattern to extend with array-equality)
    - .planning/phases/38-3d-fluid-flag-dim-3d-true/38-PATTERNS.md § test_2d_baseline.py (concrete pattern)
    - .planning/phases/38-3d-fluid-flag-dim-3d-true/38-CONTEXT.md decisions D-19, SC#1
    - .planning/phases/38-3d-fluid-flag-dim-3d-true/38-VALIDATION.md § D2 (2D byte-identical regression approach)
  </read_first>
  <action>
    Create tests/test_fluids/test_2d_baseline.py (NEW). Define a `basic_config_2d` fixture mirroring the existing 2D `basic_config` (FluidConfig enabled=True, bounds=BoundingBox(0,0,0 → 0.3,0,0.3), resolution=(32,32), dim_3d unset → False default). Build a `FluidSimulator(basic_config_2d)`, run N=10 steps (deterministic — substep_dt default 0.02, no obstacles). Capture velocity + pressure arrays: `v_np = np.asarray(fs.velocity.values)` and `p_np = np.asarray(fs.pressure.values)` (use the np.asarray fallback form that works for the 2D path — mirror existing tests). Assert finiteness AND pin the output: compute a deterministic hash (e.g. `hashlib.sha256(v_np.tobytes()).hexdigest()` + same for p_np) and assert it equals a hardcoded expected hex string captured from the CURRENT v0.5.0-2D behavior (run the test once, copy the printed hash into the test as the pinned baseline). ALSO assert array-equality against a pinned `.npy` fixture file OR the hash — pick the hash approach for simplicity. Include a docstring explaining this is the SC#1 additive-regression gate: any change to the 2D path that perturbs velocity/pressure output fails this test. Do NOT edit existing test files. Run — the test MUST pass against the current 2D path (Plan 02 preserved 2D byte-identical). Commit `test(38-04): add SC#1 2D byte-identical baseline gate`.
  </action>
  <verify>
    <automated>cd /Users/tt/Documents/RLProj && PYTHONPATH=src pytest tests/test_fluids/test_2d_baseline.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_fluids/test_2d_baseline.py` exists
    - `grep -c 'sha256\|hashlib' tests/test_fluids/test_2d_baseline.py` returns >= 1 (hash-pin approach) OR an array-equality assertion against a pinned fixture
    - `grep -c 'dim_3d' tests/test_fluids/test_2d_baseline.py` returns 0 OR explicitly asserts dim_3d=False (the test pins the 2D path)
    - PYTHONPATH=src pytest tests/test_fluids/test_2d_baseline.py -v exits 0 (2D output matches the pinned baseline)
    - No edits to tests/test_fluid_step.py or existing test_fluid_simulator.py tests: `git diff tests/test_fluid_step.py tests/test_fluids/test_fluid_simulator.py` shows no changes
  </acceptance_criteria>
  <done>SC#1 2D byte-identical baseline test passes against the pinned v0.5.0 hash; existing 2D tests untouched; committed.</done>
</task>

<task type="auto">
  <name>Task 2: SC#2 — 3D ONE_WAY stability + TWO_WAY opt-in test</name>
  <files>tests/test_fluids/test_3d_coupling.py</files>
  <read_first>
    - tests/test_fluids/test_fluid_simulator.py lines 128-140 (test_step_with_obstacle_stable — the N-step stability loop pattern to mirror)
    - .planning/phases/38-3d-fluid-flag-dim-3d-true/38-PATTERNS.md § test_3d_coupling.py (concrete pattern)
    - .planning/phases/38-3d-fluid-flag-dim-3d-true/38-CONTEXT.md decisions D-21, D-10, D-11, D-13, SC#2
    - .planning/phases/38-3d-fluid-flag-dim-3d-true/38-RESEARCH.md § Pitfall 8 (TWO_WAY instability — test asserts opt-in + documents, NOT stability)
    - src/surg_rl/scene_definition/schema.py FluidCouplingMode (Plan 01) + FluidSimulator.add_instrument signature (Plan 02)
  </read_first>
  <action>
    Create tests/test_fluids/test_3d_coupling.py (NEW). Define a `basic_config_3d_one_way` fixture: FluidConfig(enabled=True, bounds=BoundingBox(0,0,0 → 0.3,0.3,0.3), dim_3d=True, grid_size=(16,16,16), coupling_mode=FluidCouplingMode.ONE_WAY, coupling_substeps=4). Test `test_one_way_stable_n100`: build FluidSimulator, call `fs.add_instrument(pose, dims, "instrument")` with a thin-instrument pose + dims (e.g. pose=Position(x=0.15,y=0.15,z=0.15), dims=(radius=0.01, length=0.1) — use the exact mapping Plan 02 documented), loop N=100 steps asserting EVERY step: `result = fs.step(); v_np = np.asarray(fs.velocity.values); p_np = np.asarray(fs.pressure.values); assert np.all(np.isfinite(v_np)); assert np.all(np.isfinite(p_np))` (per-step sampling — VALIDATION.md Nyquist minimum). Test `test_two_way_opt_in_documented_unstable`: build a config with `coupling_mode=FluidCouplingMode.TWO_WAY` (assert it constructs — opt-in accepted), run N=100 steps with add_instrument; the test documents instability by EITHER (a) asserting it raises/returns NaN OR (b) asserting finiteness is NOT guaranteed (e.g. assert that IF it completes without exception, finiteness holds, but add a `pytest.mark.xfail` or a docstring noting TWO_WAY is documented unstable on thin instruments — pick approach (b) with `pytest.mark.xfail(reason="TWO_WAY is opt-in and documented unstable on thin instruments (D-10)")` so the test documents instability rather than asserting stability). Do NOT edit existing tests. Commit `test(38-04): add SC#2 3D coupling stability + TWO_WAY opt-in gate`.
  </action>
  <verify>
    <automated>cd /Users/tt/Documents/RLProj && PYTHONPATH=src pytest tests/test_fluids/test_3d_coupling.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_fluids/test_3d_coupling.py` exists
    - `grep -c 'FluidCouplingMode.ONE_WAY' tests/test_fluids/test_3d_coupling.py` returns >= 1
    - `grep -c 'FluidCouplingMode.TWO_WAY' tests/test_fluids/test_3d_coupling.py` returns >= 1
    - `grep -c 'add_instrument' tests/test_fluids/test_3d_coupling.py` returns >= 2 (ONE_WAY + TWO_WAY)
    - `grep -c 'np.isfinite' tests/test_fluids/test_3d_coupling.py` returns >= 4 (per-step velocity + pressure finiteness for the ONE_WAY loop)
    - `grep -c 'xfail' tests/test_fluids/test_3d_coupling.py` returns >= 1 (TWO_WAY documented unstable)
    - PYTHONPATH=src pytest tests/test_fluids/test_3d_coupling.py -v exits 0 (ONE_WAY stable N=100; TWO_WAY xfail handled)
  </acceptance_criteria>
  <done>SC#2: ONE_WAY N=100 steps finite on thin instruments; TWO_WAY opt-in accepted + xfail documenting instability; committed.</done>
</task>

<task type="auto">
  <name>Task 3: SC#4 — parametrized NaN regression + SC#5 confirmation</name>
  <files>tests/test_fluids/test_nan_regression.py</files>
  <read_first>
    - tests/test_fluids/test_fluid_simulator.py lines 128-140 + 167-176 (stability loop + finite-assert patterns)
    - .planning/phases/38-3d-fluid-flag-dim-3d-true/38-PATTERNS.md § test_nan_regression.py (concrete parametrization pattern)
    - .planning/phases/38-3d-fluid-flag-dim-3d-true/38-CONTEXT.md decisions D-20, D-08, SC#4, SC#5
    - .planning/phases/38-3d-fluid-flag-dim-3d-true/38-VALIDATION.md § Sampling Rate (per-step np.isfinite is the Nyquist minimum for per-step divergence) + § D1
    - tests/test_fluid_step.py (read-only — confirm it exists and is unchanged; do NOT edit)
  </read_first>
  <action>
    Create tests/test_fluids/test_nan_regression.py (NEW). Define a SINGLE parametrized test using `@pytest.mark.parametrize` over `(dim_3d, obstacle_kind)` where dim_3d in `{False, True}` and obstacle_kind in `{"single", "overlapping"}` (4 parametrizations total, D-20). For each: build the dim-appropriate FluidConfig (2D: resolution=(32,32), bounds 0,0,0→0.3,0,0.3; 3D: dim_3d=True, grid_size=(16,16,16), bounds 0,0,0→0.3,0.3,0.3); build FluidSimulator; for "single" add ONE obstacle (2D: `Box(vec(x=0.15,y=0.15), vec(x=0.05,y=0.05))` via add_obstacle; 3D: `infinite_cylinder(...)` via add_obstacle OR add_instrument); for "overlapping" add TWO obstacles with overlapping SDFs (exercising the `union(*geoms)` workaround — D-08). Loop N=50 steps asserting EVERY step: `np.all(np.isfinite(np.asarray(fs.velocity.values)))` AND `np.all(np.isfinite(np.asarray(fs.pressure.values)))` (per-step sampling — VALIDATION Nyquist). Use `from phi.flow import Box, vec, union` for 2D obstacle construction and `from phi.geom import infinite_cylinder` for 3D. Add a docstring noting this is the SC#4 NaN-regression gate covering BOTH dims x single/overlapping in one parametrized suite (D-20).

    SC#5 confirmation (no new test file — just run the existing suite): in the plan's `<verify>`, run `PYTHONPATH=src pytest tests/test_fluid_step.py -v` and assert it exits 0 with 5 tests collected (the v0.5.0 hook suite unchanged). Do NOT edit tests/test_fluid_step.py.

    Run the new file — all 4 parametrizations MUST pass (the 2D cases confirm the 2D path + union workaround remain finite; the 3D cases confirm the new 3D path is finite). Run test_fluid_step.py — MUST pass unchanged. Commit `test(38-04): add SC#4 NaN-regression parametrized gate + confirm SC#5`.
  </action>
  <verify>
    <automated>cd /Users/tt/Documents/RLProj && PYTHONPATH=src pytest tests/test_fluids/test_nan_regression.py -v && PYTHONPATH=src pytest tests/test_fluid_step.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_fluids/test_nan_regression.py` exists
    - `grep -c 'parametrize' tests/test_fluids/test_nan_regression.py` returns >= 1 (single parametrized test, D-20)
    - `grep -c 'np.isfinite' tests/test_fluids/test_nan_regression.py` returns >= 4 (per-step velocity + pressure finiteness for both dims)
    - `grep -c 'overlapping' tests/test_fluids/test_nan_regression.py` returns >= 2 (parametrize value + usage)
    - `grep -c 'union' tests/test_fluids/test_nan_regression.py` returns >= 1 OR the overlapping case adds 2 obstacles exercising the union(*geoms) workaround in FluidSimulator.step
    - PYTHONPATH=src pytest tests/test_fluids/test_nan_regression.py -v exits 0 (4 parametrizations pass: 2D-single, 2D-overlapping, 3D-single, 3D-overlapping)
    - PYTHONPATH=src pytest tests/test_fluid_step.py -v exits 0 with 5 tests collected (SC#5 — unchanged)
    - `git diff tests/test_fluid_step.py` shows no changes (SC#5 byte-identical)
  </acceptance_criteria>
  <done>SC#4: parametrized NaN regression passes for all 4 (dim x obstacle_kind) cases with per-step finiteness; SC#5: test_fluid_step.py 5-test suite passes unchanged; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → FluidSimulator | Deterministic in-process; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-08 | Regression (2D drift) | test_2d_baseline | high | mitigate | SHA256 hash-pin on 2D velocity+pressure output — any 2D-path perturbation fails the gate |
| T-38-09 | Regression (NaN/Inf) | test_nan_regression | high | mitigate | Per-step np.isfinite on velocity+pressure for both dims x single/overlapping — catches per-step divergence at Nyquist rate |
| T-38-10 | Regression (coupling instability) | test_3d_coupling | high | mitigate | ONE_WAY N=100 per-step finite assert; TWO_WAY xfail documenting instability |
</threat_model>

<verification>
Full fluid slice + hook suite green: `PYTHONPATH=src pytest tests/test_fluids/ tests/test_fluid_step.py -v` exits 0. This includes: SC#1 2D baseline (Plan 04 Task 1), SC#2 3D coupling (Plan 04 Task 2), SC#4 NaN regression (Plan 04 Task 3), SC#5 test_fluid_step.py (unchanged, 5 tests), plus Plan 01-03 additive tests (schema, simulator 3D, force 3D, render 3D). Before `/gsd-verify-work`: `PYTHONPATH=src pytest tests/ -v` full suite green.
</verification>

<success_criteria>
- SC#1: test_2d_baseline.py pins 2D velocity+pressure output via hash/array-equality — passes against the byte-identical 2D path
- SC#2: test_3d_coupling.py — ONE_WAY N=100 steps finite on thin instruments via add_instrument; TWO_WAY opt-in accepted + xfail documenting instability
- SC#4: test_nan_regression.py — SINGLE parametrized test over (dim_3d=False,True) x (single,overlapping), N=50 steps, per-step finite assert — all 4 cases pass
- SC#5: tests/test_fluid_step.py 5-test suite passes UNCHANGED (git diff empty on that file)
- All existing 2D tests (test_fluid_simulator.py, test_schema.py, test_render_fluid_3d.py 2D guard) pass unchanged
- Full fluid test directory + test_fluid_step.py green
</success_criteria>

<output>
Create `.planning/phases/38-3d-fluid-flag-dim-3d-true/38-04-SUMMARY.md` when done
</output>

<artifacts_produced>
Symbols created by this plan (excluded from plan-review-convergence drift verification):
- `tests/test_fluids/test_2d_baseline.py` (NEW — SC#1 2D byte-identical baseline; `basic_config_2d` fixture, hash-pin test)
- `tests/test_fluids/test_3d_coupling.py` (NEW — SC#2; `basic_config_3d_one_way` fixture, `test_one_way_stable_n100`, `test_two_way_opt_in_documented_unstable` xfail)
- `tests/test_fluids/test_nan_regression.py` (NEW — SC#4; single `@pytest.mark.parametrize` over (dim_3d, obstacle_kind) x 4 cases)
</artifacts_produced>