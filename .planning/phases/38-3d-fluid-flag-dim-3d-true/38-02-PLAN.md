---
phase: 38-3d-fluid-flag-dim-3d-true
plan: 02
type: tdd
wave: 2
depends_on: [38-01]
files_modified:
  - src/surg_rl/fluids/force_computation.py
  - src/surg_rl/fluids/fluid_simulator.py
  - tests/test_fluids/test_force_computation.py
  - tests/test_fluids/test_fluid_simulator.py
autonomous: true
requirements: [FLUID-01, FLUID-02]
tags: [fluid, phiflow, solver, coupling, 3d]

must_haves:
  truths:
    - "FluidSimulator(config) with config.dim_3d=True constructs a 3D Box(x=dx,y=dy,z=dz) + StaggeredGrid(x=Nx,y=Ny,z=Nz)"
    - "FluidSimulator.step on a 3D grid runs advect.mac_cormack + fluid.make_incompressible in 3D and returns finite velocity+pressure"
    - "FluidSimulator.add_instrument(pose, dims, name='instrument') builds an infinite_cylinder shaft + Box tip merged via union() and calls add_obstacle; raises ValueError when not dim_3d"
    - "_compute_obstacle_forces_3d returns per-obstacle (fx,fy,fz) with fy nonzero for a 3D pressure field around an obstacle"
    - "Each of fx,fy,fz is independently clamped to [-1e4, 1e4] (per-axis independent clamp, D-17)"
    - "The 2D FluidSimulator.__init__/step/add_obstacle body and 2D compute_obstacle_forces body stay byte-identical (2D tests + test_fluid_step.py pass unchanged)"
  artifacts:
    - "src/surg_rl/fluids/force_computation.py: _compute_obstacle_forces_3d(velocity, pressure, obstacles, obstacle_names, config) -> dict[str, np.ndarray]"
    - "src/surg_rl/fluids/fluid_simulator.py: FluidSimulator.__init__ 3D branch (if config.dim_3d:)"
    - "src/surg_rl/fluids/fluid_simulator.py: FluidSimulator.step 3D force-call branch"
    - "src/surg_rl/fluids/fluid_simulator.py: FluidSimulator.add_instrument(pose, dims, name='instrument')"
    - "tests/test_fluids/test_force_computation.py: 3D obstacle-mask + per-axis-clamp tests"
    - "tests/test_fluids/test_fluid_simulator.py: additive 3D init/step/obstacle/instrument test classes (TestFluidSimulatorInit3D, TestFluidSimulatorObstacles3D)"
  key_links:
    - "FluidSimulator.step 3D branch → _compute_obstacle_forces_3d (Plan 02 internal)"
    - "FluidSimulator.__init__ 3D branch → config.grid_size (Plan 01)"
    - "FluidSimulator.add_instrument → add_obstacle (raw 2D API unchanged)"
  prohibitions:
    - statement: "MUST NOT modify the 2D FluidSimulator.__init__/step/add_obstacle/clear_obstacles bodies or the 2D compute_obstacle_forces body (lines 12-63 of force_computation.py)"
      status: resolved
      verification: "PYTHONPATH=src pytest tests/test_fluids/test_fluid_simulator.py -v (existing 2D tests pass unchanged) + PYTHONPATH=src pytest tests/test_fluid_step.py -v (5-test suite unchanged)"
    - statement: "MUST NOT unify the 2D global-sum force path with the 3D obstacle-mask path (D-16 — deliberately distinct)"
      status: resolved
      verification: "grep -c 'dict.fromkeys(obstacle_names' src/surg_rl/fluids/force_computation.py (2D global-sum preserved) + 3D path uses phi.field.sample per-obstacle mask, not global sum"
    - statement: "MUST NOT change the 2D compute_obstacle_forces call signature (velocity, pressure, obstacle_names, config) — the 3D path uses a separate helper"
      status: resolved
      verification: "grep 'def compute_obstacle_forces' src/surg_rl/fluids/force_computation.py signature unchanged + new 'def _compute_obstacle_forces_3d' present"
---

<objective>
Make the PhiFlow Eulerian fluid solver dim-aware: add the 3D `Box(x,y,z)` + `StaggeredGrid(x,y,z)` construction, the 3D `advect.mac_cormack` + `fluid.make_incompressible` step, the higher-level `add_instrument(pose, dims)` API (cylinder shaft + box tip via `union(*geoms)`), and the 3D obstacle-mask + per-axis-clamp force helper. All 3D additions sit in top-of-method `if config.dim_3d:` branches or new methods/helpers; the 2D bodies stay byte-identical.

Purpose: Delivers FLUID-01 (3D construct + projection) and FLUID-02 (ONE_WAY stable coupling + per-axis clamp + add_instrument). Unblocks the SC#1/SC#2/SC#4 regression gates (Plan 04).
Output: Extended `FluidSimulator` + `force_computation.py` + additive 3D tests in `test_fluid_simulator.py` and a new `test_force_computation.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-3d-fluid-flag-dim-3d-true/38-CONTEXT.md
@.planning/phases/38-3d-fluid-flag-dim-3d-true/38-RESEARCH.md
@.planning/phases/38-3d-fluid-flag-dim-3d-true/38-PATTERNS.md
@.planning/phases/38-3d-fluid-flag-dim-3d-true/38-VALIDATION.md
@.planning/phases/38-3d-fluid-flag-dim-3d-true/38-01-PLAN.md
@src/surg_rl/fluids/fluid_simulator.py
@src/surg_rl/fluids/force_computation.py
@tests/test_fluids/test_fluid_simulator.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — write 3D force helper tests (obstacle-mask integration + per-axis clamp)</name>
  <files>tests/test_fluids/test_force_computation.py</files>
  <read_first>
    - tests/test_fluids/test_fluid_simulator.py lines 179-199 (TestFluidForceComputation::test_force_on_obstacle_nonzero — the analog force-test pattern; mirror style)
    - src/surg_rl/fluids/force_computation.py (entire file — understand the 2D signature `(velocity, pressure, obstacle_names, config)` and the 2D global-sum body that MUST stay byte-identical)
    - .planning/phases/38-3d-fluid-flag-dim-3d-true/38-PATTERNS.md § force_computation.py (concrete 3D branch + helper signature)
    - .planning/phases/38-3d-fluid-flag-dim-3d-true/38-RESEARCH.md § Code Examples (3D obstacle mask + pressure gradient — verified `phi.field.sample` + `np.gradient(axis=...)` + `.numpy('x,y,z')`)
    - .planning/phases/38-3d-fluid-flag-dim-3d-true/38-CONTEXT.md decisions D-16, D-17, D-15
  </read_first>
  <behavior>
    - test_compute_obstacle_forces_3d_returns_3tuple: build a 3D StaggeredGrid + pressure field directly via PhiFlow (Box(x=0.3,y=0.3,z=0.3) + StaggeredGrid(0.0, extrapolation.ZERO, domain, x=16,y=16,z=16)), run fluid.make_incompressible to get a pressure field, add an infinite_cylinder obstacle, call _compute_obstacle_forces_3d(velocity, pressure, obstacles, obstacle_names, config) → forces[name].shape == (3,) and all finite
    - test_3d_force_y_axis_nonzero: construct an asymmetric 3D pressure scenario (e.g. obstacle offset in y) so that fy (index 1) is nonzero — assert abs(forces["instr"][1]) > 0
    - test_3d_force_per_axis_independent_clamp: with a synthetic/extreme pressure gradient, each of fx, fy, fz is independently bounded: abs(fx)<=1e4 AND abs(fy)<=1e4 AND abs(fz)<=1e4; a spike on one axis does not shrink the others (assert |fy| close to cap while |fx|,|fz| free)
    - test_compute_obstacle_forces_3d_finite_no_nan: forces contain no NaN/Inf for a normal 3D step
    - test_2d_compute_obstacle_forces_unchanged: existing 2D `compute_obstacle_forces(velocity, pressure, names, config)` still returns (fx, 0, fz) with scalar-magnitude clamp — verify the 2D function signature is unchanged and behavior identical
  </behavior>
  <action>
    Create tests/test_fluids/test_force_computation.py (NEW file). Build a 3D `FluidConfig` fixture (`dim_3d=True`, `grid_size=(16,16,16)`, `bounds=BoundingBox(min=Position(0,0,0), max=Position(0.3,0.3,0.3))`, `enabled=True`, `coupling_mode=ONE_WAY`). Construct the 3D velocity/pressure fields directly via `from phi.flow import Box, StaggeredGrid, extrapolation, fluid, Solve, Obstacle` and `from phi.geom import infinite_cylinder` (NOT phi.flow — Pitfall 3). Call `fluid.make_incompressible(v, [Obstacle(infinite_cylinder(...))], solve=Solve(rel_tol=1e-4, abs_tol=1e-4, max_iterations=500))` to obtain a 3D pressure field. Import `_compute_obstacle_forces_3d` from `surg_rl.fluids.force_computation`. For the per-axis-clamp test, you may monkeypatch/construct a synthetic numpy-based pressure proxy OR craft an obstacle geometry that produces an asymmetric gradient — document whichever you choose in the test docstring. Run — these MUST FAIL (RED) because `_compute_obstacle_forces_3d` does not yet exist. Commit `test(38-02): add failing 3D force helper tests`.
  </action>
  <verify>
    <automated>cd /Users/tt/Documents/RLProj && PYTHONPATH=src pytest tests/test_fluids/test_force_computation.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_fluids/test_force_computation.py` exists
    - `grep -c '_compute_obstacle_forces_3d' tests/test_fluids/test_force_computation.py` returns >= 3 (imports + calls)
    - `grep -c 'infinite_cylinder' tests/test_fluids/test_force_computation.py` returns >= 1 (imports from phi.geom)
    - Running the file produces ImportError/AttributeError (RED) referencing missing `_compute_obstacle_forces_3d`
    - Existing `tests/test_fluids/test_fluid_simulator.py` still passes unchanged (no edits to it in this task)
  </acceptance_criteria>
  <done>RED: 3D force helper tests fail with missing `_compute_obstacle_forces_3d`; committed.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: GREEN — implement _compute_obstacle_forces_3d + FluidSimulator 3D __init__/step/add_instrument</name>
  <files>src/surg_rl/fluids/force_computation.py, src/surg_rl/fluids/fluid_simulator.py, tests/test_fluids/test_fluid_simulator.py</files>
  <read_first>
    - src/surg_rl/fluids/force_computation.py (entire 2D body — the 3D helper is a SEPARATE function; do NOT touch lines 12-63)
    - src/surg_rl/fluids/fluid_simulator.py lines 60-150 (2D __init__, add_obstacle, step — the 3D branches sit at the top of __init__ and inside step's force block)
    - .planning/phases/38-3d-fluid-flag-dim-3d-true/38-PATTERNS.md § fluid_simulator.py + § force_computation.py (concrete verified PhiFlow 3D calls)
    - .planning/phases/38-3d-fluid-flag-dim-3d-true/38-RESEARCH.md § Code Examples + § Pitfalls (Pitfall 1: .numpy('x,y,z') explicit dim order; Pitfall 2: np.gradient axis=, NOT math.spatial_gradient; Pitfall 3: infinite_cylinder from phi.geom; Pitfall 4: phi.field.sample, NOT approximate_fraction)
    - .planning/phases/38-3d-fluid-flag-dim-3d-true/38-CONTEXT.md decisions D-06, D-07, D-08, D-14, D-15, D-16, D-17
    - tests/test_fluids/test_fluid_simulator.py lines 13-22 (basic_config fixture — mirror for a new basic_config_3d fixture; do NOT edit the existing fixture)
  </read_first>
  <action>
    **(a) force_computation.py — add `_compute_obstacle_forces_3d` (D-16, D-17):** Add a new module-level function `def _compute_obstacle_forces_3d(velocity, pressure, obstacles, obstacle_names, config) -> dict[str, np.ndarray]` with a docstring noting it is the 3D obstacle-mask path (deliberately distinct from the 2D global-sum — D-16). Inside, lazy `import phi.field as field`. Guard: `if pressure is None or not obstacle_names: return {}`. Extract `p_np = pressure.values.numpy('x,y,z')` (explicit dim order — Pitfall 1). Compute `grad_x = np.gradient(p_np, axis=0)`, `grad_y = np.gradient(p_np, axis=1)`, `grad_z = np.gradient(p_np, axis=2)` (Pitfall 2). Compute `dims = config.bounds.get_dimensions(); nx, ny, nz = config.grid_size; dx, dy, dz = dims[0]/nx, dims[1]/ny, dims[2]/nz; cell_vol = dx*dy*dz`. For each `(obs, name)` in `zip(obstacles, obstacle_names)`: `mask = field.sample(obs.geometry, pressure)` (Pitfall 4 — NOT approximate_fraction); `mask_np = mask.numpy('x,y,z')`; `fx = -float(np.sum(grad_x * mask_np)) * cell_vol`; `fy = -float(np.sum(grad_y * mask_np)) * cell_vol`; `fz = -float(np.sum(grad_z * mask_np)) * cell_vol`; per-axis independent clamp `cap = 1e4; fx = max(-cap, min(cap, fx)); fy = max(-cap, min(cap, fy)); fz = max(-cap, min(cap, fz))` (D-17); `forces[name] = np.array([fx, fy, fz], dtype=np.float64)`. Return `forces`. Do NOT modify the existing `compute_obstacle_forces` 2D function (lines 12-63 stay byte-identical).

    **(b) fluid_simulator.py — 3D __init__ branch (D-07, D-06):** At the top of `__init__`, after `self.config = config` and `dims = config.bounds.get_dimensions()`, insert `if config.dim_3d:` block BEFORE the existing 2D `domain = Box(...)` line: `domain = Box(x=float(dims[0]), y=float(dims[1]), z=float(dims[2]))` (D-06 direct (x,y,z)→(x,y,z) mapping); `nx, ny, nz = config.grid_size` (guaranteed non-None by Plan 01's `_require_grid_size_when_dim_3d`); `self._velocity = StaggeredGrid(0.0, extrapolation.ZERO, domain, x=nx, y=ny, z=nz)`. Then `else:` block containing the EXISTING 2D `Box(x=float(dims[0]), y=float(dims[2]))` + `StaggeredGrid(... x=resolution[0], y=resolution[1])` lines (byte-identical — moved into the else branch unchanged). Keep `self._pressure`, `self._obstacles`, `self._obstacle_names`, `self._sim_time` initialization after the if/else (unchanged).

    **(c) fluid_simulator.py — 3D step force call (D-08, D-16):** The existing `advect.mac_cormack` + `union(*geoms)` + `fluid.make_incompressible(solve=Solve(rel_tol=1e-4, abs_tol=1e-4, max_iterations=500))` lines work in 3D unchanged (verified RESEARCH). In the force block (`if self._pressure is not None and self._obstacles:`), wrap the call in `if self.config.dim_3d:` → `from surg_rl.fluids.force_computation import _compute_obstacle_forces_3d; forces = _compute_obstacle_forces_3d(self._velocity, self._pressure, self._obstacles, self._obstacle_names, self.config)`; `else:` → the existing `compute_obstacle_forces(self._velocity, self._pressure, self._obstacle_names, self.config)` call (byte-identical, moved into else). The `union(*geoms)` workaround and `Solve(...)` settings are reused for both dims (D-08).

    **(d) fluid_simulator.py — add_instrument (D-15, D-14, 3D-only):** Add a NEW method `def add_instrument(self, pose, dims, name: str = "instrument") -> None`. Lazy imports inside: `from phi.geom import infinite_cylinder` (NOT phi.flow — Pitfall 3) and `from phi.flow import Box, union`. Raise `ValueError("add_instrument requires dim_3d=True")` if `not self.config.dim_3d`. Guard `pose` is `Optional[Pose]` (CLAUDE.md): if `pose is None`, raise `ValueError("pose required for add_instrument")`. Build the cylinder shaft from `pose.position` and `dims` (treat `dims` as `(shaft_radius, shaft_length)` or `(rx, ry, rz)` — document the chosen mapping in the docstring; recommended: `shaft = infinite_cylinder(x=pose.position.x, y=pose.position.y, z=pose.position.z, radius=shaft_radius, inf_dim='z')` per the Wake_Flow pattern) and a `Box` tip at the shaft end; `merged = union(shaft, tip)`; `self.add_obstacle(merged, name)`. Do NOT call `add_obstacle` directly in production env wiring (D-21 — out of scope).

    **(e) tests/test_fluids/test_fluid_simulator.py — additive 3D tests:** APPEND a NEW `basic_config_3d` fixture (do NOT edit the existing `basic_config`) and NEW classes `TestFluidSimulatorInit3D` (assert `fs.velocity.spatial_rank == 3` or equivalent 3D indicator), `TestFluidSimulatorObstacles3D` (construct 3D, `add_obstacle(infinite_cylinder(...), "cyl")` and `add_instrument(pose, dims, "instrument")`, loop N=5 steps asserting `isinstance(result, dict)` and finite velocity+pressure), `TestFluidDivergence3D` (finite-assert on 3D pressure). Mirror the existing 2D test classes' style. Do NOT edit existing 2D tests.

    Run: RED force tests (Task 1) MUST now pass; new 3D simulator tests MUST pass; existing 2D tests + test_fluid_step.py MUST pass unchanged. Commit `feat(38-02): add 3D FluidSimulator + force helper + add_instrument`.
  </action>
  <verify>
    <automated>cd /Users/tt/Documents/RLProj && PYTHONPATH=src pytest tests/test_fluids/test_force_computation.py tests/test_fluids/test_fluid_simulator.py tests/test_fluid_step.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def _compute_obstacle_forces_3d' src/surg_rl/fluids/force_computation.py` returns 1
    - `grep -c 'field.sample' src/surg_rl/fluids/force_computation.py` returns >= 1 (per-obstacle mask via phi.field.sample)
    - `grep -c 'np.gradient(p_np, axis=' src/surg_rl/fluids/force_computation.py` returns 3 (axis 0,1,2)
    - `grep -c "\.numpy('x,y,z')" src/surg_rl/fluids/force_computation.py` returns >= 2 (pressure + mask)
    - `grep -c 'def compute_obstacle_forces' src/surg_rl/fluids/force_computation.py` returns 1 (2D signature unchanged)
    - `grep -c 'def add_instrument' src/surg_rl/fluids/fluid_simulator.py` returns 1
    - `grep -c 'from phi.geom import infinite_cylinder' src/surg_rl/fluids/fluid_simulator.py` returns >= 1
    - `grep -c 'if config.dim_3d' src/surg_rl/fluids/fluid_simulator.py` returns >= 1 (3D __init__ branch)
    - 2D `compute_obstacle_forces` body (lines 12-63) byte-identical: `git diff src/surg_rl/fluids/force_computation.py` shows changes ONLY in added lines, not in the 2D function body
    - PYTHONPATH=src pytest tests/test_fluids/test_force_computation.py -v exits 0
    - PYTHONPATH=src pytest tests/test_fluids/test_fluid_simulator.py -v exits 0 (additive 3D + existing 2D)
    - PYTHONPATH=src pytest tests/test_fluid_step.py -v exits 0 (5-test hook suite unchanged — SC#5)
    - 3D force test asserts `forces["..."].shape == (3,)`, `abs(fy) > 0` for an asymmetric case, and `|fx|,|fy|,|fz| <= 1e4`
  </acceptance_criteria>
  <done>GREEN: 3D force helper + 3D FluidSimulator init/step + add_instrument implemented; all 3D tests pass; 2D tests + test_fluid_step.py pass unchanged; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config (Plan 01) → FluidSimulator | Validated config crosses; grid_size already bounded |
| PhiFlow pressure solve → force helper | Numerical output (may contain NaN on solve failure) crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-03 | Denial of service (NaN/blow-up) | 3D FluidSimulator.step + _compute_obstacle_forces_3d | high | mitigate | ONE_WAY default (static SDF, no added-mass) + per-axis independent clamp |f|<=1e4 + substepping via coupling_substeps; SC#2/SC#4 regression gates (Plan 04) assert finite per-step |
| T-38-04 | Tampering | add_instrument pose/dims | medium | mitigate | Guard `pose is None` (CLAUDE.md Optional[Pose]); raise ValueError if not dim_3d; geometry built from validated numeric pose only |
| T-38-05 | DoS via large grid | FluidSimulator 3D __init__ | high | mitigate | Relies on Plan 01 `_cap_grid_size` (<=64) + `_require_grid_size_when_dim_3d` — defense in depth |
</threat_model>

<verification>
PYTHONPATH=src pytest tests/test_fluids/test_force_computation.py tests/test_fluids/test_fluid_simulator.py tests/test_fluid_step.py -v exits 0. 2D compute_obstacle_forces body byte-identical (git diff shows only additive lines). 3D force returns (fx,fy,fz) with per-axis clamp. add_instrument raises when not dim_3d.
</verification>

<success_criteria>
- 3D FluidSimulator constructs Box(x,y,z)+StaggeredGrid(x,y,z) when dim_3d=True; 2D construction byte-identical when dim_3d=False
- 3D step runs advect.mac_cormack + make_incompressible + _compute_obstacle_forces_3d; 2D step body byte-identical
- _compute_obstacle_forces_3d returns per-obstacle (fx,fy,fz) via phi.field.sample mask + np.gradient axis=0/1/2 + per-axis independent clamp [-1e4,1e4]
- add_instrument builds infinite_cylinder shaft + Box tip via union() and calls add_obstacle; raises when not dim_3d or pose is None
- 2D tests + test_fluid_step.py (5-test SC#5 suite) pass unchanged
</success_criteria>

<output>
Create `.planning/phases/38-3d-fluid-flag-dim-3d-true/38-02-SUMMARY.md` when done
</output>

<artifacts_produced>
Symbols created by this plan (excluded from plan-review-convergence drift verification):
- `surg_rl.fluids.force_computation._compute_obstacle_forces_3d` (new 3D helper)
- `FluidSimulator.__init__` 3D `if config.dim_3d:` branch
- `FluidSimulator.step` 3D force-call `if self.config.dim_3d:` branch
- `FluidSimulator.add_instrument(pose, dims, name='instrument')` (new method)
- `tests/test_fluids/test_force_computation.py` (NEW file)
- `tests/test_fluids/test_fluid_simulator.py::basic_config_3d` fixture + `TestFluidSimulatorInit3D`/`TestFluidSimulatorObstacles3D`/`TestFluidDivergence3D` (additive)
</artifacts_produced>