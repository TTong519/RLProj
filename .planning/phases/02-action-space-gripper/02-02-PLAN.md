---
phase: 02-action-space-gripper
plan: 02
subsystem: simulators
type: execute
wave: 2
requires: [02-01]
provides: [ACT-02, ACT-03]
tags: [ik, inverse-kinematics, end-effector, pose, delta, mujoco, pybullet]
dependency_graph:
  requires: [02-01]
  provides: [ACT-02, ACT-03]
  affects: [src/surg_rl/simulators/mujoco_simulator.py, src/surg_rl/simulators/pybullet_simulator.py, tests/test_simulators.py, tests/test_rl_environment.py]
decisions:
  - "PyBullet: calculateInverseKinematics with last-link-as-EE heuristic for primitive robots"
  - "MuJoCo 1-DOF: direct Euler-to-hinge mapping without full Jacobian"
  - "MuJoCo multi-DOF: Jacobian-based damped least-squares IK with SVD regularization"
  - "Pose mode: absolute target; Delta mode: relative offset from current pose"
  - "Both modes fail gracefully to current joint state on IK failure"
metrics:
  duration_minutes: 25
  tasks_completed: 2
  files_modified: 4
  lines_added: ~335
  lines_removed: 0
---

<objective>
Implement `ENDEFFECTOR_POSE` and `ENDEFFECTOR_DELTA` action types in both MuJoCo and PyBullet simulators using inverse kinematics (IK). Remove the remaining `NotImplementedError` guards from `ActionBuilder`. Add tests verifying pose control reaches the simulator without crash.

Purpose: ACT-02 and ACT-03 are the core value-add of Phase 2 — end-effector control lets RL agents operate in Cartesian space rather than joint space, which is the natural action representation for surgical tasks.
Output: Working ENDEFFECTOR_POSE and ENDEFFECTOR_DELTA in both backends + tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-action-space-gripper/02-01-SUMMARY.md

Current code observations:
- `ActionBuilder.process_action` still raises `NotImplementedError` for ENDEFFECTOR_POSE and ENDEFFECTOR_DELTA (action.py)
- PyBullet `_apply_action` only supports POSITION_CONTROL (pybullet_simulator.py line 1115-1188)
- MuJoCo `_apply_action` writes to ctrl directly, but no IK logic exists to convert pose to joint angles
- No `set_end_effector_target()` or `_compute_ik()` methods exist in either simulator
- `SurgicalEnv._default_action_config` does not yet support pose/delta action types
- Subagent `ses_21fe1e043ffeUvUPRW5btcLOrI` produced PyBullet IK implementation (commits ec681ab, 01e10bf)
</context>

<tasks>

<task type="auto">
  <name>task 1: Remove NotImplementedError for pose/delta in ActionBuilder</name>
  <files>src/surg_rl/rl/action.py</files>
  <action>
1. Remove the entire NotImplementedError block from `process_action` since all action types are now supported.
2. Verify that ActionBuilder.process_action no longer raises for any ActionType value.
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/test_rl_observation_action.py::test_joint_torques_no_not_implemented_error -x -q</automated>
  </verify>
  <done>All action types pass through ActionBuilder without NotImplementedError</done>
</task>

<task type="subagent" task_id="ses_21fe1e043ffeUvUPRW5btcLOrI">
  <name>task 2: Implement IK in PyBullet simulator</name>
  <files>src/surg_rl/simulators/pybullet_simulator.py</files>
  <action>
Agent: Implement PyBullet IK support via subagent. Must:
1. Add `set_end_effector_target(position, orientation)` method
2. Add `_compute_ik(robot_name, target_pos, target_quat)` using calculateInverseKinematics with fallback
3. Add `_apply_action_ik(action)` for pose/delta routing  
4. Add `_apply_action` dispatch for endeffector_pose/endeffector_delta modes
5. Add tests in test_simulators.py
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/test_simulators.py -k endeffector -x -q</automated>
  </verify>
  <done>PyBullet supports pose/delta modes with IK-based joint control</done>
</task>

<task type="auto">
  <name>task 3: Implement IK in MuJoCo simulator</name>
  <files>src/surg_rl/simulators/mujoco_simulator.py</files>
  <action>
1. Add `set_action_mode("endeffector_pose")` and `set_action_mode("endeffector_delta")`
2. Add `_compute_ik(target_pos, target_quat)` with two strategies:
   - 1-DOF primitive: pitch Euler angle → hinge joint angle (direct mapping)
   - Multi-DOF: Jacobian-based damped least-squares via `mj_jacBody` + SVD regularization
3. Add `_apply_action` dispatch: pose/delta modes call `_compute_ik()` then write to ctrl
4. Store `_endeffector_target_pos`, `_endeffector_target_quat` for delta mode base reference
5. Add tests verifying zero-action doesn't crash in pose mode
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/test_simulators.py::TestMuJoCoSimulator -k endeffector_orient -x -q</automated>
  </verify>
  <done>MuJoCo supports pose/delta modes with IK-based joint control</done>
</task>

</tasks>

<verify>
- `tests/test_simulators.py` passes for both PyBullet and MuJoCo pose/delta tests
- `tests/test_rl_environment.py` passes with ENDEFFECTOR_POSE and ENDEFFECTOR_DELTA config
- All action types pass through ActionBuilder without NotImplementedError
</verify>

<done>
All action types (JOINT_POSITIONS, JOINT_VELOCITIES, JOINT_TORQUES, ENDEFFECTOR_POSE, ENDEFFECTOR_DELTA, GRIPPER, DISCRETE) are implemented and tested in both simulator backends.
</done>
