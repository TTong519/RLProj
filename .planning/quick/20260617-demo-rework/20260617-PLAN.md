---
quick_task: 20260617-demo-rework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scenes/suturing_demo.json
  - src/surg_rl/simulators/scene_builder.py
  - demos/demo.py
  - tests/test_scene_builder.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Both skin patches in suturing_demo.json render as soft FEM meshes (flexcomp), not rigid boxes"
    - "The curved_suturing_needle instrument is type='needle' (uses the registered thin-torus generator), not type='custom'"
    - "The robotic arm's last link has two visible cylindrical gripper jaw geoms, offset symmetrically in y"
    - "demos/demo.py loads scenes/suturing_demo.json without errors and the banner documents the new realism"
    - "pytest tests/test_scene_builder.py passes with the new gripper and suturing-scene tests"
  artifacts:
    - path: "scenes/suturing_demo.json"
      provides: "Soft-tissue skin patches + realistic needle-type instrument"
      contains: '"soft_body": true'
    - path: "src/surg_rl/simulators/scene_builder.py"
      provides: "Gripper jaw geoms attached to last link body in _add_robot_to_mjcf"
      contains: "_gripper_jaw"
    - path: "demos/demo.py"
      provides: "Updated banner describing the realistic suturing scene"
    - path: "tests/test_scene_builder.py"
      provides: "Regression tests for gripper jaws + soft suturing scene"
      contains: "TestSuturingDemoSceneRealism"
  key_links:
    - from: "scenes/suturing_demo.json"
      to: "src/surg_rl/simulators/scene_builder.py:_add_tissue_to_mjcf"
      via: "soft_body=true triggers the flexcomp_grid path (per D-01)"
      pattern: "getattr.*soft_body.*False"
    - from: "scenes/suturing_demo.json"
      to: "src/surg_rl/assets/mesh_generator.py:_needle"
      via: "type='needle' dispatches to the thin-torus generator (per D-02)"
      pattern: "generate_procedural_instrument.*needle"
    - from: "src/surg_rl/simulators/scene_builder.py"
      to: "current_parent (last link body)"
      via: "Two <geom> children of the last link when has_gripper=True (per D-03)"
      pattern: "_gripper_jaw"
---

<objective>
Rework `scenes/suturing_demo.json` and the scene builder so the demo scene is realistic: soft deformable skin patches (FEM flexcomp), a small curved needle (thin-torus generator), and a visible gripper jaw assembly on the end of the robotic arm. Update `demos/demo.py` to describe the new realism and add regression tests.

Purpose: The current demo shows two rigid boxes and a thumb-sized needle club with a floating chain. A new user running `python demos/demo.py --render` should see something that looks like a surgical suturing setup, not a robotics sandbox.

Output:
- `scenes/suturing_demo.json` with `soft_body: true` on both tissue patches and `type: "needle"` on the instrument
- Two cylindrical gripper jaw geoms on the arm's last link in `_add_robot_to_mjcf`
- Updated demo banner
- New tests covering the gripper geoms and the suturing-scene realism
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/20260617-demo-rework/20260617-CONTEXT.md

# Key code references (read these — they are the implementation ground truth)
- src/surg_rl/simulators/scene_builder.py:1017-1091 — _add_tissue_to_mjcf, the flexcomp path triggered by `getattr(tissue, "soft_body", False)`
- src/surg_rl/simulators/scene_builder.py:616-900 — _add_robot_to_mjcf, where gripper slide joint is added at lines 783-793
- src/surg_rl/assets/mesh_generator.py:166-186 — _needle generator (8mm major, 0.6mm wire)
- src/surg_rl/scene_definition/schema.py:702-742 — TissueConfig (soft_body field at 713, deformable at 739)
- src/surg_rl/scene_definition/schema.py:824-869 — InstrumentConfig (type field at 828)

# Reference test patterns (do not re-invent)
- tests/test_simulators.py:598-647 — TestSoftBodyMJCF (flexcomp asserts)
- tests/test_real_assets.py:250-337 — TestProceduralInstrumentTypes
- tests/test_scene_builder.py:228-340 — TestRobotDofSplitting (gripper slide pattern)

# Existing scene structure (must preserve)
- `scenes/suturing_demo.json` currently has two `tissues` entries (skin_patch_left/right) with `soft_body` not set (defaults to False)
- The instrument is `curved_suturing_needle` with `type: "custom"` and `dimensions: [0.012, 0.006, 0.002]`
- The robotic arm base is at (-0.2, -0.2, 0.5) with 7 joints (no existing jaw geoms)
- The skin patches are 0.08m x 0.06m x 0.008m boxes (smaller than the needle's current dimensions — this is part of the absurdity)
</context>

<interfaces>
From src/surg_rl/simulators/scene_builder.py:
- In `_add_robot_to_mjcf`, after the joint loop completes (~line 781), `current_parent` points at the last link body. The gripper slide joint is added as a child of `current_parent` at lines 783-793. You will add the two jaw `<geom>` children to `current_parent` immediately after the existing gripper slide joint, so the jaws sit at the tip of the last link, controlled by the same parent body.

From src/surg_rl/simulators/scene_builder.py DEFAULT_COLORS:
- `DEFAULT_COLORS["instrument_forceps"] = (0.5, 0.5, 0.5, 1.0)` — per D-03, use this gray for the jaws.

From src/surg_rl/scene_definition/schema.py:
- `TissueConfig.soft_body: bool = False` — setting it to `True` with `deformable=None` triggers the `flexcomp_grid` branch in `_add_tissue_to_mjcf` (lines 1053-1091).
- `InstrumentConfig.type: InstrumentType = InstrumentType.CUSTOM` — `InstrumentType.NEEDLE = "needle"` is the registered generator at mesh_generator.py:166.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: update suturing_demo.json (soft tissue + needle type + needle pose)</name>
  <files>scenes/suturing_demo.json</files>
  <action>
  Modify scenes/suturing_demo.json in three places per CONTEXT.md decisions D-01, D-02:

  1. Add `"soft_body": true` to BOTH `skin_patch_left` and `skin_patch_right` tissue entries. Per D-01, this routes the tissue through the existing flexcomp grid branch in _add_tissue_to_mjcf (scene_builder.py:1053-1091) without requiring a `deformable` config block. The 5x5x2 flexcomp grid and 0.002 m radius from that branch are coarse but fast — appropriate for surgical RL. Keep the existing `physics` block (stiffness 5000, density 1100, poissons_ratio 0.45) — flexcomp ignores most of these for the grid type but they remain valid schema fields.

  2. Change the `curved_suturing_needle` instrument `type` from `"custom"` to `"needle"`. Per D-02, the procedural generator at mesh_generator.py:166-186 produces a thin torus (8mm major radius, 0.6mm wire radius) — the realistic needle. Remove the `dimensions` array (the needle generator owns its geometry; leaving stale dimensions confuses the trimesh loader). Keep the `mesh` block with its `path: assets/instruments/suturing_needle.obj` — when the asset is missing the loader falls back to the procedural generator, and when the asset exists it overrides.

  3. Move the needle pose so its tip points at the right skin patch. The current pose is (0.0, 0.05, 0.41); the right patch is at (0.04, 0.02, 0.4). Use position (0.02, 0.03, 0.405) so the needle sits between the two patches (centered along x), slightly forward (y), and just above the tissue plane (z). Use orientation quaternion (w=0.707, x=0.0, y=0.707, z=0.0) — a 90-degree rotation about y that lays the torus arc flat on the workspace with the arc opening toward +x. Update the `metadata.description` to "Realistic suturing task: soft skin patches, small curved needle, visible needle driver gripper".

  Leave the `attachments` block (4 corner pins) intact — flexcomp honors them as fixed boundary conditions for the clamped skin.

  After editing, verify the JSON loads cleanly via load_scene("scenes/suturing_demo.json") and that tissue[0].soft_body == True and instruments[0].type.value == "needle".
  </action>
  <verify>
  <automated>PYTHONPATH=src python -c "from surg_rl.scene_definition import load_scene; s=load_scene('scenes/suturing_demo.json'); assert s.tissues[0].soft_body is True, s.tissues[0].soft_body; assert s.tissues[1].soft_body is True, s.tissues[1].soft_body; assert s.instruments[0].type.value == 'needle', s.instruments[0].type; print('OK: soft_body=True, type=needle')"</automated>
  </verify>
  <done>scenes/suturing_demo.json loads, both tissues have soft_body=True, instrument type is "needle", needle is repositioned and reoriented, metadata description updated.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: add gripper jaw geoms to _add_robot_to_mjcf</name>
  <files>src/surg_rl/simulators/scene_builder.py</files>
  <action>
  Add two cylindrical "jaw" geoms to the last link body in `_add_robot_to_mjcf` (scene_builder.py:616-900), immediately after the existing gripper slide joint is added at lines 784-793. Per D-03 and the CONTEXT.md "Gripper" section.

  Implementation: After the `if has_gripper: ET.SubElement(...)` block that adds the `<joint name=f"{robot.name}_gripper">` (lines 784-793), add a second block that emits two `<geom>` children of the same `current_parent` (last link body). Each jaw is a cylinder with size="0.003 0.0075 0" (3mm radius, 15mm half-length — the MuJoCo cylinder size format is (radius, half-height)). Position the two jaws at y=+/-0.005 (5mm offset in y, so they straddle the gripper axis symmetrically), z=0.005 (5mm forward in z so they extend past the joint, not through it). Orient them with eulers="0 0 0" so they stand upright.

  Color: use rgba="0.5 0.5 0.5 1.0" (gray, matches DEFAULT_COLORS["instrument_forceps"]). Add `contype="0"` and `conaffinity="0"` to both jaws so they are visual-only — they must NOT participate in MuJoCo contact dynamics. The gripper slide joint already controls a virtual jaw spacing; adding rigid collision geoms would interfere with the simplified contact model. (The jaws move with the slide via the parent body, so the visual aperture change happens automatically as the slide position varies — but we do not give the jaws their own physics.)

  Add a docstring comment explaining the visual-only rationale, similar to the inline notes already in this function (lines 696-711 and 759-771). Also bump the inline comment at line 783 ("Gripper goes on the last (deepest) link body") to mention that the jaw geoms follow in the next block.
  </action>
  <verify>
  <automated>PYTHONPATH=src python -c "from surg_rl.simulators import SceneBuilder; from surg_rl.scene_definition import RobotConfig, EndEffectorConfig, JointConfig, JointLimits, JointType, SceneDefinition, Metadata; sb=SceneBuilder(use_primitive_fallback=True); s=SceneDefinition(metadata=Metadata(name='gripper_test'), robots=[RobotConfig(name='arm', joints=[JointConfig(name='j1', type=JointType.REVOLUTE, limits=JointLimits(lower=-1, upper=1, effort=10, velocity=1), link_length=0.08)], end_effectors=[EndEffectorConfig(name='gripper', type='gripper', max_aperture=0.05, force_limit=10))]); import tempfile, pathlib; td=tempfile.mkdtemp(); p=sb.create_mjcf(s, output_path=pathlib.Path(td)/'g.xml'); c=p.read_text(); assert c.count('_gripper_jaw') == 2, c; assert c.count('contype=\"0\"') >= 2; assert 'rgba=\"0.5 0.5 0.5 1.0\"' in c; print('OK: 2 jaw geoms added')"</automated>
  </verify>
  <done>_add_robot_to_mjcf emits exactly 2 jaw <geom> children of the last link body when has_gripper is True, named {robot.name}_gripper_jaw_l and {robot.name}_gripper_jaw_r, with contype=0 conaffinity=0 (visual only), gray color, cylinder type.</done>
</task>

<task type="auto">
  <name>task 3: update demo.py banner and add scene_rework regression tests</name>
  <files>demos/demo.py, tests/test_scene_builder.py</files>
  <action>
  Two changes:

  (a) demos/demo.py: Update the printed task-stage block (lines 384-390) to describe the new realism. Replace the current stages 1-4 with: "1. Approach the curved surgical needle (thin-torus mesh)", "2. Grasp and pick up the needle with the visible gripper jaws", "3. Drive the needle through the soft skin patches (FEM flexcomp)", "4. Complete the suture connecting both patches". Also add a one-line note after the stages block (before the scene loading code at line 392) reading: "Scene: soft tissue via flexcomp, needle via procedural generator, gripper has visible jaw geoms."

  (b) tests/test_scene_builder.py: Append a new test class `TestSuturingDemoSceneRealism` to the end of the file. Three test methods:

  - `test_suturing_demo_scene_soft_body_enabled`: load the real scenes/suturing_demo.json, assert both tissues have soft_body=True and the instrument type is "needle".

  - `test_suturing_demo_scene_mjcf_has_flexcomp`: build the MJCF for the loaded scene, assert the output contains two `<flexcomp type="grid">` elements (one per skin patch) and the body name `curved_suturing_needle`.

  - `test_gripper_has_two_jaw_geoms`: build a minimal scene with one robot that has end_effectors, assert the MJCF contains exactly two `<geom>` elements with name ending in `_gripper_jaw_l` and `_gripper_jaw_r` (or matching the regex `r"_gripper_jaw[lr]"`), and that both have `contype="0"`. Use a `tmp_path` fixture and `SceneBuilder(use_primitive_fallback=True)`, mirroring the pattern in tests/test_simulators.py:601-624.

  Use the existing imports at the top of test_scene_builder.py (SceneBuilder, SceneDefinition, Metadata, RobotConfig, EndEffectorConfig, JointConfig, JointLimits, JointType, etc.). Add new imports only if strictly needed (TissueConfig, TissueType, TissueMeshDefinition are NOT currently imported — add them from surg_rl.scene_definition). Do not modify or remove any existing test class.
  </action>
  <verify>
  <automated>PYTHONPATH=src python -m pytest tests/test_scene_builder.py::TestSuturingDemoSceneRealism -v 2>&1 | tail -20</automated>
  </verify>
  <done>Demo banner describes the new realism. The new test class has 3 passing tests covering soft-body routing, flexcomp MJCF output, and gripper jaw geoms. Existing tests in test_scene_builder.py still pass (no regressions).</done>
</task>

<task type="auto">
  <name>task 4: run full quick-task test sweep + lint</name>
  <files>(no file changes — verification only)</files>
  <action>
  Run the full local test suite (skipping integration tests per the AGENTS.md convention) to confirm no regressions in scene_builder, simulators, soft_body, deformable, real_assets, or mesh_generation tests. Also run ruff on the modified files. If any test fails, fix the regression before declaring the task complete — do not leave the suite red.

  Commands (in order):
  1. PYTHONPATH=src python -m pytest tests/test_scene_builder.py tests/test_simulators.py tests/test_real_assets.py tests/test_mesh_generation.py tests/test_deformable.py -m "not integration" -v
  2. ruff check src/surg_rl/simulators/scene_builder.py demos/demo.py tests/test_scene_builder.py
  3. black --check src/surg_rl/simulators/scene_builder.py demos/demo.py tests/test_scene_builder.py

  If ruff or black report style issues, fix them with `ruff check --fix` and `black` (no flags) on the affected files. Do NOT run `ruff check --fix` on files outside the 3-file scope.
  </action>
  <verify>
  <automated>PYTHONPATH=src python -m pytest tests/test_scene_builder.py tests/test_simulators.py tests/test_real_assets.py tests/test_mesh_generation.py tests/test_deformable.py -m "not integration" 2>&1 | tail -5</automated>
  </verify>
  <done>All targeted tests pass (last line of output shows a non-zero PASSED count, exit code 0). Ruff and black report no issues on the 3 modified files.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scene JSON -> schema | scenes/suturing_demo.json is read by Pydantic v2; missing soft_body defaults to False, so omitting the field is safe. Adding "soft_body": true with no `deformable` block is the documented soft-body opt-in path (verified at scene_builder.py:1033-1053). |
| scene JSON -> mesh generator | `type: "needle"` dispatches to a registered generator (mesh_generator.py:166). Unknown types fall back to `custom` with a warning (mesh_generator.py:314-334) — graceful degradation, not a crash. |
| scene_builder -> MuJoCo MJCF | New jaw geoms use contype=0 conaffinity=0 (visual-only). They cannot introduce spurious contact forces even if a user accidentally drops a tool on them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-DR-01 | T (Tampering) | scenes/suturing_demo.json | accept | JSON is a checked-in scene file, not user input. No untrusted-party risk. |
| T-DR-02 | R (Repudiation) | demos/demo.py banner | accept | Banner is informational; not security-relevant. |
| T-DR-03 | I (Information disclosure) | jaw geoms in MJCF | accept | Visual-only, no data exposure. |
| T-DR-04 | D (Denial of service) | flexcomp grid 5x5x2 | mitigate | Grid resolution capped at 5x5x2 in scene_builder.py:1064 — surgical RL does not need finer resolution. If a future user requests 50x50x50 it will run but slow. Out of scope for this task. |
| T-DR-05 | E (Elevation of privilege) | gripper slide joint | accept | Joint already exists at scene_builder.py:784-793; adding visual geoms does not change actuator authority. |
</threat_model>
<verification>
Run before declaring complete:

1. PYTHONPATH=src python -m pytest tests/test_scene_builder.py -m "not integration" — all 3 new tests in TestSuturingDemoSceneRealism pass.
2. PYTHONPATH=src python -c "from surg_rl.scene_definition import load_scene; s=load_scene('scenes/suturing_demo.json'); assert s.tissues[0].soft_body and s.tissues[1].soft_body and s.instruments[0].type.value == 'needle'" — scene JSON loads with the expected types.
3. PYTHONPATH=src python -c "from surg_rl.simulators import SceneBuilder; from surg_rl.scene_definition import SceneDefinition, Metadata; ..." — build MJCF, assert "flexcomp" appears 2x and "_gripper_jaw" appears 2x.
4. ruff check src/surg_rl/simulators/scene_builder.py demos/demo.py tests/test_scene_builder.py — no lint errors.
5. black --check on the same 3 files — no formatting issues.
</verification>
<success_criteria>
- Both skin patches in suturing_demo.json are soft (flexcomp) — visible as FEM meshes in MuJoCo viewer.
- The curved_suturing_needle instrument is a small thin-torus mesh, not a 2x2x10 cm box.
- The arm chain ends in a visible pair of gray gripper jaws that open/close as the slide joint moves.
- demos/demo.py runs end-to-end (`python demos/demo.py --headless --steps 0`) without warnings beyond the pre-existing asset-missing warnings.
- The new TestSuturingDemoSceneRealism class has 3 passing tests.
- No existing test in test_scene_builder.py, test_simulators.py, test_real_assets.py, test_mesh_generation.py, or test_deformable.py regresses.
- ruff and black report clean on the 3 modified files.
</success_criteria>
<output>
After completion, create `.planning/quick/20260617-demo-rework/20260617-SUMMARY.md` summarizing: scene JSON diff (soft_body, type=needle, needle pose), scene_builder.py diff (jaw geoms block, contype=0 rationale), demo.py diff (banner text), test additions, and the test-run output. Then commit with message `quick(20260617-demo-rework): realistic suturing demo — soft tissue, small needle, visible gripper jaws`.</output>
