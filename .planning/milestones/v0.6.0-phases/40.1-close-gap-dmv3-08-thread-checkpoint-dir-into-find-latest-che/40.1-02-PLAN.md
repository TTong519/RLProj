---
phase: 40.1
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/surg_rl/fluids/fluid_simulator.py
  - src/surg_rl/scene_definition/schema.py
  - tests/test_fluids/test_force_computation.py
  - tests/test_fluids/test_3d_coupling.py
autonomous: true
requirements: [D-04, D-05, D-06, D-07, D-08, D-09]

must_haves:
  truths:
    - "A linear y-ramp pressure fixture produces fy = -(1/0.3) * V_mask within 1% tolerance, pinning that the CR-01 np.gradient spacing fix produces Newtons not cell-index units (D-04)"
    - "FluidSimulator.step runs a substep loop for BOTH ONE_WAY and TWO_WAY with sub_dt = dt / coupling_substeps on the 3D obstacle path (D-06)"
    - "TWO_WAY step() passes a moving Obstacle (velocity != 0) to make_incompressible, diverging from ONE_WAY's stationary Obstacle (D-05, D-08)"
    - "TWO_WAY obstacle velocity is integrated from prior-substep forces via v_obs += (F / m_obs) * sub_dt (D-08, added-mass feedback)"
    - "The structural test observes the feedback branch via obstacle.is_moving on the captured Obstacle arg (D-07a)"
    - "The thin-instrument xfail test genuinely exercises the unstable added-mass path now that TWO_WAY diverges from ONE_WAY (D-07b)"
    - "coupling_substeps field description text matches dt / coupling_substeps semantics (D-06)"
    - "The 2D step branch stays byte-identical to v0.5.0 (SC#1 — substep loop runs only on the 3D path)"
    - "ONE_WAY stability test test_one_way_stable_n100 still passes under 4x substepping (smaller sub_dt is more stable, D-06)"
    - "Two parallel plans with disjoint module sets (dreamer/ vs fluids/) run in a single wave (D-09)"
  artifacts:
    - "src/surg_rl/fluids/fluid_simulator.py — _obstacle_velocities dict state, _build_obstacles_arg method, _integrate_obstacle_velocity method, restructured step() with 3D substep loop, updated docstring"
    - "src/surg_rl/scene_definition/schema.py — coupling_substeps field description text updated to dt / coupling_substeps semantics"
    - "tests/test_fluids/test_force_computation.py — test_3d_force_y_axis_physical_magnitude added to TestComputeObstacleForces3D class"
    - "tests/test_fluids/test_3d_coupling.py — test_two_way_takes_feedback_branch (structural spy, stable) + test_two_way_thin_instrument_unstable (xfail, renamed from test_two_way_opt_in_documented_unstable)"
  key_links:
    - "FluidSimulator.step → 3D substep loop → _build_obstacles_arg → make_incompressible (TWO_WAY: Obstacle(merged, velocity=(vx,vy,vz)); ONE_WAY: Obstacle(merged))"
    - "FluidSimulator.step → _compute_obstacle_forces_3d → _integrate_obstacle_velocity (TWO_WAY only) → _obstacle_velocities state"
  prohibitions:
    - "Do NOT re-fix CR-01 — force_computation.py:67-69 np.gradient spacing fix is committed (cc117c3); this phase adds the magnitude test only (D-04)"
    - "Do NOT substep the 2D branch — fluid_simulator.step 2D path must stay byte-identical to v0.5.0 (SC#1); the substep loop runs ONLY on the 3D obstacle path"
    - "Do NOT remove coupling_mode, coupling_substeps, FluidCouplingMode, or their tests — D-05 implements real TWO_WAY, does not remove (removing is the deferred alternative explicitly NOT chosen)"
    - "Do NOT pass Obstacle velocity as np.ndarray — must be tuple/list (PhiFlow 3.4.0 Obstacle.__init__ only wraps tuple/list via wrap(); RESEARCH landmine 7)"
    - "Do NOT call apply_boundary_conditions separately — it runs inside make_incompressible (RESEARCH landmine 8); the TWO_WAY fix is purely the Obstacle(velocity=...) kwarg"
    - "Do NOT silently special-case ONE_WAY to skip the substep loop — that reintroduces WR-02 for ONE_WAY (D-06); if ONE_WAY substepping destabilizes a test, raise it, do not skip"
    - "Do NOT touch training.py, subprocess.py, or any dreamer/ file — disjoint module set per D-09"
    - "Do NOT build a magnitude-test ramp whose unclamped force exceeds 1e4 — the per-axis 1e4 clamp (D-17) masks the asserted value; the verified fixture produces |fy| approximately 0.0084 N, 6 orders below the cap"
---

<objective>
Implement real TWO_WAY obstacle-velocity feedback and a substep loop in FluidSimulator.step so the coupling_mode and coupling_substeps config knobs behave as documented (instead of being inert), and add the CR-01 physical-magnitude regression test that pins the already-committed np.gradient spacing fix produces Newtons not cell-index units. Replace the falsely-xpassing TWO_WAY xfail test with a structural test (verifies the feedback branch is taken) plus a genuine xfail (verifies thin-instrument divergence).

Purpose: Close the Phase 38 WR-01/WR-02 advisory (coupling_mode/coupling_substeps inert — both modes run byte-identical code) and the CR-01 advisory (3D force unit bug fix lacks a magnitude regression test). The user explicitly chose to IMPLEMENT real TWO_WAY + substeps rather than remove the inert fields (D-05), elevating this from cleanup to cleanup + behavioral feature.
Output: FluidSimulator.step with substep loop + TWO_WAY feedback branch, CR-01 magnitude test, TWO_WAY structural + xfail test split, and coupling_substeps schema description text updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40.1-close-gap-dmv3-08-thread-checkpoint-dir-into-find-latest-che/40.1-CONTEXT.md
@.planning/phases/40.1-close-gap-dmv3-08-thread-checkpoint-dir-into-find-latest-che/40.1-RESEARCH.md
@.planning/phases/40.1-close-gap-dmv3-08-thread-checkpoint-dir-into-find-latest-che/40.1-PATTERNS.md
@.planning/phases/40.1-close-gap-dmv3-08-thread-checkpoint-dir-into-find-latest-che/40.1-VALIDATION.md
@.planning/phases/38-fluid-3d-flag-dim-3d-true/38-REVIEW.md
@.planning/phases/38-fluid-3d-flag-dim-3d-true/38-CONTEXT.md
@src/surg_rl/fluids/fluid_simulator.py
@src/surg_rl/fluids/force_computation.py
@src/surg_rl/scene_definition/schema.py
@tests/test_fluids/test_force_computation.py
@tests/test_fluids/test_3d_coupling.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED + regression pin): Add CR-01 magnitude test, TWO_WAY structural test, and rename xfail test</name>
  <files>tests/test_fluids/test_force_computation.py, tests/test_fluids/test_3d_coupling.py</files>
  <read_first>
    - tests/test_fluids/test_force_computation.py (lines 1-135 — existing TestComputeObstacleForces3D class at :87, test_3d_force_y_axis_nonzero at :106-132, _make_synthetic_3d_pressure helper at :68-84, _make_3d_config at :28-39; module-top pytest.importorskip("phi") at :18)
    - tests/test_fluids/test_3d_coupling.py (lines 1-172 — Test3DCouplingTwoWayOptIn at :114-172, test_two_way_opt_in_accepted at :117-131 KEEP, test_two_way_opt_in_documented_unstable at :133-172 SPLIT/RENAME, Test3DCouplingOneWay::test_one_way_stable_n100 at :94-111 MUST STILL PASS, _thin_instrument_pose at :58-65, _THIN_DIMS at :65, _assert_velocity_pressure_finite at :68-88, basic_config_3d_one_way fixture at :42-55)
    - src/surg_rl/fluids/force_computation.py (lines 53-84 — _compute_obstacle_forces_3d with CR-01 fix at :67-69; do NOT modify this file)
    - .planning/phases/40.1-close-gap-dmv3-08-thread-checkpoint-dir-into-find-latest-che/40.1-RESEARCH.md (Focus Area 3, lines 419-470 — VERIFIED EXACT hand-computation: dy=0.3/16=0.01875, cell_vol=dy^3=6.5918e-6, N_mask=384, V_mask=0.00253125, expected_fy=-(1/0.3)*0.00253125=-0.0084375 N, rel err 0.0, |fy| well below 1e4 clamp; Focus Area 1, lines 344-370 — D-07a structural spy test mechanism with monkeypatch target phi.physics.fluid.make_incompressible)
    - .planning/phases/40.1-close-gap-dmv3-08-thread-checkpoint-dir-into-find-latest-che/40.1-PATTERNS.md (Pattern Assignment for test_force_computation.py lines 258-301 — magnitude test convention, add to existing class; Pattern Assignment for test_3d_coupling.py lines 304-358 — D-07 split details, spy approach, patch target landmine)
    - .planning/phases/40.1-close-gap-dmv3-08-thread-checkpoint-dir-into-find-latest-che/40.1-VALIDATION.md (Per-Task Verification Map — exact pytest node IDs for D-04, D-07a, D-07b)
  </read_first>
  <behavior>
    - test_3d_force_y_axis_physical_magnitude: a linear y-ramp p[i,j,k]=j/16 on a 16^3 grid over a 0.3m domain, with an infinite_cylinder mask at (0.15,0.15) radius 0.05, produces fy matching the hand-computed expected_fy = -(1/0.3) * V_mask within 1% relative error. This test PASSES immediately (regression pin for already-committed CR-01 fix cc117c3).
    - test_two_way_takes_feedback_branch: with a TWO_WAY FluidConfig and a thin instrument added, running one fs.step() with a monkeypatch spy on phi.physics.fluid.make_incompressible captures the obstacles arg, and captured obstacles[0].is_moving is True (TWO_WAY takes the moving branch). This test FAILS (RED) because currently ONE_WAY == TWO_WAY (both pass Obstacle(merged) with no velocity, so is_moving is False).
    - test_two_way_thin_instrument_unstable: renamed from test_two_way_opt_in_documented_unstable, keeps the same xfail(strict=False) decorator and docstring wording, runs N=100 steps with a thin instrument. After Task 2 implements real TWO_WAY, this genuinely exercises the unstable added-mass path.
  </behavior>
  <action>
    In tests/test_fluids/test_force_computation.py, add a new method test_3d_force_y_axis_physical_magnitude to the EXISTING TestComputeObstacleForces3D class (do NOT create a new class). Use the existing _make_3d_config() and _make_synthetic_3d_pressure(arr) helpers unchanged. Build a 16x16x16 numpy array where arr[:, j, :] = float(j) / 16.0 for j in range(16) (linear y-ramp). Wrap it via _make_synthetic_3d_pressure(arr). Create an Obstacle from infinite_cylinder(x=0.15, y=0.15, radius=0.05, inf_dim="z") matching the existing test_3d_force_y_axis_nonzero fixture. Call _compute_obstacle_forces_3d(None, p, [obstacle], ["cyl"], cfg). Extract fy = float(forces["cyl"][1]). Compute the expected value: dy = 0.3 / 16, cell_vol = dy ** 3, sample the mask via phi.field.sample(obstacle.geometry, p).numpy("x,y,z") to get N_mask = mask_np.sum(), V_mask = N_mask * cell_vol, expected_fy = -(1.0 / 0.3) * V_mask. Assert abs(fy - expected_fy) / abs(expected_fy) < 0.01. The docstring must cite CR-01 regression, the hand-computation (dy, V_mask, expected_fy), and note the unclamped |fy| is approximately 0.0084 N which is 6 orders below the 1e4 per-axis clamp so the clamp does NOT mask the assertion. Import phi.field and phi.geom.infinite_cylinder locally in the test body matching the sibling test style. This test PASSES immediately (the CR-01 fix is already committed at cc117c3).

    In tests/test_fluids/test_3d_coupling.py, modify class Test3DCouplingTwoWayOptIn:

    1. KEEP test_two_way_opt_in_accepted unchanged (lines 117-131).

    2. ADD test_two_way_takes_feedback_branch (D-07a, structural spy, stable, NO xfail). Build a TWO_WAY FluidConfig matching the existing thin-instrument xfail config (enabled=True, dim_3d=True, grid_size=(16,16,16), bounds=BoundingBox(min=Position(0,0,0), max=Position(0.3,0.3,0.3)), coupling_mode=FluidCouplingMode.TWO_WAY, coupling_substeps=4). Create a FluidSimulator, add a thin instrument via add_instrument(_thin_instrument_pose(), _THIN_DIMS, "instrument"). Set up a monkeypatch spy on phi.physics.fluid.make_incompressible: define a spy function that captures the obstacles argument into a dict, calls the original make_incompressible, and returns its result. Use monkeypatch.setattr("phi.physics.fluid.make_incompressible", spy) (the patch target is phi.physics.fluid because the from phi.flow import fluid inside step() binds fluid to phi.physics.fluid; RESEARCH A4). Call fs.step() once. Assert captured["obstacles"][0].is_moving is True (TWO_WAY passes a moving Obstacle). This test FAILS (RED) because currently step() always passes Obstacle(merged) with no velocity, so is_moving is False.

    3. RENAME the existing thin-instrument xfail method to test_two_way_thin_instrument_unstable (D-07b).
    <!-- planner-discipline-allow: test_two_way_opt_in_documented_unstable -->
    Keep the SAME xfail(strict=False) decorator, the SAME docstring wording (including "not a guarantee"), and the SAME N=100 thin-instrument loop with _assert_velocity_pressure_finite per step. The body stays the same; only the method name changes. After Task 2, this genuinely exercises the unstable added-mass path.

    Run the tests: magnitude PASSES (regression pin), structural FAILS (RED — is_moving is False because TWO_WAY feedback not yet implemented), renamed xfail PASSES (xfail/xpass — still runs the same stable code path until Task 2).
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/test_fluids/test_force_computation.py::TestComputeObstacleForces3D::test_3d_force_y_axis_physical_magnitude tests/test_fluids/test_3d_coupling.py::Test3DCouplingTwoWayOptIn::test_two_way_takes_feedback_branch tests/test_fluids/test_3d_coupling.py::Test3DCouplingTwoWayOptIn::test_two_way_thin_instrument_unstable -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - PYTHONPATH=src pytest tests/test_fluids/test_force_computation.py::TestComputeObstacleForces3D::test_3d_force_y_axis_physical_magnitude -v exits 0 (regression pin PASSES — CR-01 fix already committed)
    - PYTHONPATH=src pytest tests/test_fluids/test_3d_coupling.py::Test3DCouplingTwoWayOptIn::test_two_way_takes_feedback_branch -v exits non-zero (RED — is_moving is False because TWO_WAY feedback not implemented yet; assert captured obstacles[0].is_moving fails)
    - PYTHONPATH=src pytest tests/test_fluids/test_3d_coupling.py::Test3DCouplingTwoWayOptIn::test_two_way_thin_instrument_unstable -v exits 0 (xfail/xpass — still runs stable code path)
    - grep -c 'test_two_way_opt_in_documented_unstable' tests/test_fluids/test_3d_coupling.py == 0 (old name removed)
    - grep -c 'test_two_way_takes_feedback_branch' tests/test_fluids/test_3d_coupling.py == 1 (new structural test exists)
    - grep -c 'test_two_way_thin_instrument_unstable' tests/test_fluids/test_3d_coupling.py == 1 (renamed xfail exists)
    - test_two_way_opt_in_accepted still exists unchanged in Test3DCouplingTwoWayOptIn
    - force_computation.py is NOT modified (grep -c 'np.gradient' src/surg_rl/fluids/force_computation.py == existing count — CR-01 fix not re-touched)
  </acceptance_criteria>
  <done>
    CR-01 magnitude test added and PASSES (regression pin). TWO_WAY structural test added and FAILS (RED — feedback branch not taken). xfail test renamed. test_two_way_opt_in_accepted kept. force_computation.py untouched. The structural test is the TDD gate for Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement substep loop + TWO_WAY feedback in FluidSimulator.step + update schema description</name>
  <files>src/surg_rl/fluids/fluid_simulator.py, src/surg_rl/scene_definition/schema.py</files>
  <read_first>
    - src/surg_rl/fluids/fluid_simulator.py (lines 90-260 — __init__ state at :98-101, add_obstacle at :111-115, clear_obstacles at :117-119, add_instrument at :121-183, step at :185-260 with current single-solve body and 2D/3D branch at :231-257, docstring at :188-206, imports at :208)
    - src/surg_rl/scene_definition/schema.py (lines 1507-1552 — FluidCouplingMode enum, FluidConfig.coupling_mode and coupling_substeps fields; the coupling_substeps description at :1548-1551 is the text to update)
    - .planning/phases/40.1-close-gap-dmv3-08-thread-checkpoint-dir-into-find-latest-che/40.1-RESEARCH.md (Focus Area 1, lines 284-342 — VERIFIED PhiFlow 3.4.0 Obstacle(velocity=tuple) moving-wall boundary, _build_obstacles_arg and _integrate_obstacle_velocity shapes, landmine 7 tuple-not-ndarray; Focus Area 2, lines 372-417 — VERIFIED substep loop pattern, 2D byte-identical preservation, ONE_WAY stability CFL argument)
    - .planning/phases/40.1-close-gap-dmv3-08-thread-checkpoint-dir-into-find-latest-che/40.1-PATTERNS.md (Pattern Assignment for fluid_simulator.py lines 153-255 — current step() code, obstacle-state pattern, _build_obstacles_arg/_integrate_obstacle_velocity shapes, critical tuple-not-ndarray convention, docstring update note; Pattern Assignment for schema.py lines 361-401 — field description text update, grep-for-pinned-text landmine)
    - tests/test_fluids/test_3d_coupling.py (the RED structural test from Task 1 — the gate this task must make GREEN)
    - .planning/phases/40.1-close-gap-dmv3-08-thread-checkpoint-dir-into-find-latest-che/40.1-VALIDATION.md (Per-Task Verification Map — D-06 one_way_stable_n100, D-07a structural test node IDs)
  </read_first>
  <behavior>
    - test_two_way_takes_feedback_branch PASSES: TWO_WAY step() passes a moving Obstacle (is_moving True) to make_incompressible; the structural spy captures it.
    - test_one_way_stable_n100 PASSES: ONE_WAY N=100 steps stay finite under 4x substepping (smaller sub_dt is more stable by CFL).
    - test_3d_force_y_axis_physical_magnitude still PASSES: CR-01 magnitude unaffected by step() restructure (force_computation.py untouched).
    - test_two_way_thin_instrument_unstable: genuinely exercises the unstable added-mass path (xfail or xpass depending on m_obs and dims).
  </behavior>
  <action>
    In src/surg_rl/fluids/fluid_simulator.py:

    1. Add per-obstacle velocity state to __init__ (after self._obstacle_names at :100): self._obstacle_velocities: dict[str, np.ndarray] = {} keyed by obstacle name, initialized to np.zeros(3) per obstacle.

    2. Update add_obstacle (at :111-115) to register the velocity: after self._obstacle_names.append(name), add self._obstacle_velocities[name] = np.zeros(3). Update clear_obstacles (at :117-119) to also clear: self._obstacle_velocities.clear().

    3. Add a _build_obstacles_arg method: merge obstacle geometries via union(*geoms) (same as current step() at :217-219). If self.config.coupling_mode == FluidCouplingMode.TWO_WAY, use the first obstacle's velocity from self._obstacle_velocities[self._obstacle_names[0]] and return [Obstacle(merged, velocity=(float(v[0]), float(v[1]), float(v[2])))] — the velocity MUST be a tuple of Python floats, NOT a numpy array (PhiFlow 3.4.0 Obstacle.__init__ only wraps tuple/list; RESEARCH landmine 7). For ONE_WAY, return [Obstacle(merged)] (stationary, velocity=0, current behavior). Import FluidCouplingMode from surg_rl.scene_definition.schema at the module level or inside the method.

    4. Add an _integrate_obstacle_velocity method taking forces: dict and sub_dt: float. Use m_obs = 1e-3 (kg, small mass for thin-instrument added-mass instability, RESEARCH A2). For each name, f in forces.items(): self._obstacle_velocities[name] = self._obstacle_velocities[name] + (f / m_obs) * sub_dt. This is the added-mass feedback loop (D-08).

    5. Restructure step() (at :185-260) into a substep loop on the 3D path only. The 2D path MUST stay byte-identical (SC#1): keep the 2D branch's exact sequence of operations (single advect.mac_cormack, single make_incompressible with [Obstacle(merged)], single compute_obstacle_forces, self._sim_time += dt) unchanged. The cleanest structure: early-return for 2D (if not self.config.dim_3d: run the current 2D code path verbatim and return), then run the 3D substep loop. For 3D: compute sub_dt = dt / self.config.coupling_substeps, loop for _ in range(self.config.coupling_substeps), each iteration does: advect.mac_cormack(self._velocity, self._velocity, sub_dt), obstacles_arg = self._build_obstacles_arg(), make_incompressible with Solve(rel_tol=1e-4, abs_tol=1e-4, max_iterations=500) (same solve config, keep the try/except with logger.warning and self._pressure = None fallback), then if self._pressure is not None and self._obstacles: forces = _compute_obstacle_forces_3d(...) (3D branch only inside the loop), then if self.config.coupling_mode == FluidCouplingMode.TWO_WAY: self._integrate_obstacle_velocity(forces, sub_dt), then self._sim_time += sub_dt. Return forces after the loop. The loop runs for BOTH ONE_WAY and TWO_WAY (D-06 — do NOT skip the loop for ONE_WAY).

    6. Update the step() docstring (at :188-206): remove the "Inert 3D config surface" and "TWO_WAY currently aliases ONE_WAY" language (now false). Describe the real substep loop (sub_dt = dt / coupling_substeps, runs for both modes on the 3D path) and the TWO_WAY obstacle-velocity feedback (Obstacle(merged, velocity=v_obs) moving-wall boundary via make_incompressible, integrated from prior-substep forces). Note the 2D path stays byte-identical to v0.5.0 (SC#1).

    In src/surg_rl/scene_definition/schema.py, update the coupling_substeps field description at :1548-1551. Change the text from the current stale phrasing (which says substep_dt is reused as per-substep dt) to describe the actual semantics: each substep uses sub_dt = dt / coupling_substeps.
    <!-- planner-discipline-allow: reuses substep_dt as per-substep dt -->
    Keep the ge=1, le=16 bounds and default=4 unchanged. Before finalizing the wording, grep tests/ for the current coupling_substeps description text to check if any test pins it (if so, update that test too).

    Run the RED structural test from Task 1 — it must now PASS (GREEN). Run the ONE_WAY regression — it must still PASS. Run the magnitude test — it must still PASS.
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/test_fluids/test_force_computation.py::TestComputeObstacleForces3D::test_3d_force_y_axis_physical_magnitude tests/test_fluids/test_3d_coupling.py::Test3DCouplingTwoWayOptIn::test_two_way_takes_feedback_branch tests/test_fluids/test_3d_coupling.py::Test3DCouplingOneWay::test_one_way_stable_n100 -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - PYTHONPATH=src pytest tests/test_fluids/test_3d_coupling.py::Test3DCouplingTwoWayOptIn::test_two_way_takes_feedback_branch -v exits 0 (GREEN — TWO_WAY now passes a moving Obstacle)
    - PYTHONPATH=src pytest tests/test_fluids/test_3d_coupling.py::Test3DCouplingOneWay::test_one_way_stable_n100 -v exits 0 (ONE_WAY regression still finite under 4x substepping)
    - PYTHONPATH=src pytest tests/test_fluids/test_force_computation.py::TestComputeObstacleForces3D::test_3d_force_y_axis_physical_magnitude -v exits 0 (magnitude regression pin still green)
    - PYTHONPATH=src pytest tests/test_fluids/test_3d_coupling.py::Test3DCouplingTwoWayOptIn::test_two_way_thin_instrument_unstable -v exits 0 (xfail or xpass — genuinely exercises unstable path now)
    - grep -c '_build_obstacles_arg' src/surg_rl/fluids/fluid_simulator.py >= 1 (new method exists)
    - grep -c '_integrate_obstacle_velocity' src/surg_rl/fluids/fluid_simulator.py >= 1 (new method exists)
    - grep -c '_obstacle_velocities' src/surg_rl/fluids/fluid_simulator.py >= 3 (state declared in __init__, used in add_obstacle, used in _build_obstacles_arg or _integrate_obstacle_velocity)
    - grep -c 'sub_dt' src/surg_rl/fluids/fluid_simulator.py >= 1 (substep loop uses sub_dt)
    - grep -c 'reuses substep_dt as per-substep dt' src/surg_rl/scene_definition/schema.py == 0 (old description text removed)
    - grep -c 'dt / coupling_substeps' src/surg_rl/scene_definition/schema.py == 1 (new description text present)
    - force_computation.py is NOT modified (grep -c 'np.gradient' src/surg_rl/fluids/force_computation.py unchanged from baseline — CR-01 fix not re-touched)
    - PYTHONPATH=src pytest tests/test_fluids/ -v exits 0 (full fluids test suite green — additive-regression invariant)
  </acceptance_criteria>
  <done>
    FluidSimulator.step runs a substep loop on the 3D path for both modes with sub_dt = dt / coupling_substeps. TWO_WAY passes a moving Obstacle with velocity feedback integrated from forces. ONE_WAY regression stays finite. The structural test passes (is_moving True for TWO_WAY). The 2D path stays byte-identical. The schema description matches the actual semantics. force_computation.py is untouched.
  </done>
</task>

</tasks>

<verification>
- PYTHONPATH=src pytest tests/test_fluids/test_force_computation.py::TestComputeObstacleForces3D::test_3d_force_y_axis_physical_magnitude tests/test_fluids/test_3d_coupling.py::Test3DCouplingTwoWayOptIn::test_two_way_takes_feedback_branch tests/test_fluids/test_3d_coupling.py::Test3DCouplingOneWay::test_one_way_stable_n100 tests/test_fluids/test_3d_coupling.py::Test3DCouplingTwoWayOptIn::test_two_way_thin_instrument_unstable -v (all GREEN or xfail)
- PYTHONPATH=src pytest tests/test_fluids/ -v (full fluids suite green — additive-regression invariant)
- grep '_build_obstacles_arg\|_integrate_obstacle_velocity\|_obstacle_velocities' src/surg_rl/fluids/fluid_simulator.py (new methods + state present)
- grep 'dt / coupling_substeps' src/surg_rl/scene_definition/schema.py (schema description updated)
- grep 'reuses substep_dt as per-substep dt' src/surg_rl/scene_definition/schema.py returns 0 (old text removed)
- force_computation.py unchanged (CR-01 fix not re-touched)
- Full suite baseline stays green: PYTHONPATH=src pytest tests/ -v --ignore=tests/test_rl.py --ignore=tests/test_rl_callbacks.py -q (additive-regression invariant; pre-existing macOS torch SIGABRT excluded)
</verification>

<success_criteria>
- CR-01 magnitude test pins the np.gradient spacing fix produces Newtons (D-04)
- Substep loop runs for both ONE_WAY and TWO_WAY on the 3D path with sub_dt = dt / coupling_substeps (D-06)
- TWO_WAY step() passes a moving Obstacle with velocity feedback (D-05, D-08)
- Structural test verifies the feedback branch is taken (D-07a)
- Renamed xfail genuinely exercises the unstable added-mass path (D-07b)
- coupling_substeps schema description matches actual semantics (D-06)
- 2D path byte-identical to v0.5.0 (SC#1)
- ONE_WAY regression test_one_way_stable_n100 still passes (D-06 stability)
- No dreamer/ files touched (D-09 disjoint module set)
</success_criteria>

<output>
Create .planning/phases/40.1-close-gap-dmv3-08-thread-checkpoint-dir-into-find-latest-che/40.1-02-SUMMARY.md when done
</output>

## Artifacts this phase produces

| Artifact | Kind | Location | Purpose |
|----------|------|----------|---------|
| `_obstacle_velocities: dict[str, np.ndarray]` | New instance state | src/surg_rl/fluids/fluid_simulator.py __init__ | Per-obstacle velocity state keyed by name, init zeros, for TWO_WAY added-mass feedback (D-08) |
| `_build_obstacles_arg() -> list` | New method | src/surg_rl/fluids/fluid_simulator.py | Builds Obstacle list: TWO_WAY returns Obstacle(merged, velocity=tuple-of-floats); ONE_WAY returns Obstacle(merged) stationary (D-05/D-08) |
| `_integrate_obstacle_velocity(forces, sub_dt)` | New method | src/surg_rl/fluids/fluid_simulator.py | Added-mass feedback: v_obs += (F / m_obs) * sub_dt with m_obs=1e-3 kg (D-08) |
| `step()` restructured with 3D substep loop | Modified method | src/surg_rl/fluids/fluid_simulator.py:185 | Substep loop on 3D path only (sub_dt = dt / coupling_substeps, both modes); 2D path stays byte-identical (D-06, SC#1) |
| `step()` docstring updated | Modified docstring | src/surg_rl/fluids/fluid_simulator.py:188 | Removes inert-config language; describes real substep loop + TWO_WAY feedback |
| `add_obstacle` velocity registration | Modified method | src/surg_rl/fluids/fluid_simulator.py:111 | Registers np.zeros(3) in _obstacle_velocities per obstacle (D-08) |
| `clear_obstacles` velocity clear | Modified method | src/surg_rl/fluids/fluid_simulator.py:117 | Clears _obstacle_velocities alongside obstacles/names |
| `coupling_substeps` description text | Modified field description | src/surg_rl/scene_definition/schema.py:1548 | Updated from "reuses substep_dt" to "sub_dt = dt / coupling_substeps" semantics (D-06) |
| `test_3d_force_y_axis_physical_magnitude` | New test method | tests/test_fluids/test_force_computation.py (TestComputeObstacleForces3D) | CR-01 regression pin: hand-computed fy = -(1/0.3)*V_mask within 1% (D-04) |
| `test_two_way_takes_feedback_branch` | New test method | tests/test_fluids/test_3d_coupling.py (Test3DCouplingTwoWayOptIn) | D-07a structural spy: asserts TWO_WAY Obstacle.is_moving is True via make_incompressible monkeypatch |
| `test_two_way_thin_instrument_unstable` | Renamed test method | tests/test_fluids/test_3d_coupling.py (Test3DCouplingTwoWayOptIn) | D-07b: renamed from test_two_way_opt_in_documented_unstable; genuinely exercises unstable added-mass path with xfail(strict=False) |