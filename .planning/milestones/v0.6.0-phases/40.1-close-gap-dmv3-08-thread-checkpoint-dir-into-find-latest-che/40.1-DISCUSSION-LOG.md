# Phase 40.1: Close gap — DMV3-08 thread checkpoint_dir into _find_latest_checkpoint + Phase 38 advisory cleanups - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-13
**Phase:** 40.1-close-gap-dmv3-08-thread-checkpoint-dir + Phase 38 advisory cleanups
**Areas discussed:** Dead coupling knobs, CR-01 fix depth + test, checkpoint_dir resume test, Phase bundling/scope

---

## Dead coupling knobs

| Option | Description | Selected |
|--------|-------------|----------|
| Remove fields (Rec.) | Remove coupling_mode, coupling_substeps, FluidCouplingMode + their schema/coupling tests + the misleading TWO_WAY xfail. Matches "cleanups / inert fields" framing. Smallest, safest. | |
| Implement TWO_WAY + substeps | Implement real TWO_WAY obstacle-velocity feedback in step() + the substep loop. Real feature work with behavioral change + added-mass instability risk. Delivers the opt-in the config surface promises. | ✓ |
| Keep + document only | Leave fields as-is, add a docstring/schema warning that they're inert until a future phase. Lowest churn but ships a knob that silently does nothing. | |

**User's choice:** Implement TWO_WAY + substeps
**Notes:** Elevates the fluids portion from "cleanup" to "cleanup + real behavioral feature." The user opted to deliver the opt-in the config surface promises rather than collapse it. This makes D-07 (structural + xfail test) and D-08 (feedback mechanism) the meaningful downstream decisions.

**Follow-up 1 — substep scope + dt:**

| Option | Description | Selected |
|--------|-------------|----------|
| Both modes, dt/substeps (Rec.) | Substep loop for BOTH ONE_WAY and TWO_WAY; sub_dt = dt / coupling_substeps. ONE_WAY default 4 now does 4 sub-solves/step; existing stability tests assert finiteness only so still pass. | ✓ |
| TWO_WAY-only substeps | Substep loop only when coupling_mode=TWO_WAY; ONE_WAY stays single-solve (zero blast radius). Contradicts field description ("on the 3D obstacle path"). | |
| Both modes, substep_dt each | Both modes substep; sub_dt = substep_dt (field's "reuses substep_dt" phrasing). Total sim time/step = coupling_substeps*substep_dt, decoupled from env dt. Larger timing change. | |

**User's choice:** Both modes, dt/substeps
**Notes:** The field's current "reuses substep_dt as per-substep dt" description is now wrong-by-construction; planner should update the description text.

**Follow-up 2 — TWO_WAY xfail test disposition:**

| Option | Description | Selected |
|--------|-------------|----------|
| Keep xfail (Rec.) | Keep test_two_way_opt_in_documented_unstable as @xfail(strict=False). Now genuinely exercises the unstable path (was falsely xpassing). Lowest churn, faithful to Phase 38 D-10. | |
| Flip to divergence assert | Flip to a positive assertion that TWO_WAY diverges from ONE_WAY at step N. Stronger but brittle (pinning "diverges by N" when the point is instability). | |
| Structural + xfail | Replace with TWO tests: (a) CPU-cheap structural test that TWO_WAY step() takes the feedback branch; (b) keep xfail for thin-instrument divergence. Most coverage, most test code. | ✓ |

**User's choice:** Structural + xfail
**Notes:** (a) pins that the feedback branch is actually wired (the real regression risk); (b) keeps the documented-instability contract, now exercising the genuine unstable path.

---

## CR-01 fix depth + test

**Pre-question finding:** The CR-01 `np.gradient` cell-spacing fix is ALREADY committed (`cc117c3 fix(38): CR-01 pass physical cell spacing to np.gradient`, `force_computation.py:67-69`). The remaining CR-01 follow-up is the physical-magnitude regression test `38-REVIEW.md` recommended, which does NOT yet exist (the closest test, `test_3d_force_y_axis_nonzero`, only asserts `fy > 0.0`).

| Option | Description | Selected |
|--------|-------------|----------|
| Add magnitude test (Rec.) | New test: linear pressure ramp of known slope → F = -∇p · V_mask within tolerance. Hand-compute expected fy from dy + ramp slope, assert within ~1%. The exact test 38-REVIEW recommended; pins that the fix produces Newtons not cell-index units. | ✓ |
| Tighten existing y-axis test | Tighten test_3d_force_y_axis_nonzero to assert an approximate hand-computed fy (not just >0) instead of a separate test. Less new code, conflates path-exercised with magnitude-correct. | |
| Skip, tests are enough | Leave as-is; nonzero + clamp-reach + finite are enough. Accept no test pins physical Newton magnitude. Lowest effort, leaves 38-REVIEW regression open. | |

**User's choice:** Add magnitude test
**Notes:** The fix is not re-touched; this phase's CR-01 work is the test only. Magnitude test fixture must stay below the 1e4 per-axis clamp so the clamp doesn't mask the asserted value (the trap that let CR-01 slip originally).

---

## checkpoint_dir resume test

| Option | Description | Selected |
|--------|-------------|----------|
| Add CPU-runnable test (Rec.) | CPU unit test (no GPU/JAX/skipif): fake models/dreamerv3/{task}_{obs_type}/ with checkpoint.ckpt; _find_latest_checkpoint(custom_dir) returns that path; None when empty. Verifies the fix off-GPU. | |
| CPU test + subprocess stub | Above PLUS stub DreamerSubprocess to assert run_dreamer_training(resume=True, checkpoint_dir=custom) calls subprocess.load_checkpoint(custom path). Full parent resume path, needs a stub/mock. | ✓ |
| GPU-gated only | Rely on existing test_dreamerv3_checkpoint_resume.py; update it to use a custom checkpoint_dir. No CPU verification; fix unverified on macOS (SKIPs). | |

**User's choice:** CPU test + subprocess stub
**Notes:** Exercises the full parent resume path, not just the glob helper. Complements (does not replace) the GPU-gated restart-then-continue test.

**Follow-up — resume contract:**

| Option | Description | Selected |
|--------|-------------|----------|
| Keep both (defense-in-depth) (Rec.) | Keep child cp.load_or_save() at construct AND fix+test parent _find_latest_checkpoint → subprocess.load_checkpoint. Both resume from same checkpoint.ckpt; parent now wired+tested, child stays as safety net. | ✓ |
| Parent sole source | Make parent path the sole resume source of truth; remove child auto-resume. Simpler single-path, loses the safety net. | |

**User's choice:** Keep both (defense-in-depth)
**Notes:** The audit's "fix the defense-in-depth gap" reading — make both layers work, don't remove either.

---

## Phase bundling/scope

| Option | Description | Selected |
|--------|-------------|----------|
| Two plans, parallel (Rec.) | 40.1-01 dreamer checkpoint_dir gap (training.py + CPU resume test), 40.1-02 fluids advisories (CR-01 magnitude test + implement TWO_WAY + substep loop + structural/xfail tests). Disjoint module sets (dreamer/ vs fluids/), no collisions, single parallel wave. | ✓ |
| One plan, sequential | Single plan, dreamer gap first then fluids. Simpler tracking, serializes unrelated work, mixes two module domains in one files_modified list. | |
| Two separate phases | Split into 40.1 dreamer + 40.2 fluids. Cleanest but requires roadmap renumbering — heavier process overhead for a close-gap insertion. | |

**User's choice:** Two plans, parallel
**Notes:** Disjoint module sets enable a single parallel wave; no file collisions. Stays a single 40.1 roadmap entry (not renumbered).

---

## Claude's Discretion

- Exact `_find_latest_checkpoint` signature shape (additive `checkpoint_dir=None` param vs. caller-resolved-dir) — provided custom-dir resume is wired and default-dir behavior preserved (D-01).
- Exact TWO_WAY feedback PhiFlow expression in `step()` — provided it diverges from ONE_WAY on thin instruments and the structural test observes the feedback branch (D-08).
- Exact physical-magnitude test fixture (ramp slope, domain size, mask geometry, tolerance) — provided it hand-computes F = -∇p · V_mask and asserts within a stated tolerance (D-04).
- Exact subprocess stub mechanism (mock / monkeypatch / fake subclass) in the CPU resume test — provided it asserts load_checkpoint is called with the custom path (D-03).
- Whether to update the `coupling_substeps` field description text to match D-06's `dt / coupling_substeps` semantics (recommended — current "reuses substep_dt" phrasing is now wrong-by-construction).

## Deferred Ideas

- Removing `coupling_mode` / `coupling_substeps` — alternative disposition explicitly NOT chosen (D-05); documented fallback if implementation proves too destabilizing.
- WR-03 (tip Box absorbed by infinite shaft), WR-04 (zero-y-extent bounds validator) — Phase 38 advisories NOT in this phase; future fluids-cleanup phase.
- WR-05 (zip strict=True) — already fixed by `77a8e71`.
- `embodied.run.train` as training driver — locked out in Phase 40 (D-07); not revisited.
- Standalone GitHub-GPU-runner enablement + `dreamer-gpu` CI GREEN — separate ops USER ACTION ITEM, unchanged by this phase; this phase's CPU resume test does NOT substitute for GPU runtime observation.
- Renumbering 40.1 into two separate roadmap phases — explicitly NOT chosen (D-09).