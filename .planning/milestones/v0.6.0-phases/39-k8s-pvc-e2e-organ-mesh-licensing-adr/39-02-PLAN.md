---
phase: 39-k8s-pvc-e2e-organ-mesh-licensing-adr
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/0001-organ-mesh-licensing.md
  - docs/adr/README.md
autonomous: true
requirements:
  - ASET-06
user_setup: []

must_haves:
  truths:
    - docs/adr/0001-organ-mesh-licensing.md exists in MADR format (Context / Considered Options / Decision Outcome + Rationale / Consequences / References) with status: accepted
    - The ADR records procedural generation as the DEFAULT organ-mesh source and rejects surgtoolloc
    - The ADR's PRIMARY rejection rationale is modality mismatch — SurgToolLoc is 24,695 endoscopic video clips with tool-presence labels, NOT organ geometry (cited from https://surgtoolloc23.grand-challenge.org/data-description/)
    - The ADR's SECONDARY rejection rationale is licensing incompatibility — MICCAI/EndoVis challenge guidelines prohibit commercial use and redistribution (cited from https://surgtoolloc23.grand-challenge.org/challenge-guidelines/)
    - The ADR cites the public challenge-guidelines URL AND the data-description URL so the rejection is auditable
    - The ADR quotes verbatim clause 2 text containing the phrase "neither pass it on to a third party nor use it for any publication or for commercial uses"
    - docs/adr/README.md indexes ADR-0001 as the first entry
  artifacts:
    - docs/adr/0001-organ-mesh-licensing.md
    - docs/adr/README.md
  key_links:
    - ADR References section -> https://surgtoolloc23.grand-challenge.org/challenge-guidelines/
    - ADR References section -> https://surgtoolloc23.grand-challenge.org/data-description/
    - docs/adr/README.md -> docs/adr/0001-organ-mesh-licensing.md
  prohibitions:
    - The ADR MUST NOT quote the private dataset EULA (gated behind the agreement form at isi.challenges@intusurg.com) — only the PUBLIC challenge-guidelines page is citable
    - The ADR MUST NOT disclose or link to real dataset samples, downloadable artifacts, or any participant-only material
    - The ADR MUST NOT claim SurgToolLoc contains organ meshes (it does not — it is video + tool-presence labels)
    - The ADR MUST NOT mark SurgToolLoc as "accepted" or "tentative" — the decision status for procedural-generation-as-default is `accepted`; surgtoolloc is explicitly `rejected`
---

<objective>
Record the repo's first ADR documenting the organ-mesh licensing decision (ASET-06): procedural generation is the default organ-mesh source; SurgToolLoc is rejected on dual grounds — modality mismatch (endoscopic video + tool-presence labels, not organ geometry) and licensing incompatibility (MICCAI/EndoVis challenge guidelines prohibit commercial use + redistribution). The ADR cites the public challenge-guidelines URL and quotes the verbatim non-commercial clause text so future asset work has a single, auditable source of truth.

Purpose: Give future organ-mesh/asset phases a cite-able closure artifact so the surgtoolloc question does not relitigate; satisfy SC#4 (decision recorded) and SC#5 (auditable citation).
Output: docs/adr/0001-organ-mesh-licensing.md (MADR format) + docs/adr/README.md index.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-k8s-pvc-e2e-organ-mesh-licensing-adr/39-RESEARCH.md
@.planning/phases/39-k8s-pvc-e2e-organ-mesh-licensing-adr/39-PATTERNS.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/adr/0001-organ-mesh-licensing.md (MADR)</name>
  <files>docs/adr/0001-organ-mesh-licensing.md</files>
  <read_first>
    - .planning/phases/39-k8s-pvc-e2e-organ-mesh-licensing-adr/39-RESEARCH.md (## Summary Workstream B; ## Code Examples — Workstream B verbatim license clauses; ## Common Pitfalls #6 — do NOT cite the private EULA; ## Assumptions Log A3 — ADR location; ## Open Question #3 — MADR vs Nygard)
    - .planning/phases/39-k8s-pvc-e2e-organ-mesh-licensing-adr/39-PATTERNS.md (docs/adr/0001-organ-mesh-licensing.md row — "no analog, repo's first ADR"; closest formatting analog docs/ARCHITECTURE.md + README.md)
    - docs/ARCHITECTURE.md (formatting conventions — heading style, prose tone, code-fence usage) if it exists
    - .planning/REQUIREMENTS.md (ASET-06 line 38 + context line 70 — confirm the requirement text)
  </read_first>
  <action>
    Create docs/adr/0001-organ-mesh-licensing.md in MADR (Markdown Any Decision Record) format. Required sections in order:

    1. Title heading: `# ADR-0001: Organ-mesh licensing — procedural generation as default, SurgToolLoc rejected`
    2. Metadata block (key/value): `Date: 2026-06-27`, `Status: accepted`, `Deciders: surg-rl maintainers`, `Tags: assets,licensing,organ-mesh`.
    3. `## Context` — explain the v0.4.0 Phase 20 need for 4 organ OBJ meshes, the absence of real mesh assets in the repo (CLAUDE.md Known Limitations), and why a licensing decision is needed before future asset work. Reference ASET-01..05 as the carried-forward asset-track context that this ADR closes.
    4. `## Considered Options` — list at least two options with one-paragraph prose each:
       - Option 1: Procedural generation (trimesh primitives / parametric organ meshes) — DEFAULT.
       - Option 2: SurgToolLoc dataset — REJECTED.
       - (Optional) Option 3: Other public organ-mesh datasets (e.g. KAIST Abdomen, MeshLab-organ repositories) — note as future candidates if photorealism is later required, out of scope for this ADR.
    5. `## Decision Outcome` — state: procedural generation is the DEFAULT organ-mesh source; SurgToolLoc is REJECTED. Mark the decision status as `accepted`.
    6. `## Rationale` — two required grounds, BOTH cited:
       - PRIMARY (modality mismatch): SurgToolLoc is 24,695 endoscopic video clips with tool-presence labels, NOT organ geometry. There are NO organ meshes in SurgToolLoc. Cite https://surgtoolloc23.grand-challenge.org/data-description/ as the source of the modality fact.
       - SECONDARY (licensing incompatibility): The MICCAI/EndoVis challenge guidelines prohibit commercial use and redistribution. Quote clause 2 VERBATIM in a blockquote: `Your team will use the provided data only in the scope of the challenge and neither pass it on to a third party nor use it for any publication or for commercial uses.` Cite https://surgtoolloc23.grand-challenge.org/challenge-guidelines/ as the source. Optionally also quote clause 13 (`The data used for SurgToolLoc 2023 can only be used for publication purposes after the results of this challenge have been submitted for publication.`).
       - State explicitly that the dataset-specific EULA is gated behind an agreement form (isi.challenges@intusurg.com) and is NOT publicly citable; the public challenge-guidelines page is the authoritative citable source for the non-commercial / no-redistribution terms (RESEARCH.md Pitfall #6).
    7. `## Consequences` — positive: MIT-clean, reproducible, no external license dependency, matches the existing `assets/` primitive-fallback approach. Negative: lower photorealism than real organ meshes; future asset phases may need to revisit if a validated public organ-mesh dataset becomes available under a permissive license.
    8. `## References` — bullet list with the two public URLs:
       - `https://surgtoolloc23.grand-challenge.org/challenge-guidelines/`
       - `https://surgtoolloc23.grand-challenge.org/data-description/`
       - Optionally the arXiv:2305.07152 overview paper (Intuitive Surgical SurgToolLoc and SurgVU Challenges Results: 2022-2025).
       - Optionally a link to ASET-06 in REQUIREMENTS.md.

    Use the EXACT strings `challenge-guidelines` and `neither pass it on to a third party nor use it for any publication or for commercial uses` — the SC#5 auditability acceptance gate greps for both. Do NOT paraphrase the clause. Do NOT quote the private EULA. Do NOT link to downloadable dataset files.
  </action>
  <verify>
    <automated>test -f docs/adr/0001-organ-mesh-licensing.md && grep -q 'ADR-0001' docs/adr/0001-organ-mesh-licensing.md && grep -q 'Status: accepted' docs/adr/0001-organ-mesh-licensing.md && grep -q 'procedural generation' docs/adr/0001-organ-mesh-licensing.md && grep -qi 'rejected' docs/adr/0001-organ-mesh-licensing.md && grep -q 'challenge-guidelines' docs/adr/0001-organ-mesh-licensing.md && grep -q 'data-description' docs/adr/0001-organ-mesh-licensing.md && grep -F 'neither pass it on to a third party nor use it for any publication or for commercial uses' docs/adr/0001-organ-mesh-licensing.md && grep -qi 'modality' docs/adr/0001-organ-mesh-licensing.md && grep -qi 'endoscopic' docs/adr/0001-organ-mesh-licensing.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/adr/0001-organ-mesh-licensing.md exists and contains `ADR-0001`
    - The ADR contains `Status: accepted`
    - The ADR contains the phrase `procedural generation` (as the default) and the word `rejected` (for surgtoolloc)
    - The ADR contains the literal `challenge-guidelines` (the SC#5 auditability grep token)
    - The ADR contains the literal `data-description`
    - The ADR contains the verbatim clause text `neither pass it on to a third party nor use it for any publication or for commercial uses` (the SC#5 auditability grep token)
    - The ADR references both URLs: `https://surgtoolloc23.grand-challenge.org/challenge-guidelines/` and `https://surgtoolloc23.grand-challenge.org/data-description/`
    - The ADR names modality mismatch (endoscopic video + tool-presence labels, not organ geometry) as the PRIMARY rationale and licensing as the SECONDARY rationale
    - The ADR does NOT quote a private EULA and does NOT link to downloadable dataset files
  </acceptance_criteria>
  <done>ADR-0001 exists in MADR format, status accepted, procedural generation = default, surgtoolloc = rejected on dual cited grounds, verbatim clause 2 quoted, both public URLs in References — SC#4 and SC#5 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/adr/README.md index</name>
  <files>docs/adr/README.md</files>
  <read_first>
    - docs/adr/0001-organ-mesh-licensing.md (created in Task 1 — index entry must match its title + status)
    - docs/README.md (if it exists — match the repo's doc-index page style) or README.md (root) for heading/prose conventions
  </read_first>
  <action>
    Create docs/adr/README.md as a short index page for the ADR directory. Heading: `# Architecture Decision Records`. A one-paragraph intro stating that ADRs record significant decisions and their rationale so future work has a single source of truth, and that ADR-0001 is the repo's first. A table with columns: `ADR | Title | Status | Date`. One row: `0001 | Organ-mesh licensing — procedural generation as default, SurgToolLoc rejected | accepted | 2026-06-27` linking `0001` to `./0001-organ-mesh-licensing.md`. Optionally note the MADR format convention for future ADRs.
  </action>
  <verify>
    <automated>test -f docs/adr/README.md && grep -q '0001' docs/adr/README.md && grep -q 'organ-mesh-licensing.md' docs/adr/README.md && grep -qi 'accepted' docs/adr/README.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/adr/README.md exists and contains `0001`, a link to `0001-organ-mesh-licensing.md`, and the word `accepted`
    - The index title and ADR-0001 row match the title and status in docs/adr/0001-organ-mesh-licensing.md
  </acceptance_criteria>
  <done>docs/adr/README.md indexes ADR-0001 with its title, status, and date, linking to the ADR file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public web (challenge-guidelines URL) -> ADR document | The ADR quotes verbatim text from a public challenge page; the citation must remain verifiable by a future auditor without a login wall |
| ADR document -> public repo readers | The ADR is a public-facing document in the repo; it must not disclose private/participant-only material |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-06 | Information Disclosure | ADR quotes the private SurgToolLoc dataset EULA (gated behind isi.challenges@intusurg.com agreement form) | medium | mitigate | ADR cites ONLY the public challenge-guidelines page; the action explicitly forbids quoting the private EULA and states the EULA is gated; acceptance criterion greps for the public-clause text, not the private EULA |
| T-39-07 | Information Disclosure | ADR links to downloadable dataset samples or participant-only artifacts | medium | mitigate | ADR references only the two public challenge-site URLs (guidelines + data-description); the action forbids links to downloadable dataset files; the References section is restricted to public URLs |
| T-39-08 | Repudiation | ADR decision not auditable (no verbatim clause, no URL) | medium | mitigate | ADR quotes clause 2 verbatim AND cites the challenge-guidelines URL; the SC#5 acceptance gate greps for both `challenge-guidelines` and the verbatim clause text; a future auditor can re-fetch the URL and find the clause |
| T-39-09 | Spoofing / Tampering | ADR misrepresents SurgToolLoc as containing organ meshes | low | mitigate | ADR states the modality fact (24,695 endoscopic video clips + tool-presence labels, NOT organ geometry) and cites the data-description URL; the prohibition block forbids claiming organ meshes exist in SurgToolLoc |
</threat_model>

<verification>
- `test -f docs/adr/0001-organ-mesh-licensing.md` and the SC#5 grep tokens (`challenge-guidelines`, `neither pass it on to a third party nor use it for any publication or for commercial uses`) are present
- `test -f docs/adr/README.md` and the index links to the ADR file
- Manual spot-check: open the two cited URLs and confirm the quoted clause 2 text appears verbatim on the challenge-guidelines page (RESEARCH.md verified this on 2026-06-27)
</verification>

<success_criteria>
- SC#4: An ADR document records the organ-mesh licensing decision — procedural generation is the default; surgtoolloc is rejected with cited rationale (modality mismatch primary; MICCAI/EndoVis non-commercial clause secondary)
- SC#5: The ADR cites the public challenge-guidelines URL AND quotes the verbatim license clause text so the rejection is auditable
- ASET-06 closed
</success_criteria>

<output>
Create `.planning/phases/39-k8s-pvc-e2e-organ-mesh-licensing-adr/39-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (Plan 02)

- `docs/adr/0001-organ-mesh-licensing.md` — MADR-format ADR (status: accepted) recording procedural-generation-as-default, surgtoolloc-rejected, with verbatim clause 2 + both public challenge-site URLs
- `docs/adr/README.md` — ADR directory index (ADR-0001 row, status accepted, date 2026-06-27, link to the ADR file)