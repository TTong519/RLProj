---
phase: 40-real-dreamerv3-integration-sentinel-flip
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/surg_rl/dreamer/subprocess.py
  - src/surg_rl/dreamer/wrapper.py
  - tests/dreamer/test_dreamerv3_regression_guard.py
  - tests/test_dreamer_subprocess.py
autonomous: true
requirements:
  - DMV3-07
  - DMV3-09
user_setup: []

must_haves:
  truths:
    # DMV3-09 (regression guard half)
    - "test_dreamerv3_regression_guard.py fails closed if _build_agent can yield a None-valued return (stub regression)"
    # DMV3-07 (agent construction half — DMV3-07 split with 40-02 which owns train/eval/save/load)
    - "_build_agent constructs a real dreamerv3.Agent via the PyPI 1.5.0 object API (4-arg ctor) and returns a bundle carrying agent + env + checkpoint + step + replay"
    # SC#5 / JAX isolation
    - "No top-level 'import jax' or 'import dreamerv3' in subprocess.py module scope; all such imports live inside _build_agent's function body (and downstream stubs)"
    - "Importing surg_rl.dreamer.subprocess in the parent process does NOT pull jax or dreamerv3 into sys.modules (extended JAX-leak guard)"
    # D-05 / D-04
    - "_build_agent reuses GymToEmbodiedWrapper (NOT embodied.wrappers.FromGym) and targets Agent(obs_space, act_space, step, config) — 4 args, PyPI 1.5.0"
    # D-09 checkpoint registration at construct time
    - "_build_agent registers embodied.Checkpoint at models/dreamerv3/{task}_{obs_type}/checkpoint.ckpt with cp.agent / cp.replay / cp.step and calls cp.load_or_save() to resume-or-init"
  artifacts:
    - "tests/dreamer/test_dreamerv3_regression_guard.py (NEW — source-inspection, no GPU)"
    - "tests/test_dreamer_subprocess.py (extended JAX-leak guard)"
    - "src/surg_rl/dreamer/subprocess.py _build_agent real implementation"
    - "src/surg_rl/dreamer/wrapper.py action_space dict-shape fix (discretion — VERIFY against installed Agent ctor)"
  key_links:
    - "_run_subprocess_loop CONFIG handler (subprocess.py:80-84) calls _build_agent(config) and passes the returned bundle to _train_loop / _evaluate / _save_checkpoint / _load_checkpoint"
    - "_build_agent → GymToEmbodiedWrapper → SurgicalEnv (child-process construction, JAX-safe)"
    - "_build_agent → embodied.Checkpoint.load_or_save() (DMV3-08 resume hook, exercised by 40-03 resume test)"
  prohibitions:
    # SC#1 unchanged surfaces
    - "DO NOT modify _JsonStdout, DreamerSubprocess, _subprocess_main, _run_subprocess_loop message dispatch, XLA_PYTHON_CLIENT_MEM_FRACTION=0.4, XLA_PYTHON_CLIENT_PREALLOCATE=false"
    # SC#5
    - "NO 'import jax' or 'import dreamerv3' at module top of subprocess.py (must stay inside function bodies)"
    - "DO NOT import embodied.wrappers.FromGym (pins gym==0.19.0, conflicts with gymnasium>=0.29.0) — use the project GymToEmbodiedWrapper per D-05"
    # D-04
    - "DO NOT target the current-repo factory API (make_agent/make_replay/make_stream/make_logger + elements.*) — the pin installs the PyPI 1.5.0 object API"
    - "DO NOT change the dreamerv3~=1.5.0 pin or introduce the elements package"
    # D-09
    - "DO NOT add a .pt compat shim or dual-glob — .pt naming is retired (40-03 retires the glob)"
    # scope
    - "DO NOT modify _train_loop / _evaluate / _save_checkpoint / _load_checkpoint (owned by 40-02)"
    - "DO NOT flip tests/dreamer/test_dreamerv3_subprocess_e2e.py (owned by 40-02)"
    - "DO NOT change DreamerConfig schema (additive phase, D-06)"
---

<objective>
Real `_build_agent` implementation + the DMV3-09 regression guard + extended JAX-leak guard.

Purpose: Replace the first of the 5 stubs (`_build_agent`) with a real DreamerV3 agent
construction against the PyPI 1.5.0 object API, and stand up the two CPU-runnable guards
(regression guard + import-leak guard) that protect SC#5 and the sentinel flip. This is
the TDD RED→GREEN foundation: the regression guard is RED against the current stub
(`_build_agent` yields a None-valued return) and GREEN once the real implementation lands
in the same plan. The other 4 stubs (_train_loop / _evaluate / _save_checkpoint /
_load_checkpoint) are owned by 40-02; this plan only touches `_build_agent` and the two
CPU guard tests.

Output: `tests/dreamer/test_dreamerv3_regression_guard.py` (NEW), extended
`tests/test_dreamer_subprocess.py` JAX-leak guard, real `_build_agent` in
`src/surg_rl/dreamer/subprocess.py`, optional `action_space` dict-shape fix in
`src/surg_rl/dreamer/wrapper.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-real-dreamerv3-integration-sentinel-flip/40-CONTEXT.md
@.planning/phases/40-real-dreamerv3-integration-sentinel-flip/40-RESEARCH.md
@.planning/phases/40-real-dreamerv3-integration-sentinel-flip/40-AI-SPEC.md
@src/surg_rl/dreamer/subprocess.py
@src/surg_rl/dreamer/wrapper.py
@src/surg_rl/dreamer/training.py
@src/surg_rl/dreamer/__init__.py
@tests/dreamer/test_dreamerv3_subprocess_e2e.py
@tests/test_dreamer_subprocess.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Regression guard + extended JAX-leak guard</name>
  <files>tests/dreamer/test_dreamerv3_regression_guard.py, tests/test_dreamer_subprocess.py</files>
  <read_first>
    - src/surg_rl/dreamer/subprocess.py (current _build_agent stub at lines 125-129 — the None-valued return the guard must catch)
    - src/surg_rl/dreamer/__init__.py (LazyImport guard — the parent-package import path the leak guard protects)
    - tests/test_dreamer_subprocess.py (existing TestProcessIsolationImport at lines 110-127 — the partial JAX-leak guard to extend; note line 126 already asserts 'jax' not in sys.modules after import)
    - tests/dreamer/test_dreamerv3_subprocess_e2e.py (lines 42-49 — the module-level skipif pattern; the regression guard MUST NOT carry this skipif so it runs on macOS CPU)
    - 40-RESEARCH.md "Sentinel Inversion Design" section (the _build_agent is None regression guard spec)
    - 40-CONTEXT.md D-10 (regression guard runs without GPU, in normal CPU matrix on every PR)
  </read_first>
  <behavior>
    - Test 1 (test_dreamerv3_regression_guard.py — no GPU, no skipif): inspecting the source of surg_rl.dreamer.subprocess._build_agent and finding a None-valued return statement → test FAILS with a stub-regression message. Against the CURRENT stub it MUST fail (RED).
    - Test 2 (test_dreamerv3_regression_guard.py): the guard uses inspect.getsource(_build_agent) + either an AST Return(value=Constant(None)) check or a string-containment check — either mechanism fails closed when a None-valued return is present.
    - Test 3 (tests/test_dreamer_subprocess.py extended): after `import surg_rl.dreamer.subprocess`, BOTH 'jax' AND 'dreamerv3' are absent from sys.modules (current test only checks 'jax' at line 126 — extend it to also assert 'dreamerv3' not in sys.modules).
    - Test 4 (tests/test_dreamer_subprocess.py extended): a source-inspection assertion that subprocess.py module scope contains no top-level 'import jax' or 'import dreamerv3' statement (use inspect.getsource of the module + ast.walk for ast.Import nodes at module level whose names include jax/dreamerv3/embodied).
  </behavior>
  <action>
    Create tests/dreamer/test_dreamerv3_regression_guard.py as a NEW module with NO module-level
    pytestmark skipif (it MUST run on macOS CPU per D-10). Import inspect and ast. Define a test
    that imports surg_rl.dreamer.subprocess._build_agent, calls inspect.getsource(_build_agent),
    and asserts the source contains no None-valued return statement — use ast.parse on the source
    and walk for ast.Return nodes whose .value is ast.Constant(value=None) (this is more robust
    than a naive string-containment check and avoids false positives on docstrings/comments
    mentioning None). The failure message must name DMV3-09 and explain the stub-regression guard
    purpose. Do NOT add any GPU/dreamerv3/jax skipif to this module — it runs unconditionally.

    Extend tests/test_dreamer_subprocess.py: in the existing TestProcessIsolationImport class
    (line 110), augment test_no_jax_or_dreamerv3_loaded_in_main_process (line 119) so it asserts
    BOTH 'jax' not in sys.modules AND 'dreamerv3' not in sys.modules after a fresh import of
    surg_rl.dreamer.subprocess (pop both from sys.modules first). Add a NEW test in the same
    class that uses inspect.getsource + ast.walk to assert subprocess.py has no module-level
    ast.Import or ast.ImportFrom nodes importing jax, dreamerv3, or embodied (these must live
    inside function bodies only — SC#5). Do NOT modify any other test class in that file.

    Run the regression guard test — it MUST fail (RED) against the current _build_agent stub
    because the stub's terminator yields None. The extended JAX-leak guard tests MUST pass
    already (the current code is JAX-isolation-clean). Commit the RED state with
    test(40-01): add _build_agent regression guard (RED) + extend JAX-leak guard.
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/dreamer/test_dreamerv3_regression_guard.py tests/test_dreamer_subprocess.py::TestProcessIsolationImport -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/dreamer/test_dreamerv3_regression_guard.py exists and has NO pytestmark skipif (grep -v '^#' file | grep -c 'pytest.mark.skipif' == 0)
    - PYTHONPATH=src pytest tests/dreamer/test_dreamerv3_regression_guard.py -v exits NON-zero (RED) against the current stub
    - test_dreamerv3_regression_guard.py failure message contains the token DMV3-09
    - tests/test_dreamer_subprocess.py::TestProcessIsolationImport asserts both 'jax' and 'dreamerv3' absent from sys.modules after import
    - tests/test_dreamer_subprocess.py::TestProcessIsolationImport has a NEW module-level-import source-inspection test asserting no top-level jax/dreamerv3/embodied imports in subprocess.py
    - PYTHONPATH=src pytest tests/test_dreamer_subprocess.py::TestProcessIsolationImport -v exits 0 (existing JAX isolation stays clean)
  </acceptance_criteria>
  <done>
    Regression guard test exists, runs on CPU without skipif, and FAILS against the current
    None-valued stub return. Extended JAX-leak guard passes. RED state committed.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Real _build_agent against PyPI 1.5.0 object API</name>
  <files>src/surg_rl/dreamer/subprocess.py, src/surg_rl/dreamer/wrapper.py</files>
  <read_first>
    - src/surg_rl/dreamer/subprocess.py (current _build_agent stub lines 125-129; the dispatch at lines 80-84, 87-122 that calls _build_agent and gates on 'agent is None'; the cleanup agent.close() at line 122 wrapped in contextlib.suppress)
    - src/surg_rl/dreamer/wrapper.py (GymToEmbodiedWrapper — observation_space lines 50-72, action_space lines 74-81; the wrapper already produces image/state + is_first/is_last/is_terminal dict keys)
    - src/surg_rl/dreamer/training.py (_create_scene_for_task lines 14-169 and _create_env lines 172-180 — the helpers _build_agent must call to construct the SurgicalEnv inside the child; the {task}_{obs_type} checkpoint dir convention at line 235)
    - 40-RESEARCH.md "PyPI 1.5.0 Agent constructor" (Agent(obs_space, act_space, step, config) — 4 args; step is embodied.Counter(); act_space extracted via act_space['action']), "PyPI 1.5.0 Checkpoint API" (attribute-registration pattern cp.agent/cp.replay/cp.step then cp.load_or_save()), and "PyPI 1.5.0 FromGym" (use GymToEmbodiedWrapper NOT FromGym)
    - 40-CONTEXT.md D-04 (PyPI 1.5.0 object API, no factory functions), D-05 (GymToEmbodiedWrapper), D-09 (embodied.Checkpoint + *.ckpt, attribute registration), D-06 (obs_type stays pixels/state)
    - 40-AI-SPEC.md Section 4 "Core Pattern" and "State Management" (the bundle shape + checkpoint registration)
    - tests/dreamer/test_dreamerv3_regression_guard.py (the RED test from Task 1 — the GREEN must make it pass)
  </read_first>
  <behavior>
    - _build_agent source contains no None-valued return statement → the Task 1 regression guard PASSES (GREEN).
    - _build_agent returns a bundle object (dict or small namespace) carrying keys/attrs: agent, env, checkpoint, step, replay (the downstream _train_loop / _evaluate / _save_checkpoint / _load_checkpoint in 40-02 unpack this bundle).
    - All jax / dreamerv3 / embodied / optax imports are INSIDE the _build_agent function body (SC#5) — no new module-top imports in subprocess.py.
    - The bundle's env is a GymToEmbodiedWrapper wrapping a SurgicalEnv built via _create_scene_for_task + _create_env (reused, not reimplemented).
    - The bundle's checkpoint is an embodied.Checkpoint pointing at models/dreamerv3/{task}_{obs_type}/checkpoint.ckpt with cp.agent / cp.replay / cp.step registered, and cp.load_or_save() has been called (resume-or-init per D-09).
  </behavior>
  <action>
    Replace the body of _build_agent in src/surg_rl/dreamer/subprocess.py (currently lines
    125-129). Keep the function signature _build_agent(config: dict[str, Any]) -> Any unchanged
    (the CONFIG handler at line 83 calls it positionally). Inside the function body — and ONLY
    inside the function body — import embodied, dreamerv3.agent.Agent, embodied.replay.Replay,
    and the project helpers surg_rl.dreamer.wrapper.GymToEmbodiedWrapper +
    surg_rl.dreamer.training._create_scene_for_task + _create_env (lazy/local imports per SC#5
    and CLAUDE.md rl-subpackage import-chain fragility rule). Construct the scene via
    _create_scene_for_task(config['task'], config['obs_type'], tuple(config['pixel_resolution']))
    and the env via _create_env(scene); wrap it in GymToEmbodiedWrapper(env, obs_type=...,
    pixel_resolution=...). Build an embodied.Counter() for the step argument (NOT a plain int —
    the Agent ctor extracts act_space['action'] and registers step on the checkpoint). Build the
    agent config via embodied.Config(...) from the config dict (VERIFY the exact ctor against the
    installed dreamerv3 1.5.0 package — the AI-SPEC Section 4 notes items marked VERIFY). Construct
    Agent(wrapped.obs_space, wrapped.act_space, step, agent_config) — 4 positional args, PyPI 1.5.0
    object API per D-04. Construct an embodied.replay.Replay (VERIFY ctor args — capacity/length
    from config). Construct the checkpoint: cp = embodied.Checkpoint(Path('models/dreamerv3') /
    f"{config['task']}_{config['obs_type']}" / 'checkpoint.ckpt'); register cp.step = step,
    cp.agent = agent, cp.replay = replay (Checkpoint.__setattr__ requires save()/load() on each —
    Agent, Replay, and Counter all qualify; do NOT register the env or driver). Call
    cp.load_or_save() to resume-or-init per D-09. Return a bundle dict (or small SimpleNamespace)
    with keys/attrs agent, env, checkpoint, replay, step so the downstream stubs in 40-02 can
    unpack it. Ensure the bundle is safe for the cleanup at line 122 — a plain dict has no close()
    method and contextlib.suppress(Exception) swallows the AttributeError; alternatively give the
    bundle a close() that closes env and agent (Claude's discretion). Do NOT modify the
    _run_subprocess_loop dispatch, the 'agent is None' gates, _JsonStdout, _subprocess_main, or
    the XLA env-var setup (SC#1). Do NOT touch _train_loop / _evaluate / _save_checkpoint /
    _load_checkpoint (40-02 owns them).

    In src/surg_rl/dreamer/wrapper.py, update the action_space property (lines 74-81) so it
    returns a dict with keys 'action' (the existing spaces.Box) and 'reset' (a spaces.Discrete(2)
    or spaces.Box shape (1,) bool) — the Agent ctor extracts act_space['action'] per research A4
    and AI-SPEC Section 4. VERIFY against the installed Agent ctor; if the bare spaces.Box return
    is already accepted, leave the property unchanged and note that in the summary. Keep the
    observation_space property (lines 50-72) unchanged — it already emits image/state +
    is_first/is_last/is_terminal.

    After writing, run the regression guard test — it MUST now PASS (GREEN: no None-valued
    return remains in _build_agent source). Run the extended JAX-leak guard — it MUST still
    pass (no new module-top imports). Run the full CPU test suite to confirm no regressions:
    PYTHONPATH=src pytest tests/ -m 'not integration' -v. Commit GREEN with
    feat(40-01): real _build_agent (PyPI 1.5.0 object API) + checkpoint registration.
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/dreamer/test_dreamerv3_regression_guard.py tests/test_dreamer_subprocess.py::TestProcessIsolationImport -v && PYTHONPATH=src pytest tests/ -m 'not integration' -v -q</automated>
  </verify>
  <acceptance_criteria>
    - PYTHONPATH=src pytest tests/dreamer/test_dreamerv3_regression_guard.py -v exits 0 (GREEN — _build_agent has no None-valued return)
    - src/surg_rl/dreamer/subprocess.py _build_agent contains the call Agent( with 4 positional args (obs_space, act_space, step, config) — grep -v '^#' file | grep -c 'Agent(' >= 1
    - src/surg_rl/dreamer/subprocess.py has NO module-level 'import jax' or 'import dreamerv3' or 'import embodied' (ast.walk at module level returns no such Import nodes — the extended JAX-leak guard test passes)
    - src/surg_rl/dreamer/subprocess.py _build_agent contains embodied.Checkpoint( and cp.load_or_save (checkpoint registration + resume-or-init per D-09)
    - src/surg_rl/dreamer/subprocess.py _build_agent contains GymToEmbodiedWrapper (D-05 reuse, NOT FromGym)
    - src/surg_rl/dreamer/subprocess.py _build_agent contains no reference to make_agent / make_replay / make_stream / make_logger / elements (D-04 — PyPI 1.5.0 object API, not the factory API)
    - PYTHONPATH=src pytest tests/ -m 'not integration' -v exits 0 (no regressions in the CPU suite; GPU-gated dreamer tests still skip on macOS)
    - wrapper.py action_space property returns a dict with an 'action' key (OR the summary documents that the installed Agent ctor accepts the bare Box — discretion per D-05/A4)
  </acceptance_criteria>
  <done>
    _build_agent is a real implementation against the PyPI 1.5.0 object API, returns a bundle
    (agent + env + checkpoint + replay + step), registers embodied.Checkpoint with
    cp.load_or_save() for DMV3-08 resume, keeps all JAX/dreamerv3/embodied imports inside the
    function body (SC#5), and the regression guard + JAX-leak guard both pass on CPU.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent↔child subprocess pipe | multiprocessing.Pipe (intra-process, not network); JSON-over-stdio. The CONFIG dict crosses here. No new boundary introduced by this plan. |
| child↔disk (models/dreamerv3/) | embodied.Checkpoint writes checkpoint.ckpt to local disk; no network exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-01 | Tampering | CONFIG message dict deserialized in child | low | accept | The CONFIG dict is already deserialized via json.loads in _run_subprocess_loop (unchanged SC#1); no new untrusted-input path is introduced by _build_agent — it reads config keys from a dict that already crossed the vetted pipe. |
| T-40-02 | Information Disclosure | Checkpoint file (pickle of numpy arrays) at models/dreamerv3/{task}_{obs_type}/ | low | accept | Checkpoints are on local disk with process-inherited permissions; no network exposure. Pickle deserialization is via embodied.Checkpoint (vetted in Phase 24). Same risk profile as existing Phase 24/30 checkpoint path. |
| T-40-03 | Denial of Service | JAX GPU memory preallocation | medium | mitigate | XLA_PYTHON_CLIENT_MEM_FRACTION=0.4 + PREALLOCATE=false set in _subprocess_main BEFORE import jax (UNCHANGED SC#1 — this plan does not touch _subprocess_main). Mitigation already in place. |
</threat_model>

<verification>
- Regression guard (CPU): PYTHONPATH=src pytest tests/dreamer/test_dreamerv3_regression_guard.py -v → GREEN
- JAX-leak guard (CPU): PYTHONPATH=src pytest tests/test_dreamer_subprocess.py::TestProcessIsolationImport -v → GREEN
- Full CPU suite: PYTHONPATH=src pytest tests/ -m 'not integration' -v → GREEN (GPU-gated dreamer tests skip on macOS)
- Source assertions: _build_agent has no None-valued return; no module-top jax/dreamerv3/embodied imports; uses Agent(4-arg) + embodied.Checkpoint + GymToEmbodiedWrapper; no factory API / elements / FromGym references.
</verification>

<success_criteria>
- DMV3-07 (partial — agent construction): _build_agent returns a real Agent bundle against the PyPI 1.5.0 object API.
- DMV3-09 (partial — regression guard): test_dreamerv3_regression_guard.py passes on CPU and would fail closed on any future None-valued return.
- SC#5 preserved: no JAX/dreamerv3/embodied imports at module top of subprocess.py; parent-package import does not pull jax/dreamerv3.
- D-04/D-05/D-09 honored: PyPI 1.5.0 object API, GymToEmbodiedWrapper, embodied.Checkpoint with load_or_save().
- No regressions in the CPU test suite.
</success_criteria>

<output>
Create .planning/phases/40-real-dreamerv3-integration-sentinel-flip/40-01-SUMMARY.md when done.
</output>

## Artifacts this phase produces

### Plan 40-01 (this plan)
| File | Symbol | Kind |
|------|--------|------|
| tests/dreamer/test_dreamerv3_regression_guard.py | test_dreamerv3_regression_guard (module + test functions) | NEW test — source-inspection, no GPU |
| tests/test_dreamer_subprocess.py | TestProcessIsolationImport (extended: +dreamerv3 sys.modules check, +module-level-import ast guard) | MODIFIED test — extended JAX-leak guard |
| src/surg_rl/dreamer/subprocess.py | _build_agent (real implementation) | MODIFIED — stub → real Agent bundle construction |
| src/surg_rl/dreamer/wrapper.py | GymToEmbodiedWrapper.action_space (dict shape) | MODIFIED (discretion) — 'action' key for Agent ctor |

### Symbols downstream plans should treat as newly-created (not pre-existing API surface)
- `_build_agent` return bundle shape: `{agent, env, checkpoint, replay, step}` — 40-02's `_train_loop` / `_evaluate` / `_save_checkpoint` / `_load_checkpoint` unpack this bundle.
- `test_dreamerv3_regression_guard.py` — new test module; 40-02 must not duplicate it.