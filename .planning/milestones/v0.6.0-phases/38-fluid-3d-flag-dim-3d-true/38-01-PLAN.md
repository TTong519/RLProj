---
phase: 38-fluid-3d-flag-dim-3d-true
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/surg_rl/scene_definition/schema.py
  - tests/test_fluids/test_schema.py
autonomous: true
requirements: [FLUID-01, FLUID-03]
tags: [fluid, schema, pydantic, validation]

must_haves:
  truths:
    - "FluidConfig accepts dim_3d: bool (default False) additively without touching resolution/_cap_resolution"
    - "FluidConfig accepts grid_size: tuple[int,int,int]|None (default None) additively"
    - "FluidConfig accepts coupling_mode: FluidCouplingMode (default ONE_WAY) and coupling_substeps: int (default 4, ge=1, le=16)"
    - "FluidConfig(dim_3d=True, grid_size=None) raises ValidationError (the SC#3 memory-blow-up guard — hard error, no silent default)"
    - "D-04: _cap_grid_size rejects len!=3, any dim<4, any dim>64; passes None through; accepts anisotropic (Nx,Ny,Nz) 3-tuples (e.g. 64×32×64), each dimension independently bounded — not cubic-only"
    - "Existing 2D FluidConfig fields/defaults and _cap_resolution behave byte-identically (existing test_schema.py tests pass unchanged)"
  artifacts:
    - "src/surg_rl/scene_definition/schema.py: class FluidCouplingMode(str, Enum) with ONE_WAY='one_way', TWO_WAY='two_way'"
    - "src/surg_rl/scene_definition/schema.py: FluidConfig.dim_3d field"
    - "src/surg_rl/scene_definition/schema.py: FluidConfig.grid_size field"
    - "src/surg_rl/scene_definition/schema.py: FluidConfig.coupling_mode field"
    - "src/surg_rl/scene_definition/schema.py: FluidConfig.coupling_substeps field"
    - "src/surg_rl/scene_definition/schema.py: FluidConfig._cap_grid_size field_validator"
    - "src/surg_rl/scene_definition/schema.py: FluidConfig._require_grid_size_when_dim_3d model_validator(mode='after')"
    - "tests/test_fluids/test_schema.py: additive tests for all new fields/validators"
  key_links:
    - "FluidConfig.dim_3d ⇒ FluidSimulator 3D branch (Plan 02) and render_fluid_3d (Plan 03)"
    - "FluidConfig.grid_size ⇒ FluidSimulator 3D StaggeredGrid resolution (Plan 02)"
    - "FluidConfig.coupling_mode/coupling_substeps ⇒ 3D obstacle force path (Plan 02)"
  prohibitions:
    - statement: "MUST NOT modify or rename FluidConfig.resolution, _cap_resolution, FluidBoundaryType, boundary_type, or any existing field/validator default"
      status: resolved
      verification: "PYTHONPATH=src pytest tests/test_fluids/test_schema.py -v (existing tests pass unchanged) + grep -c '_cap_resolution' src/surg_rl/scene_definition/schema.py unchanged"
    - statement: "MUST NOT add a silent default for grid_size when dim_3d=True (hard ValidationError is the SC#3 guard)"
      status: resolved
      verification: "test_grid_size_required_when_dim_3d asserts ValidationError"
---

<objective>
Add the 3D fluid config surface to `FluidConfig` additively (Pydantic v2): the `dim_3d` flag, a separate 3D `grid_size` field with a hard-required-when-dim_3d cross-field rule (SC#3 memory-blow-up guard), the `FluidCouplingMode` str-Enum (ONE_WAY default / TWO_WAY opt-in), and the `coupling_substeps` int. All additions are additive — the 2D `resolution`/`_cap_resolution`/`FluidBoundaryType` surface stays byte-identical.

Purpose: Unblocks all downstream 3D work (FluidSimulator 3D, render_fluid_3d, regression tests). Implements D-01..D-05, D-09, D-12, D-03 (cross-field validator), and the SC#3 memory-bounded-grid guard.
Output: Extended `FluidConfig` schema + additive `test_schema.py` tests covering defaults, validator bounds, anisotropic acceptance, and the required-when-dim_3d hard error.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-fluid-3d-flag-dim-3d-true/38-CONTEXT.md
@.planning/phases/38-fluid-3d-flag-dim-3d-true/38-RESEARCH.md
@.planning/phases/38-fluid-3d-flag-dim-3d-true/38-PATTERNS.md
@.planning/phases/38-fluid-3d-flag-dim-3d-true/38-VALIDATION.md
@src/surg_rl/scene_definition/schema.py
@tests/test_fluids/test_schema.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — write additive schema tests for dim_3d/grid_size/coupling_mode/coupling_substeps validators</name>
  <files>tests/test_fluids/test_schema.py</files>
  <read_first>
    - tests/test_fluids/test_schema.py (existing _cap_resolution tests at lines 47-78, test_defaults 13-25, test_serialization 80-95 — mirror their style; do NOT edit existing tests)
    - src/surg_rl/scene_definition/schema.py lines 1500-1532 (FluidBoundaryType + FluidConfig + _cap_resolution — the exact pattern to mirror)
    - src/surg_rl/scene_definition/schema.py lines 196-205 (BoundingBox.validate_bounds model_validator(mode='after') — the cross-field pattern)
    - .planning/phases/38-fluid-3d-flag-dim-3d-true/38-PATTERNS.md § schema.py (concrete field defs + validator bodies to expect)
    - .planning/phases/38-fluid-3d-flag-dim-3d-true/38-CONTEXT.md decisions D-01..D-05, D-09, D-12, D-03
  </read_first>
  <behavior>
    - test_defaults_dim_3d_off: FluidConfig(enabled=True, bounds=<2D bbox>) has dim_3d==False, grid_size is None, coupling_mode==FluidCouplingMode.ONE_WAY, coupling_substeps==4
    - test_grid_size_required_when_dim_3d: FluidConfig(enabled=True, bounds=<3D bbox with y extent>, dim_3d=True, grid_size=None) raises pydantic.ValidationError
    - test_grid_size_ok_when_dim_3d_true: FluidConfig(..., dim_3d=True, grid_size=(24,24,24)) validates; anisotropic (64,32,64) validates
    - test_cap_grid_size_rejects_too_small: grid_size=(3,24,24) raises ValidationError
    - test_cap_grid_size_rejects_too_large: grid_size=(65,24,24) raises ValidationError
    - test_cap_grid_size_rejects_wrong_len: grid_size=(24,24) raises ValidationError (must be 3-tuple)
    - test_coupling_substeps_bounds: coupling_substeps=0 raises; =17 raises; =1 and =16 accepted
    - test_coupling_mode_enum_values: FluidCouplingMode.ONE_WAY.value == "one_way"; TWO_WAY.value == "two_way"; str(FluidCouplingMode.ONE_WAY) is the enum value (mirrors FluidBoundaryType str-Enum behavior)
    - test_serialization_coupling_mode: model_dump() preserves coupling_mode as Enum (per CLAUDE.md — convert .value before YAML)
  </behavior>
  <action>
    Append a NEW test class `TestFluidConfig3D` (and helpers) to tests/test_fluids/test_schema.py. Use a 3D bounds fixture `BoundingBox(min_corner=Position(x=0.0,y=0.0,z=0.0), max_corner=Position(x=0.3,y=0.3,z=0.3))` for the dim_3d=True cases and the existing 2D fixture for defaults. Import `FluidCouplingMode` from `surg_rl.scene_definition.schema` and `pydantic.ValidationError`. Use `pytest.raises(ValidationError)`. Do NOT edit existing tests/classes. Run the suite — these tests MUST FAIL (RED) because the schema fields/validators do not yet exist. Commit with `test(38-01): add failing 3D FluidConfig schema tests`.
  </action>
  <verify>
    <automated>cd /Users/tt/Documents/RLProj && PYTHONPATH=src pytest tests/test_fluids/test_schema.py -k "dim_3d or grid_size or coupling or cap_grid_size or anisotropic" -v</automated>
  </verify>
  <acceptance_criteria>
    - New tests exist in a TestFluidConfig3D class (or similarly named) appended to test_schema.py; existing test classes/lines unchanged
    - `grep -c 'test_grid_size_required_when_dim_3d' tests/test_fluids/test_schema.py` returns >= 1
    - `grep -c 'test_cap_grid_size_rejects_too_large' tests/test_fluids/test_schema.py` returns >= 1
    - Running the selection above produces failures (RED) referencing missing `dim_3d`/`grid_size`/`FluidCouplingMode` attributes or ValidationError-not-raised
  </acceptance_criteria>
  <done>RED: 9+ new additive schema tests fail with AttributeError/ValidationError-not-raised; existing test_schema.py tests still pass; committed.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — add FluidCouplingMode + dim_3d/grid_size/coupling fields + _cap_grid_size + _require_grid_size_when_dim_3d to FluidConfig</name>
  <files>src/surg_rl/scene_definition/schema.py</files>
  <read_first>
    - src/surg_rl/scene_definition/schema.py lines 1500-1532 (FluidBoundaryType + FluidConfig + _cap_resolution — exact str-Enum + field + field_validator pattern)
    - src/surg_rl/scene_definition/schema.py lines 196-205 (model_validator(mode='after') pattern: raise ValueError, return self)
    - src/surg_rl/scene_definition/schema.py lines 12-22 (model_rebuild / lazy DifficultyLevel — do NOT disturb)
    - .planning/phases/38-fluid-3d-flag-dim-3d-true/38-PATTERNS.md § schema.py (concrete field defs and validator bodies)
    - CLAUDE.md § Pydantic v2 (model_validator(mode='after'): only raise + return self here — no model_copy needed; Enum values stay Enum in model_dump)
  </read_first>
  <action>
    Additively extend FluidConfig (D-01..D-05, D-09, D-12, D-03) per D-NN:
    - Define `class FluidCouplingMode(str, Enum)` immediately after `FluidBoundaryType` (D-09): `ONE_WAY = "one_way"`, `TWO_WAY = "two_way"`, with a one-line docstring. Mirror `FluidBoundaryType` style exactly.
    - Add four fields to `FluidConfig` AFTER `initial_velocity` and BEFORE `_cap_resolution` (do not reorder existing fields):
      * `dim_3d: bool = Field(default=False, description="Enable 3D Eulerian grid fluids")` (D-01)
      * `grid_size: tuple[int, int, int] | None = Field(default=None, description="3D grid resolution (Nx, Ny, Nz); REQUIRED when dim_3d=True. Recommended 24^3=(24,24,24). Each dim capped at 64.")` (D-02)
      * `coupling_mode: FluidCouplingMode = Field(default=FluidCouplingMode.ONE_WAY, description="3D fluid/solid coupling mode (ONE_WAY stable default; TWO_WAY opt-in, unstable on thin instruments)")` (D-09)
      * `coupling_substeps: int = Field(default=4, ge=1, le=16, description="Internal coupling substeps per env step on the 3D obstacle path; reuses substep_dt as per-substep dt.")` (D-12, D-13)
    - Add `@field_validator("grid_size") @classmethod def _cap_grid_size(cls, v: tuple[int,int,int] | None) -> tuple[int,int,int] | None` (D-05): if v is None, return None; if `len(v) != 3` raise ValueError("grid_size must be (nx, ny, nz)"); if any dim < 4 raise ValueError("grid_size must be at least 4 in each dimension"); if any dim > 64 raise ValueError("grid_size capped at 64 per dimension"); return v. Distinct from `_cap_resolution` (min 4, cap 128, len==2). Do NOT touch `_cap_resolution`.
    - Add `@model_validator(mode="after") def _require_grid_size_when_dim_3d(self) -> "FluidConfig"` (D-03): `if self.dim_3d and self.grid_size is None: raise ValueError("grid_size is required when dim_3d=True (set grid_size explicitly; recommended 24^3)")`; `return self` (per CLAUDE.md: only raise + return self — no model_copy needed since no field write). Mirror the `validate_bounds` pattern at lines 196-205.
    - Do NOT modify `resolution`, `_cap_resolution`, `FluidBoundaryType`, `boundary_type`, or any existing field/validator. Do NOT call `model_rebuild()` unless a forward-ref error surfaces (FluidCouplingMode is defined before FluidConfig, so no cycle).
    Run the RED tests; they MUST now pass (GREEN). Then run the full test_schema.py + test_fluid_simulator.py to confirm 2D byte-identical behavior. Commit `feat(38-01): add 3D FluidConfig fields + validators`.
  </action>
  <verify>
    <automated>cd /Users/tt/Documents/RLProj && PYTHONPATH=src pytest tests/test_fluids/test_schema.py -v && PYTHONPATH=src pytest tests/test_fluids/test_fluid_simulator.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class FluidCouplingMode' src/surg_rl/scene_definition/schema.py` returns >= 1
    - `grep -c 'dim_3d: bool' src/surg_rl/scene_definition/schema.py` returns >= 1
    - `grep -c 'grid_size: tuple\[int, int, int\] | None' src/surg_rl/scene_definition/schema.py` returns >= 1
    - `grep -c '_cap_grid_size' src/surg_rl/scene_definition/schema.py` returns >= 2 (decorator + def)
    - `grep -c '_require_grid_size_when_dim_3d' src/surg_rl/scene_definition/schema.py` returns >= 2
    - `grep -c '_cap_resolution' src/surg_rl/scene_definition/schema.py` unchanged (2 — decorator + def); the body of `_cap_resolution` is byte-identical (cap 128, len 2)
    - PYTHONPATH=src pytest tests/test_fluids/test_schema.py -v exits 0 (all new + existing tests pass)
    - PYTHONPATH=src pytest tests/test_fluids/test_fluid_simulator.py -v exits 0 (2D byte-identical — no behavior change)
    - `FluidConfig(enabled=True, bounds=<3D bbox>, dim_3d=True, grid_size=None)` raises ValidationError (verified by test)
  </acceptance_criteria>
  <done>GREEN: all new schema tests pass; existing 2D schema + simulator tests pass unchanged; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User config (JSON/YAML) → FluidConfig | Untrusted author input crosses here; Pydantic validators are the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Tampering/DoS (OOM) | FluidConfig.grid_size | high | mitigate | `_cap_grid_size` rejects any dim > 64; `_require_grid_size_when_dim_3d` hard-errors on missing grid_size — forces conscious cubic-cost choice (SC#3) |
| T-38-02 | Elevation of privilege | n/a (no auth surface) | low | accept | Pure config schema; no privilege boundary |
</threat_model>

<verification>
PYTHONPATH=src pytest tests/test_fluids/test_schema.py -v exits 0 (additive tests for dim_3d/grid_size/coupling_mode/coupling_substeps + existing 2D tests unchanged). PYTHONPATH=src pytest tests/test_fluids/test_fluid_simulator.py -v exits 0 (2D byte-identical — schema change does not perturb 2D FluidSimulator behavior).
</verification>

<success_criteria>
- FluidConfig has additive dim_3d, grid_size, coupling_mode, coupling_substeps fields with the exact defaults/bounds from D-01..D-05/D-09/D-12/D-13
- FluidCouplingMode str-Enum mirrors FluidBoundaryType style (ONE_WAY/TWO_WAY)
- _cap_grid_size enforces len==3, 4<=dim<=64, None passes, anisotropic accepted
- _require_grid_size_when_dim_3d raises ValidationError when dim_3d=True and grid_size is None (SC#3 guard)
- Existing 2D FluidConfig surface (resolution, _cap_resolution, FluidBoundaryType, boundary_type, all defaults) byte-identical; existing test_schema.py tests pass unchanged
</success_criteria>

<output>
Create `.planning/phases/38-fluid-3d-flag-dim-3d-true/38-01-SUMMARY.md` when done
</output>

<artifacts_produced>
Symbols created by this plan (excluded from plan-review-convergence drift verification):
- `FluidCouplingMode` (str Enum: ONE_WAY, TWO_WAY) — src/surg_rl/scene_definition/schema.py
- `FluidConfig.dim_3d` field
- `FluidConfig.grid_size` field
- `FluidConfig.coupling_mode` field
- `FluidConfig.coupling_substeps` field
- `FluidConfig._cap_grid_size` field_validator
- `FluidConfig._require_grid_size_when_dim_3d` model_validator(mode='after')
- `tests/test_fluids/test_schema.py::TestFluidConfig3D` (additive test class + helpers)
</artifacts_produced>