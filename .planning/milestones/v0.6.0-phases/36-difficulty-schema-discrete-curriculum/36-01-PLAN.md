---
phase: 36-difficulty-schema-discrete-curriculum
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/surg_rl/rl/difficulty.py
  - tests/test_difficulty_config.py
autonomous: true
requirements:
  - TASK-06
user_setup: []

must_haves:
  truths:
    - "DifficultyLevelConfig accepts the four override fields (tissue_stiffness / target_precision_tolerance / tool_position_noise / time_limit) as Optional[float] and validates types"
    - "Out-of-range override values raise ValidationError at schema time (D-07, ASVS V5 input validation)"
    - "In-range override values construct successfully; unset fields default to None"
    - "rl/difficulty.py remains a zero-in-project-import leaf (no `from surg_rl.` / `import surg_rl.` statements) — SC#5"
  artifacts:
    - path: "src/surg_rl/rl/difficulty.py"
      provides: "DifficultyLevelConfig Pydantic v2 leaf model with 4 Optional[float] override fields + 4 field_validator range checks"
      contains: "class DifficultyLevelConfig(BaseModel):"
    - path: "tests/test_difficulty_config.py"
      provides: "TDD RED→GREEN gate for DifficultyLevelConfig validation + SC#5 leaf import audit"
      contains: "test_leaf_no_inproject_imports"
  key_links:
    - from: "tests/test_difficulty_config.py"
      to: "src/surg_rl/rl/difficulty.py"
      via: "from surg_rl.rl.difficulty import DifficultyLevelConfig"
      pattern: "from surg_rl\\.rl\\.difficulty import DifficultyLevelConfig"
---

<objective>
Establish the `DifficultyLevelConfig` Pydantic v2 leaf model — the dependency root of the phase's difficulty-override vocabulary — using TDD RED→GREEN. The model carries the four locked override fields as `Optional[float]` with `field_validator` range checks against the verified D-07 global bounds, and stays a zero-in-project-import leaf (SC#5) so no Pydantic cross-package cycle is (re)introduced.

Purpose: This leaf is the foundation every downstream artifact (wiring module, scheduler, tests) imports. Landing it first in isolation keeps the import graph one-way and gives the truth-table test (Plan 02) a concrete type to compose with.
Output: `src/surg_rl/rl/difficulty.py` extended with `DifficultyLevelConfig`; `tests/test_difficulty_config.py` created with passing validation + leaf-audit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-difficulty-schema-discrete-curriculum/36-CONTEXT.md
@.planning/phases/36-difficulty-schema-discrete-curriculum/36-RESEARCH.md
@.planning/phases/36-difficulty-schema-discrete-curriculum/36-PATTERNS.md
@.planning/phases/36-difficulty-schema-discrete-curriculum/36-VALIDATION.md
@src/surg_rl/rl/difficulty.py
@src/surg_rl/scene_definition/schema.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Failing tests for DifficultyLevelConfig validation + leaf import audit</name>
  <files>tests/test_difficulty_config.py</files>
  <read_first>
    - src/surg_rl/rl/difficulty.py (the leaf file to extend — current state + module docstring leaf contract)
    - tests/test_difficulty_levels.py (test style analog: imports, parametrize shape, class organization)
    - .planning/phases/36-difficulty-schema-discrete-curriculum/36-RESEARCH.md (Pitfall 1 verified D-07 bounds table; Validation Architecture SC#1/SC#5 test design)
    - .planning/phases/36-difficulty-schema-discrete-curriculum/36-PATTERNS.md (File 4 analog excerpt — imports + parametrize shape)
  </read_first>
  <behavior>
    - In-range construction: DifficultyLevelConfig(tissue_stiffness=100.0, target_precision_tolerance=0.01, tool_position_noise=0.03, time_limit=60.0) succeeds with all four fields set.
    - Default None: DifficultyLevelConfig() yields tissue_stiffness is None, target_precision_tolerance is None, tool_position_noise is None, time_limit is None.
    - Out-of-range rejection (D-07 verified bounds): tissue_stiffness=999.0 raises ValidationError; tissue_stiffness=10.0 raises ValidationError; target_precision_tolerance=0.001 raises ValidationError (below 0.002); target_precision_tolerance=0.5 raises ValidationError (above 0.3); tool_position_noise=0.2 raises ValidationError (above 0.08); time_limit=200.0 raises ValidationError (above 180.0); time_limit=10.0 raises ValidationError (below 30.0).
    - Type rejection: tissue_stiffness="high" raises ValidationError.
    - SC#5 leaf audit: importing surg_rl.rl.difficulty and reading its source yields zero occurrences of the substring "surg_rl." outside comments (the module stays an import-free leaf).
  </behavior>
  <action>
    Create tests/test_difficulty_config.py. Module docstring: "Tests for DifficultyLevelConfig Pydantic v2 leaf model (Phase 36, TASK-06 / SC#1 / SC#5)."

    Imports: `import pytest`, `from pydantic import ValidationError`, `from surg_rl.rl.difficulty import DifficultyLevelConfig, DifficultyLevel`. Use `import surg_rl.rl.difficulty as _difficulty_module` for the SC#5 source audit.

    Class `TestDifficultyLevelConfig` with:
    - test_default_none: construct DifficultyLevelConfig() and assert each of the four fields is None.
    - test_in_range_construction: construct with all four fields set to in-range values (tissue_stiffness=100.0, target_precision_tolerance=0.01, tool_position_noise=0.03, time_limit=60.0) and assert field values round-trip.
    - A parametrized test `test_out_of_range_rejected` over (field_name, bad_value) tuples: tissue_stiffness=999.0, tissue_stiffness=10.0, target_precision_tolerance=0.001, target_precision_tolerance=0.5, tool_position_noise=0.2, time_limit=200.0, time_limit=10.0 — each expects ValidationError. Use `pytest.raises(ValidationError)`.
    - test_type_rejection: DifficultyLevelConfig(tissue_stiffness="high") expects ValidationError.

    Class `TestLeafImportAudit` (SC#5) with `test_leaf_no_inproject_imports`: read `_difficulty_module.__file__`, open the source, strip comment-only lines (lines whose stripped form starts with `#`), then assert `"surg_rl."` not in the remaining source. This enforces the zero-in-project-import leaf contract per D-08 / SC#5.

    Run `PYTHONPATH=src pytest tests/test_difficulty_config.py -v` — tests MUST fail (RED) because DifficultyLevelConfig does not yet exist (ImportError at collection). Commit: `test(36-01): add failing RED tests for DifficultyLevelConfig + leaf audit`.
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/test_difficulty_config.py -v 2>&1 | grep -E "(ImportError|NameError|ModuleNotFoundError)" exits 0; git diff --stat HEAD~1 confirms tests/test_difficulty_config.py added</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_difficulty_config.py exists and contains `class TestDifficultyLevelConfig` and `class TestLeafImportAudit`
    - `grep -c 'test_out_of_range_rejected' tests/test_difficulty_config.py` returns >= 1
    - `grep -c 'test_leaf_no_inproject_imports' tests/test_difficulty_config.py` returns 1
    - `PYTHONPATH=src pytest tests/test_difficulty_config.py -v` exits non-zero (RED) with ImportError/NameError for DifficultyLevelConfig
  </acceptance_criteria>
  <done>RED test file exists; all tests fail at collection/import because DifficultyLevelConfig is not yet defined; commit landed as `test(36-01): ...`.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Implement DifficultyLevelConfig leaf model with verified D-07 range validators</name>
  <files>src/surg_rl/rl/difficulty.py</files>
  <read_first>
    - src/surg_rl/rl/difficulty.py (current leaf — append below DifficultyLevel; preserve module docstring leaf contract)
    - src/surg_rl/scene_definition/schema.py (lines ~1480-1506 — field_validator idiom + model_rebuild late-import pattern reference)
    - .planning/phases/36-difficulty-schema-discrete-curriculum/36-RESEARCH.md (Pitfall 1 verified global bounds table; Pattern 3 _FloatMixin)
    - .planning/phases/36-difficulty-schema-discrete-curriculum/36-PATTERNS.md (File 1 analog — target DifficultyLevelConfig shape)
  </read_first>
  <behavior>
    - All RED tests in tests/test_difficulty_config.py flip to GREEN (exit 0).
    - DifficultyLevelConfig(tissue_stiffness=100.0, target_precision_tolerance=0.01, tool_position_noise=0.03, time_limit=60.0) constructs successfully.
    - Out-of-range values for each of the four fields raise ValidationError.
    - `grep -v '^\s*#' src/surg_rl/rl/difficulty.py | grep -c 'surg_rl\.'` returns 0 (leaf stays import-free).
  </behavior>
  <action>
    Extend src/surg_rl/rl/difficulty.py BELOW the existing DifficultyLevel class. Append imports `from typing import Optional` and `from pydantic import BaseModel, field_validator` immediately after the existing `from enum import Enum` line (keep the module docstring and the leaf contract intact — NO `from surg_rl...` imports).

    Add class `DifficultyLevelConfig(BaseModel)` with docstring "Per-level difficulty override config (Phase 36, TASK-06). Leaf: zero in-project imports (SC#5)." and four fields, each `Optional[float] = None`:
    - tissue_stiffness
    - target_precision_tolerance
    - tool_position_noise
    - time_limit

    Add four `@field_validator("<field>")` + `@classmethod` methods, each returning `v` after bounds-checking against the VERIFIED D-07 global bounds (per D-07 CORRECTED — `min/max over all endpoints`, NOT "min lo / max hi"; 36-RESEARCH.md Pitfall 1):
    - tissue_stiffness: if v is not None and not (50.0 <= v <= 300.0): raise ValueError("tissue_stiffness out of global union bounds [50.0, 300.0]")
    - target_precision_tolerance: if v is not None and not (0.002 <= v <= 0.3): raise ValueError("target_precision_tolerance out of global union bounds [0.002, 0.3]")
    - tool_position_noise: if v is not None and not (0.01 <= v <= 0.08): raise ValueError("tool_position_noise out of global union bounds [0.01, 0.08]")
    - time_limit: if v is not None and not (30.0 <= v <= 180.0): raise ValueError("time_limit out of global union bounds [30.0, 180.0]")

    Do NOT add any other method, field, or in-project import. Do NOT edit DifficultyLevel. Run `PYTHONPATH=src pytest tests/test_difficulty_config.py -v` — all tests MUST pass (GREEN). Commit: `feat(36-01): implement DifficultyLevelConfig leaf model with D-07 range validators`.
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/test_difficulty_config.py -v exits 0; grep -v '^\s*#' src/surg_rl/rl/difficulty.py | grep -c 'surg_rl\.' returns 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class DifficultyLevelConfig(BaseModel):' src/surg_rl/rl/difficulty.py` returns 1
    - `grep -c 'tissue_stiffness' src/surg_rl/rl/difficulty.py` returns >= 2 (field + validator)
    - `grep -c '50.0 <= v <= 300.0' src/surg_rl/rl/difficulty.py` returns 1
    - `grep -c '0.002 <= v <= 0.3' src/surg_rl/rl/difficulty.py` returns 1
    - `grep -c '0.01 <= v <= 0.08' src/surg_rl/rl/difficulty.py` returns 1
    - `grep -c '30.0 <= v <= 180.0' src/surg_rl/rl/difficulty.py` returns 1
    - `grep -v '^\s*#' src/surg_rl/rl/difficulty.py | grep -c 'surg_rl\.'` returns 0 (SC#5 leaf audit)
    - `PYTHONPATH=src pytest tests/test_difficulty_config.py -v` exits 0 (GREEN)
    - `PYTHONPATH=src pytest tests/test_difficulty_levels.py tests/test_dynamics.py -v` exits 0 (additive-regression gate: existing v0.4.0+v0.4.2 suite unchanged)
  </acceptance_criteria>
  <done>All DifficultyLevelConfig tests GREEN; existing difficulty/dynamics suite still GREEN; leaf stays import-free; commit landed as `feat(36-01): ...`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| researcher authoring → Pydantic schema | override-field values cross from user-authored config into the validated model at construction time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | DifficultyLevelConfig override fields (researcher typo / out-of-range authoring) | mitigate | Pydantic v2 `field_validator` range checks on all 4 fields against the verified D-07 global bounds — out-of-range raises ValidationError at schema time (ASVS V5 input validation) |
| T-36-02 | Denial of Service | rl/difficulty.py leaf import graph (Pydantic cross-package cycle regression) | mitigate | leaf stays zero in-project imports (SC#5); enforced by test_leaf_no_inproject_imports grep audit |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — only stdlib (enum, typing) + already-pinned pydantic v2 are used |
</threat_model>

<verification>
- `PYTHONPATH=src pytest tests/test_difficulty_config.py -v` exits 0 (SC#1 + SC#5 covered).
- `grep -v '^\s*#' src/surg_rl/rl/difficulty.py | grep -c 'surg_rl\.'` returns 0 (SC#5 leaf audit).
- `PYTHONPATH=src pytest tests/test_difficulty_levels.py tests/test_dynamics.py -v` exits 0 (additive-regression gate; no existing test edited).
</verification>

<success_criteria>
- DifficultyLevelConfig Pydantic v2 model exists in src/surg_rl/rl/difficulty.py with the four override fields and verified D-07 range validators (SC#1, TASK-06).
- rl/difficulty.py remains a zero-in-project-import leaf (SC#5).
- Existing v0.4.0 + v0.4.2 difficulty/dynamics suite passes unchanged (SC#4 additive-regression gate preserved).
</success_criteria>

<output>
Create `.planning/phases/36-difficulty-schema-discrete-curriculum/36-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces (Plan 01)

- `DifficultyLevelConfig` (class, `src/surg_rl/rl/difficulty.py`) — Pydantic v2 BaseModel, 4 `Optional[float]` override fields + 4 `field_validator` range checks
- `tests/test_difficulty_config.py` (new test file path) — `TestDifficultyLevelConfig`, `TestLeafImportAudit` classes