---
phase: 37-scene-level-difficulty-blocks-env-wiring
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/surg_rl/scene_definition/schema.py
  - tests/test_difficulty_blocks.py
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements: [TASK-08]
user_setup: []

must_haves:
  truths:
    - "A scene JSON with difficulty_blocks for all three levels (EASY/MEDIUM/HARD) loads via SceneLoader and the blocks round-trip through Pydantic v2 validation with authored values preserved"
    - "A scene JSON WITHOUT difficulty_blocks still loads with scene.task.difficulty_blocks is None (6 existing v0.4.0 scenes unaffected)"
    - "difficulty_blocks is the canonical spelling across PROJECT.md, schema, and STATE.md; the prior plural-s drift spelling is gone"
  artifacts:
    - path: "src/surg_rl/scene_definition/schema.py"
      provides: "TaskConfig.difficulty_blocks string forward-ref field + extended late-import block"
      contains: "difficulty_blocks"
    - path: "tests/test_difficulty_blocks.py"
      provides: "TestSceneBlocksRoundTrip (SC#1) + TestNamingAudit (SC#5) + test_existing_scenes_load_without_blocks"
      contains: "class TestSceneBlocksRoundTrip"
    - path: ".planning/PROJECT.md"
      provides: "Reconciled line 82 naming to difficulty_blocks dict shape"
    - path: ".planning/STATE.md"
      provides: "Reconciled lines 82, 131 naming drift note to past-tense Phase 37 attribution"
  key_links:
    - from: "tests/test_difficulty_blocks.py::TestSceneBlocksRoundTrip"
      to: "src/surg_rl/scene_definition/schema.py::TaskConfig.difficulty_blocks"
      via: "SceneLoader().load_from_string(json_str) -> scene.task.difficulty_blocks dict"
      pattern: "difficulty_blocks"
    - from: "src/surg_rl/scene_definition/schema.py"
      to: "src/surg_rl/rl/difficulty.py"
      via: "late import of DifficultyLevelConfig at schema.py:1501 + TaskConfig.model_rebuild() at :1506"
      pattern: "from surg_rl.rl.difficulty import"
---

<objective>
Add the `difficulty_blocks: dict[DifficultyLevel, DifficultyLevelConfig] | None` field to `TaskConfig` (scene-author input surface) via the v0.4.2 string forward-ref + late-import + `model_rebuild()` canon, and reconcile the `difficulty_blocks` naming drift across PROJECT.md and STATE.md. This is the schema half of TASK-08 (SC#1 round-trip + SC#5 naming audit).

Purpose: Scene JSON authors need a per-level override field on the task so a hard-mode scene produces a hard-mode environment without code changes. The field is consumed read-only by Plan 02's `_setup_rewards` precedence branch. SC#5 closes the carried-forward naming-drift debt.

Output: `TaskConfig.difficulty_blocks` live in schema.py; `tests/test_difficulty_blocks.py` with SC#1 round-trip + SC#5 naming-audit tests (RED then GREEN); PROJECT.md:82 + STATE.md:82,131 reconciled to the canonical `difficulty_blocks` dict shape.

<decisions>
Adopted from 37-RESEARCH.md Open Questions (no CONTEXT.md exists):
- **Q3 — field shape:** `dict[DifficultyLevel, DifficultyLevelConfig] | None` directly on `TaskConfig` (flat for scene authors; only `rl.difficulty` symbols needed; NO `dynamics.*` import in schema.py — Pitfall 4).
- **Q2 — config.difficulty:** ADD `difficulty: float = 0.5` to `SurgicalEnvConfig` — deferred to Plan 02 (env-wiring plan), not this plan.
- **Q1 — apply_params scope:** MINIMAL (Option a) — deferred to Plan 02 (rewards refactor).
- **Q4 — curriculum coexistence:** blocks apply ONLY under a resolved `DifficultyLevel` enum — enforced in Plan 02's `_setup_rewards` branch; this plan only ships the schema field.
</decisions>
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-scene-level-difficulty-blocks-env-wiring/37-RESEARCH.md
@.planning/phases/37-scene-level-difficulty-blocks-env-wiring/37-PATTERNS.md
@.planning/phases/37-scene-level-difficulty-blocks-env-wiring/37-VALIDATION.md
@.planning/phases/36-difficulty-schema-discrete-curriculum/36-01-SUMMARY.md
@src/surg_rl/scene_definition/schema.py
@src/surg_rl/rl/difficulty.py
@src/surg_rl/scene_definition/loader.py
@tests/test_difficulty_levels.py
@tests/test_difficulty_config.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): tests/test_difficulty_blocks.py — SC#1 round-trip + SC#5 naming audit</name>
  <files>tests/test_difficulty_blocks.py</files>
  <read_first>
    - tests/test_difficulty_levels.py (1-100, 316-401) — imports + TestDifficultyIntegration idiom (HARD_FIXTURE, SceneLoader().load, scene.task assertions)
    - tests/test_difficulty_config.py (1-30, 137-215) — TestComposeDifficultyOverrides imports + caplog idiom
    - src/surg_rl/scene_definition/schema.py (1087-1121) — TaskConfig current fields (difficulty_level forward-ref shape to mirror)
    - src/surg_rl/rl/difficulty.py (14-97) — DifficultyLevel enum + DifficultyLevelConfig fields + D-07 bounds
    - src/surg_rl/scene_definition/loader.py (545-610) — SceneLoader.load / load_from_string signature
    - .planning/phases/37-scene-level-difficulty-blocks-env-wiring/37-PATTERNS.md (348-462) — test class organization + SC#1/SC#5 idioms
  </read_first>
  <behavior>
    - TestSceneBlocksRoundTrip.test_scene_with_blocks_round_trips: author a scene JSON string with task.difficulty_blocks for all 3 levels (EASY/MEDIUM/HARD, each carrying target_precision_tolerance inside D-07 bounds [0.002, 0.3]), SceneLoader().load_from_string(json, format="json") succeeds, scene.task.difficulty_blocks is a dict with DifficultyLevel keys, each value is a DifficultyLevelConfig with the authored override, and JSON re-serialization preserves the values.
    - TestSceneBlocksRoundTrip.test_existing_scenes_load_without_blocks: the 6 v0.4.0 task scenes (simple_suturing, knot_tying, needle_insertion, grasping, cutting, dissection) load with scene.task.difficulty_blocks is None (mirror test_difficulty_levels.py::test_all_phase27_scenes_load_with_difficulty_level_none parametrize idiom). This is the canonical SC#1 negative regression name — shared with 37-03 Task 2 and 37-VALIDATION.md row 37-SC1-neg.
    - TestSceneBlocksRoundTrip.test_malformed_blocks_rejected: a scene with an out-of-range override (target_precision_tolerance=999.0) raises pydantic.ValidationError at load time (ASVS V5 / T-37-01).
    - TestNamingAudit.test_no_drift_spelling_in_canonical_docs: subprocess grep for the drift spelling across .planning/PROJECT.md, .planning/STATE.md, src/surg_rl/ returns nonzero exit (no hits). Excludes .planning/milestones/ historical archives.
  </behavior>
  <action>
    Create tests/test_difficulty_blocks.py. Mirror the imports + module docstring pattern from tests/test_difficulty_levels.py:1-25 (pytest, Path, from surg_rl.rl import DifficultyLevel, from surg_rl.rl.difficulty import DifficultyLevelConfig, from surg_rl.scene_definition.loader import SceneLoader, from pydantic import ValidationError). Add `import subprocess` and `from pathlib import Path` for the naming audit.

    Define class TestSceneBlocksRoundTrip with three tests per the behavior block. Use SceneLoader().load_from_string(json_str, format="json") with an inline JSON string shaped per 37-RESEARCH.md:558-572 (task.task_type="suturing", task.difficulty_level=1.0, task.difficulty_blocks={"EASY":{"target_precision_tolerance":0.02},"MEDIUM":{"target_precision_tolerance":0.005},"HARD":{"target_precision_tolerance":0.002}}). Assert scene.task is not None, scene.task.difficulty_blocks is not None, isinstance(scene.task.difficulty_blocks, dict), DifficultyLevel.EASY in scene.task.difficulty_blocks, scene.task.difficulty_blocks[DifficultyLevel.EASY].target_precision_tolerance == 0.02, and the same for MEDIUM/HARD. For re-serialization, assert scene.task.difficulty_blocks[DifficultyLevel.HARD].target_precision_tolerance == 0.002 after a round-trip via scene.model_dump() then TaskConfig.model_validate(dump["task"]) (or SceneLoader round-trip).

    For test_existing_scenes_load_without_blocks, parametrize over the 6 scene filenames (mirror test_difficulty_levels.py:383-401), load each via SceneLoader().load(str(Path(__file__).parent.parent / "scenes" / scene_file)), assert scene.task.difficulty_blocks is None. Skip if the file does not exist. This is the canonical SC#1 negative name referenced by 37-03 Task 2 and 37-VALIDATION.md row 37-SC1-neg — name it exactly `test_existing_scenes_load_without_blocks`.

    For test_malformed_blocks_rejected, build an inline JSON with target_precision_tolerance=999.0 (outside D-07 [0.002, 0.3]) and assert pytest.raises(pydantic.ValidationError) around SceneLoader().load_from_string.

    Define class TestNamingAudit with test_no_drift_spelling_in_canonical_docs: run subprocess.run(["grep", "-rn", "difficulty_levels", ".planning/PROJECT.md", ".planning/STATE.md", "src/surg_rl/"], capture_output=True, text=True) and assert result.returncode != 0 (grep exits 1 when no matches). NOTE: this test will FAIL in RED (the drift spelling still exists in PROJECT.md:82 + STATE.md:82 until Task 3 reconciles it) — that is the intended RED state for the naming audit.

    <!-- planner-discipline-allow: difficulty_levels -->

    RED gate: run `PYTHONPATH=src pytest tests/test_difficulty_blocks.py -v` — expect collection failure (ImportError: cannot import name 'difficulty_blocks' or AttributeError on scene.task.difficulty_blocks) for the round-trip tests, and the naming-audit test fails because the drift spelling is still present. Commit with `test(37-01):` prefix.
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/test_difficulty_blocks.py -v 2>&1 | tee /tmp/37-01-red.log; test ${PIPESTATUS[0]} -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_difficulty_blocks.py exists and contains `class TestSceneBlocksRoundTrip` and `class TestNamingAudit`
    - RED confirmed: `PYTHONPATH=src pytest tests/test_difficulty_blocks.py -v` exits nonzero BEFORE the schema field is added (ImportError or AttributeError on difficulty_blocks)
    - The inline JSON fixture uses override values inside D-07 bounds (0.02, 0.005, 0.002 — all within [0.002, 0.3])
    - The naming-audit test invokes `subprocess.run(["grep", "-rn", "difficulty_levels", ...])` and asserts nonzero exit
    - Commit message starts with `test(37-01):`
  </acceptance_criteria>
  <done>tests/test_difficulty_blocks.py created with TestSceneBlocksRoundTrip (3 tests) + TestNamingAudit (1 test); all fail in RED (field missing / drift spelling present); committed as `test(37-01):`.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): schema.py — TaskConfig.difficulty_blocks field + late-import extension</name>
  <files>src/surg_rl/scene_definition/schema.py</files>
  <read_first>
    - src/surg_rl/scene_definition/schema.py (1087-1121) — TaskConfig class, the existing difficulty_level field shape to mirror verbatim
    - src/surg_rl/scene_definition/schema.py (1491-1506) — the existing late-import + TaskConfig.model_rebuild() block to EXTEND (do not duplicate)
    - src/surg_rl/rl/difficulty.py (14-97) — DifficultyLevel + DifficultyLevelConfig (the types to import; confirm leaf has zero in-project imports)
    - .planning/phases/37-scene-level-difficulty-blocks-env-wiring/37-PATTERNS.md (23-82) — exact field pattern + late-import extension + placement
  </read_first>
  <behavior>
    - TestSceneBlocksRoundTrip tests pass (round-trip preserves authored values; None default for scenes without blocks; malformed rejected with ValidationError).
    - TestNamingAudit still fails until Task 3 — that is expected (this task only ships the schema field, not the doc reconciliation).
    - The 6 v0.4.0 task scenes still load (difficulty_blocks defaults to None — no scene breaks).
    - No Pydantic cross-package cycle: `python -c "import surg_rl.scene_definition.schema, surg_rl.dynamics.difficulty_wiring, surg_rl.dynamics.curriculum"` succeeds.
  </behavior>
  <action>
    Edit src/surg_rl/scene_definition/schema.py inside class TaskConfig (after the existing difficulty_level field at ~line 1121, before BenchmarkConfig at ~line 1124). Add the field with the EXACT shape from 37-PATTERNS.md:62-72:

    `difficulty_blocks: "dict[DifficultyLevel, DifficultyLevelConfig] | None" = Field(default=None, description="Per-level difficulty override blocks keyed by DifficultyLevel (EASY/MEDIUM/HARD). None = no overrides; use the float difficulty path. Each value is a DifficultyLevelConfig carrying 0-4 SET override fields. When present, SurgicalEnv._setup_rewards composes overrides additively over interpolate_params(level.value) via compose_difficulty_overrides.")`

    Include the `# noqa: F821` comment mirroring the existing difficulty_level field (forward ref resolved at bottom of file). Type as `dict[DifficultyLevel, DifficultyLevelConfig] | None` (string forward-ref) — NOT `DiscreteCurriculumConfig | None` (that would require a dynamics.* import and re-open the cross-package cycle, Pitfall 4).

    Extend the EXISTING late-import block at schema.py:1501-1506 — do NOT duplicate it. The current block is `from surg_rl.rl.difficulty import (DifficultyLevel,)`. Change it to `from surg_rl.rl.difficulty import (DifficultyLevel, DifficultyLevelConfig,)` (add DifficultyLevelConfig as a second tuple member, preserving the existing noqa: E402 and noqa: F401 comments). The single existing `TaskConfig.model_rebuild()` call at :1506 resolves BOTH forward refs (difficulty_level and difficulty_blocks).

    Do NOT add any `from surg_rl.dynamics.*` import to schema.py (Pitfall 4 — would create a scene_definition -> dynamics edge). Do NOT add a second model_rebuild() call. Do NOT edit DifficultyLevelConfig (it is a P36-01 leaf, read-only).

    Use the Edit tool (not sed/echo) for both edits. Verify the opening parenthesis on the multi-line import is intact after editing (CLAUDE.md import rule).

    GREEN gate: run `PYTHONPATH=src pytest tests/test_difficulty_blocks.py::TestSceneBlocksRoundTrip -v` — all 3 round-trip tests pass. TestNamingAudit still fails (Task 3 fixes the docs). Commit with `feat(37-01):` prefix.
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/test_difficulty_blocks.py::TestSceneBlocksRoundTrip -v && python -c "import surg_rl.scene_definition.schema, surg_rl.dynamics.difficulty_wiring, surg_rl.dynamics.curriculum" && PYTHONPATH=src pytest tests/test_difficulty_levels.py tests/test_difficulty_config.py tests/test_discrete_curriculum.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'difficulty_blocks:' src/surg_rl/scene_definition/schema.py` returns 1
    - `grep -c 'DifficultyLevelConfig' src/surg_rl/scene_definition/schema.py` returns at least 2 (field annotation + late import)
    - `grep -c 'from surg_rl.dynamics' src/surg_rl/scene_definition/schema.py` returns 0 (no dynamics import — Pitfall 4 guard)
    - `grep -c 'TaskConfig.model_rebuild' src/surg_rl/scene_definition/schema.py` returns 1 (single call, not duplicated)
    - `PYTHONPATH=src pytest tests/test_difficulty_blocks.py::TestSceneBlocksRoundTrip -v` exits 0 (3 passed)
    - `python -c "import surg_rl.scene_definition.schema, surg_rl.dynamics.difficulty_wiring, surg_rl.dynamics.curriculum"` exits 0 (no cross-package cycle)
    - `PYTHONPATH=src pytest tests/test_difficulty_levels.py tests/test_difficulty_config.py tests/test_discrete_curriculum.py -v` exits 0 (additive-regression gate, TASK-09 — no existing test edited)
    - `ruff check src/surg_rl/scene_definition/schema.py` and `black --check src/surg_rl/scene_definition/schema.py` both clean
    - Commit message starts with `feat(37-01):`
  </acceptance_criteria>
  <done>TaskConfig.difficulty_blocks field live; round-trip tests green; no dynamics.* import in schema.py; single model_rebuild() call; existing difficulty suite unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile naming drift in PROJECT.md + STATE.md (SC#5)</name>
  <files>.planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>
    - .planning/PROJECT.md (20-90) — line 82 has the drift spelling; line 23 has the list[3] old shape
    - .planning/STATE.md (75-95, 125-135) — line 82 drift note + line 131 deferred-items row
    - .planning/phases/37-scene-level-difficulty-blocks-env-wiring/37-PATTERNS.md (473-513) — exact current/target text for both files
  </read_first>
  <action>
    Use the Edit tool (scoped replacement, NOT Write) on .planning/PROJECT.md.

    Edit 1 — PROJECT.md:82: replace the drift-spelling line with the canonical dict-shape line. Target text: `- TASK-02 scene-level `difficulty_blocks: dict[DifficultyLevel, DifficultyLevelConfig]` blocks — Phase 37 (TASK-08) ships; D-29-03 exclusion lifted` (replaces the prior `difficulty_levels: list[3]` spelling).

    Edit 2 — PROJECT.md:23 (if it references the old `list[3]` shape): reconcile to `difficulty_blocks: dict[DifficultyLevel, DifficultyLevelConfig] | None` (37-RESEARCH.md Open Q3 dict shape). Read the line first to confirm the exact current text before editing.

    Use the Edit tool on .planning/STATE.md.

    Edit 3 — STATE.md:82: update the naming-drift note to past-tense Phase 37 attribution. Target text: `- [v0.6.0 research]: Naming drift — `difficulty_blocks` (PROJECT.md) vs `difficulty_levels` (STATE.md) → canonical = `difficulty_blocks`; reconciled in Phase 37 (TASK-08/SC#5). Drift spelling removed from PROJECT.md and STATE.md.`

    <!-- planner-discipline-allow: difficulty_levels -->

    Edit 4 — STATE.md:131: update the deferred-items row shape to the dict form. Target text: `| TASK-02 | Scene-level `difficulty_blocks: dict[DifficultyLevel, DifficultyLevelConfig] \| None` | Phase 37 (TASK-08) | Naming reconciled to `difficulty_blocks`; shape = dict-keyed (RESEARCH.md Open Q3) |` (replaces the prior `list[3]` shape).

    Do NOT edit any other lines in either file (scoped replacements only — a whole-file Write would destroy unrelated content). Do NOT edit historical milestone archives under .planning/milestones/ (they are excluded from the SC#5 grep).

    GREEN gate: run `PYTHONPATH=src pytest tests/test_difficulty_blocks.py::TestNamingAudit -v` — the naming-audit test now passes (grep returns nonzero exit, no drift spelling in canonical docs).
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/test_difficulty_blocks.py::TestNamingAudit -v && grep -rn "difficulty_levels" .planning/PROJECT.md .planning/STATE.md src/surg_rl/ 2>/dev/null; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "difficulty_levels" .planning/PROJECT.md .planning/STATE.md src/surg_rl/` returns no hits (grep exit 1) — excluding .planning/milestones/
    - `grep -c "difficulty_blocks" .planning/PROJECT.md` returns at least 1 (canonical spelling present)
    - PROJECT.md:82 contains `difficulty_blocks: dict[DifficultyLevel, DifficultyLevelConfig]` (dict shape, not list[3])
    - STATE.md:82 contains `reconciled in Phase 37` (past-tense attribution)
    - STATE.md:131 contains `dict[DifficultyLevel, DifficultyLevelConfig] | None` (dict shape)
    - `PYTHONPATH=src pytest tests/test_difficulty_blocks.py::TestNamingAudit -v` exits 0
    - `PYTHONPATH=src pytest tests/test_difficulty_blocks.py -v` exits 0 (all 4 tests green: 3 round-trip + 1 naming audit)
  </acceptance_criteria>
  <done>Drift spelling gone from PROJECT.md + STATE.md; dict shape canonical; TestNamingAudit green; all of test_difficulty_blocks.py green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scene-author → SceneLoader | Untrusted scene JSON input crosses here; `difficulty_blocks` is a new author-facing field parsed by Pydantic v2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering | TaskConfig.difficulty_blocks (scene-author input) | mitigate | Pydantic v2 validation: DifficultyLevel enum dict-key membership + nested DifficultyLevelConfig D-07 range validators (P36-01 shipped, inherited automatically). Malformed blocks raise ValidationError at SceneLoader.load time. Verified by test_malformed_blocks_rejected. |
| T-37-02 | Denial of Service | schema.py late-import block (Pydantic cross-package cycle) | mitigate | String forward-ref + extend existing late-import at schema.py:1501 with DifficultyLevelConfig (rl.difficulty leaf only); NO dynamics.* import in schema.py (Pitfall 4). Single TaskConfig.model_rebuild() resolves both forward refs. Verified by cycle-check import test. |
| T-37-03 | Information Spoofing | difficulty_blocks override silently inert (Pitfall 2) | accept for this plan | The override-consumption seam lives in Plan 02 (_setup_rewards + apply_params). This plan ships only the schema field; the inert-surface documentation is Plan 02's truth-table responsibility. The field round-trip is verified here. |
| T-37-SC | Tampering | package installs | accept | No package installs this plan — only stdlib + already-pinned pydantic v2 + in-project leaf reuse. |
</threat_model>

<verification>
- `PYTHONPATH=src pytest tests/test_difficulty_blocks.py -v` → 4 passed (SC#1 round-trip + SC#5 naming audit)
- `PYTHONPATH=src pytest tests/test_difficulty_levels.py tests/test_difficulty_config.py tests/test_discrete_curriculum.py tests/test_dynamics.py -v` → all passed (additive-regression gate, TASK-09 — no existing test edited)
- `python -c "import surg_rl.scene_definition.schema, surg_rl.dynamics.difficulty_wiring, surg_rl.dynamics.curriculum"` → OK (no Pydantic cross-package cycle)
- `grep -rn "difficulty_levels" .planning/PROJECT.md .planning/STATE.md src/surg_rl/` → no hits (SC#5 naming audit)
- `ruff check src/surg_rl/scene_definition/schema.py` + `black --check src/surg_rl/scene_definition/schema.py` → clean
</verification>

<success_criteria>
- SC#1: Scene JSON with difficulty_blocks for all 3 levels round-trips through Pydantic v2 (TestSceneBlocksRoundTrip green)
- SC#5: difficulty_blocks canonical across PROJECT.md, schema, STATE.md; drift spelling gone (TestNamingAudit green + grep audit)
- TASK-09: existing difficulty suite unchanged (additive-regression gate green)
</success_criteria>

<artifacts_this_phase_produces>
- `src/surg_rl/scene_definition/schema.py :: TaskConfig.difficulty_blocks` — new Pydantic v2 string forward-ref field `dict[DifficultyLevel, DifficultyLevelConfig] | None = None` (scene-author input surface)
- `src/surg_rl/scene_definition/schema.py :: late-import block` — extended to import DifficultyLevelConfig alongside DifficultyLevel (single model_rebuild resolves both forward refs)
- `tests/test_difficulty_blocks.py :: TestSceneBlocksRoundTrip` — 3 tests (round-trip, None-default for 6 scenes, malformed-rejected)
- `tests/test_difficulty_blocks.py :: TestNamingAudit` — 1 test (grep audit for drift spelling)
- `.planning/PROJECT.md :: line 82 + line 23` — reconciled to `difficulty_blocks: dict[DifficultyLevel, DifficultyLevelConfig]` dict shape
- `.planning/STATE.md :: line 82 + line 131` — reconciled to past-tense Phase 37 attribution + dict shape
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/37-scene-level-difficulty-blocks-env-wiring/37-01-SUMMARY.md` when done
</output>