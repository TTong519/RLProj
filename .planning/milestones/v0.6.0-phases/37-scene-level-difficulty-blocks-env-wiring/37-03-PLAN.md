---
phase: 37-scene-level-difficulty-blocks-env-wiring
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - tests/test_difficulty_blocks.py
autonomous: true
requirements: [TASK-08]
user_setup: []

must_haves:
  truths:
    - "Loading all 6 v0.4.0 task scene fixtures (suturing, knot_tying, needle_insertion, grasping, cutting, dissection) with each of the three difficulty levels succeeds and produces a constructed SurgicalEnv with _reward_fn set (regression gate)"
    - "Existing v0.4.2 fixture suturing_difficulty_hard.json still loads and produces the same difficulty scalar (1.0) it did before this phase (back-compat gate)"
    - "If env.step() aborts on macOS (pre-existing per 36-03-SUMMARY, NOT caused by this phase), the gate degrades to construct-only and asserts env._reward_fn is not None rather than stepping"
  artifacts:
    - path: "tests/test_difficulty_blocks.py"
      provides: "TestSixSceneThreeLevelRegression (SC#3, 6x3 parametrized) + TestHardFixtureScalarEquivalence (SC#4)"
      contains: "class TestSixSceneThreeLevelRegression"
  key_links:
    - from: "tests/test_difficulty_blocks.py::TestSixSceneThreeLevelRegression"
      to: "scenes/{simple_suturing,knot_tying,needle_insertion,grasping,cutting,dissection}.json"
      via: "SceneLoader().load(scene_path) -> SurgicalEnv(SurgicalEnvConfig(scene=..., render_mode=None)) construction"
      pattern: "simple_suturing.json"
    - from: "tests/test_difficulty_blocks.py::TestHardFixtureScalarEquivalence"
      to: "tests/fixtures/scenes/suturing_difficulty_hard.json"
      via: "SceneLoader().load(hard_fixture) -> scene.task.difficulty_level == DifficultyLevel.HARD -> env._task_difficulty == 1.0"
      pattern: "suturing_difficulty_hard.json"
---

<objective>
Land the SC#3 6-scene × 3-level regression gate and the SC#4 hard-fixture back-compat scalar gate. These are the final two success criteria for TASK-08. They verify that the additive schema field (Plan 01) + env precedence branch (Plan 02) did not break any existing scene's load/construct/step path, and that the v0.4.2 hard fixture produces the byte-identical difficulty scalar it did before this phase.

Purpose: SC#3 closes the "no regression" loop — the 6 v0.4.0 task scenes (which have no difficulty_blocks) must still construct + step under each of the 3 difficulty levels. SC#4 closes the back-compat loop — the v0.4.2 hard fixture's scalar must not drift. Both are regression gates (asserts existing behavior holds), not new-behavior tests.

Output: TestSixSceneThreeLevelRegression (18 parametrized cases) + TestHardFixtureScalarEquivalence appended to test_difficulty_blocks.py; all green.

<decisions>
Adopted from 37-RESEARCH.md + orchestrator instructions:
- **Host-sensitivity (SC#3):** The 6×3 gate must include a construct-only fallback — if `env.step()` aborts on macOS (pre-existing per 36-03-SUMMARY, NOT caused by this phase), assert `SurgicalEnv` constructs and `_setup_rewards` ran (`env._reward_fn is not None`) rather than stepping. Mirror the parametrized 6×3 idiom in 37-PATTERNS.md:393-415.
- **Q1/Q2/Q3/Q4:** Already shipped in Plans 01-02; this plan only consumes them read-only.
</decisions>
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/37-scene-level-difficulty-blocks-env-wiring/37-RESEARCH.md
@.planning/phases/37-scene-level-difficulty-blocks-env-wiring/37-PATTERNS.md
@.planning/phases/37-scene-level-difficulty-blocks-env-wiring/37-01-SUMMARY.md
@.planning/phases/37-scene-level-difficulty-blocks-env-wiring/37-02-SUMMARY.md
@.planning/phases/36-difficulty-schema-discrete-curriculum/36-03-SUMMARY.md
@src/surg_rl/rl/environment.py
@src/surg_rl/scene_definition/schema.py
@src/surg_rl/scene_definition/loader.py
@tests/test_difficulty_blocks.py
@tests/test_difficulty_levels.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 spike — verify 6 task scenes construct + step headless on the target host</name>
  <files>tests/test_difficulty_blocks.py</files>
  <read_first>
    - scenes/simple_suturing.json, scenes/knot_tying.json, scenes/needle_insertion.json, scenes/grasping.json, scenes/cutting.json, scenes/dissection.json (the 6 v0.4.0 task fixtures — verify task.task_type is set and task.difficulty_level is null and no difficulty_blocks key)
    - tests/test_rl_environment.py (grep for the construct + step pattern from minimal_scene.json — the proven-viable path per 37-RESEARCH.md A5)
    - src/surg_rl/rl/environment.py (59-96, 180-212) — SurgicalEnvConfig + __init__ order
    - .planning/phases/36-difficulty-schema-discrete-curriculum/36-03-SUMMARY.md (118-131) — the pre-existing macOS abort list (test_rl.py, test_benchmark_*.py, test_dreamer_benchmark_integration.py, test_rl_callbacks.py, test_tracking_callbacks.py — NOT caused by this phase)
    - .planning/phases/37-scene-level-difficulty-blocks-env-wiring/37-RESEARCH.md (655-672) — Environment Availability + host-sensitivity fallback
  </read_first>
  <action>
    Run a Wave 0 spike to determine whether the 6 task scenes construct + step under SurgicalEnv headless on this host. For each of the 6 scenes, run a one-off python invocation (PYTHONPATH=src python3 -c "...") that: loads the scene via SceneLoader().load(scene_path), sets scene.task.difficulty_level to DifficultyLevel.HARD (mutate the loaded object — the scenes ship with difficulty_level null), constructs SurgicalEnv(SurgicalEnvConfig(scene=scene, render_mode=None)), calls env.reset(), calls env.step(env.action_space.sample()), and prints whether it succeeded or aborted.

    Based on the spike result, decide the SC#3 gate shape:
    - If all 6 scenes construct + step cleanly: the TestSixSceneThreeLevelRegression gate will assert full step (env.reset() + env.step() returns a well-formed 5-tuple).
    - If any scene aborts at env.step() (C-level macOS abort per 36-03-SUMMARY): the gate degrades to construct-only — assert SurgicalEnv constructs without exception AND env._reward_fn is not None (proves _setup_rewards ran). Log the abort as pre-existing (NOT caused by this phase).

    Do NOT edit any source file in this task — the spike is a read-only probe. Record the spike outcome in the SUMMARY.

    Commit no code in this task; the spike result informs Task 2's test shape.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -c "from surg_rl.scene_definition.loader import SceneLoader; from surg_rl.rl.environment import SurgicalEnv, SurgicalEnvConfig; from surg_rl.rl.difficulty import DifficultyLevel; from pathlib import Path; import traceback; results={}; scenes=['simple_suturing','knot_tying','needle_insertion','grasping','cutting','dissection']; [results.update({s: (lambda p: (lambda: (lambda scene: (setattr(scene.task,'difficulty_level',DifficultyLevel.HARD), SurgicalEnv(SurgicalEnvConfig(scene=scene, render_mode=None)))(SceneLoader().load(str(p))))() )() if Path(p).exists() else 'MISSING') (Path('scenes')/(s+'.json'))}) for s in scenes]; print(results)" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The spike runs against all 6 scene files and records per-scene whether construct + step succeeded or aborted
    - The spike outcome determines the TestSixSceneThreeLevelRegression assertion shape (full-step vs construct-only fallback)
    - No source file is edited in this task
    - The spike outcome is documented in the plan SUMMARY (for the verifier to confirm the gate shape matches the host)
  </acceptance_criteria>
  <done>Spike complete; per-scene construct/step outcome recorded; SC#3 gate shape (full-step vs construct-only) determined for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Append SC#3 6×3 regression gate + SC#4 hard-fixture back-compat gate to test_difficulty_blocks.py</name>
  <files>tests/test_difficulty_blocks.py</files>
  <read_first>
    - tests/test_difficulty_blocks.py (full — the file Plans 01-02 built; append after TestPrecedenceTruthTable)
    - tests/test_difficulty_levels.py (316-401) — TestDifficultyIntegration + HARD_FIXTURE idiom (the pattern to mirror for SC#3 + SC#4)
    - tests/fixtures/scenes/suturing_difficulty_hard.json (300-314) — the v0.4.2 back-compat fixture (difficulty_level: 1.0, no difficulty_blocks)
    - src/surg_rl/rl/environment.py (59-96, 484-525) — SurgicalEnvConfig (now has difficulty field from Plan 02) + _setup_rewards
    - .planning/phases/37-scene-level-difficulty-blocks-env-wiring/37-PATTERNS.md (393-462) — SC#3 6×3 parametrize idiom + SC#4 hard-fixture idiom
    - .planning/phases/37-scene-level-difficulty-blocks-env-wiring/37-VALIDATION.md (43-50) — per-task verification map for SC#3 + SC#4
  </read_first>
  <action>
    Append to tests/test_difficulty_blocks.py (after the last class from Plan 02).

    Define class TestSixSceneThreeLevelRegression with @pytest.mark.parametrize over (scene_file, level_name, level_scalar) covering the 6 scene files x 3 levels (18 cases):
    scene_files = ["simple_suturing.json", "knot_tying.json", "needle_insertion.json", "grasping.json", "cutting.json", "dissection.json"]
    levels = [("EASY", DifficultyLevel.EASY, 0.0), ("MEDIUM", DifficultyLevel.MEDIUM, 0.5), ("HARD", DifficultyLevel.HARD, 1.0)]

    For each case: load the scene via SceneLoader().load(str(Path(__file__).parent.parent / "scenes" / scene_file)); assert scene.task is not None; assert scene.task.difficulty_blocks is None (the 6 v0.4.0 scenes have no blocks — Plan 01's None default); set scene.task.difficulty_level = level (mutate the loaded object — the scenes ship with difficulty_level null); construct SurgicalEnv(SurgicalEnvConfig(scene=scene, render_mode=None)); assert env._reward_fn is not None (proves _setup_rewards ran — the construct-only baseline assertion).

    Then, per the Task 1 spike outcome:
    - If the spike showed the 6 scenes step cleanly: also call env.reset() + env.step(env.action_space.sample()) and assert the step returns a well-formed 5-tuple (obs, reward, terminated, truncated, info) with no exception.
    - If the spike showed any scene aborts at env.step() on this host: wrap the env.step() call in a try/except that catches the abort, marks the case as construct-only-passed via pytest.skip("env.step() aborts on macOS — pre-existing per 36-03-SUMMARY, NOT caused by Phase 37") OR asserts env._reward_fn is not None (the construct-only gate). The skip message MUST cite the pre-existing macOS abort (NOT this phase).

    Define class TestHardFixtureScalarEquivalence with test_hard_fixture_scalar_unchanged:
    FIXTURE_DIR = Path(__file__).parent / "fixtures" / "scenes"; HARD_FIXTURE = FIXTURE_DIR / "suturing_difficulty_hard.json". If not HARD_FIXTURE.exists(): pytest.skip. Load via SceneLoader().load(str(HARD_FIXTURE)); assert scene.task is not None; assert scene.task.difficulty_level == DifficultyLevel.HARD; assert scene.task.difficulty_blocks is None (the v0.4.2 fixture has no blocks). Construct SurgicalEnv(SurgicalEnvConfig(scene=scene, render_mode=None)); assert env._task_difficulty == 1.0 (the byte-identical v0.4.2 scalar — SC#4 back-compat gate). Capture the expected scalar as a literal 1.0 in the assertion (not a computed value — byte-identical comparison).

    The SC#1 negative test `test_existing_scenes_load_without_blocks` is already shipped by Plan 01 Task 1 (canonical name — do NOT re-add or duplicate it). Assert it is present in the file via `grep -c 'def test_existing_scenes_load_without_blocks' tests/test_difficulty_blocks.py` returning 1 as part of this task's gate (guards against accidental removal during Plan 02/03 appends). The test parametrizes over the 6 scenes, loads each, and asserts scene.task.difficulty_blocks is None (SC#1 negative — the 6 existing scenes are unaffected by the new field).

    GREEN gate: run `PYTHONPATH=src pytest tests/test_difficulty_blocks.py -v` → ALL tests green (SC#1 + SC#2 + SC#3 + SC#4 + SC#5). Run `PYTHONPATH=src pytest tests/test_difficulty_levels.py tests/test_difficulty_config.py tests/test_discrete_curriculum.py tests/test_dynamics.py tests/test_rl_environment.py -v` → all pass (TASK-09 additive gate — the full targeted subset). Commit with `test(37-03):` prefix.
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/test_difficulty_blocks.py tests/test_difficulty_levels.py tests/test_difficulty_config.py tests/test_discrete_curriculum.py tests/test_dynamics.py tests/test_rl_environment.py -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_difficulty_blocks.py contains `class TestSixSceneThreeLevelRegression` and `class TestHardFixtureScalarEquivalence`
    - TestSixSceneThreeLevelRegression is parametrized over 6 scene files x 3 levels (18 cases) and asserts env._reward_fn is not None for each (construct-only baseline) plus env.step() 5-tuple OR construct-only fallback per the Task 1 spike
    - TestHardFixtureScalarEquivalence asserts env._task_difficulty == 1.0 (byte-identical v0.4.2 scalar) for suturing_difficulty_hard.json
    - `PYTHONPATH=src pytest tests/test_difficulty_blocks.py -v` exits 0 (all SC#1-5 tests green)
    - `PYTHONPATH=src pytest tests/test_difficulty_levels.py tests/test_difficulty_config.py tests/test_discrete_curriculum.py tests/test_dynamics.py tests/test_rl_environment.py -v` exits 0 (TASK-09 additive gate)
    - The 6 v0.4.0 scenes' difficulty_blocks is None (SC#1 negative — existing scenes unaffected)
    - Commit message starts with `test(37-03):`
  </acceptance_criteria>
  <done>SC#3 6×3 regression gate + SC#4 hard-fixture back-compat gate appended and green; all 5 SCs verified; TASK-09 additive gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scenes/*.json → SceneLoader → SurgicalEnv | The 6 v0.4.0 task fixtures (trusted repo fixtures) are loaded + constructed; the gate asserts no regression in the load/construct/step path |
| tests/fixtures/scenes/suturing_difficulty_hard.json → SurgicalEnv | The v0.4.2 back-compat fixture (trusted repo fixture) is loaded; the gate asserts the difficulty scalar is byte-identical to v0.4.2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-09 | Denial of Service | env.step() abort on macOS (pre-existing) | accept | The 36-03-SUMMARY logged C-level aborts in test_rl.py/test_benchmark_*.py on macOS are caused by MuJoCo/PyBullet backend loading, NOT by this phase. The SC#3 gate degrades to construct-only (assert env._reward_fn is not None) when env.step() aborts, and cites the pre-existing cause. The abort is documented in the SUMMARY, not hidden. |
| T-37-10 | Tampering | back-compat scalar drift (SC#4) | mitigate | TestHardFixtureScalarEquivalence asserts env._task_difficulty == 1.0 (literal) for the v0.4.2 hard fixture — any drift in the precedence chain (e.g. a regression in _setup_rewards that changes the scalar) fails the gate. Byte-identical comparison. |
| T-37-SC | Tampering | package installs | accept | No package installs this plan — only test additions. |
</threat_model>

<verification>
- `PYTHONPATH=src pytest tests/test_difficulty_blocks.py -v` → all SC#1-5 tests green (the complete phase gate)
- `PYTHONPATH=src pytest tests/test_difficulty_levels.py tests/test_difficulty_config.py tests/test_discrete_curriculum.py tests/test_dynamics.py tests/test_rl_environment.py -v` → all passed (TASK-09 additive gate — the full targeted subset)
- Manual confirm (per 37-VALIDATION.md Manual-Only): the full suite's only failures are the pre-existing macOS backend aborts (test_rl.py, test_benchmark_*.py, test_dreamer_benchmark_integration.py, test_rl_callbacks.py, test_tracking_callbacks.py) — NOT caused by this phase
- `grep -c 'class TestSixSceneThreeLevelRegression' tests/test_difficulty_blocks.py` returns 1
- `grep -c 'class TestHardFixtureScalarEquivalence' tests/test_difficulty_blocks.py` returns 1
</verification>

<success_criteria>
- SC#3: 6 v0.4.0 task scenes × 3 difficulty levels construct + step (or construct-only fallback if macOS abort) — TestSixSceneThreeLevelRegression green
- SC#4: suturing_difficulty_hard.json back-compat scalar unchanged (env._task_difficulty == 1.0) — TestHardFixtureScalarEquivalence green
- TASK-09: existing difficulty suite passes unchanged (additive-regression gate green)
- All 5 SCs now verified (SC#1 + SC#5 in Plan 01, SC#2 in Plan 02, SC#3 + SC#4 here)
</success_criteria>

<artifacts_this_phase_produces>
- `tests/test_difficulty_blocks.py :: TestSixSceneThreeLevelRegression` — 18 parametrized cases (6 scenes × 3 levels), construct + step OR construct-only fallback per the Wave 0 spike
- `tests/test_difficulty_blocks.py :: TestHardFixtureScalarEquivalence` — SC#4 back-compat scalar gate (env._task_difficulty == 1.0 for suturing_difficulty_hard.json)
- `tests/test_difficulty_blocks.py :: test_existing_scenes_load_without_blocks` — SC#1 negative (6 existing scenes have difficulty_blocks is None); shipped by Plan 01, presence re-asserted in Plan 03 gate (no duplication)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/37-scene-level-difficulty-blocks-env-wiring/37-03-SUMMARY.md` when done
</output>