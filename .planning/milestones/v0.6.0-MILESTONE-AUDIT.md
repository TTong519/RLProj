---
milestone: v0.6.0
name: Carried-Forward Debt Closure
audited: 2026-07-12
status: gaps_found
phases_in_scope:
  - 36-difficulty-schema-discrete-curriculum
  - 37-scene-level-difficulty-blocks-env-wiring
  - 38-fluid-3d-flag-dim-3d-true
  - 39-k8s-pvc-e2e-organ-mesh-licensing-adr
  - 40-real-dreamerv3-integration-sentinel-flip
total_plans: 16
scores:
  requirements: "10/13 satisfied (3 partial — DMV3-07, DMV3-08, DMV3-10: source complete + CPU guards GREEN, runtime GPU-GREEN unobserved)"
  phases: "4/5 verified passed (36, 37, 38, 39); 1 human_needed (40 — 3 behavior-unverified truths deferred to dreamer-gpu CI per INV-8)"
  integration: "12/12 cross-phase connections wired; 0 missing; 0 orphaned exports"
  flows: "3/4 E2E flows fully working; 1/4 (real DreamerV3 training) structurally complete, runtime GPU-gated"
nyquist:
  compliant_phases: [36, 37, 38]
  partial_phases: [39, 40]
  missing_phases: []
  overall: partial
gaps:
  requirements:
    - id: "DMV3-07"
      status: "partial"
      phase: "40-real-dreamerv3-integration-sentinel-flip"
      claimed_by_plans: ["40-01-PLAN.md", "40-02-PLAN.md"]
      completed_by_plans: ["40-02-SUMMARY.md (requirements-completed: [DMV3-07, DMV3-09, DMV3-10])"]
      verification_status: "human_needed (PRESENT_BEHAVIOR_UNVERIFIED)"
      evidence: "5 stub functions replaced with real dreamerv3.Agent implementations in src/surg_rl/dreamer/subprocess.py (_build_agent 4-arg Agent ctor :192; _train_loop manual Driver + agent.train :298-324; _evaluate agent.policy rollouts :412; _save/_load_checkpoint delegate to cp.save/cp.load :498,519,524). CPU regression guard + JAX-leak guard GREEN. Positive real-agent training (finite loss, completes) only executes on a GPU host; flipped E2E test SKIPs on macOS per INV-8. GPU GREEN deferred to dreamer-gpu CI job pending GitHub GPU Actions runner enablement."
    - id: "DMV3-08"
      status: "partial"
      phase: "40-real-dreamerv3-integration-sentinel-flip"
      claimed_by_plans: ["40-03-PLAN.md"]
      completed_by_plans: ["40-03-SUMMARY.md (requirements-completed: [DMV3-08])"]
      verification_status: "human_needed (PRESENT_BEHAVIOR_UNVERIFIED)"
      evidence: "*.ckpt glob (training.py:194, no .pt shim); checkpoint.ckpt write path (training.py:346); task+checkpoint_dir threaded to child (training.py:258-259, 40-04-prep fix 81588c2); resume test exists and SKIPs cleanly on macOS. Resume invariant (step counter survives subprocess restart) only executes on GPU; deferred to CI. WARNING: parent _find_latest_checkpoint uses hardcoded models/dreamerv3/{task}_{obs_type} path not threaded checkpoint_dir (training.py:191) — redundant-but-harmless because child cp.load_or_save() handles resume; fragile defense-in-depth gap."
    - id: "DMV3-10"
      status: "partial"
      phase: "40-real-dreamerv3-integration-sentinel-flip"
      claimed_by_plans: ["40-04-PLAN.md"]
      completed_by_plans: ["40-02-SUMMARY.md", "40-04-SUMMARY.md (requirements-completed: [DMV3-10])"]
      verification_status: "human_needed (PRESENT_BEHAVIOR_UNVERIFIED)"
      evidence: "dreamer-gpu CI job structurally complete + YAML valid (ci.yml:209-269; ubuntu-latest-4-core-gpu; jax[cuda12]~=0.4.20 installed BEFORE .[dev,dreamer]; pytest tests/dreamer/ -v -rs; DREAMER_TOTAL_STEPS=1000; timeout-minutes:15; not-per-PR gate). Structural-only assertions (grep MSE<0.01/reward_mae<0.5/DEFAULT_THRESHOLDS == 0). macOS SKIPs cleanly per INV-8. GREEN pending GitHub organizational GPU Actions runner enablement (ops step, not code)."
  integration:
    - from: "src/surg_rl/dreamer/training.py:_find_latest_checkpoint"
      to: "threaded checkpoint_dir (custom-dir resume path)"
      issue: "WARNING — parent resume-lookup uses hardcoded models/dreamerv3/{task}_{obs_type} (training.py:191), not the threaded checkpoint_dir argument. With a custom checkpoint_dir + resume=True, parent subprocess.load_checkpoint(latest) is never called (latest is None). Currently masked by the child's cp.load_or_save() at construct (subprocess.py:217) which handles resume regardless. Fragile defense-in-depth gap — a future change removing the child load_or_save would silently restart from step 0 for custom dirs. Affects DMV3-08 (currently MET via child path)."
      severity: warning
  flows: []
tech_debt:
  - phase: 36-difficulty-schema-discrete-curriculum
    items:
      - "Pre-existing mypy errors in curriculum.py (L387 apply_parameters untyped, L414 pybullet stub import, L618 episode_end_with_task_result untyped) — pre-baseline, Phase 36 mypy delta = 0 new errors"
      - "Pre-existing macOS torch/libomp SIGABRT on Python 3.14.6 in tests/test_rl.py + tests/test_rl_callbacks.py (import torch crashes before any Phase 36 surface) — environment issue, not a regression"
  - phase: 37-scene-level-difficulty-blocks-env-wiring
    items:
      - "Stale doc comment in tests/test_difficulty_blocks.py:81 (says float-value coercion; actual is name-based via DifficultyLevel[key]) — comment-only, behavior correct"
      - "Pre-existing ruff/black debt in src/surg_rl/rl/environment.py (F821 ControllerBridge, SIM105) + 10 pre-existing ruff errors in tests/test_difficulty_blocks.py — baseline debt, 0 new from Phase 37"
  - phase: 38-fluid-3d-flag-dim-3d-true
    items:
      - "WARNING (CR-01): 3D force unit bug — np.gradient in force_computation.py:53-55 omits physical cell spacing → forces off by factors of dx/dy/dz (3D-only). Not in any SC (SC#2 is stability/no-NaN, not magnitude). Recommend follow-up: fix + add physical-magnitude regression test."
      - "WARNING (WR-01): coupling_mode (ONE_WAY/TWO_WAY) declared + validated but never consumed by step() — both modes run identical code. The TWO_WAY xfail unexpectedly passed (1 xpassed) consistent with this. Recommend: implement TWO_WAY feedback OR remove the field."
      - "WARNING (WR-02): coupling_substeps declared + validated (ge=1, le=16) but never consumed — no substep loop. Defense-in-depth knob is inert."
      - "WARNING (WR-03): add_instrument tip Box geometrically absorbed by infinite-z shaft for the equal-half-size case used by every test — tested morphology is effectively shaft-only."
      - "WARNING (WR-04): schema accepts dim_3d=True with zero-y-extent BoundingBox → degenerate 3D domain dy=0 → silent zero forces. No validator guards bounds extent."
      - "INFO (WR-05): zip(obstacles, obstacle_names) without strict=True silently truncates on length mismatch — code-quality; lists aligned in practice."
  - phase: 39-k8s-pvc-e2e-organ-mesh-licensing-adr
    items:
      - "WARNING (WR-01): kubectl wait --for=condition=complete in tests/k8s/test_pvc_e2e.py:86,94 cannot distinguish Complete from Failed → opaque 120s CI hang on Job failure. Diagnosability improvement, not a correctness violation. Re-verification 2026-07-09 confirmed CI GREEN."
      - "INFO: # fmt: skip pragmas on long kubectl wait calls (lines 86, 94) — intentional, keeps acceptance-grep substring on a single line."
  - phase: 40-real-dreamerv3-integration-sentinel-flip
    items:
      - "INFO: try/except Exception: pass around logger→stderr redirection in subprocess.py:262-281 — best-effort, _JsonStdout already protects the JSON pipe."
      - "INFO: _evaluate returns reconstruction_mse/reward_mae as 0.0 finite placeholders (subprocess.py:459-460) — documented stub for 2 of 5 EVAL keys (DMV3-10 finiteness only); real world-model forward values deferred to GPU refinement."
      - "INFO: agent.close() on a dict bundle raises AttributeError, swallowed by contextlib.suppress(Exception) (subprocess.py:120-122) — cosmetic non-blocking cleanup."
      - "INFO: _gpu_available() in tests/dreamer/test_dreamerv3_subprocess_e2e.py:32-47 does import torch at function body — module-level pytestmark skipif calls it at collection time; on Python 3.14 + torch+libomp this can SIGABRT (pre-existing, logged in deferred-items.md). Run under pyenv 3.13.3 or the GPU CI runner."
      - "USER ACTION ITEM: Enable GitHub-hosted GPU Actions runners on the repo account (ubuntu-latest-4-core-gpu label) so dreamer-gpu CI job GREEN is observed — closes DMV3-07 runtime, DMV3-08 resume runtime, DMV3-10 CI GREEN."
---

# v0.6.0 Carried-Forward Debt Closure — Milestone Audit

**Audited:** 2026-07-12
**Status:** gaps_found
**Milestone Goal:** Close the four carried-forward tech-debt items deferred from v0.4.0–v0.5.0 — real DreamerV3 integration, the TASK-02 per-level difficulty schema, K8s PVC e2e + organ-mesh licensing decision, and the 3D fluid flag. Pure closure: no new user-facing features. Every item additive — the v0.4.0 + v0.4.2 + v0.5.0 test baseline passes unchanged.

## Scope

| Phase | Subsystem | Plans | Completed | Verification |
|-------|-----------|-------|-----------|--------------|
| 36-difficulty-schema-discrete-curriculum | rl/difficulty + dynamics/curriculum | 3/3 | 2026-06-25 | passed (7/7 truths) |
| 37-scene-level-difficulty-blocks-env-wiring | scene_definition + rl | 3/3 | 2026-06-25 | passed (5/5 truths) |
| 38-fluid-3d-flag-dim-3d-true | scene_definition + fluids | 4/4 | 2026-06-27 | passed (5/5 truths) |
| 39-k8s-pvc-e2e-organ-mesh-licensing-adr | k8s + assets-licensing | 2/2 | 2026-06-27 | passed (re-verified 2026-07-09, SC#1 CI GREEN closed) |
| 40-real-dreamerv3-integration-sentinel-flip | dreamer + infra | 4/4 | 2026-07-12 | human_needed (2/5 truths verified; 3 PRESENT_BEHAVIOR_UNVERIFIED — GPU-gated per INV-8) |

All 5 phase VERIFICATION.md files present. All 5 phase VALIDATION.md files present. No unverified phases. No orphaned requirements (13/13 mapped in REQUIREMENTS.md traceability table, all checked `[x]`).

## Requirements Coverage (3-Source Cross-Reference)

Sources cross-referenced for each REQ-ID: (a) phase VERIFICATION.md requirements table, (b) SUMMARY.md `requirements-completed` frontmatter, (c) REQUIREMENTS.md traceability table checkbox.

| REQ-ID | Phase | VERIFICATION.md | SUMMARY frontmatter | REQUIREMENTS.md | Final Status |
|--------|-------|-----------------|----------------------|------------------|--------------|
| TASK-06 | 36 | passed, SATISFIED | — (36 SUMMARYs have no reqs frontmatter) | `[x]` Complete | **satisfied** |
| TASK-07 | 36 | passed, SATISFIED | — | `[x]` Complete | **satisfied** |
| TASK-09 | 36 | passed, SATISFIED | — | `[x]` Complete | **satisfied** |
| TASK-08 | 37 | passed, SATISFIED | — (37 SUMMARYs have no reqs frontmatter) | `[x]` Complete | **satisfied** |
| FLUID-01 | 38 | passed, SATISFIED | 38-04-SUMMARY: [FLUID-01,02,03] | `[x]` Complete | **satisfied** |
| FLUID-02 | 38 | passed, SATISFIED | 38-04-SUMMARY: [FLUID-01,02,03] | `[x]` Complete | **satisfied** |
| FLUID-03 | 38 | passed, SATISFIED | 38-04-SUMMARY: [FLUID-01,02,03] | `[x]` Complete | **satisfied** |
| DEPLOY-01 | 39 | passed (re-verified 2026-07-09, SC#1 CI GREEN observed), SATISFIED | — (39 SUMMARYs have no reqs frontmatter) | `[x]` Complete | **satisfied** |
| ASET-06 | 39 | passed, SATISFIED | — | `[x]` Complete | **satisfied** |
| DMV3-07 | 40 | human_needed, PARTIAL (PRESENT_BEHAVIOR_UNVERIFIED) | 40-02-SUMMARY: [DMV3-07,09,10] | `[x]` Complete | **partial** |
| DMV3-08 | 40 | human_needed, PARTIAL (PRESENT_BEHAVIOR_UNVERIFIED) | 40-03-SUMMARY: [DMV3-08] | `[x]` Complete | **partial** |
| DMV3-09 | 40 | PASS (fully verified on CPU) | 40-02-SUMMARY: [DMV3-07,09,10] | `[x]` Complete | **satisfied** |
| DMV3-10 | 40 | human_needed, PARTIAL (PRESENT_BEHAVIOR_UNVERIFIED) | 40-02 + 40-04-SUMMARY: [DMV3-10] | `[x]` Complete | **partial** |

**Score: 10/13 satisfied, 3 partial, 0 unsatisfied, 0 orphaned.**

The 3 partial requirements (DMV3-07, DMV3-08, DMV3-10) are all on Phase 40 and all gated on a single ops action: enabling GitHub-hosted GPU Actions runners and observing the first `dreamer-gpu` CI job GREEN. Source code is complete, CPU-runnable guards are GREEN (regression guard, JAX-leak guard, `.ckpt` glob unit tests), and GPU-gated tests SKIP cleanly on macOS per INV-8 by design. The Phase 40 verifier explicitly flagged this as a "user action item (flagged for milestone audit)."

## FAIL Gate

Per the audit FAIL gate, any `unsatisfied` requirement forces `gaps_found`. No requirement is `unsatisfied` (all 13 are code-complete and claimed by plans). However, 3 requirements are `partial` with their runtime verification (GPU CI GREEN) explicitly outstanding — the milestone's headline closure item (real DreamerV3 training E2E on CI GPU host, DMV3-10) has NOT been observed green. This is a real, documented DoD gap, so the milestone status is `gaps_found` rather than `passed` or `tech_debt`.

## Cross-Phase Integration (gsd-integration-checker)

**Result: 12/12 cross-phase connections wired. 0 missing. 0 orphaned exports. 1 WARNING. No BLOCKER.**

| Wiring Edge | Status |
|-------------|--------|
| 36 → 37: `TaskConfig.difficulty_blocks: dict[DifficultyLevel, DifficultyLevelConfig]` via v0.4.2 model_rebuild() cycle pattern | ✓ WIRED |
| 37: `SurgicalEnv._setup_rewards` lazy-local imports `compose_difficulty_overrides` + `TASK_REWARD_REGISTRY` (environment.py:542-543) → `apply_params` on 6 task rewards | ✓ WIRED |
| 37: 4-level precedence truth-table (blocks > task.difficulty_level > config.difficulty > 0.5) | ✓ WIRED (35/35 tests pass) |
| 38: `FluidConfig.dim_3d` → `FluidSimulator` 3D branch → `_compute_obstacle_forces_3d` → `render_fluid_3d`; 2D byte-identical preserved | ✓ WIRED |
| 38: `BaseSimulator.fluid_step(dt)` hook preserved (v0.5.0 5-test suite unchanged) | ✓ WIRED |
| 39: `k8s/overlays/e2e/{pvc,e2e-write-job,read-job}.yaml` claimName `surg-rl-checkpoints` consistent ×3 ← e2e test ← CI `k8s-e2e` job installs `.[dev,k8s-test]` (pytest-kind>=22.11.1) | ✓ WIRED (CI GREEN re-verified 2026-07-09) |
| 39: `docs/adr/0001-organ-mesh-licensing.md` + `docs/adr/README.md` index | ✓ WIRED |
| 40: `run_dreamer_training` threads `task`+`checkpoint_dir` to child via `dreamer_config` (training.py:258-259, 40-04-prep fix 81588c2) | ✓ WIRED |
| 40: `_build_agent` real 4-arg `dreamerv3.Agent` ctor + `embodied.Checkpoint` resume-or-init | ✓ WIRED (structurally; runtime GPU-gated) |
| 40: `_find_latest_checkpoint` globs `*.ckpt` matching training.py write paths (no `.pt` shim) | ✓ WIRED |
| 40: sentinel flip — E2E tests renamed + flipped positive; regression guard AST-walks `_build_agent` for `return None` | ✓ WIRED (DMV3-09 CPU-verified) |
| 40: `dreamer-gpu` CI job (GPU runner, jax[cuda12] first, pytest tests/dreamer/, timeout-minutes:15, not-per-PR) | ✓ WIRED (structurally; GREEN pending GPU runner) |
| 40: JAX-isolation invariant — 0 module-top jax/dreamerv3/embodied imports in subprocess.py | ✓ WIRED (empirically verified via sys.modules) |

**Additive-regression invariant: PASS.** Baseline sweep (excluding `tests/test_rl.py` + `tests/test_training.py` which hit the known pre-existing macOS torch/libomp SIGABRT on Python 3.14 — NOT a v0.6.0 regression): **1438 passed, 22 skipped, 29 deselected, 1 xpassed, 0 failed/errored**. Targeted phase 36–40 sweep: 192 passed in 3.79s. The v0.5.0 5-test `fluid_step` suite passes byte-identical; the v0.4.0 + v0.4.2 curriculum suite passes unchanged.

### Integration WARNING (non-blocking)

- **`training.py:191` `_find_latest_checkpoint` hardcoded path mismatch.** Parent resume-lookup uses `models/dreamerv3/{task}_{obs_type}`, not the threaded `checkpoint_dir` argument. With a custom `checkpoint_dir` + `resume=True`, the parent's `subprocess.load_checkpoint(latest)` is never called (`latest` is None). Currently masked by the child's `cp.load_or_save()` at construct (subprocess.py:217), which handles resume regardless. Fragile defense-in-depth gap — affects DMV3-08 (currently MET via the child path). Recommend: thread `checkpoint_dir` into `_find_latest_checkpoint` or document the child-path-as-source-of-truth contract.

## E2E Flows

| Flow | Status | Breaks At |
|------|--------|-----------|
| (a) Researcher loads a hard-mode scene → gets a hard-mode env | ✓ COMPLETE | — (35/35 tests; integration test construct+reset+step on HARD fixture) |
| (b) Researcher enables 3D fluid → stable 3D sim | ✓ COMPLETE | — (validators gate grid_size; 3D step branch + per-axis clamp; 2D byte-identical) |
| (c) K8s checkpoint persists across pod restart | ✓ COMPLETE (CI-verified 2026-07-09) | — (write→pod restart→read byte-equality on bound PVC; CI GREEN observed) |
| (d) Researcher trains real DreamerV3 agent via subprocess | ⚠️ STRUCTURALLY COMPLETE, RUNTIME GPU-GATED | runtime GREEN pending GitHub GPU runner enablement (DMV3-07/08/10) |

## Nyquist Compliance

The Nyquist capability is active (verify:post step hook for `validate-phase` present). Per-phase VALIDATION.md classification:

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete | Status | Action |
|-------|---------------|-------------------|------------------|--------|--------|
| 36 | exists | true | false | COMPLIANT | — |
| 37 | exists | true | false | COMPLIANT | — |
| 38 | exists | true | true | COMPLIANT | — |
| 39 | exists | false | false | PARTIAL (template unfilled — Per-Task Verification Map still has `{N}-01-01` placeholders) | `/gsd-validate-phase 39` |
| 40 | exists | false | false | PARTIAL (template unfilled — Per-Task Verification Map still has `{N}-01-01` placeholders) | `/gsd-validate-phase 40` |

**Overall: partial.** Phases 39 and 40 have VALIDATION.md files but their per-task verification maps were never filled in (frontmatter `status: draft`, `nyquist_compliant: false`). Note: both phases nonetheless have full VERIFICATION.md reports with empirical test evidence, so this is a documentation/Nyquist-process gap, not a verification gap — running `/gsd-validate-phase 39` and `/gsd-validate-phase 40` retroactively would close it.

## Tech Debt Summary

| Phase | Items |
|-------|-------|
| 36 | 2 (both pre-existing baseline: mypy errors in curriculum.py, macOS torch/libomp SIGABRT) |
| 37 | 3 (1 stale doc comment; 2 pre-existing baseline ruff/black debt) |
| 38 | 6 advisory code-quality findings (CR-01 force unit bug; WR-01 coupling_mode dead; WR-02 coupling_substeps dead; WR-03 tip absorbed; WR-04 zero-y bounds; WR-05 zip strict) — none in any SC |
| 39 | 2 (WR-01 kubectl wait condition=complete diagnosability; fmt:skip pragmas) |
| 40 | 5 (logger redirect try/except; EVAL 0.0 placeholders for 2/5 keys; agent.close() on dict; _gpu_available torch-import SIGABRT pre-existing; GPU runner enablement user action item) |

**Total: ~18 items across 5 phases** — predominantly INFO/pre-existing baseline. The actionable follow-ups are: (1) enable GPU runner + observe dreamer-gpu GREEN [closes the 3 partial requirements], (2) Phase 38 CR-01 3D force unit bug + inert coupling_mode/substeps knobs, (3) thread checkpoint_dir into `_find_latest_checkpoint` (DMV3-08 defense-in-depth), (4) retroactive `/gsd-validate-phase 39/40` to fill Nyquist maps.

## Bottom Line

The v0.6.0 milestone is **structurally complete** — all 5 phases executed (16/16 plans), all 13 requirements are code-complete and claimed by plans, cross-phase integration is fully wired (12/12, no blockers), the additive-regression invariant holds (1438 passed baseline), and 3 of 4 E2E flows are fully working (including the K8s PVC e2e, CI-verified green on 2026-07-09).

The single outstanding gap is the **GPU runtime GREEN for Phase 40** (DMV3-07 runtime, DMV3-08 resume runtime, DMV3-10 CI GREEN) — source complete + CPU guards GREEN, but the `dreamer-gpu` CI job has not been observed green because GitHub-hosted GPU Actions runners are not yet enabled on the repo account. This is a single ops action item, explicitly flagged by the Phase 40 verifier for this milestone audit. Until that GREEN is observed, the milestone's headline closure item ("real DreamerV3 training runs E2E on the CI GPU host") is not certified, so the audit status is `gaps_found`.

---

_Audited: 2026-07-12_
_Auditor: Claude (gsd-audit-milestone)_
_Integration check: gsd-integration-checker (12/12 wired, 1 WARNING, 0 BLOCKER)_